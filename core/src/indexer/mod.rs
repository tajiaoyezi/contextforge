//! task-1.3 (AC4): Phase 2 `indexer` placeholder — Tantivy full-text +
//! SQLite metadata/chunk store + incremental update. No logic yet; Phase 2.

/// True once this placeholder module is wired into the crate (AC4 test anchor).
pub fn placeholder_ready() -> bool {
    true
}
