//! task-1.3 (AC4): Phase 4 `retriever` placeholder — BM25/metadata/filter
//! retrieval + explainable trace. No logic yet; lands in Phase 4.

/// True once this placeholder module is wired into the crate (AC4 test anchor).
pub fn placeholder_ready() -> bool {
    unimplemented!("task-1.3 GREEN: retriever placeholder wired")
}
