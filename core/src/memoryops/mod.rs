//! task-1.3 (AC4): Phase 5 `memoryops` placeholder — dedup / conflict /
//! stale-marking / provenance merge / audit. No logic yet; lands in Phase 5.

/// True once this placeholder module is wired into the crate (AC4 test anchor).
pub fn placeholder_ready() -> bool {
    unimplemented!("task-1.3 GREEN: memoryops placeholder wired")
}
