// Package main is a tiny sample binary used by the ContextForge quickstart
// fixture. It exists so the indexer has a Go source file to classify in
// addition to the markdown content.
package main

import "fmt"

func main() {
	fmt.Println("sample-project: hello from the quickstart fixture")
}
