# Project Memory

This is the Hermes project memory fixture used by task-9.4 cli import tests.

## Conventions

- Default branch is master
- v0.2 ships in May 2026

## Key decisions

The cli-pipeline phase (ADR-013) closes the v0.1 spec drift in `contextforge import`.
