# ContextForge v0.7.0 Evidence — Console 22-endpoint Conformance 100% PASS

**日期**：2026-05-24
**主干**：`master`
**当前 HEAD**：`111aee1` (`Merge PR: chore(docs) v0.7.0 release docs — Console 22-endpoint 100% PASS (E14)`)

**v0.7.0 tag SHA**：`748393f` (annotated tag pointing to master @ `111aee1`; `git ls-remote --tags origin v0.7.0` 验证 push 成功)

## 合入记录（Phase 14 + ADR-017 Accepted + release docs）

- PR #89 `feat/task-14.1-rust-eval-grpc` → `d54c4e5` (merged 1027378): 0014_eval_runs.sql migration + SqliteEvalStore (5 methods, 7 unit tests, JSON roundtrip type preserve verified) + EvalService 3 RPC proto add-only + data_plane/eval.rs EvalServer impl + 3 unit tests + 2 integration tests; DataPlaneStores 加 Option<eval> + with_eval() + full() takes 8 params; serve_full 真接 SqliteEvalStore; register_services + server_with_services 都加 6th EvalServiceServer.
- PR #90 `feat/task-14.2-go-eval-rest` → `fbe78ae` (merged fcdc81f): EvalClient interface + Deps.Eval; 2 REST handlers (POST /v1/eval-runs spawn runEvalAsync + GET); eval_runner.go runEvalAsync goroutine (light-weight harness via BuiltinGoldenQuestions + mock pass-all + recall@5/10/precision@5 metrics + case_results array + defer-recover panic → status=failed); MemEvalStore + 2s timer auto-advance fallback; evalClient gRPC 3 wrappers + protoToEvalRun; degradedEval; console_api_serve wires Eval in both modes; e2e_grpc Step 9e real Rust daemon; **scripts/console_smoke.sh v5** 18 → 20 endpoint flow + Step 19/20 eval (POST + poll until terminal + recall@5 verify).
- PR #91 `chore/phase-14-closeout-and-adr-017-accepted` → `191c04b` (merged d22ef8d): Phase 14 / Task 14.{1,2} 全 Done + adapter Phase 14 + Task 14.{1,2} = Done + Phase 14 spec §6 AC1-7 全 [x] verified-by + **ADR-017 Status: Proposed → Accepted** (6 D-clauses 一次性 promote 跨 v0.5/v0.6/v0.7 3 phase) + ADR-014 D1 mapping 表 in PR body.
- PR #92 `chore/v0.7.0-release-docs` → `2cd9033` (merged 111aee1): README v0.7 段 + RELEASE_NOTES v0.7.0 章节 + docs/releases/v0.7.0-{evidence,artifacts}.md.
- PR `chore/v0.7.0-evidence-sha-backfill` → `<本 PR 自身 SHA — merge 后回填>`: release docs SHA 占位填实 (与 v0.5.0/v0.6.0 SHA backfill 同款 pattern).

## S2V 状态

- `docs/s2v-adapter.md`: Phase 14 `eval-rest-surface` = `Done` (closeout PR #91 内同步).
- `docs/s2v-adapter.md`: Task 14.1/14.2 全 `Done`.
- `task-14.1-rust-eval-grpc-service.md` = `Done` (5/5 AC).
- `task-14.2-go-eval-rest-handlers.md` = `Done` (6/6 AC; trade-off: light-weight harness via BuiltinGoldenQuestions, retriever-backed recall 留 v1.x).
- `phase-14-eval-rest-surface.md` = `Done`, §6 AC1-7 全 [x] verified-by 显式.
- `docs/decisions/adr-017-console-contract-completion-22-endpoint.md` = **Accepted** (2026-05-24 via Phase 14 closeout PR #91; 6 D-clauses 跨 3 phase 完整 ship).
- `docs/decisions/adr-014-cross-phase-exit-criteria-validation.md` = `Accepted` (v0.3/v0.4/v0.5/v0.6 四次激活后 **v0.7 第五次激活** — 制度稳定性跨 5 phase 验证通过).

## 验证证据

### 1. cargo test -p contextforge-core

```bash
cargo test -p contextforge-core
```

实测：
- 17+ test groups all PASS, 0 failed
- 94 lib tests (含 10 new task-14.1: 7 store + 3 server; 既有 task-13.x 14 memory + task-12.x search 等不退化)
- 3 memory_integration tests (Phase 13 不退化)
- 2 eval_integration tests (Phase 14 new)
- 既有 indexjob_real_runner / search_real_retriever / data_plane_integration 等不退化

### 2. go test ./...

```bash
go test ./...
```

实测：
- 43 packages PASS
- `internal/consoleapi`: e2e_grpc 含 Step 8a/8b/9/9b/9c/9d/9e 全 PASS（真接 Rust daemon 跨 v0.5/v0.6/v0.7 新 endpoint）
- `internal/consoleapi/grpcclient`: 任 Workspace/Job/Search/Memory/Eval 5 wrapper 不退化
- `internal/cli`: degraded fallback impls 全 compile (degradedMemory + degradedEval)
- `test/conformance`: v0.4-v0.6 既有 endpoints 不退化

### 3. Phase 14 console real smoke v5

```bash
bash scripts/console_smoke.sh
```

实测 (REAL mode default, v5 20 endpoint flow):
- `[mode] real`
- `[real][1/4..4/4]`: cargo build + spawn core/api ✅
- `[wait] /v1/health` responsive at attempt 1: ✅
- `[flow] 20 endpoint flow (Console 22-endpoint conformance)`:
  - `[1/20..8/20]` (v0.4 baseline): health / workspace CRUD / index-job enqueue + poll / search ✅
  - `[9/20]` task-12.1 PATCH config (412→200) ✅
  - `[10/20]` task-12.1 GET active filter (+ missing → 400) ✅
  - `[11/20]` task-12.2 GET source-chunks (chk_xxx found) ✅
  - `[12/20]` task-12.3 GET search trace (+ unknown → 404) ✅
  - `[13/20..18/20]` task-13.2 memory 5 endpoint (sqlite3 seed optional; 412+204+exclude all PASS) ✅
  - `[19/20]` task-14.2 POST /v1/eval-runs → 200 + status=running ✅ (eval_run_id=eval-XXXXX)
  - `[20/20]` task-14.2 GET /v1/eval-runs/<id> poll → **terminal at attempt 1: status=succeeded; metrics contains recall@5 ✅**
  - `[end]` GET /v1/observability/events ✅
- `CONSOLE_REAL_SMOKE_EXIT=0` 最末行: ✅

### 4. ADR-014 D2 lint (PR-touched lines)

E11-E13 PR-touched spec files:
- task-14.1 spec body: anti-pattern lines all annotated [SPEC-OWNER:task-14.2] / [SPEC-DEFER:phase-future.rust-native-eval-runner / phase-15.eval-progress-streaming] → 0 unannotated
- task-14.2 spec body: anti-pattern lines all [SPEC-DEFER:phase-15.eval-orphan-reaper / console-eval-cancel] → 0 unannotated
- phase-14 spec body: 1 anti-pattern line annotated → 0 unannotated
- adapter Status flips: 0 anti-pattern hits
- **制度稳定性第五次激活 (跨 v0.3/v0.4/v0.5/v0.6/v0.7 5 phase) PASS**

### 5. release_smoke.sh

```bash
RELEASE_SMOKE_CONSOLE=1 bash scripts/release_smoke.sh
```

实测 `PHASE_RELEASE_SMOKE_EXIT=0` + console_smoke.sh v5 内嵌跑 20-step 全过 + phase11_console_real=ok 段不退化.

### 6. **Console 22-endpoint conformance — 100% PASS** ⭐

ContextForge v0.4-v0.7 ship 全 22 Console contract v1 endpoints:

| Wave | Phase | Tasks | Endpoints |
|---|---|---|---|
| v0.4 baseline | Phase 11 | task-11.{1-4} | health + workspace CRUD (3) + index-job enqueue/get/cancel (3) + search + events = 9 |
| Wave 1+2 (v0.5) | Phase 12 | task-12.{1,2,3} | PATCH workspace config + GET active jobs + cancel 204 + GET source-chunks + GET search trace = 5 (4 endpoints + 1 behavior change) |
| Wave 3 (v0.6) | Phase 13 | task-13.{1,2} | GET memory + GET memory/{id} + POST pin + POST deprecate + POST soft-delete = 5 |
| Wave 4 (v0.7) | Phase 14 | task-14.{1,2} | POST eval-runs + GET eval-runs/{id} = 2 |
| **Total** | | | **9 + 5 + 5 + 2 = 22 → ✅ all shipped** (20 distinct routes; 2 shared via filter shape per playbook) |

Console UI HTTPAdapter v1.0 端到端 22-endpoint 调用代码已 cross-repo ship.
ContextForge end smoke v5 20/20 全 PASS = **双方握手成功** standard signal landed.

### 7. Console 接入指南 (post-ship onboarding reference)

为支持 Console 团队从 `CONSOLE_API_CORE_MODE=mock → http` 真接 ContextForge，
v0.7.0 同步产出一份独立的 cross-repo onboarding 文档：

- **`docs/releases/v0.7.0-integration.md`** — 11 项 P0/P1/P2 接入对账：
  - **P0**：daemon 部署形态 / 20 route 对账表 / auth 模式 / X-Confirm 完整规则
  - **P1**：OpenAPI/Schema 现状与 11 endpoint schema 补完 / error response shape / observability events 数据契约 / 数据 fixture 与 prereq
  - **P2**：health endpoint 细节 / long-running operation 行为 / logs/metrics 现状

该文档同时记录了 v0.7 一组已知 doc/code drift（如 `openapi.yaml` v0.3 stale、
`?wait=` 当前未在 REST tier 真 block、`/v1/health` 不暴露 5 链路细分），
透明披露给 Console 团队避免联调期反复。

> 本 §7 是 evidence doc 的 cross-repo 接入指引；integration.md 才是权威字段表。
> 任何字段层不一致以 `internal/contractv1/contractv1.go` + `internal/consoleapi/router.go`
> 为最终单一权威。

## 平台矩阵

| 平台 | cargo test | go test | console_smoke.sh v5 REAL | release_smoke.sh | Notes |
|---|---|---|---|---|---|
| Linux (x86_64) | ✅ | ✅ | ✅ | ✅ | 默认 dev 环境 |
| WSL2 Ubuntu | ✅ | ✅ | ✅ | ✅ | Phase 14 主开发 |
| macOS arm64 | ⏳ | ⏳ | ⏳ | ⏳ | CI matrix follow-up |
| Git Bash on Windows | ✅ | ✅ | ✅ (cygpath; sqlite3 optional) | ⏳ | task-14.{1,2} 本地实测平台 |

## Upgrade path (v0.6.0 → v0.7.0)

1. `git fetch --tags && git checkout v0.7.0`
2. `cargo build -p contextforge-core --release && go build ./cmd/contextforge`
3. 重启 daemon：旧 daemon SIGTERM；新 binary 启动 in 既有 data_dir
   会 create eval.db + apply 0014_eval_runs.sql (幂等)
4. **Console UI 端**：HTTPAdapter v1.0 已含全 22-endpoint 调用 (cross-repo
   v0.x.y 历史 ship); v0.7.0 ship 后 Console UI 可关闭 MockAdapter 切到
   production HTTP mode 真接 ContextForge
5. smoke 验证：`bash scripts/console_smoke.sh` 期望 `CONSOLE_REAL_SMOKE_EXIT=0`
6. ops curl 工作流: deprecate / soft-delete 需 X-Confirm header 或 ?confirm=true query

## Rollback path

按 ADR-017 §Rollback 8 条 + ADR-014 cross-validation gate evaluate:
- ADR-016 D1 SoT 反向 / D3 thin proxy 违反 → revert 单 PR
- ADR-017 D2 X-Confirm 412 误伤 → 扩 OR 语义到 User-Agent allowlist
- ADR-017 D3 cancel 204 破坏旧 client → handlers.go 回退 StatusOK
- EvalRunner goroutine 内存压力 → 限并发 N (sync.Semaphore) [SPEC-DEFER:phase-15.eval-concurrency-limit]

## Cross-repo follow-up (per playbook §E14)

v0.7.0 ship 后发送 cross-repo notification 给 ContextForge-Console 团队
(GitHub issue 或邮件，content: 「ContextForge v0.7.0 ship → 22-endpoint
conformance suite 全 PASS；Console UI 可切到 production HTTP mode 关闭
MockAdapter」+ 附 evidence doc URL + release URL).
