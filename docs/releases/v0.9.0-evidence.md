# ContextForge v0.9.0 Evidence — v0.9.0-backlog-completion (10/11 closed) + release infra

**日期**：2026-05-28
**主干**：`master`
**当前 HEAD**：`4c4f40b` (`chore(spec): Phase 17 + ADR-022 scaffolding — is-pinned-amendment (Pending) (#116)`)

**v0.9.0 tag SHA**：annotated tag pointing to master @ `889a6b5` (`feat(ci): release-verify workflow — GHA-based pull+run+health (Phase 16 E7) (#115)`) — verified via `git rev-parse v0.9.0^{}` = `889a6b579057fdd1b8fafdae93ae30600f7b819c`

**v0.9.0-rc1 tag SHA**：annotated tag pointing to master @ `41cba29` (`chore(spec): Phase 16 closeout — Status → Done + §10 + adapter sync (Phase 16 E6) (#114)`) — `git rev-parse v0.9.0-rc1^{}` = `41cba29a3f74904fb2554c2958c720eb1ec7c071`

## 合入记录 (Phase 16 + release infra + Phase 17 scaffolding)

- PR #108 `chore/phase-16-spec` → E1 spec PR. Phase 16 spec + 4 task specs (Ready); adapter §Phase 16 / §Tasks 16.1-16.4; PRD §Implementation Phases Phase 16 段; test/features/phase-16-v0.9.0-backlog-completion.feature scenarios; ADR-014 D2 lint 0 unannotated hits. (Phase 16 不引入新 ADR)
- PR #110 `feat/task-16.1-tracestore-sqlite-persistence` (merged `d4c0f27`): E2 task-16.1 P4 #10. SQLite migration 0015_search_traces.sql (5 cols + idx_search_traces_ts_desc) + new `core/src/data_plane/search_persist.rs::SqliteTracePersist` (open/put/get/list/load_warm) + `TraceStore` write-through redesign (hot cache LRU cap=1000 + SQLite SoT best-effort dual-write) + `SearchServer::new_with_persist` + serve_full integration + warm_restore on startup. cargo test --workspace full PASS (+5 search_persist unit tests + 1 search_persist_integration test).
- PR #111 `feat/task-16.2-events-real-long-poll` (merged `12d66df`): E3 task-16.2 P4 #11. `handleEvents` 真传 parseWaitParam → `deps.Events.Recent(limit, wait)` → grpcclient.eventsClient.Recent 两阶段长轮询 (phase 1 ctx.WithTimeout block 等首 event ≤ wait; phase 2 短 drainTimeout=100ms drain immediately-available events) + `MemStore.Recent(limit, wait)` fallback sleep min(wait, 1s) 模拟 + 4 grpcclient unit tests (test_recent_blocks_until_first_event / test_recent_drains_within_100ms / test_recent_handles_ctx_deadline_exceeded / test_recent_handles_recv_error) + ctx.DeadlineExceeded gRPC wrap fix + 2 handlers_test.go tests + e2e_grpc Step 11b real long-poll wait roundtrip. go test ./... 22 packages PASS.
- PR #112 `feat/task-16.3-ghcr-image-push-ci` (merged `2ecc347`): E4 task-16.3 P3 #8. New `.github/workflows/release.yml` (`v*` tag push + workflow_dispatch tag input → docker/setup-buildx-action@v3 + docker/login-action@v3 + docker/build-push-action@v5 → push ghcr.io/${owner}/contextforge-daemon:{tag} + :latest) + new `.github/workflows/ci.yml` (PR + push master → cargo-test + go-test + spec-lint 3 parallel jobs).
- PR #113 `feat/task-16.4-compose-production-example` (merged `61e07d7`): E5 task-16.4 P3 #9. `deploy/docker-compose.production.yml` 双服务 stack (contextforge-core bind 0.0.0.0:50551 + console-api-serve --grpc-addr=contextforge-core:50551, ADR-018 fallback deny 沿用, 卷 contextforge-data 持久化, healthcheck curl /v1/health) + `.env.production.example` (CONSOLE_API_AUTH_TOKEN / CONTEXTFORGE_LOG_LEVEL / CONTEXTFORGE_ALLOW_WILDCARD_BIND opt-in) + `docs/deploy/production.md` (9 sections: Quick start / image / data / health / auth / upgrade / k8s skeleton / troubleshooting / perf) + scripts/console_smoke.sh v7 27-step (24 + step 25 ?wait=2s + step 26 TraceStore restart + step 27 compose-prod gated) + Rust env opt-in `CONTEXTFORGE_ALLOW_WILDCARD_BIND=1` (PR review fix c21315b). cargo test core_skeleton 5 passed.
- PR #114 `chore/phase-16-closeout` (merged `41cba29`): E6 closeout. task/phase/adapter Status: Ready → Done + §10 Completion Notes; ADR-014 D2 lint 0 hits (no new ADR — 4 tasks all extend existing decisions).
- PR #115 `feat/task-16-release-verify` (merged `889a6b5`): E7 release-verify workflow. New `.github/workflows/verify-image.yml` (workflow_dispatch tag input → pull + run + /v1/health probe via curl + opt-in detailed=true 5-component breakdown for ADR-020 verification).
- v0.9.0-rc1 annotated tag (after PR #114 E6 closeout, before PR #115 E7): pointing to `41cba29`; verify-image.yml on v0.9.0-rc1 GHA run 26555768957 GREEN in 18s; release.yml on v0.9.0-rc1 push GHA run 26546571156 GREEN in 9m24s.
- v0.9.0 annotated tag (after PR #115 E7): pointing to `889a6b5`; release.yml on v0.9.0 push GHA run 26555811063 GREEN in 8m6s; verify-image.yml on v0.9.0 GHA run 26556137023 GREEN in 11s.
- PR #116 `chore/phase-17-adr-022-scaffolding` (merged `4c4f40b`, post-v0.9.0 tag): Phase 17 + ADR-022 + task-17.1 + adapter §Phase 17 / §Task 17.1 / §ADR 022 / §BDD + PRD §Phase 17 段 — Status: Pending (Phase 17 + task-17.1) / Proposed (ADR-022) await Console cross-repo trigger; **不属 v0.9.0 release scope** — ADR-022 attached to Phase 17 not v0.9.0。

## S2V 状态

- `docs/s2v-adapter.md`: Phase 16 `v0.9.0-backlog-completion` = `Done` (closeout PR #114 内同步)；Phase 17 `is-pinned-amendment` = `Pending` (scaffolding PR #116 内新增).
- `docs/s2v-adapter.md`: Task 16.1 / 16.2 / 16.3 / 16.4 全 `Done`; Task 17.1 `Pending`.
- 4 task spec Status = `Done` (Phase 16):
  - `task-16.1-tracestore-sqlite-persistence.md` (6/6 AC; cargo+go test baseline 不退化; AC1-AC3 fully verified via search_persist tests + integration test)
  - `task-16.2-events-real-long-poll.md` (4/4 AC; e2e_grpc Step 11b verified + 4 grpcclient unit tests)
  - `task-16.3-ghcr-image-push-ci.md` (6 AC; AC1-AC5 fully verified; AC3/AC4/AC6 runtime verify via release verify segment in this same goal flow)
  - `task-16.4-compose-production-example.md` (8 AC; AC5-AC8 fully verified; AC1-AC4 runtime verify via compose-prod up segment, gated COMPOSE_PROD_SMOKE=1)
- `phase-16-v0.9.0-backlog-completion.md` = `Done`, §6 AC1-6 全 [x] verified-by 显式, §8 DoD 9/10 [x] (剩 cross-repo notify 移至 user-forwarded outside autonomous flow).
- 1 task spec Status = `Pending` (Phase 17):
  - `task-17.1-memory-is-pinned-field.md` (8 AC; await cross-repo Console contractv1.go amend PR)
- `phase-17-is-pinned-amendment.md` = `Pending` (await cross-repo signal).
- `docs/decisions/adr-022-memory-is-pinned-field-amendment.md` = **Proposed** (PR #116 scaffold; Status: Proposed → Accepted promotion will happen via Phase 17 closeout PR).
- `docs/decisions/adr-014-cross-phase-exit-criteria-validation.md` = `Accepted` (v0.3/v0.4/v0.5/v0.6/v0.7/v0.8 六次激活后 **v0.9 第七次激活** — 制度稳定性跨 7 phase 验证通过).

## 验证证据

### 1. cargo test --workspace

```bash
cargo test --workspace
```

实测 (Phase 16 task ship 时记录)：
- full PASS (Phase 11-15 既有 + Phase 16 新增 task-16.1 search_persist tests + memory_persist_integration tests 不退化)
- task-16.1 增 5 search_persist unit tests (open / put_then_get_returns_trace / put_then_load_warm_restores_recent_1000 / get_returns_none_for_missing_key / list_orders_by_ts_desc) + 1 search_persist_integration test (`core/tests/search_persist_integration.rs::test_tracestore_persists_across_restart`)
- 0 failed, 0 ignored
- 1 既有 unused-import warning unrelated to Phase 16

### 2. go test ./...

```bash
go test ./...
```

实测 (Phase 16 task ship 时记录)：
- 22 packages PASS
- `internal/consoleapi`: 含 task-16.2 (2 handlers_test.go::TestHandleEvents_Wait5s_Blocks_When_NoEvent + TestHandleEvents_Returns_Early_OnEvent) + e2e_grpc Step 11b
- `internal/consoleapi/grpcclient`: 4 task-16.2 new tests (test_recent_blocks_until_first_event / test_recent_drains_within_100ms / test_recent_handles_ctx_deadline_exceeded / test_recent_handles_recv_error)
- `test/conformance`: 22-endpoint Console contract conformance 不退化

### 3. release.yml v0.9.0-rc1

```
GHA run: 26546571156
Status: completed / success
Duration: 9m24s
Trigger: push v0.9.0-rc1 tag
Workflow: Release — push image to GHCR
Artifacts: ghcr.io/tajiaoyezi/contextforge-daemon:v0.9.0-rc1 + :latest
```

### 4. verify-image.yml v0.9.0-rc1

```
GHA run: 26555768957
Status: completed / success
Duration: 18s
Trigger: workflow_dispatch (tag=v0.9.0-rc1)
Workflow: Verify release image (pull + run + /v1/health)
Steps: docker login ghcr.io → docker pull image:v0.9.0-rc1 → docker run -d -p ... → curl /v1/health (expect 200) → curl /v1/health?detailed=true (expect 5-component breakdown)
```

### 5. release.yml v0.9.0

```
GHA run: 26555811063
Status: completed / success
Duration: 8m6s
Trigger: push v0.9.0 tag at 889a6b5
Workflow: Release — push image to GHCR
Artifacts: ghcr.io/tajiaoyezi/contextforge-daemon:v0.9.0 + :latest
```

### 6. verify-image.yml v0.9.0

```
GHA run: 26556137023
Status: completed / success
Duration: 11s
Trigger: workflow_dispatch (tag=v0.9.0)
Workflow: Verify release image (pull + run + /v1/health)
Steps: docker login ghcr.io → docker pull image:v0.9.0 → docker run -d -p ... → curl /v1/health (expect 200) → curl /v1/health?detailed=true (expect 5-component breakdown)
```

### 7. scripts/console_smoke.sh v7 syntax verify

```bash
bash -n scripts/console_smoke.sh
```

实测：syntax OK；v7 27-step flow (既有 v6 24 + step 25 `?wait=2s` + step 26 TraceStore restart roundtrip + step 27 compose-prod gated COMPOSE_PROD_SMOKE=1)。

Daemon-level `CONSOLE_REAL_SMOKE_EXIT=0` 留 v0.9.0 ship 前 / E7 verify-image runtime-side 验证 — 本 release docs PR 不重跑 (per fake-evidence policy)。

### 8. ADR-014 D2 lint output (Phase 17 scaffolding PR #116)

```bash
bash scripts/spec_drift_lint.sh --touched origin/master
```

```
✅ spec_drift_lint --touched origin/master: 0 unannotated hits in changed lines
(PR #116 含 5 hits after initial commit; lint fix commit a0bacce reword / annotate
所有 5 hits ([SPEC-OWNER:task-17.1] / 重述 / 字符替换) → 最终 0 hits CI green)
```

### 9. Console PR #91/#93 backlog 10/11 closed mapping (累计 Phase 15 + Phase 16)

| Item | Backlog signal | Task | PR | Phase |
|---|---|---|---|---|
| P0 #1 | MemStore inmem-fallback 503 drill-down | task-15.1 | #99 | 15 |
| P0 #2 | `memory.*` event 桥接 缺失 | task-15.2 | #100 | 15 |
| P1 #3 | Dashboard "已索引块" 缺 backend | task-15.3 | #101 | 15 |
| P1 #4 | Eval 列表 缺 endpoint | task-15.4 | #102 | 15 |
| P1 #5 | Dashboard "最近查询" 缺 backend | task-15.5 | #103 | 15 |
| P2 #7 | CoreHealthCard 5 链路 缺 | task-15.6 | #104 | 15 |
| **P4 #10** | TraceStore daemon-restart 即丢历史 | task-16.1 | #110 | 16 |
| **P4 #11** | events `?wait=` 等价 batch polling | task-16.2 | #111 | 16 |
| **P3 #8** | ghcr.io image push 缺 CI/CD | task-16.3 | #112 | 16 |
| **P3 #9** | docker-compose.production.yml 缺示例 | task-16.4 | #113 | 16 |

Remaining (Phase 17):
| Item | Backlog signal | Task | PR | Status |
|---|---|---|---|---|
| P2 #6 | `MemoryItem.is_pinned` 字段缺失 | task-17.1 | scaffold #116 | Pending (await Console cross-repo trigger) |

### 10. Cross-repo onboarding (Console v0.9 standby readiness)

- Console UI 端可启动 standby PR：
  - Dashboard 历史查询面板验证跨 daemon 重启不丢历史 (P4 #10 visual closure)
  - Events stream UI 验证 real long-poll latency 提升 (P4 #11 visual closure)
  - Production deploy 切换 `docker pull ghcr.io/tajiaoyezi/contextforge-daemon:v0.9.0` (替代本地 docker build)
  - **Phase 17 启动信号**: Console 主仓 PR ship `internal/contractv1/contractv1.go::MemoryItem.IsPinned bool add-only field` merged → 用户人工转发 Console PR merge SHA → ContextForge 主 agent 启动 task-17.1 实施 (ADR-022 D5 trigger)
- Cross-repo coordinate: ContextForge → 用户转发 → Console 主 Agent

## 平台矩阵

| Platform | Build | Test |
|---|---|---|
| Linux x86_64 (Ubuntu 22.04) | source build OK + GHCR image OK | cargo + go test PASS (CI 实测) |
| WSL2 | source build OK | cargo + go test PASS |
| macOS (arm64) | source build OK (nice-to-have) | manual |
| Windows native | source build OK + go test PASS | cargo + go test PASS (本 ship 主开发平台) |

## Upgrade path (v0.8.0 → v0.9.0)

详 RELEASE_NOTES.md v0.9.0 §Upgrade path. 主要点：
- Console UI / SDK 客户端继续工作 (`Events.wait` semantic 仅 server-side 生效；client 透明)
- 二进制升级 v0.8.0 → v0.9.0 不破坏既有部署 (无 BREAKING)
- SQLite migration 0015 自动应用 (IF NOT EXISTS 幂等)
- Docker users: `docker pull ghcr.io/tajiaoyezi/contextforge-daemon:v0.9.0` (替代本地 docker build)

## Rollback path

若 v0.9.0 ship 后发现非预期问题：
1. `git revert <v0.9.0 merge SHA>` 回退到 v0.8.0 (master HEAD `622155b` 或 v0.8.0 tag)
2. ship v0.9.0.1 patch + 标具体 task 16.x Reverted
3. SQLite migration 0015 不撤回 (新表无 backward break — 既有 v0.8 binary 不读 search_traces 表)
4. 不撤回 v0.8.0 ADR-020 / ADR-021 / v0.7.2 ADR-018 / v0.7.0 ADR-017 (跨版本独立)

## Cross-repo follow-up

通知 Console 团队 ContextForge v0.9.0 release ship 完成：
- 4/5 P3+P4 backlog closed (累计 10/11 = 91%)
- ghcr.io image pull 替代本地 docker build；compose-prod 范例 ready
- TraceStore 持久化 + events real long-poll → Dashboard 历史查询 + events stream UX 提升

**Phase 17 启动信号**:
- 用户人工转发本 v0.9.0 release page → Console 主 Agent
- Console 主 Agent 启动 contractv1.go `MemoryItem.IsPinned bool` add-only field amend PR (ADR-022 D4 第 1 步)
- 完成 + 回报 → ContextForge 主 agent 验证 Console master HEAD 含字段 → Phase 17 + task-17.1 Status Pending → Ready → 启动实施 → v0.10 ship → 11/11 = 100% backlog closure

剩余 Console PR #91/#93 backlog 1 项 = P2 #6 `MemoryItem.is_pinned` 留 Phase 17 (scaffold ship 已完成 PR #116)。
