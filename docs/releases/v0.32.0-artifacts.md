# ContextForge v0.32.0 Artifacts — console-api-retrieval-signal-forward (console-api `?hybrid=true` REST forward + `hybrid_score` provenance + rerank `reason` visibility + ADR-044 ratified)

> Artifact inventory for the v0.32.0 minor release (Phase 39). Mirrors the v0.31.0 artifacts structure.
> The default build is unchanged: 0-backend-algorithm-change, 0-new-dependency, 0-migration；默认行为 / proto 既有字段号 / 既有契约不变（proto add-only 既有字段号 1-7 / 1-16 冻结，ADR-015 D1）。
> 兑现 ADR-025 `[SPEC-DEFER:phase-future.console-api-hybrid-forward]` honest-defer + 据实重界定 ADR-043 `[SPEC-DEFER:phase-future.console-api-rerank-forward]`——把**已存在于检索内核但对外 REST 不可达 / 不可见**的两个信号（hybrid BM25+vector RRF 融合 + rerank `reason` provenance）经 console_data_plane proto add-only + 数据面 dispatch + Go console-api 转发**首次贯通到对外 `POST /v1/search`**。**0 backend 算法改动「贯通而非重写」**：复用既有 `search_hybrid`（task-21.1）+ `reranker_from_env`（task-38.2）+ `?semantic` 转发范式（task-20.1）+ `vector_score` provenance 范式（task-32.3）；console_data_plane proto add-only `SearchRequest.hybrid=8`（镜像 `v1/search.proto:28`）+ `SearchResultItem.hybrid_score=17`（镜像 `v1 RetrievalResult.hybrid_score=15`），既有字段号 1-7 / 1-16 全冻结。**0 新依赖、0 migration、默认 `hybrid=false` 字节等价**（ADR-004/008/015），不设 `hybrid` → 走既有 semantic / BM25 路径逐字节等价。**诚实校正（ADR-013）**：reranker 保持服务端 env 驱动（`CONTEXTFORGE_RERANKER_PROVIDER`，ADR-043 D3），`?rerank=true` per-request 与 env 驱动冲突 → 记为 superseded、不实现，改交付 rerank `reason` provenance 对外 REST 可见。**与 remote embedding / reranker 不同**：hybrid 信号无外部付费 API 依赖（console 数据面 hybrid 分支用 model-free `DeterministicEmbeddingProvider` + 0-dep `BruteForceVectorBackend`，0 网络），故 hybrid dispatch + 转发由**每次 CI run 全程守护**（TEST-39.1.* / 39.2.* 入默认 `cargo test` / `go test`，无 honest-defer skip）。既有 22-endpoint + 5 Memory RPC + 运行时二进制默认行为不变。No breaking contract bump。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：新增 `contractv1.SearchRequest.Hybrid bool`（json `hybrid`，镜像 `Semantic`）+ `SearchResult.HybridScore float32`（json `hybrid_score`，镜像 `VectorScore`，cross-repo add-only signal ADR-014 D4）+ `handleSearch` `?hybrid` OR-merge（镜像 `?semantic`）+ `grpcclient.Search` 转发 `Hybrid` + `protoToSearchResult` 映射 `HybridScore`（`Reason` rerank provenance 既有映射保留）。对外 `POST /v1/search`（body `{"hybrid":true}` 或 `?hybrid=true`）首次贯通 hybrid。运行时既有 22-endpoint + 5 Memory RPC 行为不变；默认 `hybrid=false` → byte-equivalent 走既有 semantic / BM25 路径（向后兼容）；reranker 保持 env 驱动、不加 `?rerank`。
- `contextforge-core`（Rust 数据面 daemon）：console_data_plane proto add-only `SearchRequest.hybrid=8` + `SearchResultItem.hybrid_score=17`（`buf generate proto` 重生 `console_data_plane.pb.go`；既有字段号 1-7 / 1-16 冻结，ADR-015 D1）；`core/src/data_plane/search.rs` `query()` 三分支 dispatch `if req.hybrid {..} else if req.semantic {..} else {BM25}`，hybrid 分支镜像 `server.rs` hybrid 路径 + 数据面 semantic 分支（`DeterministicEmbeddingProvider` + hardcoded `BruteForceVectorBackend` + `search_hybrid` + `retrieval_method="hybrid"` + 复用 `reranker_from_env` opt-in）+ `hybrid_score` 填充（镜像 `vector_score` 条件）。`search_hybrid` / RRF 融合 / `reranker_from_env` backend 逻辑 0 改动（自 Phase 21 / 38 起未动）；默认 `hybrid=false` → byte-equivalent（默认控制流不变）；console 数据面 hybrid 分支 hardcoded `BruteForceVectorBackend`（env-factory backend 仅 `server.rs`，续延后 `[SPEC-DEFER:phase-future.console-data-plane-vector-backend-factory]`）。`cargo test -p contextforge-core --lib` = lib all PASS（默认路径不变，216 tests）。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.32.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network）+ cosign keyless 签 + SBOM + provenance（承 v0.21.0–v0.31.0）。linux/arm64 仍 DEFERRED。
- Feature 构建：本 release **0 新 feature flag**（hybrid 复用既有 `search_hybrid` / `reranker_from_env`，无 feature gate）；feature-build 矩阵 9/9 不退化。

## 2. 源码档

- master tag SHA：`<backfill: tag SHA>`（v0.32.0 annotated tag 对象 `<backfill: tag obj>`，用户授权下主 agent push（ADR-012））
- Phase 39 代码路径：
  - task-39.1（PR #252，MERGED；console-dataplane-hybrid-proto-and-dispatch）：`proto/contextforge/console_data_plane/v1/console_data_plane.proto`（add-only `SearchRequest.hybrid=8` + `SearchResultItem.hybrid_score=17`，既有字段号 1-7 / 1-16 冻结）+ `console_data_plane.pb.go`（`buf generate proto` 重生，rawDesc 重编码无 message/service 重排）+ `core/src/data_plane/search.rs`（`query()` hybrid dispatch 分支 + `hybrid_score` 填充）+ `core/tests/search_real_retriever.rs`（`test_dataplane_hybrid_dispatch`，TEST-39.1.1）+ `core/src/data_plane/search.rs` 同 crate test（`test_hybrid_proto_field_numbers`，TEST-39.1.2，prost wire tag）。镜像 `server.rs` hybrid 路径 + 数据面 semantic 分支结构；hybrid 分支 hardcoded `BruteForceVectorBackend`；`hybrid_score` 端到端携带非推断（ADR-013）。**0 新依赖 / 0 migration**。
  - task-39.2（PR #253，MERGED；console-api-hybrid-forward-and-rerank-visibility）：`internal/contractv1/contractv1.go`（add-only `SearchRequest.Hybrid` + `SearchResult.HybridScore`）+ `internal/consoleapi/handlers.go`（`handleSearch` `?hybrid` OR-merge）+ `internal/consoleapi/grpcclient/grpcclient.go`（`Search` 转发 `Hybrid` + `protoToSearchResult` 映射 `HybridScore`）+ `internal/consoleapi/search_semantic_test.go`（`TestTask392_HandleSearchHybridORMerge` + `TestTask392_HybridSemanticIndependent`，TEST-39.2.1）+ `internal/consoleapi/grpcclient/grpcclient_test.go`（`TestTask392_GrpcClient_Search_ForwardsHybrid` + `TestTask392_ProtoToSearchResult_CarriesHybridScoreAndReason`，TEST-39.2.2）+ `internal/contractv1/hybrid_field_test.go`（json round-trip）。reranker 保持 env 驱动、不加 `?rerank`（`?rerank=true` per-request superseded by ADR-043 D3）；rerank `reason` provenance 对外 REST 可见（`protoToSearchResult` `Reason` 映射）。**0 新 Go dep / 0 proto 再改**（消费 task-39.1 字段）。
  - task-39.3（this PR；closeout）：`scripts/console_smoke.sh`（smoke v29，banner 注 v28→v29，staging offset，step `[48/48]` REAL 模式 `?hybrid=true` 端到端断言）；`internal/cli/smoke_syntax_test.go`（`TestTask393` no-regression [37/37]..[47/47]）；`docs/releases/v0.32.0-{evidence,artifacts}.md`；`README.md` v0.32 段 + `:350` 措辞替换；`RELEASE_NOTES.md` v0.32.0 段；`docs/decisions/adr-044-...md`（ratify Proposed→Accepted，per-D）+ ADR-025 / ADR-043 add-only Phase-39 Amendment（标记 hybrid-forward fulfilled / rerank-forward 重界定 fulfilled + `?rerank` superseded，**不溯改 D-body**，ADR-014 D5）；`docs/specs/phases/phase-39-...md`（§6 闭合）；`docs/s2v-adapter.md`；`test/features/phase-39-...feature`；smoke + smoke_syntax_test defer marker 据实更新。

## 3. 验证脚本

```bash
# 默认构建（0 backend 算法改动 / 0 新依赖 / 0 network / 0 migration）
cargo test --workspace                                     # all PASS (core lib 216 + search_real_retriever 5)
go test ./...                                              # all pass (incl TestTask393 + TestTask392_*)
cargo clippy -p contextforge-core --all-targets           # 0 warning
go vet ./...                                               # clean
bash -n scripts/console_smoke.sh                           # exit 0

# task-39.1 — 数据面 hybrid dispatch + proto 字段号（无外部依赖，每次默认 test 守护）
cargo test -p contextforge-core --test search_real_retriever test_dataplane_hybrid_dispatch   # TEST-39.1.1
cargo test -p contextforge-core --lib test_hybrid_proto_field_numbers                          # TEST-39.1.2 (prost wire tag 0x40 / 0x8D 0x01)

# task-39.2 — Go console-api hybrid 转发 + rerank provenance 可见
go test ./internal/consoleapi/... -run TestTask392        # TEST-39.2.1 (?hybrid OR-merge) + TEST-39.2.2 (grpcclient forward/map + Reason)
go test ./internal/contractv1/... -run TestTask392        # Hybrid / HybridScore json round-trip

# proto add-only 重生（既有字段号冻结校验：diff 仅 hybrid / hybrid_score + rawDesc）
buf generate proto && git diff --stat proto/

# 端到端 smoke v29（step 48 → [48/48]；REAL 模式 ?hybrid=true → retrieval_method=hybrid；既有 step 不退化）
go test ./internal/cli/ -run TestTask393

# D5 spec-drift lint
bash scripts/spec_drift_lint.sh --touched origin/master   # 0 unannotated hits
```

## 4. 镜像推送

- `release.yml`（`v*` tag push 触发）→ buildx → ghcr login → build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.32.0` + `:latest`（**linux/amd64**，net-zero 与 v0.31.0 一致）→ cosign keyless 签 + attest SPDX SBOM + provenance:mode=max（承 task-28.2 机制）。
- run ID + digest：`release.yml` run `<backfill: run id>`（**`success`** 待回填，build-push + cosign sign + attest SBOM + provenance:mode=max 步全绿）；`ghcr.io/tajiaoyezi/contextforge-daemon:v0.32.0` + `:latest` = `<backfill: ghcr digest>`；git tag SHA `<backfill: tag SHA>`；cosign Rekor tlog idx `<backfill: tlog sign>`（sign）/ `<backfill: tlog attest>`（attest SBOM）。
- hybrid-forward evidence artifact：本 release 的 hybrid 贯通 + rerank provenance 可见证据为**本机 + CI 真实测试**（TEST-39.1.1 数据面 hybrid dispatch `retrieval_method="hybrid"` + `hybrid_score`>0 / TEST-39.1.2 proto 字段号 8 / 17 / TEST-39.2.1 `?hybrid` OR-merge / TEST-39.2.2 `grpcclient` 转发 `Hybrid` + 映射 `HybridScore` + rerank `Reason` / smoke v29[48/48] REAL 模式 `?hybrid=true` → `"retrieval_method":"hybrid"`）。**与 remote embedding / reranker 不同**：hybrid 无外部付费 API 依赖（console 数据面 hybrid 分支 model-free + 0-dep backend，0 网络），故由**每次 CI run 全程守护**，无 honest-defer skip。

## 5. cosign verify（by-digest）

```bash
# 解析 tag → digest，按 digest（非 tag）验签（承 v0.21.0 follow-up 教训：cosign verify 务必按 digest）
DIGEST=$(cosign triangulate --type digest ghcr.io/tajiaoyezi/contextforge-daemon:v0.32.0)
cosign verify \
  --certificate-identity-regexp 'https://github.com/tajiaoyezi/contextforge/.github/workflows/release.yml@.*' \
  --certificate-oidc-issuer https://token.actions.githubusercontent.com \
  ghcr.io/tajiaoyezi/contextforge-daemon@${DIGEST}
# 期望 digest = <backfill: ghcr digest>
```

## 6. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-044 console-api-retrieval-signal-forward | **Accepted**（per-D，D1 proto add-only + 数据面 hybrid dispatch 🟢 + D2 Go console-api hybrid 转发 + rerank provenance 可见 🟢 + D3 rerank-forward 重界定 🟢 + D4 默认字节等价 + 0 dep 🟢） | task-39.3 据 39.1/39.2 真实非合成验证 ratify：D1 Accepted（console_data_plane proto add-only `hybrid=8` / `hybrid_score=17` 既有字段号 1-7 / 1-16 冻结 + `query()` 三分支 dispatch + `hybrid_score` 填充镜像 `vector_score`，TEST-39.1.1 数据面 hybrid dispatch + TEST-39.1.2 proto 字段号 via prost wire tag）/ D2 Accepted（`contractv1.SearchRequest.Hybrid` + `SearchResult.HybridScore` add-only + `handleSearch` `?hybrid` OR-merge + `grpcclient` 转发/映射，对外 `POST /v1/search` 贯通 hybrid + rerank `reason` 可见，TEST-39.2.1 + TEST-39.2.2）/ D3 Accepted（reranker 保持 env 驱动、`?rerank=true` per-request superseded by ADR-043 D3 不实现、改交付 rerank provenance 可见性，据实记录不矛盾实现 ADR-013）/ D4 default hybrid=false / reranker unset 字节等价 + 0 新 dep + proto add-only 既有契约不变（既有字段号冻结，ADR-015 D1）|
| ADR-025 hybrid-scoring-fusion（console-api-hybrid-forward defer） | Accepted（+ Phase 39 add-only Amendment：console-api-hybrid-forward fulfilled） | task-39.3 add-only 标记 ADR-025 `[SPEC-DEFER:phase-future.console-api-hybrid-forward]` 已兑现（hybrid 经 console_data_plane proto + 数据面 dispatch + Go 转发贯通对外 REST），**不溯改 D-body / Ratification (v0.14.0)**（ADR-014 D5）；hybrid 融合质量本 ADR 已 ratify（top-1 0.6667 / MRR 0.7881），Phase 39 是对外可达性贯通、非重测质量 |
| ADR-043 embedding-remote-reranker-live（console-api-rerank-forward defer） | Accepted（+ Phase 39 add-only Amendment：console-api-rerank-forward 重界定 fulfilled + `?rerank` per-request superseded） | task-39.3 add-only 标记 `console-api-rerank-forward` 按 provenance-visibility 口径 fulfilled（rerank `reason` 对外 REST 可见）+ `?rerank=true` per-request superseded by D3（reranker 服务端 env 驱动、非 per-request），**不溯改 ADR-043 D-body / Ratification (v0.31.0)**（ADR-014 D5）|
| ADR-013 据实延后/校正 | Accepted（守线） | rerank-forward 重界定据实记录（`?rerank` per-request superseded、不夸大为已实现 per-request）；`hybrid_score` 端到端携带、非由 score 推断（hybrid 命中 `=h.score`、非 hybrid `=0.0`）；console 数据面 hybrid 分支 hardcoded backend 据实记延后 `[SPEC-DEFER:phase-future.console-data-plane-vector-backend-factory]`；大语料 hybrid / rerank 对外 REST 召回质量基准延后 `[SPEC-DEFER:phase-future.vector-large-corpus-perf]`；tag SHA / run id / digest / tlog 未知前以 `<backfill>` 占位不伪造凭据 |
| ADR-014 add-only / cross-validation | Accepted（守线，第三十次激活） | console_data_plane proto add-only（既有字段号 1-7 / 1-16 冻结）+ 数据面 hybrid dispatch 分支 + Go console-api add-only（默认 hybrid=false byte-equivalent / 0 backend 算法改动 / 0 migration）+ ADR-025 / ADR-043 Amendment 不溯改正文（D5）|
| ADR-004 / ADR-008 / ADR-015 / ADR-012 | Accepted（守线） | 默认 `hybrid=false` 字节等价 + reranker unset 字节等价（ADR-004）；0 新依赖（ADR-008，复用 `search_hybrid` / `reranker_from_env` / `buf` 工具链）；proto add-only 既有字段号冻结（ADR-015 D1，既有 client 兼容）；tag-release 须授权（本轮经用户显式授权 ADR-012）|

## 7. 版本声明（contract bump 评估）

- 本 release 新增 console_data_plane proto add-only `SearchRequest.hybrid=8` + `SearchResultItem.hybrid_score=17`（既有字段号 1-7 / 1-16 冻结）+ 数据面 hybrid dispatch 分支 + Go `contractv1.SearchRequest.Hybrid` / `SearchResult.HybridScore` + `handleSearch` `?hybrid` OR-merge + `grpcclient` 转发/映射 + smoke v29——**proto 既有字段号全冻结（add-only 新字段 8 / 17）、无 RPC / endpoint 删改、无默认运行时行为改动（默认 `hybrid=false` → byte-equivalent 走既有 semantic / BM25）、0 migration**（无 feature gate，`search_hybrid` / `reranker_from_env` 复用既有）。
- 默认构建 0 backend 算法改动 / 0 新代码依赖 / 0 network（ADR-004 / ADR-008，hybrid 分支 model-free + 0-dep backend）；reranker 保持 env 驱动、不加 `?rerank`（per-request superseded）。
- 故 **无 breaking contract bump**；既有 v0.6–v0.31 client + 既有索引/数据无需改动（proto3 未知字段忽略、contractv1 add-only legacy-absent defaults false / 0）。**console-api-hybrid-forward 收口**（ADR-025 honest-defer 兑现）+ **console-api-rerank-forward 重界定**（ADR-043 D3 据实承接）。

## 8. 后续模板（console-api-retrieval-signal-forward follow-on）

- console 数据面 hybrid 分支接入 env-factory vector backend（统一 `server.rs` 与 console 数据面的 backend 选型，消除既有 asymmetry）`[SPEC-DEFER:phase-future.console-data-plane-vector-backend-factory]`。
- 大语料 hybrid / rerank 对外 REST 召回质量基准（NDCG / MRR 大基准，超 dogfood 小语料）`[SPEC-DEFER:phase-future.vector-large-corpus-perf]`。
- Console UI hybrid / rerank explain 面板（融合分可视化 / rerank 前后排序对比 UI）`[SPEC-OWNER:phase-future.console-semantic-explain]`（跨仓库 Console 领域）。
- linux/arm64 原生 runner multi-arch `[SPEC-DEFER:phase-future.multi-arch-native-runner]`。

## 9. 存档指引

- closeout PR 合入后，**用户授权下**主 agent push `v0.32.0` annotated tag 触发 `release.yml`；post-tag-push backfill PR 填实 tag SHA + run ID + 镜像 digest + cosign 凭据（承 v0.8–v0.31 pattern；§2/§4/§5 角括号 `<backfill: ...>` 标记由本 backfill PR 回填）。
- Dockerfile workspace member COPY 完整性预检（承 v0.11.0 PR #139 教训）：本 release **无新 migration、proto add-only（既有字段号冻结）**——改动为 console_data_plane proto add-only 2 字段（`buf generate` 重生既有 `console_data_plane.pb.go`）+ `core/src/data_plane/search.rs` hybrid dispatch（既有 `core/` member）+ Go console-api add-only（既有 member）+ smoke/test/docs，无新依赖 / 无新 crate member；镜像为默认构建（0 新依赖 / 0 network，linux/amd64）。
