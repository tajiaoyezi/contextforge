# ContextForge v0.34.0 Artifacts — tokenizer-default-on (code/CJK tokenizer `code_cjk` 翻为新建 collection 生产默认 `resolve_tokenizer()` + Go `[retrieval] tokenizer` config 桥 `setTokenizerEnv` + ADR-046 ratified)

> Artifact inventory for the v0.34.0 minor release (Phase 41). Mirrors the v0.33.0 artifacts structure.
> 默认构建 0 新依赖（`code_cjk` 纯 std；jieba `cjk_segmenter` 仍 feature-gated）/ 0 network；**首次刻意默认行为变更**（新建 collection `content` 倒排词项 `TEXT`→`code_cjk`，**非 byte-equivalent**）由 ADR-046 承接 + 三重安全（既有 collection 经 `open_in_dir` 不受影响 / `CONTEXTFORGE_TOKENIZER=default`·`[retrieval]` opt-out 回 legacy `TEXT` byte-equiv / 不自动迁移用户数据，经既有 `reindex_with_tokenizer` 主动升级）。
> 兑现 ADR-029:54 + ADR-035 D3 `[SPEC-DEFER:phase-future.tokenizer-default-on]`——做出 Phase 30 / ADR-035 §D3 据「翻默认是产品决策」诚实延后的产品决策。**诚实校正（ADR-013）**：实测 recall delta +0.1250 recall@5/@10（current 16-题 golden，default 0.8750 → code_cjk 1.0000；Phase 24 原始 11-题 golden 为 +0.0909）；jieba `cjk_segmenter` 默认不取（0-dep baseline + Phase 30 实测 vs bigram delta=+0.0000）。**翻默认 + config 桥无外部依赖**（Rust env-resolution + 本地索引/检索 + Go config→env wire，0 网络），故 TEST-41.1.* / 41.2.* 由**每次 CI run 全程守护**（无 honest-defer skip）。既有 22-endpoint + 运行时二进制对外契约不变。No breaking contract bump（proto / REST 零 delta；改动是索引 analyzer 默认绑定 + add-only config 段）。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：`internal/config/config.go` add-only `RetrievalConfig{Tokenizer string}` + `Config.Retrieval` + `[retrieval]` 段 encode/decode round-trip（镜像 `VectorConfig`/`[vector]`）+ `assignRetrieval`；`cmd/contextforge/main.go` add `setTokenizerEnv`（镜像 `setVectorEnv`：`[retrieval] tokenizer` 非空且 `CONTEXTFORGE_TOKENIZER` 未设 → 导出，env-wins、无段不导出、missing config 静默 / 真 parse-err stderr WARN）接线 doServe + doMCP。tokenizer 非密钥（无 api-key 字段）。运行时既有 22-endpoint + REST 契约不变；无 `[retrieval]` 段 → 不导出 → core 默认 code_cjk（翻默认在 Rust 默认生效）。
- `contextforge-core`（Rust 数据面 daemon）：`core/src/server.rs` add `resolve_tokenizer()`（pub fn）+ `parse_tokenizer(Option<&str>)`（pub(crate) 纯函数）：unset/""→`code_cjk` 翻默认 / `"default"`→`DEFAULT_TOKENIZER` opt-out 回 legacy `TEXT` / `"cjk_segmenter"`→feature 在则 jieba·缺则 WARN+code_cjk / unknown→WARN+code_cjk（不静默落 TEXT）；`CoreService::index` RPC（`server.rs:141`）+ `jobs/index_session_backend.rs:151` 由 `IndexSession::open(..)` 改 `open_with_tokenizer(.., &resolve_tokenizer())`。`IndexSession::open`（库便捷入口，`indexer/mod.rs:502`）+ `DEFAULT_TOKENIZER`（常量，`:183`）+ `open_with_tokenizer`（`:511`，create-vs-open meta.json 分支）+ `reindex_with_tokenizer`（`:920`，迁移工具）backend 逻辑 0 改动；既有 collection 经 `open_in_dir` 读回持久化 schema 不受影响。`cargo test -p contextforge-core --lib` = 222 PASS（220 baseline + TEST-41.1.1 + TEST-41.1.2）。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.34.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network）+ cosign keyless 签 + SBOM + provenance（承 v0.21.0–v0.33.0）。linux/arm64 仍 DEFERRED。
- Feature 构建：本 release **0 新 feature flag**（jieba `cjk_segmenter` 自 Phase 30 已存）；feature-build 矩阵 10/10 不退化。

## 2. 源码档

- master tag SHA：`aa36c2c9eff20f6fb2bda5fd06ce784b36372c39`（v0.34.0 annotated tag 对象 `ec5b2209a42831f85b4d54c01da4ad170650b85a`，用户**另行授权**下主 agent push（ADR-012））
- Phase 41 代码路径：
  - task-41.1（PR #262，MERGED；tokenizer-default-on）：`core/src/server.rs`（add `resolve_tokenizer()` + `parse_tokenizer()` + import `CODE_CJK_TOKENIZER`/`DEFAULT_TOKENIZER`/`CJK_SEGMENTER_TOKENIZER` + `CoreService::index` :141 改 `open_with_tokenizer(.., &resolve_tokenizer())` + in-crate TEST-41.1.1）+ `core/src/jobs/index_session_backend.rs`（:151 改 `open_with_tokenizer(.., &crate::server::resolve_tokenizer())`）+ `core/src/indexer/mod.rs`（in-crate TEST-41.1.2 生产路径绑定 + 既有 collection 安全，用 `content_tokenizer_name` + `parse_tokenizer`）。`IndexSession::open` / `DEFAULT_TOKENIZER` 0 改动。首次刻意默认变更非 byte-equiv 由 ADR-046 D1/D4 承接。**0 新依赖（`code_cjk` 纯 std）/ 既有 collection 安全**。
  - task-41.2（PR #263，MERGED；tokenizer-config-bridge）：`internal/config/config.go`（add-only `RetrievalConfig{Tokenizer}` + `Config.Retrieval` + `[retrieval]` encode/decode + `assignRetrieval`）+ `cmd/contextforge/main.go`（add `setTokenizerEnv` + doServe/doMCP 接线）+ `internal/config/config_test.go`（TEST-41.2.1 round-trip）+ `cmd/contextforge/main_test.go`（TEST-41.2.2 env-bridge）。tokenizer 非密钥；Rust core 0 toml dep。**Rust core 0 toml dep / 无段默认 code_cjk**。
  - task-41.3（this PR；closeout）：`scripts/console_smoke.sh`（smoke v31，step `[50/50]` REAL camel 子词 `runner`(of JobRunner) 经 code_cjk 默认命中 distinguishing）；`internal/cli/smoke_syntax_test.go`（`TestTask413` no-regression [37/37]..[49/49]）；`docs/releases/v0.34.0-{evidence,artifacts}.md`；`README.md` v0.34 段；`RELEASE_NOTES.md` v0.34.0 段；`docs/decisions/adr-046-...md`（ratify Proposed→Accepted，per-D）+ ADR-029 / ADR-035 add-only Phase-41 Amendment（标默认开启维度 / D3 产品决策 fulfilled，**不溯改 D-body**，ADR-014 D5）；`docs/specs/phases/phase-41-...md`（§6 闭合）；`docs/s2v-adapter.md`；`test/features/phase-41-...feature`；`docs/roadmap.md`（§3.23 推进记录 + §4 add-only）。

## 3. 验证脚本

```bash
# 默认构建（0 新依赖 / 0 network / code_cjk 纯 std）
cargo test --workspace                                     # all PASS (core lib 222 = 220 baseline + 2 tokenizer tests)
go test ./...                                              # all pass (incl TestTask413 + TestTask412RetrievalConfig + TestSetTokenizerEnv)
cargo clippy -p contextforge-core --all-targets -- -D warnings  # 0 warning
go vet ./...                                               # clean
bash -n scripts/console_smoke.sh                           # exit 0

# task-41.1 — tokenizer 翻默认 + env opt-out（无外部依赖，每次默认 test 守护）
cargo test -p contextforge-core --lib test_41_1           # TEST-41.1.1 (parse_tokenizer env matrix) + TEST-41.1.2 (production binds code_cjk + opt-out TEXT + existing-collection safe)

# task-41.1 — 实测真实 recall delta（当前 16-题 golden）
cargo run -p contextforge-core --example phase24_tokenizer_recall  # before(default) 0.8750 -> after(code_cjk) 1.0000, delta recall@5/@10=+0.1250 mrr=+0.0625

# task-41.2 — Go [retrieval] config 桥（无外部依赖，每次默认 test 守护）
go test ./internal/config/... -run TestTask412RetrievalConfig   # TEST-41.2.1 ([retrieval] tokenizer round-trip + 既有段不受影响 + 向后兼容)
go test ./cmd/contextforge/... -run TestSetTokenizerEnv         # TEST-41.2.2 (env-wins / 无段不导出 / 非空导出)

# 端到端 smoke v31（step 50 → [50/50]；REAL 模式 camel 子词 runner 经 code_cjk 命中；既有 step 不退化）
go test ./internal/cli/ -run TestTask413

# D5 spec-drift lint
bash scripts/spec_drift_lint.sh --touched origin/master   # 0 unannotated hits
```

## 4. 镜像推送

- `release.yml`（`v*` tag push 触发）→ buildx → ghcr login → build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.34.0` + `:latest`（**linux/amd64**，net-zero 与 v0.33.0 一致）→ cosign keyless 签 + attest SPDX SBOM + provenance:mode=max（承 task-28.2 机制）。
- run ID + digest：`release.yml` run `27083795263`（conclusion `success`，build-push + cosign sign + attest SBOM + provenance:mode=max 步全绿）；`ghcr.io/tajiaoyezi/contextforge-daemon:v0.34.0` + `:latest` = `sha256:39b2db3b657ef2c89f75efe08ac10bef6a5585adb3f089670f5c46947ca3e49d`；git tag SHA `aa36c2c9eff20f6fb2bda5fd06ce784b36372c39`；cosign Rekor tlog idx `1742901000`（sign）/ `1742902913`（attest SBOM）。
- tokenizer-default-on evidence artifact：本 release 的翻默认 + config 桥证据为**本机 + CI 真实测试**（TEST-41.1.1 parse_tokenizer env 矩阵 / TEST-41.1.2 生产路径绑定 code_cjk + opt-out TEXT + 既有 collection 安全 / 实测 recall delta +0.1250 / TEST-41.2.1 `[retrieval]` round-trip / TEST-41.2.2 setTokenizerEnv env-wins / smoke v31[50/50] REAL camel 子词 `runner` 经 code_cjk 命中 distinguishing）。**无外部付费 API 依赖**（Rust env-resolution + 本地索引 + Go config→env，0 网络），故由**每次 CI run 全程守护**，无 honest-defer skip。

## 5. cosign verify（by-digest）

```bash
# 解析 tag → digest，按 digest（非 tag）验签（承 v0.21.0 follow-up 教训：cosign verify 务必按 digest）
DIGEST=$(cosign triangulate --type digest ghcr.io/tajiaoyezi/contextforge-daemon:v0.34.0)
cosign verify \
  --certificate-identity-regexp 'https://github.com/tajiaoyezi/contextforge/.github/workflows/release.yml@.*' \
  --certificate-oidc-issuer https://token.actions.githubusercontent.com \
  ghcr.io/tajiaoyezi/contextforge-daemon@${DIGEST}
# 期望 digest = sha256:39b2db3b657ef2c89f75efe08ac10bef6a5585adb3f089670f5c46947ca3e49d
```

## 6. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-046 tokenizer-default-on | **Accepted**（per-D，D1 production 默认翻 code_cjk 🟢/🟡 + D2 env opt-out + Go config 桥 🟢 + D3 recall delta 复测 + honest-defer 边界 🟡/🟢 + D4 首次刻意默认变更承接 + 0-dep/0-network + opt-out byte-equiv 🟢） | task-41.3 据 41.1/41.2 真实非合成验证 ratify：D1 Accepted（`resolve_tokenizer` env-resolution + 生产两调用点接线 + 既有 collection schema-driven 安全 + 实测 recall delta +0.1250，TEST-41.1.1/41.1.2）/ D2 Accepted（`CONTEXTFORGE_TOKENIZER` env opt-out + Go `[retrieval]` config 桥 env-wins 无段默认 code_cjk Rust 0 toml dep，TEST-41.2.1/41.2.2）/ D3 Accepted（recall delta +0.1250 小 golden caveat 大语料续 SPEC-DEFER + jieba 默认不取 + 既有 collection 不自动迁移 honest-defer）/ D4 Accepted（首次刻意默认变更由本 ADR 承接 + 0 新 dep + 0 network + opt-out 回 legacy byte-equiv + 既有 collection 不受影响）|
| ADR-029 code-and-cjk-tokenizer-and-eval-hardening（tokenizer-default-on defer） | Accepted（+ Phase 41 add-only Amendment：默认开启维度 fulfilled） | task-41.3 add-only 标记 ADR-029:54 `[SPEC-DEFER:phase-future.tokenizer-default-on]` 默认开启维度已兑现（`code_cjk` 翻新建 collection 生产默认 + opt-out + 既有 collection 安全），**不溯改 D-body / §Negative / Ratification (v0.17.0)**（ADR-014 D5）|
| ADR-035 cjk-true-segmenter-and-tokenizer-default（D3 default flip defer） | Accepted（+ Phase 41 add-only Amendment：D3 产品决策 fulfilled） | task-41.3 add-only 标记 ADR-035 D3 `[SPEC-DEFER:phase-future.tokenizer-default-on]` full default flip 产品决策已做出（默认翻 `code_cjk`，jieba `cjk_segmenter` 仍 feature opt-in）；`RetrieverConfig.tokenizer` 仍按 D3 方案 B schema-driven 对称（vestigial 状态不改，续 `[SPEC-DEFER:phase-future.retriever-config-tokenizer-routing]`），**不溯改 D-body / D5 / Ratification (v0.23.0)**（ADR-014 D5）|
| ADR-004 local-first-privacy-baseline（刻意默认变更例外） | Accepted（守线引用，刻意例外由 ADR-046 承接） | 本 phase 首次刻意改默认行为（新建 collection `TEXT`→`code_cjk` 非 byte-equiv）由 ADR-046 显式承接；ADR-004「opt-out 后 legacy byte-equiv + 不自动迁移用户数据」safety intent 据实保持，不溯改 ADR-004 正文 |
| ADR-013 据实延后/校正 | Accepted（守线） | 首次刻意默认变更据实定性非夸大 byte-equiv；recall delta +0.1250 据当前 golden 实测、不沿用 Phase 24 旧 +0.0909、不预填；jieba 默认不取据实（0-dep + Phase 30 delta=0）；既有 collection 不自动迁移据实；tag SHA / run id / digest / tlog 未知前以 `<backfill>` 占位不伪造凭据 |
| ADR-008 / ADR-012 / ADR-014 / ADR-015 | Accepted（守线，ADR-014 第三十二次激活） | 0 新依赖（`code_cjk` 纯 std；jieba feature-gated 不进默认构建，ADR-008）；实现经用户「继续实现」授权、tag-release 须另行授权（ADR-012）；`resolve_tokenizer` 新增 + Go `[retrieval]` add-only + ADR-029/035 Amendment 不溯改正文（ADR-014 D5）；proto / REST 零 delta（无 ADR-015 契约改动）|

## 7. 版本声明（contract bump 评估）

- 本 release 新增 `core/src/server.rs` `resolve_tokenizer()` env-resolution + 生产索引两调用点接线 + Go `[retrieval] tokenizer` add-only config 段 + `setTokenizerEnv` env 桥 + smoke v31——**proto / REST / RPC / endpoint 零 delta**（改动是索引 analyzer 默认绑定 + add-only config 段，对外契约不变）。**首次刻意默认行为变更**：新建 collection `content` 倒排词项 `TEXT`→`code_cjk`（**非 byte-equivalent**，由 ADR-046 承接）；既有 collection 经 `open_in_dir` 不受影响；`CONTEXTFORGE_TOKENIZER=default`/`[retrieval]` opt-out 回 legacy `TEXT`；0 schema migration（不改 chunks / Tantivy schema 字段集，仅 analyzer 绑定）。
- 默认构建 0 新代码依赖 / 0 network（ADR-004 默认行为例外由 ADR-046 承接 / ADR-008，`code_cjk` 纯 std；jieba `cjk_segmenter` 仍 feature-gated）。
- 故 **无 breaking contract bump**（proto/REST 零 delta、Go `[retrieval]` add-only 段向后兼容、既有 collection 不受影响）；既有 v0.6–v0.33 client 无需改动。**tokenizer-default-on 收口**（ADR-029/035 default flip defer 兑现）；用户可经 `CONTEXTFORGE_TOKENIZER=default` opt-out 回 v0.33.0 行为。

## 8. 后续模板（tokenizer-default-on follow-on）

- jieba 真分词 `cjk_segmenter` 默认开启（重词典 dep + Phase 30 实测无增益）`[SPEC-DEFER:phase-future.cjk-segmenter-default-on]`。
- 既有 collection 升级时自动 reindex 到 `code_cjk`（不自动改用户数据，用户经 `reindex_with_tokenizer` 主动）`[SPEC-DEFER:phase-future.tokenizer-auto-reindex-on-upgrade]`。
- 大语料 tokenizer recall 基准（超小 golden）`[SPEC-DEFER:phase-future.tokenizer-large-corpus-recall]`；`RetrieverConfig.tokenizer` vestigial 字段真路由（ADR-035 D3 schema-driven 对称）`[SPEC-DEFER:phase-future.retriever-config-tokenizer-routing]`。
- 仍真延后不强行扩面：vector backend dim 强校验 `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]`（须 feature build）/ chunk source_type/agent_scope 过滤 `[SPEC-DEFER:phase-future.chunk-source-type-filter]` + `[SPEC-DEFER:phase-future.chunk-agent-scope-filter]`（须 import-path schema migration）；linux/arm64 原生 runner multi-arch `[SPEC-DEFER:phase-future.multi-arch-native-runner]`。

## 9. 存档指引

- closeout PR 合入后，**用户另行授权下**主 agent push `v0.34.0` annotated tag 触发 `release.yml`；post-tag-push backfill PR 填实 tag SHA + run ID + 镜像 digest + cosign 凭据（承 v0.8–v0.33 pattern；§2/§4/§5/§6 角括号 `<backfill: ...>` 标记由本 backfill PR 回填）。
- Dockerfile workspace member COPY 完整性预检（承 v0.11.0 PR #139 教训）：本 release **无新 migration、proto / REST 零 delta**——改动为 `core/src/server.rs` + `core/src/jobs/index_session_backend.rs`（既有 `core/` member）+ Go config / main / console-api（既有 member）add-only + smoke/test/docs，无新依赖 / 无新 crate member；镜像为默认构建（0 新依赖 / 0 network，linux/amd64）。
