# ContextForge v0.12.0 Evidence — vector-retrieval-integration (end-to-end semantic search + ADR-023 ratified)

> v0.12.0 ships Phase 19: the Phase 18 vector-backend infrastructure becomes a **live, end-to-end
> semantic retrieval path**, and ADR-023 is **ratified on real-embedding recall** (ADR-013: real
> `FastEmbedProvider` run, no synthetic / fabricated figures). Default retrieval stays BM25; the
> semantic path is opt-in; the default build remains dependency-free.

## 1. 合入记录 (Phase 19, tasks 19.1–19.7)

| task | PR | delivery |
|---|---|---|
| scaffolding | #141 | Phase 19 spec + 7 task specs |
| 19.1 | #142 | `EmbeddingProvider` trait + `DeterministicEmbeddingProvider` (0-dep default) + `FastEmbedProvider` (feature-gated `embedding-fastembed`) + spike evidence |
| 19.2 | #143 | default backend wired into `Retriever` (`with_embedder` + `with_vector_searcher` + `index_chunks_semantic` + `search_semantic`) |
| 19.3 | #144 | `/v1/search?semantic=true` Go→Rust gRPC semantic path + proto add-only (`semantic`/`vector_score`/`embedding_provider`) + 0-dep `BruteForceVectorBackend` |
| 19.4 | #145 | smoke v9 30-step (step 29 semantic REST + step 30 eval `--semantic`) + `contextforge eval run --semantic` dual-path CLI |
| 19.5 | #146 | real dogfood embedding `SemanticRecall@K` (fastembed) + `docs/spikes/phase-19-real-recall.md` |
| 19.6 | #147 | ADR-023 Proposed→Accepted + ADR-006 A1→Active + ADR-008 embedding-crate amendment |
| 19.7 | this PR | Phase 19 closeout + v0.12.0 release docs |

## 2. S2V 状态

- Phase 19 `vector-retrieval-integration`: Draft → **Done**, Tasks 0 → 7。
- phase-19 §6 AC1-6 全 `[x]`（据 19.1–19.6 真实交付；AC5 ratify 据真实 recall，非合成，ADR-013）。
- Phase 18 §6 AC3（ADR-023 ratify）/ AC4（生产向量检索集成）在 Phase 19 解决——记录于 ADR-023 Amendment + task-19.6 §10，**不溯改 Phase 18 spec**（ADR-014 D5）。

## 3. 验证证据

### 1. 真实 SemanticRecall@K（task-19.5, real `FastEmbedProvider` all-MiniLM-L6-v2 dim 384, exact cosine, WSL2 Ubuntu 26.04 / rustc 1.96.0）

| 语料 | recall@5 | recall@10 | top-1 | MRR | gate (≥0.70) |
|---|---|---|---|---|---|
| Phase 18 synthetic seed | 1.0 (non-discriminating) | 1.0 (non-discriminating) | — | — | aspirational |
| **Phase 19 real all-MiniLM-L6-v2** | **0.8333** (25/30) | **0.9333** (28/30) | 0.60 | 0.70 | **PASS** |

per-category: config / error / historical / log = 1.0；code-location @5=0.6/@10=0.8；agent-memory-rule @5=0.4/@10=0.8。数据源 + 方法 + ADR-013 数据源声明见 `docs/spikes/phase-19-real-recall.md`；复跑 `cargo run -p contextforge-core --example phase19_real_recall --features embedding-fastembed`。

### 2. cargo test --workspace（默认 feature；各 PR CI 实测）

默认构建不启用 `embedding-fastembed`/`vector-*` feature（example 编为 no-op 空 main，0 新 dep）。各 task PR CI cargo-test 三门绿后自主合入；`bench` 新增 `test_19_5_real_dogfood_fixture_format`（fixture dim-384 校验）PASS。

### 3. go test ./...（各 PR CI 实测）

`internal/cli` 新增 `TestTask194_AC1/AC2_AC3/AC4`（eval `--semantic` 双路 + smoke v9 语法）+ `internal/daemon` `TestTask193_SemanticQueryParamSetsFlag` + 既有 `TestTask81` 不退化，全 PASS。

### 3b. smoke v9（诚实记录）

- **脚本正确性**：`bash -n scripts/console_smoke.sh` exit 0（CI go-test `TestTask194_AC4_SmokeV9SyntaxAndSteps`）+ step header 全量迁 `[N/28]→[N/30]` + step 29（`/v1/search?semantic=true` 合约保形）/ step 30（`eval run --semantic` 双路）标号在库（grep `[29/30]`/`[30/30]` ≥ 2，无残留 `/28]`）。
- **新增 step 29/30 行为验证**：经 task-19.4 单测——`TestTask194_AC2_AC3`（eval `--semantic` 60-call 双路 + gate 行 + gate-fail 仍 exit 0）+ task-19.3 `TestTask193_SemanticQueryParamSetsFlag`（REST `?semantic=true` 置位）；ADR-013 step 30 仅断言双路报告成形不预判召回。
- **端到端 REAL run**：在合规 Linux host（committed 脚本为 LF）`bash scripts/console_smoke.sh` 跑至 `CONSOLE_REAL_SMOKE_EXIT=0`。本地 WSL 复跑：step 1–25 + 迁号 `[21/30]…[26/30]` REAL 全跑通，但**既有 step 26**（task-16.1 `kill -9` core + restart roundtrip，v0.9.0 落地，非 Phase 19）在非交互 WSL bash 下于重启后停住（exit 0，无 `FAIL:` 断言触发——重启/job-control 时序 quirk，非 Phase 19 回归），未推进到 step 29/30。故新 step 29/30 以上述单测 + `bash -n` + 标号为准验证；完整 daemon-restart REAL marker 由合规 Linux 环境 / release smoke 复跑定（承 task-19.4 §10 "REAL smoke 由 CI/Linux 复跑定稿"）。

### 4. CI 状态（各 task PR 三门绿后自主合入）

| PR | cargo-test | go-test | spec-lint |
|---|---|---|---|
| #142 | PASS | PASS | PASS |
| #143 | PASS | PASS | PASS |
| #144 | PASS (phase9 LockBusy flake re-run → green) | PASS | PASS |
| #145 | PASS (phase9 LockBusy flake re-run → green) | PASS | PASS |
| #146 | PASS (phase9 LockBusy flake re-run → green) | PASS | PASS |
| #147 | PASS | PASS | PASS |

注：`core/tests/phase9_index_smoke.rs:170` 的 Tantivy `LockBusy` 为既有间歇 flake（与 Phase 19 无关），命中时 `gh run rerun --failed` 复跑至绿后合入，never-merge-red 守线。

### 5. ADR-014 D2 lint（this closeout PR self-check）

`bash scripts/spec_drift_lint.sh --touched master` → 0 未标注命中（CI spec-lint gate 复核）。

### 6. AC 达成（路径 A：端到端语义检索 live）

| Phase 19 §6 AC | 状态 | 依据 |
|---|---|---|
| AC1 EmbeddingProvider + deterministic + real provider + spike | ✅ met | task-19.1（#142）+ `docs/spikes/phase-19-embedding-{candidates,fastembed}.md` |
| AC2 默认 backend 接 Retriever + index/query embedding | ✅ met | task-19.2（#143）；BM25 不退化 |
| AC3 `/v1/search?semantic=true` Go→Rust gRPC + proto add-only | ✅ met | task-19.3（#144）；22-endpoint + proto-freeze 守护 PASS |
| AC4 smoke v9 30-step | ✅ met | task-19.4（#145）；step 29 semantic REST + step 30 eval `--semantic` |
| AC5 真实 SemanticRecall@K + ADR-023 ratify | ✅ met | task-19.5（#146，@10=0.9333）+ task-19.6（#147，ADR-023 Accepted）；据真实非合成数据（ADR-013） |
| AC6 ADR-014 cross-validation gate（第十次激活） | ✅ met | 本 closeout PR body（D1-D5） |

## 4. 平台矩阵

| Platform | Build | Note |
|---|---|---|
| Linux x86_64 (WSL2 Ubuntu 26.04) | 默认 + `embedding-fastembed` build OK | real recall 实测平台（rustc 1.96.0，fastembed rustls + ONNX `all-MiniLM-L6-v2`） |
| Windows MSVC (dev box) | 默认 + `embedding-fastembed` build OK | fastembed rustls 跨平台（task-19.1 实测 Win MSVC 1m11s）；默认构建 0 新 dep |
| Linux (GHA runner) | 默认 build + cargo/go test PASS | CI 三门；`embedding-fastembed`/`vector-*` 默认关闭，CI 不构建模型/backend |

## 5. Upgrade / Rollback

- Upgrade：默认构建行为不变（BM25-only，0 新 dep），无迁移。语义路径 opt-in（`?semantic=true` / `eval run --semantic`）；real-model 语义检索需 `--features embedding-fastembed`。
- Rollback：`git tag -d v0.12.0` + 删 GitHub Release / ghcr tag；默认镜像与 v0.11.0 行为兼容（BM25-only），回退非破坏。

## 6. ADR-014 第十次激活 record

- **D1（mapping）**：Phase 19 §6 AC1-6 ↔ task-19.1–19.7 §6 AC（各 task spec §7 追踪表）。
- **D2（lint）**：各 task PR + 本 closeout `spec_drift_lint.sh --touched master` 0 未标注命中（CI spec-lint）。
- **D3（verified-by）**：每 AC 显式 owner TEST-id（task spec §6/§7）。
- **D4（自治）**：主 agent ADR-012 自治据真实数据推进；红线 never-merge-red + 禁据合成 ratify（ADR-013）守线。
- **D5（不溯改）**：Phase 1-18 spec 未溯改；Phase 18 AC3/AC4 解决记录写在 ADR-023 Amendment + task-19.6/19.7 §10（`git diff --stat master -- docs/specs/phases/phase-18-vector-backend-selection.md` 空）。

## 7. tag / 镜像 SHA（post-tag-push backfill）

- v0.12.0 annotated tag SHA：`dcbe09bba4bc6f636186d7df1f32447ab96d1ddc`（master @ #148 merge，用户授权后 push，2026-05-30）。
- `release.yml` run ID：`26685041851`（conclusion **success**）。
- 镜像 digest：`ghcr.io/tajiaoyezi/contextforge-daemon:v0.12.0` + `:latest` = `sha256:6f0ae8fbf956dcdeaeea29e0f0a98f9dbbada8d3ca8bf0ae5c3c79fa448eca6d`。
- 验证：`docker pull ghcr.io/tajiaoyezi/contextforge-daemon:v0.12.0`。
- 承 v0.8 / v0.10 / v0.11 post-tag-push backfill PR pattern。
