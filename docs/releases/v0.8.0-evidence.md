# ContextForge v0.8.0 Evidence — Console functional gap closure (6/11 backlog)

**日期**：2026-05-26
**主干**：`master`
**当前 HEAD**：`c3e6698` (`Merge PR: chore(spec) Phase 15 closeout — ADR-020/021 → Accepted + phase-15 Done (E8)`)

**v0.8.0 tag SHA**：`<E10 tag push 后回填>` (annotated tag pointing to master @ `<本 release docs PR merge SHA>`; `git ls-remote --tags origin v0.8.0` 验证 push 成功)

## 合入记录 (Phase 15 + ADR-020/021 Accepted + release docs)

- PR #98 `chore/phase-15-spec-and-adr-020-021` → `1716b89` + `6ba26d4` (merged `e3b5286`): E1 spec PR. ADR-020 (health-component-breakdown) + ADR-021 (memory-event-bus-bridge) Proposed; phase-15 spec + 6 task specs (Ready); adapter §Phase 15 / §Tasks 15.1-15.6 / §ADR 020/021 / §BDD; PRD §Implementation Phases Phase 15 段; test/features/phase-15-console-functional-gap-closure.feature 14 scenarios; ADR-014 D2 lint 0 unannotated hits.
- PR #99 `feat/task-15.1-memstore-chunk-trace-cache` → `9fcfdc8` (merged `1c7580e`): E2 task-15.1 P0 #1. MemStore chunkCache + traceCache + FIFO eviction (cap=256); trace_id == query_id 对齐; 4 unit tests (ChunkCacheHit / TraceCacheHit / CacheMiss / FIFO). go test 22 packages 全 PASS, 既有 22-endpoint conformance 不退化.
- PR #100 `feat/task-15.2-memory-event-bus-bridge` → `ba15cd0` (merged `131670c`): E3 task-15.2 P0 #2 / ADR-021. emit_audit → emit_audit_and_event; audit_op_to_event_type + build_memory_event helpers; 3 handler call sites updated; 6 new unit tests (pin / unpin / deprecate / soft_delete / SendError swallow / filter). cargo workspace 100 lib + 17 integration test files 全 PASS.
- PR #101 `feat/task-15.3-chunks-stats-endpoint` → `c93968f` + `02ff942` (merged `5e6c30d`): E4 task-15.3 P1 #3. SearchService.GetChunksStats RPC add-only; Retriever.num_docs + count_indexed_since methods; today_start_iso Howard Hinnant 算法 (no chrono dep); 4 new search tests + 3 Go router/memstore tests.
- PR #102 `feat/task-15.4-list-eval-runs-endpoint` → `84f215b` (merged `3dae0e6`): E5 task-15.4 P1 #4. EvalService.List RPC add-only; ListEvalRunsFilter + SqliteEvalStore.list (rusqlite params_from_iter dynamic WHERE); limit clamp 1..=200 default 50; 4 store + 2 server + 3 router tests; 既有 task-14.2 不退化.
- PR #103 `feat/task-15.5-query-history-endpoint` → `f67500e` + `c3a1136` (merged `168dd85`): E6 task-15.5 P1 #5. SearchService.ListQueries RPC add-only; TraceRecord wrapper (RetrievalTrace 不动 — ADR-015 D1 保留); TraceStore.put 签名 crate-internal BREAKING (workspace_id + ts_unix 追加); TraceStore.list (clamp 1..=100 default 20); 3 new search tests + 2 router tests.
- PR #104 `feat/task-15.6-health-component-detail` → `df3ec20` (merged `5264fd6`): E7 task-15.6 P2 #7 / ADR-020. new HealthService.GetDetailed RPC add-only; ComponentHealth message; new `core/src/health.rs` (HealthChecker.check_all 5 probes + aggregate_status + EMBED_ENV_MUTEX serialization); new `core/src/data_plane/health.rs` (HealthCheckServer gRPC adapter); Go HealthClient + ?detailed=true branch + synthesize fallback; smoke v6 4 new steps; 7 health + 1 data_plane::health + 3 router tests PASS.
- PR #105 `chore/phase-15-closeout` → `26453f0` (merged `c3e6698`): E8 closeout. ADR-020 + ADR-021 Status Proposed → Accepted; phase-15 §Status Done + §6 7 AC 全 [x] + §8 DoD 10/12 [x]; adapter §Phase 15 + §ADR 020/021 → Done/Accepted; PR body 含 ADR-014 D1 mapping 表 + D2 lint 0 unannotated hits 输出.
- PR `chore/v0.8.0-release-docs` → `<本 PR 自身 SHA>` (merged `<本 PR merge SHA>`): E9 release docs. README v0.8 段 + RELEASE_NOTES v0.8.0 章节 + docs/releases/v0.8.0-{evidence,artifacts}.md + integration.md §附录 A update.
- (E10 tag + Release page — created post-merge of release docs PR; SHA backfill in evidence § "v0.8.0 tag SHA" 上方.)

## S2V 状态

- `docs/s2v-adapter.md`: Phase 15 `console-functional-gap-closure` = `Done` (closeout PR #105 内同步).
- `docs/s2v-adapter.md`: Task 15.1 - 15.6 全 `Done`.
- 6 task spec Status = `Done`:
  - `task-15.1-memstore-chunk-trace-cache.md` (5/5 AC including AC5 deferred to task-15.6)
  - `task-15.2-memory-event-bus-bridge.md` (5/5 AC)
  - `task-15.3-chunks-stats-endpoint.md` (6/6 AC)
  - `task-15.4-list-eval-runs-endpoint.md` (7/7 AC)
  - `task-15.5-query-history-endpoint.md` (7/7 AC)
  - `task-15.6-health-component-detail.md` (8/8 AC)
- `phase-15-console-functional-gap-closure.md` = `Done`, §6 AC1-7 全 [x] verified-by 显式, §8 DoD 10/12 [x] (剩 v0.8.0 release tag + cross-repo notify 移至 E10).
- `docs/decisions/adr-020-health-component-breakdown.md` = **Accepted** (2026-05-26 via Phase 15 closeout PR #105; D1-D5 完整 ship through task-15.6 PR #104).
- `docs/decisions/adr-021-memory-event-bus-bridge.md` = **Accepted** (2026-05-26 via Phase 15 closeout PR #105; D1-D4 完整 ship through task-15.2 PR #100).
- `docs/decisions/adr-014-cross-phase-exit-criteria-validation.md` = `Accepted` (v0.3/v0.4/v0.5/v0.6/v0.7 五次激活后 **v0.8 第六次激活** — 制度稳定性跨 6 phase 验证通过).

## 验证证据

### 1. cargo test --workspace

```bash
cargo test --workspace
```

实测：
- 121 lib tests PASS (含 task-15.x 新增 33+ test: 4 task-15.1 + 6 task-15.2 + 4 task-15.3 + 4+2 task-15.4 + 3 task-15.5 + 7+1 task-15.6; 既有 task-11/12/13/14 不退化)
- 17 integration test files all PASS (memory_integration / eval_integration / data_plane_integration / search_real_retriever 等)
- 0 failed, 0 ignored
- 1 既有 unused-import warning unrelated to Phase 15

### 2. go test ./...

```bash
go test ./...
```

实测：
- 22 packages PASS
- `internal/consoleapi`: 含 task-15.1 (4 cache) + task-15.3 (2 chunks-stats) + task-15.4 (3 list eval) + task-15.5 (2 queries) + task-15.6 (3 health-detail) 共 14 新 router/memstore test + 既有 e2e_grpc 不退化
- `internal/consoleapi/grpcclient`: 既有 + 新 method (interface compliance)
- `test/conformance`: 22-endpoint Console contract conformance 不退化

### 3. scripts/console_smoke.sh v6 syntax verify

```bash
bash -n scripts/console_smoke.sh
```

实测：syntax OK；v6 24-step flow (既有 20 + 4 new step for chunks-stats / eval-runs list / queries / health-detail)。

Daemon-level `CONSOLE_REAL_SMOKE_EXIT=0` 留 v0.8.0 ship 前 / E10 tag push 前 manual smoke test (per fake-evidence policy)。

### 4. ADR-014 D2 lint output

```bash
# Bash version too slow on Windows (>10 min); Python equivalent of scan_all + mode_touched used.
python .tmp-lint.py
```

```
Scope scanned: docs/specs/ (218 all-tree hits — Phase 1-14 historical; ADR-014 D5 不溯改)
PR-touched lines containing anti-pattern w/o annotation: 0

OK: spec_drift_lint --touched origin/master: 0 unannotated hits in PR-changed lines
```

(Background bash equivalent confirmed `exit 0` on E1 spec PR; Python equivalent used for subsequent task PR + closeout / release docs PR.)

### 5. release_smoke.sh (v0.8.0 release prep)

```bash
# 留 E10 tag push 前 manual smoke (本 release docs PR 不触).
bash scripts/release_smoke.sh
```

预期：`PHASE_RELEASE_SMOKE_EXIT=0` + 既有 phase11/12/13/14 段 OK + 新 `phase15_console_functional_gap_closure=ok` 段 (本 PR 不修改 release_smoke.sh — 与 v0.7 一致：仅在 release tag push 前手动跑)。

### 6. Console PR #91/#93 backlog 6/11 closed mapping

| Item | Backlog signal | Task | PR | Verifying test |
|---|---|---|---|---|
| P0 #1 | MemStore inmem-fallback 503 drill-down | task-15.1 | #99 | `memstore_test.go::TestMemStore_ChunkCacheHit_AfterSearch` |
| P0 #2 | `memory.*` event 桥接 缺失 | task-15.2 | #100 | `data_plane::memory::tests::test_pin_emits_event_bus_memory_pin` |
| P1 #3 | Dashboard "已索引块" 缺 backend | task-15.3 | #101 | `data_plane::search::tests::test_get_chunks_stats_empty_data_dir_returns_zero` |
| P1 #4 | Eval 列表 缺 endpoint | task-15.4 | #102 | `eval::store::tests::test_list_returns_rows_ordered_by_started_at_desc` |
| P1 #5 | Dashboard "最近查询" 缺 backend | task-15.5 | #103 | `data_plane::search::tests::test_trace_store_list_returns_recent_first` |
| P2 #7 | CoreHealthCard 5 链路 缺 | task-15.6 | #104 | `health::tests::test_check_all_returns_5_components_and_under_500ms` |

### 7. Cross-repo onboarding (Console v0.8 standby PR readiness)

- Console UI 端可启动 standby PR：
  - Dashboard "已索引块" / "平均召回率" / "最近查询" 3 KPI panel 改 HTTPAdapter 真接 `/v1/stats/chunks` / `/v1/eval-runs` / `/v1/queries`
  - CoreHealthCard 改 5 链路细分 panel 真接 `/v1/health?detailed=true`
  - Memory 详情面板 "操作历史" 列表自动有数据（events stream 拉到 `memory.*` event_type）
- Console contractv1.go 客户端同步加 `ChunksStats` / `QueryRecord` / `ComponentHealth` Go struct + `CoreHealth.Components` 字段 (add-only 不破坏旧 client)。
- Cross-repo coordinate: ContextForge → 用户转发 → Console 主 Agent 启动 standby chore PR (按本 evidence §6 endpoint 表)。

## 平台矩阵

| Platform | Build | Test |
|---|---|---|
| Linux x86_64 (Ubuntu 22.04+) | source build OK | cargo + go test PASS |
| WSL2 | source build OK | cargo + go test PASS |
| macOS (arm64) | source build OK (nice-to-have) | manual |
| Windows native | source build OK + go test PASS | cargo + go test PASS (本 ship 主开发平台) |

## Upgrade path (v0.7.2 → v0.8.0)

详 RELEASE_NOTES.md v0.8.0 §Upgrade path. 主要点：
- Console UI / SDK 客户端继续工作（add-only schema; v0.7 client 解析 v0.8 JSON 忽略未知字段）
- 二进制升级 v0.7.2 → v0.8.0 不破坏既有部署（ADR-018 fallback 行为不变）
- 新 endpoint opt-in 试用：`curl /v1/stats/chunks` / `/v1/eval-runs` / `/v1/queries` / `/v1/health?detailed=true`

## Rollback path

若 v0.8.0 ship 后发现非预期问题：
1. `git revert c3e6698` (closeout merge SHA) → revert 到 v0.7.2 (master @ `5264fd6` 或 v0.7.2 tag)
2. ship v0.8.0.1 patch + 标 ADR-020/021 Reverted
3. 不撤回 v0.7.2 ADR-018 / v0.7.0 ADR-017 (跨版本独立)

## Cross-repo follow-up

通知 Console 团队 ContextForge v0.8.0 release ship 完成 → Console UI 端 standby PR 启动：
- Dashboard 3 KPI 真接 (3 个 endpoint)
- CoreHealthCard 5 链路 (1 个 endpoint)
- Memory 操作历史自动有数据 (events stream `memory.*` filter)

剩余 Console PR #91/#93 backlog 5 项 (P2#6 / P3 #8/#9 / P4 #10/#11) 留 Phase 16 / v0.9.0。
