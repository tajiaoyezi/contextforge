# ContextForge v0.39.0 Evidence — v1.0-docs-and-release-flow (v1.0 收口冲刺第二步：D3 文档对齐 + D4 GitHub Release 流程；ADR-050 D3/D4 ratify)

> v0.39.0 ships Phase 46（v1.0-docs-and-release-flow，v1.0 收口冲刺第二步）。承 ADR-050 v1.0 定义，交付剩余两维：**D3 文档对齐**（task-46.1 README 重构 776→153 行，删 38 changelog 段已在 RELEASE_NOTES.md + 删 v0.2 limitations 过时段 + Features 汇总段 + maturity label Pre-1.0 不虚标 + pin v0.28.0→v0.38.0 + Releases 段；task-46.2 CHANGELOG.md Keep a Changelog 1.1.0 + docs/decisions/README.md 49 ADR 5-category 分类导航）+ **D4 GitHub Release 流程**（task-46.3 release.yml 加 softprops/action-gh-release@v2 step tag push 触发 + RELEASE_NOTES.md body 提取 + cosign/SBOM provenance footer + contents:write permission）。**0 代码逻辑改动 / 0 新 dep / 0 network / 0 schema migration / 0 proto**——纯文档 + 1 CI step，遵 ADR-013：
> - **ADR-050 D3/D4 ratify**（D1/D2 Phase 45 已 ratify；D3/D4 本 phase 交付 → ADR-050 完整 ratify 待 Phase 47 v1.0.0）；ADR-007 add-only Amendment（分发定义补 GitHub Release 对象）。
> - **README maturity label 诚实**（Pre-1.0 收口中，不虚标 v1.0——v1.0.0 flip 在 Phase 47）。
> - **D4 首次实践**：v0.39.0 tag push 将首次触发 GitHub Release 对象自动创建（body 从 RELEASE_NOTES.md v0.39.0 段提取 + signed-image/SBOM provenance footer）。

## 1. 合入记录 (Phase 46)

| task | PR | delivery |
|---|---|---|
| 46.x | #288 | plan：phase-46 spec + 3 task specs + BDD feature + roadmap §3.28 + adapter |
| 46.1+46.2 | #289 | README 重构（776→153，删 38 changelog + Features + maturity label + pin v0.38.0）+ CHANGELOG.md（Keep a Changelog）+ docs/decisions/README.md（49 ADR 分类导航）|
| 46.3 | this | release.yml GitHub Release step（softprops/action-gh-release@v2）+ smoke v35→v36[55/55] + TestTask463 + release docs + ADR-050 D3/D4 ratify + ADR-007 Amendment + roadmap/adapter |

## 2. S2V 状态
- Phase 46 `v1.0-docs-and-release-flow`: Ready → **Done**, Tasks 0 → 3。
- §6 AC1-5 全 `[x]`。`go test ./...` 全过（含 TestTask463 + TestTask454 no-regression）；**0 代码逻辑改动 / 0 dep / 0 migration / 0 proto**；纯文档 + 1 CI step。

## 3. 验证证据
- TEST-46.1.1（docs grep）：README Features 段 + maturity label Pre-1.0 + 0 个 What's new 段（38→0）+ 无 v0.2 limitations + pin v0.38.0（无 v0.28.0）+ Quick Start 保留 + 无 "does not publish a GitHub Release" 过时声明 PASS。
- TEST-46.2.1（docs grep）：CHANGELOG.md Keep a Changelog 1.1.0 banner + 版本倒序（v0.38 before v0.30）+ v0.30-v0.38 全覆盖 PASS。
- TEST-46.2.2（docs grep）：docs/decisions/README.md 49 ADR table entries（adr-019 跳号 prose 注明）+ 5 category 分组 PASS。
- TEST-46.3.1（TestTask463）：release.yml softprops/action-gh-release@v2 + contents: write + body_path PASS。
- TEST-46.3.2（TestTask463）：README 无 "does not publish a GitHub Release object" 过时声明 PASS。
- TEST-46.3.3（TestTask463）：smoke v36[55/55] + bash -n 语法 + no-regression denominators [37/37]..[54/54] PASS。
- 全量 `go test ./...` PASS；`bash -n scripts/console_smoke.sh` PASS。

## 4. ADR
- **ADR-050**（v1.0-definition）：Proposed → **D3/D4 ratify**（Phase 46；D1/D2 已 Phase 45 ratify → 完整 4 维度 ratify 待 Phase 47 v1.0.0）。
- **ADR-007**（minimal-tarball-distribution）：add-only Amendment（分发定义补 GitHub Release 对象，D4 落地）。
- ADR-013（README maturity label 不虚标 v1.0；D4 缺口真实可验证）/ ADR-014（第三十七次激活 D1-D5）/ ADR-004/008（守 0 dep baseline）守线引用。

## 5. 发版凭据（post-tag-push backfill，ADR-013）
- tag SHA：`<backfill: tag-sha>`
- tag object：`<backfill: tag-object>`
- release run：`<backfill: run-id>`
- ghcr digest：`<backfill: ghcr-digest>`
- cosign tlog：`<backfill: tlog-sign> / <backfill: tlog-attest>`
- GitHub Release object：`<backfill: release-url>`（D4 首次实践）
