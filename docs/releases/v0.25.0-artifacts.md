# ContextForge v0.25.0 Artifacts — vector-backend-config-plumbing-and-completeness (server two-hot-path backend config + sqlite-vec factory arm + console vector_score provenance + retrieval-filter no-op contract honesty + ADR-037 ratified)

> Artifact inventory for the v0.25.0 minor release (Phase 32). Mirrors the v0.24.0 artifacts structure.
> The default build is unchanged: 0-new-dependency, 0-network；默认行为 / proto 既有字段 / 既有契约不变。
> server.rs hybrid+semantic 两热路径经 `resolve_vector_backend`/`parse_vector_backend` 读 env 选 backend（unset/"" → BruteForce 字节等价，默认行为不变）+ factory 加 `sqlite-vec` arm（feature 门控）+ console_data_plane.proto add-only `vector_score=16` + retriever filter WARN → 准确 no-op 契约，均不破既有契约。既有 22-endpoint + 5 Memory RPC + 运行时二进制默认行为不变。No breaking contract bump。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：grpcclient `protoToSearchResult` 映射 `VectorScore`（语义命中真实值 / BM25 = 0）→ contractv1 `SearchResult` add-only `VectorScore`（ADR-015 add-only，承 task-20.1 Semantic 先例）。运行时既有 22-endpoint + 5 Memory RPC 行为不变。
- `contextforge-core`（Rust 数据面 daemon）：`server.rs` hybrid(:340)+semantic(:382) 两热路径经 `resolve_vector_backend`/`parse_vector_backend` 选 backend（读 `CONTEXTFORGE_VECTOR_BACKEND` + 可选 `CONTEXTFORGE_VECTOR_DIM`，镜像 `resolve_data_dir` env pattern；unset/"" → BruteForce 字节等价）；`factory.rs` `select_vector_backend` 加 `sqlite-vec` arm（feature `vector-sqlite` double-half cfg 门控，镜像 qdrant/lancedb）；`data_plane/search.rs` producer 写 `vector_score`（"vector" 命中=cosine / BM25=0，镜像 v1 server.rs:407）；`retriever/mod.rs` filter WARN → 准确 no-op 契约。默认构建 0 新依赖 / 0 network；`cargo test --workspace` = 199 passed, 0 failed。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.25.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network）+ cosign keyless 签 + SBOM + provenance（承 v0.21.0–v0.24.0）。linux/arm64 仍 DEFERRED。
- Feature 构建：本 release 无新 feature flag；`sqlite-vec` arm 经既有 `vector-sqlite` feature 门控（默认 off → factory honest Err，naming sqlite-vec + vector-sqlite）。feat-on REAL x86_64-pc-windows-msvc `cargo test --features vector-sqlite` 构建通过（arm wiring 真实验证）。

## 2. 源码档

- master tag SHA：`<backfill: v0.25.0 annotated tag SHA>`（v0.25.0 annotated tag，无人值守授权下主 agent push（ADR-012））
- Phase 32 代码路径：
  - task-32.1（PR #212, squash c7358ed；server backend config plumbing）：`core/src/server.rs`（hybrid :340 + semantic :382 两热路径经 `resolve_vector_backend`/`parse_vector_backend` 选 backend，读 `CONTEXTFORGE_VECTOR_BACKEND` + 可选 `CONTEXTFORGE_VECTOR_DIM`；unset/"" → BruteForce 字节等价；unknown/feature-off → factory honest Err 经 `Status::internal` 透出，无静默 fallback，ADR-013）+ TEST-32.1.1（env name+dim parse/trim/blank→0 + unknown honest Err）+ TEST-32.1.2（unset → ("",0) → brute-force 字节等价）。0 新依赖。
  - task-32.2（PR #213, squash 76a3137；sqlite-vec factory arm）：`core/src/retriever/vector/factory.rs`（`select_vector_backend` 加 `"sqlite-vec"` arm，feature `vector-sqlite` double-half cfg 门控：feat on → `SqliteVecBackend::new()`（`name()`="sqlite-vec"）/ feat off → honest Err naming sqlite-vec + vector-sqlite）+ TEST-32.2.1（feat-off honest Err）+ TEST-32.2.2（selection-matrix wiring）→ factory 6/6；feat-on REAL x86_64-pc-windows-msvc `cargo test --features vector-sqlite` 构建通过 → `sqlite_vec_with_feature_returns_sqlite_vec_backend` + matrix feat-on 分支 ok（arm wiring 真实验证，非仅结构）。in-process matrix recall/latency CELL honest-defer `[SPEC-DEFER:phase-future.sqlite-vec-inprocess-matrix]`。0 新依赖（sqlite-vec 在 Cargo.toml 已 optional）。
  - task-32.3（PR #214, squash eaa37bd；console vector_score + filter 契约诚实）：(A) `proto/contextforge/console/v1/console_data_plane.proto`（`SearchResultItem` add-only `vector_score=16`，parity v1 search proto `RetrievalResult.vector_score=13`）+ buf generate proto regenerated Go binding（真实 rawDesc descriptor shift 0xdc→0xff，非 EOL churn；grpc/v1 文件 revert 保持 surgical）+ `core/src/data_plane/search.rs`（producer 写 `vector_score`："vector"=cosine / BM25=0，镜像 v1 server.rs:407）+ Go grpcclient `protoToSearchResult` 映射 `VectorScore` → contractv1 `SearchResult` add-only `VectorScore`（ADR-015 add-only，承 task-20.1 Semantic 先例；ContextForge-Console contractv1.go 镜像应加同字段——跨仓 add-only signal，ADR-014 D4，向后兼容）；(B) `core/src/retriever/mod.rs:325`（误导 WARN → 准确 no-op 契约：chunks 表 indexer SQL_SCHEMA §5.3 FROZEN 无 source_type/agent_scope 列，`SearchResult.source_type` 为 `DEFAULT_SOURCE_TYPE` 常量 + agent_scope 恒空，agent_scope 为 memory 层概念（memory_items, migration 0013）；真实 chunk filter 为 import-path feature，非确定性 nit → 新 backlog `[SPEC-DEFER:phase-future.chunk-source-type-filter]` + `[SPEC-DEFER:phase-future.chunk-agent-scope-filter]`）+ TEST-32.3.1（grpcclient 携 VectorScore：语义真实值 / BM25=0）+ TEST-32.3.2（filter no-op：非空 source_type/agent_scope filter 结果与空 filter 字节等同）。
  - task-32.4（this PR）：`scripts/console_smoke.sh`（v22 step 41）；`internal/cli/smoke_syntax_test.go`（`TestTask324`）；`docs/releases/v0.25.0-{evidence,artifacts}.md`；`README.md` v0.25 段；`RELEASE_NOTES.md` v0.25.0 段；`docs/decisions/adr-037-...md`（ratify Proposed→Accepted，per-D）+ ADR-034 Phase 32 add-only Amendment；`docs/specs/phases/phase-32-vector-backend-config-plumbing-and-completeness.md`（§ 闭合）；`docs/s2v-adapter.md`；`test/features/phase-32-vector-backend-config-plumbing-and-completeness.feature`。

## 3. 验证脚本

```bash
# 默认构建（0 新依赖 / 0 network）
cargo test --workspace                                    # 199 passed, 0 failed
go test ./...                                             # all pass (incl TestTask323)
cargo clippy --workspace --all-targets -- -D warnings     # 0 warning
go vet ./...                                              # clean
gofmt -l .                                               # clean

# task-32.1 server backend config plumbing
cargo test -p contextforge-core resolve_vector_backend   # TEST-32.1.1 / TEST-32.1.2

# task-32.2 sqlite-vec factory arm（默认 feat-off honest Err + selection-matrix wiring → 6/6）
cargo test -p contextforge-core retriever::vector::factory
# feat-on（REAL x86_64-pc-windows-msvc，arm wiring 真实验证）
cargo test -p contextforge-core --features vector-sqlite retriever::vector::factory

# task-32.3 console vector_score provenance + filter no-op 契约
go test ./internal/grpcclient/ -run TestTask323           # VectorScore: semantic real / BM25=0
cargo test -p contextforge-core retriever                 # filter no-op byte-equiv

# 端到端 smoke v22（step 41；既有 step 不退化）
bash -n scripts/console_smoke.sh
go test ./internal/cli/ -run TestTask324

# D5 spec-drift lint
bash scripts/spec_drift_lint.sh --touched origin/master   # 0 unannotated hits
```

## 4. 镜像推送

- `release.yml`（`v*` tag push 触发）→ buildx → ghcr login → build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.25.0` + `:latest`（**linux/amd64**，net-zero 与 v0.24.0 一致）→ cosign keyless 签 + attest SPDX SBOM + provenance:mode=max（承 task-28.2 机制）。
- run ID + digest：`release.yml` run `<backfill: release run id>`（**success**，build-push + cosign sign + attest SBOM + provenance:mode=max 步全绿）；`ghcr.io/tajiaoyezi/contextforge-daemon:v0.25.0` + `:latest` = `<backfill: ghcr digest sha256:...>`；git tag SHA `<backfill: v0.25.0 tag SHA>`；cosign Rekor tlog idx `<backfill: sign tlog idx>`（sign）/ `<backfill: attest SBOM tlog idx>`（attest SBOM）。

## 5. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-037 vector-backend-config-plumbing-and-completeness | **Accepted**（per-D，D2 PARTIAL：arm/门控/wiring 真实达成、matrix CELL honest-defer） | task-32.4 据 32.1/32.2/32.3 真实非合成验证 ratify：D1 Accepted（TEST-32.1.1/.2；默认字节等价；0 新依赖）/ D2 PARTIAL（arm + double-half 门控 + in-process selection-matrix WIRING Accepted，含 REAL MSVC feat-on 构建 TEST-32.2.1/.2 6/6；in-process matrix recall/latency CELL honest-defer `[SPEC-DEFER:phase-future.sqlite-vec-inprocess-matrix]`）/ D3 Accepted（vector_score=16 端到端 Rust producer→Go grpcclient→contractv1；filter WARN → 准确 no-op；真实 chunk filter honest-defer 新 backlog `[SPEC-DEFER:phase-future.chunk-source-type-filter]`）/ D4 honest-defer 边界重申 / D5 baseline 不变（cargo/go/clippy/vet 绿；add-only proto 字段 + factory arm + no-op 契约）|
| ADR-034 vector-store-honest-defaults | Accepted（+ Phase 32 add-only Amendment） | task-32.2 加 `sqlite-vec` factory arm 使其经 `select_vector_backend` 可选（task-32.1 config-plumbed），补齐 factory backend 覆盖（brute/qdrant/lancedb/sqlite-vec）；in-process recall/latency matrix CELL 仍 honest-defer `[SPEC-DEFER:phase-future.sqlite-vec-inprocess-matrix]`（不溯改 D1–D5 正文 / Phase 29 Ratification，ADR-014 D5） |
| ADR-023 vector-persistence | Accepted（守线） | sqlite-vec arm 选择路径接通（承 Phase 18/23 MSVC parity；matrix CELL 见 `[SPEC-DEFER:phase-future.sqlite-vec-inprocess-matrix]`） |
| ADR-013 据实延后 | Accepted（守线） | matrix CELL `[SPEC-DEFER:phase-future.sqlite-vec-inprocess-matrix]` + 真实 chunk source_type/agent_scope filter 受阻如实不伪造 `[SPEC-DEFER:phase-future.chunk-source-type-filter]` |
| ADR-014 add-only / 跨仓 signal | Accepted（守线） | proto `vector_score=16` add-only + contractv1 跨仓 add-only signal（D4）+ Amendment 不溯改正文（D5） |
| ADR-004 / ADR-008 / ADR-012 | Accepted（守线） | 默认 0 新依赖 / 0 network；tag-release 须授权（本轮已授权） |

## 6. 版本声明（contract bump 评估）

- 本 release 新增 server 两热路径 backend env-config（unset/"" → BruteForce 字节等价）+ `sqlite-vec` factory arm（feature 门控）+ console_data_plane.proto add-only `vector_score=16` + contractv1 add-only `VectorScore` + retriever filter no-op 契约更正 + smoke v22——**无 proto 既有字段 / RPC / endpoint 删改、无默认运行时行为改动**（默认 backend 路径字节等价）。
- 默认构建 0 新代码依赖 / 0 network（ADR-004 / ADR-008）。
- 故 **无 breaking contract bump**；既有 v0.6–v0.24 client + 既有索引/数据无需改动。**vector-backend-config-plumbing-and-completeness 收口**。

## 7. 后续模板（vector-backend follow-on）

- sqlite-vec in-process recall/latency matrix CELL（需本地 MSVC feature 构建 + 真实语料）`[SPEC-DEFER:phase-future.sqlite-vec-inprocess-matrix]`。
- chunk source_type filter（import-path source_type tagging + schema migration）`[SPEC-DEFER:phase-future.chunk-source-type-filter]`；chunk agent_scope filter（memory 层概念）`[SPEC-DEFER:phase-future.chunk-agent-scope-filter]`。
- vector backend 配置文件（非 env）`[SPEC-DEFER:phase-future.vector-backend-config-file]`；vector dim 自协商 `[SPEC-DEFER:phase-future.vector-dim-auto-negotiation]`。
- （承前）qdrant live recall `[SPEC-DEFER:phase-future.qdrant-live-recall]`；lancedb build prereq CI `[SPEC-DEFER:phase-future.lancedb-build-prereq-ci]`；multi-arch 原生 arm64 runner `[SPEC-DEFER:phase-future.multi-arch-native-runner]`；GitHub 原生 attestation `[SPEC-DEFER:phase-future.github-native-attestation]`；compose 真实 TLS cert 自动签发 `[SPEC-DEFER:phase-future.compose-tls-auto-cert]`。

## 8. 存档指引

- closeout PR 合入后，**无人值守授权下**主 agent push `v0.25.0` annotated tag 触发 `release.yml`；post-tag-push backfill PR 填实 tag SHA + run ID + 镜像 digest + cosign 凭据（承 v0.8–v0.24 pattern；本文 `<backfill>` 角括号标记待回填）。
- Dockerfile workspace member COPY 完整性预检（承 v0.11.0 PR #139 教训）：本 release **无新增 migration / 无新增运行时 proto 文件**——`console_data_plane.proto` add-only 字段 regenerated 的 `*.pb.go` 随 `proto/` member COPY，Rust 源码改动均在既有 member 内；镜像为默认构建（0 新依赖 / 0 network，linux/amd64）。
