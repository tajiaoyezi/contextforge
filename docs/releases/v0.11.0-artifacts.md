# ContextForge v0.11.0 Artifacts — vector-backend-selection (infra + spike + ADR-023 Proposed)

**v0.11.0 tag**：annotated tag → `<TAG_SHA>` (post-tag-push backfill)
**release.yml run**：[`<RUN_ID>`](https://github.com/tajiaoyezi/contextforge/actions) (post-tag-push backfill)
**ghcr.io image**：`ghcr.io/tajiaoyezi/contextforge-daemon:v0.11.0` + `:latest`
**parent release**：v0.10.0 (`cbaafe6`)

> 缩范围：v0.11.0 = 向量 backend **基础设施 + 选型** 里程碑。镜像内容 = 现有控制面/数据面二进制（向量 backend feature 默认关闭,镜像行为与 v0.10.0 BM25-only 一致）。详 `docs/releases/v0.11.0-evidence.md` §诚实缩范围声明。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / REST / MCP / Console contract）— 不变（无 Go 向量集成；`internal/eval` 加 SemanticRecall@K 度量,默认 BM25-only 路径）。
- `contextforge-core`（Rust 数据面 daemon）— 默认 feature build 不含向量 backend（`NoopVectorBackend`,BM25-only）。向量 backend 经 `vector-{hnsw,sqlite,qdrant,lancedb}` feature 选择性启用（非默认）。
- ghcr 镜像 = 默认 feature 构建（与 v0.10.0 同构,向量 feature 关闭）。

## 2. 源码档

- master @ `<CLOSEOUT_SHA>`（Phase 18 closeout）；v0.11.0 tag → 同 SHA。
- Phase 18 代码：`core/src/retriever/vector/{mod,traits,types,noop,hnsw,sqlite_vec,qdrant,lance_db}.rs` + `bench/` crate + `internal/eval/eval.go`（SemanticRecall@K）。
- spike evidence：`docs/spikes/phase-18-{hnsw,sqlite-vec,qdrant,lancedb,comparison}.md`。

## 3. 验证脚本

```bash
cargo test --workspace            # 默认 feature,0 failed,无向量 backend 编译
go test ./...                     # 含 internal/eval 4 新单测
bash scripts/spec_drift_lint.sh --touched master   # 0 unannotated hits
# spike 复跑（Linux,各 feature）：
PROTOC=<protoc> cargo run --release -p contextforge-bench \
  --features vector-hnsw,vector-sqlite,vector-qdrant,vector-lancedb \
  -- --backend <name> --n 100000 --dim 64 --seed 1 --m 1000
```

## 4. 镜像推送

`release.yml` on `v0.11.0` tag push（`v*` pattern）→ Docker Buildx → ghcr login → build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.11.0` + `:latest`。run ID + 时长 post-tag-push backfill 填。

## 5. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-023 vector-backend-default | **Proposed** | 分层选型 D1-D6；`→ Accepted` 后置（需真实 embedding recall） |
| ADR-006 recall-eval-acceptance-gate | Accepted + **Amendment A1** | SemanticRecall@K gate（Proposed,pending provider） |
| ADR-014 cross-phase-exit-criteria-validation | Accepted | 第九次激活（v0.11） |

无 ADR 晋升到 Accepted（区别于 v0.10.0 的 ADR-022 Proposed→Accepted）——本 release 选型 provisional。

## 6. 权衡矩阵（4 backend,n=100k）

| 维度 | sqlite-vec | hnsw | qdrant | lancedb |
|---|---|---|---|---|
| 部署模型 | 嵌入式+磁盘 | 进程内内存 | 外部 server | 嵌入式+磁盘列式 |
| P95 latency | 3.198ms（exact） | **0.871ms（ANN）** | 0.947ms（ANN+gRPC） | 10.893ms（flat） |
| index RSS | **90.7MB** | 180MB | 91.6 + ~166 server | 90.8MB |
| 索引构建 | 760ms | 28432ms | 385ms | **50ms** |
| 跨平台构建 | Linux/gcc（Win MSVC ✗） | **全平台,0 native** | client OK + 需 server | Linux + protoc |
| 持久化 | **原生 SQLite（ADR-002）** | 内存（需序列化） | server-managed | 磁盘 Lance dataset |

## 7. 后续模板（vector-retrieval-integration follow-on）

后继 `[SPEC-OWNER:phase-future.vector-retrieval-integration]` 消费本 release 的：①三 trait + `with_vector_searcher` seam；②4 backend cfg-gated 实现；③`SemanticRecall@K` 度量 + 门禁；④ADR-023 分层选型。需补：embedding provider（fastembed/candle/ONNX）+ 默认 backend 接生产 retriever + smoke v9 + ADR-023 ratify（真实 recall）。

## 8. 版本声明

v0.11.0 minor。无 contract v1 bump（无 SearchResponse 字段落生产）；无 SQLite migration；无 breaking。默认构建行为 = v0.10.0 BM25-only（向量 feature 默认关闭）。

## 9. 存档指引

12 个月后查 artifacts：`git show v0.11.0` → master @ `<TAG_SHA>`；ghcr `ghcr.io/tajiaoyezi/contextforge-daemon:v0.11.0`；spike 数据 `docs/spikes/phase-18-*.md`；选型 `docs/decisions/adr-023-vector-backend-default.md`。
