# ContextForge v0.38.0 Evidence — v1.0-api-cli-freeze (v1.0 收口冲刺第一步：ADR-050 立 v1.0 锚点 + D2 API/CLI 冻结；daemon REST 移除 501 [BREAKING] + CLI --version/--help + example.toml；ADR-050 部分 ratify D1/D2)

> v0.38.0 ships Phase 45（v1.0-api-cli-freeze，v1.0 收口冲刺第一步）。**项目从未立过 v1.0 锚点**（PRD/roadmap/README 四处查证一致）——ADR-050 正式定义 v1.0.0 = 功能成熟度收口（D1）+ API/CLI 冻结（D2，本 phase）+ 文档对齐（D3，Phase 46）+ GitHub Release 流程（D4，Phase 46-47）。不含 multi-user/认证/自动更新/arm64（推 v2.0）。本 phase 交付 D2：task-45.2 daemon REST 移除 2 个 501 未实装端点（v1.0 前 BREAKING）+ chunk_count honest-defer + task-45.3 CLI version/--help + example.toml 补全。**0 新 dep** / **0 network** / **0 schema migration**——遵 ADR-013：
> - **ADR-050 部分 ratify**（D1 能力已满足 + D2 API/CLI 冻结 Phase 45 交付；D3/D4 待 Phase 46/47）；ADR-007 add-only Amendment（v1.0 分发定义收窄为务实收口）。
> - **daemon REST 移除 501**（task-45.2，PR #284）：`POST /v1/import` + `POST /v1/eval/run`（§2A 决策 B 有意留下，console-api 已覆盖）——v1.0 前 major 边界 BREAKING。chunk_count honest-defer（Go daemon 无 SQLite 库，指向 console-api `/v1/stats/chunks`）。
> - **CLI 冻结**（task-45.3，PR #285）：`version` 子命令 + 顶层 `--help`（修复 -h exit 2）+ example.toml 补全 `[embedding]`/`[vector]`/`[reranker]`/`[retrieval]`。

## 1. 合入记录 (Phase 45)

| task | PR | delivery |
|---|---|---|
| 45.1 | #283 | v1.0-definition：ADR-050 Proposed（4 维度 + 不含清单 + v2.0 路线）+ roadmap §v1.0 锚点段 |
| 45.2 | #284 | daemon-rest-api-freeze：移除 2 个 501 未实装端点（import/eval/run，console-api 已覆盖）[BREAKING] + chunk_count honest-defer + rest_test（Task452_RemovedEndpointsAre404）|
| 45.3 | #285 | cli-version-help：version 子命令 + 顶层 --help（修复 -h exit 2）+ example.toml 补全 4 检索 section + TestTask14_AC4 want 更新 |
| 45.x | this | closeout：smoke v35[54/54] + TestTask454 + release docs + ADR-050 部分 ratify（D1/D2）+ ADR-007 Amendment + roadmap/adapter |

## 2. S2V 状态
- Phase 45 `v1.0-api-cli-freeze`: Draft → **Done**, Tasks 0 → 4。
- §6 AC1-5 全 `[x]`。默认 `cargo test --workspace` + `go test ./...` 全过；**0 新 dep / 0 network / 0 migration**；daemon REST 移除是 v1.0 前 breaking（release notes 显式记）。

## 3. 验证证据
- TEST-45.2.1 `Task452_RemovedEndpointsAre404`：移除端点返 404 非 501 PASS。
- TEST-45.3.1 `TestTask453_VersionPrintsVersion`：version 打印 PASS。
- TEST-45.3.2 `TestTask453_TopLevelHelpDoesNotExit2`：--help/-h/help 不 exit 2 PASS。
- TEST-45.3.3 `TestTask453_ExampleTomlCoversRetrievalSections`：example.toml 4 section PASS。
- 全量 `go test ./...` + `cargo test --workspace` + clippy + gofmt clean。

## 4. ADR
- **ADR-050**（v1.0-definition）：Proposed → **部分 ratify D1/D2**（Phase 45；D3 Phase 46 / D4 Phase 46-47 / v1.0.0 完整 ratify Phase 47）。
- **ADR-007**（minimal-tarball-distribution）：add-only Amendment（v1.0 分发定义收窄为务实收口，自动更新/arm64 推 v2.0）。
- ADR-017（悬空 v1.0 gate 正式承接）/ ADR-015（proto FROZEN）/ ADR-004/008/013/014 守线引用。
