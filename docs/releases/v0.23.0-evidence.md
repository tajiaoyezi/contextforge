# ContextForge v0.23.0 Evidence — cjk-true-segmenter (jieba 真分词 analyzer feature-gated + 双站点注册 + reindex 迁移工具 + 真实 CJK recall delta + ADR-035 ratified)

> v0.23.0 ships Phase 30: 把 Phase 24 的 0-dep 重叠 bigram CJK analyzer（`配置加载`→`配置`/`置加`/`加载`）
> 升级为 **feature-gated 真分词器**（`cjk-segmenter`，jieba-rs，`配置加载`→`配置`/`加载`），bigram 保留作
> 0-dep fallback。真分词 analyzer 在索引站点（`IndexSession::open_with_tokenizer`）+ 查询站点
> （`Retriever::open_with_config`）**双站点对称注册**（漏注册→召回静默退化，task-24.1 R4）；新增
> `IndexSession::reindex_with_tokenizer` 既有索引迁移工具（绑定持久化 `meta.json` ⇒ 切 analyzer 须
> re-index）。**默认构建仍 0 新 dep + 默认 tokenization 不变**（ADR-004 / ADR-035 D5），三门 + lint 不退化。
> 真实 recall delta 实测：**真分词相对 bigram file-level 召回 +0.0000（小语料持平，诚实零 delta，
> ADR-013）**，两者均 +0.125 over default。**ADR-035** 据 D1–D5 per-D ratify `Proposed → Accepted`
> （D3 default flip honest-defer 部分）；**ADR-029** add-only Amendment（Phase 30）。

## 1. 合入记录 (Phase 30, tasks 30.1–30.3)

| task | PR | delivery |
|---|---|---|
| 30.1 | #202 | `cjk-segmenter` feature（jieba-rs 0.7.4 optional，主 agent R7 chore + ADR-008 add-only，默认 off → 0 新 dep）+ `CJK_SEGMENTER_TOKENIZER` 常量 + `tokenize_cjk_segmenter`（jieba.cut 真分词，code 段复用）+ `CjkSegmenterTokenizer` + `build_cjk_segmenter_analyzer` + `register_cjk_segmenter` + index/query 双站点注册（feature-gated）。`cargo test --features cjk-segmenter --lib test_30_1` **2 passed**（`配置加载→配置/加载` 真词边界 `assert_ne!` 区别 bigram + 双站点 round-trip）；bigram `code_cjk` 保留 0-dep fallback；TEST-30.1.1–30.1.4 |
| 30.2 | #203 | `IndexSession::reindex_with_tokenizer`（读 SQLite chunk + drop/重建 Tantivy 绑定 new analyzer + 重加，向后兼容）+ `RetrieverConfig.tokenizer` schema-driven 对称文档化（方案 B，vestigial）+ phase24-harness 第三 path（真分词）+ golden +5 CJK case（11→16，经 Go validator）。**实测（16 q / 14 file）：default 0.8750/0.8750 → bigram 1.0/1.0 → segmenter 1.0/1.0；delta(seg−bigram)=+0.0000**（诚实零 delta，ADR-013）；TEST-30.2.1–30.2.4 |
| 30.3 | this PR | Phase 30 closeout：v0.23.0 release docs + smoke v20 step 39 + ADR-035 据 D1-D5 per-D ratify（D3 default flip honest-defer）+ ADR-029 add-only Amendment + phase-30 §6 闭合 + adapter + phase-30 BDD feature |

## 2. S2V 状态

- Phase 30 `cjk-true-segmenter`: Draft → **Done**, Tasks 0 → 3。
- phase-30 §6 AC1-4 全 `[x]`（据 30.1–30.3 真实交付；AC2 tokenizer-default-on default flip 维度 honest-defer 如实标注，迁移工具 + 评估达成 → 部分 ratify）。
- 默认构建恒 0 新 dep / 0 network（ADR-004）；真分词器全在 `cjk-segmenter` feature 下，默认 `content` tokenization + 6-field schema 不变；bigram `code_cjk` 0-dep fallback 保留；不溯改 Phase 1-29 spec（ADR-014 D5）。
- **本 release 推进 roadmap §3.12 两 marker**：cjk-true-segmenter（真分词器 feature-gated 真实达成）/ tokenizer-default-on（迁移工具 + 评估达成，full default flip 诚实延后）。受阻态（重词典 lindera / 大语料 recall / default flip）据 ADR-013 **如实记录不伪造**。

## 3. 验证证据

### 1. cjk-true-segmenter feature + 双站点注册（task-30.1，🟢，0 新 dep at default）

`cargo test -p contextforge-core --features cjk-segmenter --lib test_30_1` → **2 passed; 0 failed**：
- TEST-30.1.1：jieba `配置加载 → [配置, 加载]`（真词边界）+ `assert_ne!` 显式区别于 bigram `[配置, 置加, 加载]`；混合 `getConfig配置加载` → get/config + 配置/加载。
- TEST-30.1.2：index 站点（`open_with_tokenizer`）+ query 站点（`open_with_config`）双注册对称——opt-in collection（IndexSession 写 + Retriever 查）round-trip 真词 `配置` 双站点命中（无静默退化，task-24.1 R4）。
- jieba-rs 0.7.4 在 `x86_64-pc-windows-msvc`（rustc 1.95.0）`cargo build --features cjk-segmenter` exit 0——纯 Rust 无构建受阻（未触发 lindera 重词典 🔴 风险）。

### 2. reindex 迁移工具 + tokenizer-default-on 评估 + 真实 recall delta（task-30.2，🟢/🟡）

- TEST-30.2.2（🟢，默认构建可验）：`IndexSession::reindex_with_tokenizer` default→code_cjk 迁移——迁移前 default 绑定 `置加` 不命中 → 迁移后 bigram `置加` + code `config` 命中（经 Retriever 只读查询），chunk 保留（SQLite 真相源不动）；TEST-30.2.2b（feature）default→cjk_segmenter 真词 `配置` 命中。
- TEST-30.2.1（🟡 真实 recall delta，16 q / 14 file，`cargo run --features cjk-segmenter --example phase24_tokenizer_recall`）：

  | analyzer | recall@5 | recall@10 | top1 | mrr |
  |---|---|---|---|---|
  | default | 0.8750 | 0.8750 | 0.8750 | 0.8750 |
  | bigram (code_cjk) | 1.0000 | 1.0000 | 0.8750 | 0.9375 |
  | segmenter (cjk_seg) | 1.0000 | 1.0000 | 0.8750 | 0.9375 |

  **delta(seg−bigram)=+0.0000 全指标**；delta(seg−default)=+0.1250 recall / +0.0625 mrr。**诚实结论（ADR-013，不外推）**：小语料下真分词相对 bigram **无 file-level 召回提升**（两者均完整召回 CJK case；真分词价值在 token 洁净/精度非此规模召回，bigram 0-dep fallback 仍有价值）——如实记录零 delta，不伪造。
- **tokenizer-default-on 评估**：迁移工具已备 + schema-driven 对称文档化（方案 B：`RetrieverConfig.tokenizer` vestigial，search 据 schema/`meta.json` 派生）；**默认仍 opt-in，full default flip 据「翻默认是产品决策」诚实延后** `[SPEC-DEFER:phase-future.tokenizer-default-on]`。

### 3. 扩 CJK golden 经 Go validator（task-30.2，🟢）

`go test ./internal/eval/...` PASS：`golden-semantic.jsonl` 11→16 行（+5 CJK case：可观测性/增量索引/生命周期/跨进程/向量检索，各映射强关联 ADR 文件），经 `ValidateGoldenSemantic`（schema/dup/category，`internal/eval/eval.go:231-280`）守门；既有 11 行 add-only 不删改。

### 3b. smoke v20（诚实记录）

- `bash -n scripts/console_smoke.sh` exit 0（CI go-test 语法门 + `TestTask303_SmokeV20CjkTrueSegmenterStep`）。
- v20 step 39 文档化 cjk-true-segmenter 状态（真分词 analyzer / 双站点 / reindex 工具 / +0.0000 delta）；`cjk-segmenter` feature-gated 无 console-api 运行时面，step 39 验默认构建仍 scaffold（默认 tokenization + 0-dep bigram fallback 不变，ADR-004）。既有 v13-v19 块 denominator 不溯改（新增 `[39/39]` add-only，ADR-014 D5）。

### 4. CI 状态（各 task PR 四门绿后自主合入）

| PR | cargo-test | go-test | spec-lint | lint |
|---|---|---|---|---|
| 30.1 (#202 真分词 analyzer + 双站点) | PASS（rerun：phase9 flake） | PASS | PASS | PASS |
| 30.2 (#203 reindex 工具 + recall delta) | PASS | PASS | PASS | PASS |
| 30.3 (this PR closeout) | PASS | PASS | PASS | PASS |

注：`core/tests/phase9_index_smoke.rs` Tantivy `LockBusy` 既有间歇 flake（与 Phase 30 无关）；#202 命中 → `gh run rerun` 全量复跑至绿（承 v0.13–v0.22 pattern），never-merge-red 守线。CI 默认不构建 `cjk-segmenter` feature（真分词单测/召回经 dev box feature build 实测）。

### 5. ADR-014 第二十一次激活 record（this closeout PR self-check）

`bash scripts/spec_drift_lint.sh --touched origin/master` → 0 未标注命中（CI spec-lint gate 复核）。各 task PR（30.1/30.2）触及行亦 0 未标注命中。

### 6. AC 达成（cjk-true-segmenter + tokenizer-default-on 评估 + 真实 recall delta + v0.23.0 收口）

| Phase 30 §6 AC | 状态 | 依据 |
|---|---|---|
| AC1 真分词器 feature + 双站点对称（🟢 / 🔴 dict dep） | ✅ met | task-30.1（#202）；jieba 真分词 2/2 + 双站点 round-trip（§3.1）；jieba 纯 Rust 无重词典受阻 |
| AC2 tokenizer-default-on + migration + 真实 recall delta（🟡） | ✅ met（default flip honest-defer 如实） | task-30.2（#203）；reindex 工具 + schema-driven 对称 + 真实 delta +0.0000 诚实零（§3.2）；default flip 延后 |
| AC3 默认 0-dep 不变 + v0.23.0 closeout | ✅ met | task-30.3（this PR）；默认 `cargo test --workspace` 0 failed + 0 新 dep + release docs + smoke v20 + ADR-035 per-D ratify |
| AC4 ADR-014 cross-validation gate（第二十一次激活） | ✅ met | 本 closeout PR body（D1-D5，见 §6） |

## 4. 平台矩阵

| Platform | Build | Note |
|---|---|---|
| Windows MSVC (dev box) | 默认 build + cargo/go test PASS；clippy `-D warnings` exit 0；smoke `bash -n` exit 0 | `cjk-segmenter`（jieba-rs 0.7.4）feature build + 真分词单测 + recall harness 实测（rustc 1.95.0，2026-06-03） |
| Linux (GHA runner) | 默认 build + cargo/go test + spec-lint + lint PASS | CI 四门；默认不构建 cjk-segmenter feature（真分词单测/召回经 dev box 实测） |
| 镜像 linux/amd64 | release.yml 推 GHCR（默认构建 0 新 dep / 0 network） | 与 v0.22.0 baseline 一致；cjk-segmenter feature-gated 不入默认镜像 |

## 5. Upgrade / Rollback

- Upgrade：默认行为不变（0 新代码 dep / 0 network；默认 `content` tokenization + 6-field schema 不变，既有 v0.6–v0.22 client + 既有默认索引无需改动）。新增 `cjk-segmenter` feature + jieba-rs（optional）+ `reindex_with_tokenizer` 工具均不影响默认路径；bigram `code_cjk` opt-in 保留。
- Rollback：`git tag -d v0.23.0` + 删 GitHub Release / ghcr tag；与 v0.22.0 行为兼容（真分词全 feature-gated，默认运行时不变）。

## 6. ADR-014 第二十一次激活 record

- **D1（mapping）**：Phase 30 §6 AC1-4 ↔ task-30.1（§6 AC1-4）/ task-30.2（§6 AC1-4）/ task-30.3（§6 AC1-3）。AC1↔30.1（真分词 + 双站点）/ AC2↔30.2（default-on 评估 + migration + delta）/ AC3↔30.3（release+ADR+§6）/ AC4↔本 PR body。
- **D2（lint）**：各 task PR（30.1/30.2）+ 本 closeout `spec_drift_lint.sh --touched origin/master` 0 未标注命中（CI spec-lint）。
- **D3（verified-by）**：每 AC 显式 owner TEST-id（TEST-30.1.x / 30.2.x / 30.3.x）+ 真实证据（真分词 2/2 + 双站点 round-trip / reindex round-trip 默认+feature / 真实 recall delta +0.0000 诚实零 / Go validator PASS）。
- **D4（自治）**：主 agent ADR-012 自治据真实数据推进；never-merge-red + 禁据合成 ratify（ADR-013）守线；ADR-035 据 D1（真分词 feature 真实达成）+ D2（双站点对称真实达成）+ D3（reindex 工具达成；**default flip honest-defer 不强 ratify、不伪造已翻**）+ D4（真实 recall delta 含诚实零 delta）+ D5（默认 baseline 不变）真实非合成验证 ratify Accepted；受阻维度（lindera 重词典 / 大语料 / default flip）据真实受限**如实记录**（不伪造）。tag / release outward-facing 不可逆 → 须用户显式授权（沿用 v0.13-v0.22 红线，不自行 tag）。
- **D5（不溯改）**：Phase 1-29 spec 未溯改；真分词器 + 双站点 + reindex 工具 + recall delta 落地 + ratify 记录写在 ADR-035 Ratification + ADR-029 add-only Amendment（Phase 30）+ task-30.1-30.3 §10 + 本文件；jieba-rs 经 ADR-008 add-only（optional）；smoke 既有 v13-v19 块 denominator 不溯改（新增 v20 step 39，add-only）；`internal/cli/smoke_syntax_test.go` 加 `TestTask303_*`（add-only）；golden 既有 11 行 add-only 不改；ADR-014 history Phase 1-29 不 retro-edit（21st activation add-only）。

## 7. tag / 镜像 SHA（post-tag-push backfill）

post-tag-push backfill（无人值守授权下主 agent push（ADR-012） tag 触发 `release.yml` 后填实，承 v0.8–v0.22 pattern）：

- v0.23.0 annotated tag SHA：`<backfill: v0.23.0 tag SHA @ task-30.3 closeout merge>`
- `release.yml` run ID：`<backfill: release run id + conclusion>`
- 镜像 digest：`ghcr.io/tajiaoyezi/contextforge-daemon:v0.23.0` + `:latest` = `<backfill: sha256 digest>`（linux/amd64）
- cosign keyless 签名凭据：`<backfill: Rekor tlog idx (sign + attest SBOM)>`
