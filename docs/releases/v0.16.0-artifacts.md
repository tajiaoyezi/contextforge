# ContextForge v0.16.0 Artifacts — vector-persistence-and-cross-platform (hnsw 持久化 + sqlite-vec MSVC + ADR-028 ratified)

> Artifact inventory for the v0.16.0 minor release (Phase 23). Mirrors the v0.15.0 artifacts structure.
> The default build is unchanged, 0-vector-dependency, and BM25-baseline. Vector persistence (hnsw
> `save`/`load`) and sqlite-vec cross-platform are feature/deploy choices (`vector-hnsw` /
> `vector-sqlite`). No breaking contract bump — `VectorIndexConfig.persistence_path` is an existing
> field first consumed this release; no proto change.

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：本 release 零 Go 行为变更（smoke v13 step 32 文档化 Phase 23 状态；既有命令不变）。
- `contextforge-core`（Rust 数据面 daemon）：默认构建 = BM25 + 0-dep `BruteForceVectorBackend` 语义/hybrid 路径（**brute-force 支持行级增量追加**，task-23.3）；`HnswBackend`（`vector-hnsw`）现支持 `save`/`load` 图持久化 + rebuild-on-load；`SqliteVecBackend`（`vector-sqlite`）现在 Windows MSVC 真实可构建+运行（task-23.2）。
- Feature 构建：`--features vector-hnsw`（hnsw 图持久化往返）；`--features vector-sqlite`（sqlite-vec vec0，Linux gcc + Windows MSVC 均可构建）；`--features embedding-fastembed`/`embedding-remote`/`reranker-fastembed` 见 ADR-027/026。

## 2. 源码档

- master tag SHA：`a945a29560e23a3aab61b750f595d036d91c1195`（v0.16.0 annotated tag，无人值守授权下主 agent push，2026-05-31）
- Phase 23 代码路径：
  - task-23.1（#168）：`core/src/retriever/vector/hnsw.rs`（`PersistedHnsw` + `save`/`load` 路径 B + `open` 接通 `persistence_path` + 3 测试）。
  - task-23.2（#169）：`core/src/retriever/vector/sqlite_vec.rs`（`#[cfg(test)] mod tests` 契约测试，0 backend 逻辑改动）、`docs/spikes/phase-23-sqlite-vec-cross-platform.md`。
  - task-23.3（this PR）：`core/src/retriever/vector/brute_force.rs`（TEST-23.3.1 增量追加）、`scripts/console_smoke.sh`（v13 step 32）、`internal/cli/smoke_syntax_test.go`（`TestTask233_*`）、`docs/releases/v0.16.0-{evidence,artifacts}.md`、`test/features/phase-23-vector-persistence-and-cross-platform.feature`。

## 3. 验证脚本

```bash
# 默认构建（0 vector dep）
cargo test --workspace
go test ./...

# feature 下持久化 / 跨平台
cargo test --features vector-hnsw -p contextforge-core retriever::vector::hnsw       # hnsw 持久化往返
cargo test --features vector-sqlite -p contextforge-core retriever::vector::sqlite_vec  # sqlite-vec（Linux + Windows MSVC 均可）

# 端到端 smoke v13（含 step 32 Phase 23 状态；任意 MODE）
bash scripts/console_smoke.sh        # 期望末行 CONSOLE_REAL_SMOKE_EXIT=0

# D2 spec-drift lint
bash scripts/spec_drift_lint.sh --touched origin/master
```

## 4. 镜像推送

- `release.yml`（`v*` tag push 触发）→ Docker Buildx → ghcr login → build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.16.0` + `:latest`（linux/amd64）。
- 镜像 = **默认 feature 构建**：BM25 + 0-dep 语义/hybrid 路径，与 v0.15.0 baseline 行为兼容；hnsw 持久化 / sqlite-vec 跨平台为 feature-gated 非默认镜像内容（ADR-023 D5）。
- run ID + digest：`release.yml` run `26710926292`（**success**，2026-05-31）；`ghcr.io/tajiaoyezi/contextforge-daemon:v0.16.0` + `:latest` = `sha256:3df47e05252810b2b87da58aa308806e99df150e827ece2746f22fa638f1f1ae`；git tag SHA `a945a29560e23a3aab61b750f595d036d91c1195`。

## 5. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-028 vector-persistence-strategy | **Accepted** | task-23.3 据 task-23.1/23.2 真实非合成验证 ratify（Proposed→Accepted）：D1 hnsw 图持久化（路径 B 往返 3/3 PASS）/ D2 sqlite-vec 跨平台（Windows MSVC 真实构建+运行通过，解除 Phase 18 stop-condition）/ D3 增量索引（brute-force/sqlite-vec 行级追加 + hnsw 延后）/ D4 默认 0-vector-dep 不变 均经真实测试验证；sqlite-vec caveat（单机/CI 默认不构建）+ hnsw 增量延后如实记录（ADR-013） |
| ADR-023 vector-backend-default | Accepted（+ add-only Amendment） | task-23.3 add-only Amendment 推进 Follow-ups：D2「hnsw in-memory-only / rebuild-on-restart」前提经 task-23.1 图持久化**解除**；「sqlite-vec Windows-MSVC-build-blocked / dev-prod parity imperfect」经 task-23.2 真实构建**缩小**（不溯改 ADR-023 D1-D6 正文，D5） |
| ADR-013 no-fake-evidence | Accepted | 守线：ADR-028 ratify 据真实 cargo build/test；sqlite-vec MSVC 真实凭据 + caveat 如实；hnsw 增量延后不伪造；本 phase 无 recall 数值 |
| ADR-014 cross-phase-exit-criteria-validation | Accepted | 第十四次激活（v0.16） |
| ADR-008 core-library-selection | Accepted | task-23.1 路径 B + task-23.2 路径(a) 均**0 新 dep**（serde/serde_json 已 direct；sqlite-vec 维持 =0.1.9 pin）——无 ADR-008 依赖变更 Amendment |
| ADR-002 sqlite-tantivy-layered-storage | Accepted | sqlite-vec vec0 复用 rusqlite bundled（task-23.2 跨平台构建不改 storage 分层） |

## 6. 版本声明（contract bump 评估）

- `VectorIndexConfig.persistence_path`（task-18.1 既有字段）本 release 首次被 `HnswBackend::open` 消费（`None` → 纯内存现状，向后兼容）。proto 零 delta（向量持久化为 core 库 backend 层，无 proto 字段）。Console Contract v1 shape 不变；31/32-step smoke 不破坏。
- 故 **无 breaking contract bump**；既有客户端无需改动。持久化 / 跨平台 / 增量均为 feature-gated backend 能力。

## 7. 后续模板（vector-persistence follow-on）

- 持久化图接进 `server.rs` 语义热路径（重启复用已存图替代按需重建）——本 release 落 backend 层 `save`/`load` 能力，热路径接入留后续。
- 向量增量索引完整化（hnsw 增量插入 + 单 chunk DELETE）`[SPEC-DEFER:phase-future.vector-incremental-index]`——本 release 落 brute-force/sqlite-vec 行级追加 + 评估口径，建图类增量受 crate 限延后。
- sqlite-vec on-disk 编码细化 `[SPEC-DEFER:phase-future.sqlite-vec-on-disk]`；sqlite-vec 跨 CI MSVC 持续守护（本 release 单机真实凭据，跨环境守护后续）。
- hnsw 原生序列化（路径 A，`instant-distance` with-serde）替代路径 B 重建——大语料「加载即用」优化后续。

## 8. 存档指引

- closeout PR 合入后，无人值守授权下主 agent 自主 push `v0.16.0` annotated tag 触发 `release.yml`；post-tag-push backfill PR 填实 tag SHA + run ID + 镜像 digest（承 v0.8–v0.15 pattern）。
- Dockerfile workspace member COPY 完整性预检（承 v0.11.0 PR #139 教训）：本 release 改动均在 `core/` member 内（已 COPY），新增 `test/features/phase-23-*.feature` 不影响 Dockerfile；release 前确认 COPY 覆盖。
