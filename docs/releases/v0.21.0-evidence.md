# ContextForge v0.21.0 Evidence — release-ci-hardening (multi-arch（arm64 deferred）+ 匿名可拉取守护 + cosign 签名 + SBOM + provenance + CI 强 lint + ADR-033 ratified)

> v0.21.0 ships Phase 28: 硬化 v0.1–v0.20 一路沿用的发布 / CI 流水线，兑现 roadmap §3.9 / PRD §524 /
> RELEASE_NOTES §451 一路 `[SPEC-DEFER:phase-future.*]` 延后的四项发布硬化 marker。**匿名（logged-out）可拉取守护**
> 加入 `verify-image.yml`（守 v0.10.0 「shipped PRIVATE → 403」回归，真实 run VERIFIED）；**multi-arch arm64**
> 经 QEMU emulation 实测构建不可行（≥45 min 仍在编 Rust deps），**DEFERRED**——`release.yml` 保 linux/amd64 单架构
> （net-zero）+ arm64 `[SPEC-DEFER:phase-future.multi-arch-native-runner]`（task-28.1）；**cosign keyless 签名 +
> cosign attest SPDX SBOM（syft）+ build-push provenance:mode=max** 加入 `release.yml`，`verify-image.yml` 加
> `cosign verify` + `verify-attestation`（GitHub-native attestation 在 user-owned 私有 repo 不可用 → 切 cosign，
> 机制经 local registry 端到端 VERIFIED；**真实 GHCR 签名 / attestation 在 v0.21.0 release run 兑现——尚未发生**）
> （task-28.2）；**CI `lint` job**（clippy `-D warnings` + gofmt + go vet，全阻断）加入 `ci.yml`，真实存量经 CI/LF
> 权威实测后**全量修复**（task-28.3）。全部为 `.github/workflows/*` + surgical clippy/gofmt 修复层改动；镜像运行时
> 行为 + 默认构建 0-network / 0 新依赖 baseline 不变（ADR-004），0 新代码依赖。**ADR-033** 据 D1–D4 **真实非合成**
> 验证 ratify `Proposed → Accepted`（D1 PARTIAL：anon-pull 真实、arm64 DEFERRED）；**ADR-007** 经 add-only
> Amendment 记录部署发布面扩展到 multi-arch 签名 OCI（arm64 deferred）。

## 1. 合入记录 (Phase 28, tasks 28.1–28.4)

| task | PR | delivery |
|---|---|---|
| 28.1 | #188 | `verify-image.yml` add-only 未鉴权（logged-out / 清 token）匿名 `docker pull` 守护步——断言 GHCR 包公开可拉取（守 v0.10.0 PRIVATE → 403 回归），真实 run `26788773926` **success** VERIFIED；multi-arch arm64 经 `setup-qemu-action` workflow_dispatch 实测 **不可行**（run `26757640892` 在 45 min cancelled，QEMU emulation 仍在编 Rust deps）→ `release.yml` 保 `platforms: linux/amd64` 单架构（net-zero）+ arm64 `[SPEC-DEFER:phase-future.multi-arch-native-runner]` 如实延后；`TEST-28.1.1–28.1.2`（verify-image anon-pull step + release.yml platforms 守线，**0 新 dep**） |
| 28.2 | #189 | `release.yml` 加 `id-token: write` + **cosign keyless 签** manifest digest + **cosign attest** SPDX **SBOM（syft）** + `docker/build-push-action` `provenance: mode=max`；`verify-image.yml` 加 `cosign verify` + `cosign verify-attestation`。GitHub-native attestation（`actions/attest-*`）**先试但在 user-owned 私有 repo 不可用**（run `26789731232` failure）→ 切 cosign（= ADR-033 §D2 原始意图）；机制经 **local registry 端到端 VERIFIED**（run `26799480280` **success**：cosign sign + attest + verify + verify-attestation 全通过）；**真实 GHCR 镜像签名 / attestation 已于 v0.21.0 release run（用户授权 2026-06-02）兑现**（run `26822648951` success：cosign sign + cosign attest SBOM 步绿 + Rekor tlog idx 1703992493 / 1703993393，digest `sha256:f2cb3aadd5608a42e247aad562cb64f1c260dee1a5f4909c862e6484044d1e46`）；`TEST-28.2.1–28.2.2`（cosign sign+attest+verify 机制 + verify-image cosign 步，**0 新代码 dep**，cosign/syft 为 CI action 工具） |
| 28.3 | #190 | `ci.yml` add-only `lint` job（clippy `-D warnings` + gofmt + go vet，全阻断）；既有 `cargo-test`/`go-test`/`spec-lint` 不退化。**真实存量（CI/LF 权威）**：gofmt 15 real files（local 96 = 15 real + 81 Windows-CRLF false positive，初测误判经 CI 纠正）/ go vet 0 / clippy ~33——**全量修复**（gofmt -w + strip-pipe；clippy --fix + manual + 2 targeted allow：generated `pb`/`pb_console` `clippy::all` + `EventBus::send` `result_large_err`）；clippy `-D warnings` exit 0 + `cargo test --workspace` all pass（core lib 187 passed）；`lint` job CI 绿（PR #190 run `26820737785`）；`TEST-28.3.1–28.3.2`（lint job 入门 + 三门不退化 + 存量修复，**0 新代码 dep**） |
| 28.4 | this PR | Phase 28 closeout：v0.21.0 release docs + smoke v18 step（发布硬化文档化 + 既有 step 不退化）+ ADR-033 ratify（Proposed→Accepted，D1 PARTIAL）+ ADR-007 add-only Amendment + phase-28 §6 闭合 + adapter + phase-28 BDD feature |

## 2. S2V 状态

- Phase 28 `release-ci-hardening`: Draft → **Done**, Tasks 0 → 4。
- phase-28 §6 AC1-5 全 `[x]`（据 28.1–28.4 真实交付；AC1 arm64 维度 DEFERRED 如实标注，amd64 + 匿名 pull 达成 → 部分 ratify）。
- 默认构建恒 0 新依赖 / 0 network（ADR-004）；全部改动在 `.github/workflows/*` + surgical clippy/gofmt 修复——镜像运行时行为 + 默认构建 baseline 不变；0 新代码依赖（cosign / syft / QEMU 为 CI action 工具，非 Cargo / go.mod direct dep）；不溯改 Phase 1-27 spec（ADR-014 D5）。
- **本 release 推进 roadmap §3.9 四 marker**：multi-arch（arm64 经实测 DEFERRED→native-runner）/ 匿名可拉取守护（真实达成）/ 镜像供应链证明 cosign 签名 + SBOM + provenance（机制 VERIFIED；真实 GHCR 签名在 release run 兑现）/ CI 强 lint（阻断门入 + 存量全修）。受阻态（arm64 emulation / GitHub-native attestation 私有 repo 不可用 / 真实签名尚未发生）据 ADR-013 **如实记录不伪造**。

## 3. 验证证据

### 1. multi-arch 镜像 + 匿名可拉取验证（task-28.1，0 新 dep）

`verify-image.yml` workflow_dispatch run `26788773926` **success**：
- TEST-28.1.1：`verify-image.yml` add-only 未鉴权（logged-out / 清 `docker logout ghcr.io`）`docker pull ghcr.io/tajiaoyezi/contextforge-daemon:latest` 步 exit 0——断言 GHCR 包公开可拉取（守 v0.10.0 GHCR 初始 PRIVATE → Console team 匿名 pull 403 回归）；既有鉴权 pull + `/v1/health` `contract_version=v1` + `:latest` digest parity 步保留不退化。
- TEST-28.1.2：multi-arch arm64 经 `docker/setup-qemu-action@v3` + `platforms: linux/amd64,linux/arm64` workflow_dispatch 实测——run `26757640892` 在 **45 min cancelled**（QEMU emulation 下 Rust `cargo build --release` arm64 仍在编 deps，未达可推 manifest 阶段，坐实 R1 emulation 超时风险）。
- **诚实记录（ADR-013）**：arm64 **未 ship**（DEFERRED）——`release.yml` 保 `platforms: linux/amd64` 单架构（net-zero，与 v0.20.0 baseline 一致），arm64 原生 runner / 交叉编译替代 QEMU `[SPEC-DEFER:phase-future.multi-arch-native-runner]` 如实延后；AC1 arm64 维度不标全勾（amd64 + 匿名 pull 达成 → 部分 ratify，不伪造 arm64 manifest 存在）。

### 2. cosign 签名 + SBOM + provenance（task-28.2，0 新代码 dep）

local registry 端到端 run `26799480280` **success**：
- TEST-28.2.1：`release.yml` 加 `permissions: id-token: write`（OIDC keyless）+ push 后 `sigstore/cosign-installer` + `cosign sign` 签 build-push-action 输出的 manifest digest（非 tag）+ `cosign attest` SPDX **SBOM（`anchore/sbom-action` syft）** + `docker/build-push-action` `provenance: mode=max`；机制经 **local registry** `cosign sign` + `cosign attest` + `cosign verify` + `cosign verify-attestation` **全通过**（run `26799480280` success）。
- TEST-28.2.2：`verify-image.yml` 加 `cosign verify`（验签）+ `cosign verify-attestation`（SBOM / provenance attestation 存在性 + predicate-type 断言）步。
- **诚实记录（ADR-013）**：(a) **GitHub-native attestation**（`actions/attest-build-provenance` / `actions/attest-sbom`）**先试但在 user-owned 私有 repo 不可用**（run `26789731232` failure）→ 切 **cosign**（= ADR-033 §D2 原始意图，keyless Fulcio/Rekor）；(b) **真实 GHCR 镜像签名 / SBOM / attestation 在 v0.21.0 release run（用户授权，本 closeout 之后）兑现——尚未发生**；本 release 落「机制经 local registry 端到端 VERIFIED」，真实 GHCR digest 签名属 release-run 时刻产物（§7 post-tag-push backfill 填实）；不伪造「真实 GHCR 镜像已签」（ADR-013）。
- 0 新代码依赖：cosign / syft 为 CI action 工具（`sigstore/cosign-installer` / `anchore/sbom-action`），非 Cargo / go.mod direct dep（`Cargo.toml` / `Cargo.lock` / `go.mod` 未改）。

### 3. CI 强 lint（clippy / gofmt / go vet 阻断门 + 存量全修）（task-28.3，0 新代码 dep）

`cargo clippy --workspace -- -D warnings` exit 0 + `cargo test --workspace` all pass（core lib **187 passed**）+ `lint` job CI 绿（PR #190 run `26820737785`）：
- TEST-28.3.1：`ci.yml` add-only `lint` job——`cargo clippy --workspace -- -D warnings`（全阻断）+ `gofmt -l`（卡红）+ `go vet ./...`（卡红）；既有 `cargo-test` / `go-test` / `spec-lint` 三 job 不退化。
- TEST-28.3.2：**真实存量经 CI/LF 权威实测**（ADR-013 非合成）——**gofmt 15 real files**（local `gofmt -l` 96 = 15 real + 81 Windows-CRLF false positive；初测 96 系本机 CRLF 误判，经 CI（LF checkout）纠正为 15 real）/ **go vet 0** / **clippy ~33**；据存量清零可达 → 选**阻断**（`-D warnings`），全量修复：gofmt `-w` + strip-pipe（CRLF 不入仓）；clippy `--fix` + manual + **2 targeted allow**（generated `pb` / `pb_console` `#![allow(clippy::all)]`——生成码不手改；`EventBus::send` `#[allow(clippy::result_large_err)]`——既有签名保不变 surgical）。
- **诚实记录（ADR-013）**：local 96 ≠ CI 15 的差异**如实记录**（Windows-CRLF false positive，CI/LF 为权威）；2 处 `allow` 为 surgical 边界（生成码 + 既有签名），非大面积重构既有代码（surgical 红线守住）。

### 3b. smoke v18（诚实记录）

- **脚本正确性**：`bash -n scripts/console_smoke.sh` exit 0（CI go-test 语法门 + `TestTask284_SmokeV18ReleaseCiHardeningStep`）。
- **v18 step 新增**：v18 加发布硬化文档化 step——记 multi-arch 状态（amd64 shipped / arm64 DEFERRED→native-runner）+ cosign 签名 / SBOM / provenance 机制（VERIFIED via local registry；真实 GHCR 签名 at release run）+ CI `lint` 门状态（clippy `-D warnings` + gofmt + go vet 阻断、存量全修）。既有 step（含 v13-v17 块）denominator 不溯改（ADR-014 D5）。
- **ADR-013**：发布硬化为 CI / release 层产物——smoke step 文档化状态；机制由真实 CI run（verify-image / local-registry / lint job run IDs，§3.1-3.3）权威验证；真实 GHCR 签名 / multi-arch manifest 属 release run 时刻（不在 smoke 内伪造）。

### 4. CI 状态（各 task PR 三门 + lint 门绿后自主合入）

| PR | cargo-test | go-test | spec-lint | lint（自 28.3 起） |
|---|---|---|---|---|
| 28.1 (#188 anon-pull guard + arm64 deferred) | PASS | PASS | PASS | n/a（lint job 28.3 引入） |
| 28.2 (#189 cosign sign + SBOM + provenance) | PASS | PASS | PASS | n/a（lint job 28.3 引入） |
| 28.3 (#190 CI 强 lint + 存量全修) | PASS | PASS | PASS | PASS（run `26820737785`） |
| 28.4 (this PR closeout) | PASS | PASS | PASS | PASS |

注：`core/tests/phase9_index_smoke.rs` Tantivy `LockBusy` 既有间歇 flake（与 Phase 28 无关）；命中时 master-push 后 `gh run rerun <id>` 全量复跑至绿（承 v0.13–v0.20 pattern），never-merge-red 守线。

### 5. ADR-014 D2 lint（this closeout PR self-check）

`bash scripts/spec_drift_lint.sh --touched origin/master` → 0 未标注命中（CI spec-lint gate 复核；本机 scoped touched 直查复核）。各 task PR（28.1/28.2/28.3）触及行亦 0 未标注命中。

### 6. AC 达成（multi-arch（arm64 deferred）+ 匿名可拉取 + cosign 签名/SBOM/provenance + CI 强 lint + v0.21.0 收口）

| Phase 28 §6 AC | 状态 | 依据 |
|---|---|---|
| AC1 multi-arch manifest list + 未鉴权匿名 pull 守护 | ✅ met（arm64 DEFERRED 如实标注） | task-28.1（#188）；anon-pull 真实 run `26788773926` success（§3.1）；**arm64 经 QEMU emulation 实测不可行（run `26757640892` 45min cancelled）→ DEFERRED `[SPEC-DEFER:phase-future.multi-arch-native-runner]`**，release.yml 保 linux/amd64（amd64 + 匿名 pull 达成 → 部分 ratify，不伪造 arm64 manifest） |
| AC2 cosign keyless 签名 + SBOM + provenance attestation + cosign verify | ✅ met（机制 VERIFIED + 真实 GHCR 签名已兑现） | task-28.2（#189）；cosign sign + attest + verify + verify-attestation 经 local registry 端到端 run `26799480280` success（§3.2）；GitHub-native attestation 私有 repo 不可用 → 切 cosign（如实记录）；**真实 GHCR 镜像签名已于 v0.21.0 release run `26822648951` 兑现**（cosign sign + attest 步绿 + Rekor tlog，digest `sha256:f2cb…4d1e46`，§7） |
| AC3 CI 强 lint（clippy/gofmt/go vet 阻断 + 存量结论如实） | ✅ met | task-28.3（#190）；`lint` job 入 `ci.yml`（clippy `-D warnings` + gofmt + go vet 全阻断）+ 真实存量 CI/LF 权威（gofmt 15 / go vet 0 / clippy ~33）全修 + clippy exit 0 + cargo test 187 passed + 三门不退化（§3.3） |
| AC4 v0.21.0 release docs + smoke v18 + ADR-033 ratify + ADR-007 add-only Amendment + phase §6 闭合 | ✅ met | task-28.4（this PR）；release docs（本文件 + artifacts）+ ADR-033 Accepted（D1 PARTIAL，§Artifacts §5）+ ADR-007 Amendment + smoke v18（§3b） |
| AC5 ADR-014 cross-validation gate（第十九次激活） | ✅ met | 本 closeout PR body（D1-D5，见 §6） |

## 4. 平台矩阵

| Platform | Build | Note |
|---|---|---|
| Windows MSVC (dev box) | 默认 build + cargo/go test PASS；clippy `-D warnings` exit 0；smoke `bash -n` exit 0 | local `gofmt -l` 96（15 real + 81 Windows-CRLF false positive，CI/LF 权威 15）——存量经 CI 纠正后全修（rustc 1.95.0，2026-06-01） |
| Linux (GHA runner) | 默认 build + cargo/go test + spec-lint + **lint job** PASS | CI 四门（cargo-test / go-test / spec-lint + 新 lint）；verify-image.yml 匿名 pull run `26788773926` success；cosign 机制 local registry run `26799480280` success；lint job run `26820737785` PASS |
| linux/amd64 镜像 | release.yml 推 GHCR（默认构建 0 新 dep / 0 network） | 与 v0.20.0 baseline 一致（单架构 net-zero）；cosign 签名 / SBOM / provenance 在 v0.21.0 release run 兑现（§7 backfill） |
| linux/arm64 镜像 | **DEFERRED** | QEMU emulation 实测不可行（run `26757640892` 45min cancelled）→ `[SPEC-DEFER:phase-future.multi-arch-native-runner]`（原生 runner / 交叉编译） |

## 5. Upgrade / Rollback

- Upgrade：默认行为不变（0 新代码 dep / 0 network；镜像运行时行为 + 默认构建 baseline 不变，既有 v0.6–v0.20 client 无需改动），无强制迁移。本 release 全部改动在 `.github/workflows/*`（release.yml cosign + provenance / verify-image.yml 匿名 pull + cosign verify / ci.yml lint job）+ surgical clippy/gofmt 修复——发布 / CI / 质量门层，运行时二进制与 v0.20.0 行为兼容。
- Rollback：`git tag -d v0.21.0` + 删 GitHub Release / ghcr tag；与 v0.20.0 行为兼容（workflow 改动可独立 revert，运行时无变化）。cosign 签名 / SBOM / provenance / 匿名 pull 守护均为 build/publish/CI 层 add-only，回退不影响既有镜像运行。**arm64 镜像本 release 未 ship**（DEFERRED），无回退影响。

## 6. ADR-014 第十九次激活 record

- **D1（mapping）**：Phase 28 §6 AC1-5 ↔ task-28.1（§6 AC1-2）/ task-28.2（§6 AC1-2）/ task-28.3（§6 AC1-2）/ task-28.4（§6 AC2-3）。AC1↔28.1（multi-arch（arm64 DEFERRED）+ 匿名 pull）/ AC2↔28.2（cosign 签名 + SBOM + provenance）/ AC3↔28.3（CI 强 lint）/ AC4↔28.4（release+ADR+§6）/ AC5↔本 PR body。
- **D2（lint）**：各 task PR（28.1/28.2/28.3）+ 本 closeout `spec_drift_lint.sh --touched origin/master` 0 未标注命中（CI spec-lint）。
- **D3（verified-by）**：每 AC 显式 owner TEST-id（TEST-28.1.x / TEST-28.2.x / TEST-28.3.x）+ 真实 run IDs（anon-pull `26788773926` / cosign local-registry `26799480280` / lint `26820737785` / arm64-deferred `26757640892` cancelled / native-attestation `26789731232` failure）。
- **D4（自治）**：主 agent ADR-012 自治据真实数据推进；never-merge-red + 禁据合成 ratify（ADR-013）守线；ADR-033 据 D1（anon-pull 真实 success；**arm64 DEFERRED 如实记录——不伪造 manifest**）+ D2（cosign sign+attest+verify 机制 local registry 端到端 success；**真实 GHCR 签名 at release run——尚未发生**；GitHub-native attestation 私有 repo 不可用如实记录）+ D3（lint 门入 + 真实存量 CI/LF 权威全修）+ D4（镜像运行时 / 默认 0-dep / 0-network baseline 不变）真实非合成验证 ratify Accepted（D1 PARTIAL）；受阻维度据真实受限**如实记录**（不伪造）。tag / release outward-facing 不可逆 → 须用户显式授权（沿用 v0.13-v0.20 红线，不自行 tag）。
- **D5（不溯改）**：Phase 1-27 spec 未溯改；multi-arch（arm64 deferred）+ 匿名 pull + cosign 签名 + SBOM + provenance + CI 强 lint 落地 + ratify 记录写在 ADR-033 Ratification + ADR-007 add-only Amendment + task-28.1-28.4 §10 + 本文件；全部改动在 `.github/workflows/*` + surgical clippy/gofmt 修复（既有 workflow job + 运行时二进制 + proto 不动）；smoke 既有 v13-v17 块 denominator 不溯改（新增 v18 step，add-only）；`internal/cli/smoke_syntax_test.go` 加 `TestTask284_*`（add-only，未改 Phase 23-27 test）；ADR-014 history Phase 1-27 不 retro-edit（19th activation add-only）。

## 7. tag / 镜像 SHA（post-tag-push backfill）

post-tag-push backfill（用户授权 2026-06-02 主 agent push（ADR-012） tag 后填实，承 v0.8–v0.20 pattern）；**v0.21.0 真实 GHCR 镜像签名 / SBOM / provenance attestation 已于此 release run 兑现**：

- v0.21.0 annotated tag SHA：`08ba7202921c7e8cfcee9dfb76c2a634dd3f0c94`（master @ task-28.4 closeout merge #191）
- `release.yml` run ID：`26822648951`（conclusion **success**；cosign sign + cosign attest SBOM + provenance:mode=max 步全绿，真实 GHCR digest keyless 签名）
- 镜像 digest：`ghcr.io/tajiaoyezi/contextforge-daemon:v0.21.0` + `:latest` = `sha256:f2cb3aadd5608a42e247aad562cb64f1c260dee1a5f4909c862e6484044d1e46`（linux/amd64；arm64 DEFERRED）
- cosign keyless 签名凭据：release run 内 ephemeral keys + **Rekor 透明日志** tlog idx `1703992493`（cosign sign）/ `1703993393`（cosign attest SBOM），`.sig` 推 GHCR。独立 `cosign verify` + `cosign verify-attestation` 经 `verify-image.yml` workflow_dispatch run `26823246086`（tag=v0.21.0）。
- 验证：`docker pull ghcr.io/tajiaoyezi/contextforge-daemon:v0.21.0`；`cosign verify ghcr.io/tajiaoyezi/contextforge-daemon:v0.21.0 --certificate-identity-regexp '^https://github.com/tajiaoyezi/contextforge/\.github/workflows/release\.yml@.*$' --certificate-oidc-issuer https://token.actions.githubusercontent.com`
