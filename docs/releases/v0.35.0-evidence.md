# ContextForge v0.35.0 Evidence — chunk-source-type-filter (把 chunk 检索的 `source_type` 过滤从 Phase 32 / ADR-037 据实记的 documented no-op 落地为真实过滤；source_type 由 file_path 确定性派生 0 schema migration + console-api `?source_type=` forward + agent_scope 据 grounding honest-defer + ADR-047 ratified)

> v0.35.0 ships Phase 42（chunk-source-type-filter，承 Phase 32 / ADR-037 据实延后的 chunk filter 血脉）——**0 新 dep**（`classify_source_type` 纯 std）/ **0 network** / **0 schema migration**（chunks/files/provenance §5.3 FROZEN）/ **空 filter byte-equiv**——遵 ADR-013「诚实优先于凑数」：
> - **chunk-source-type-filter**（兑现 ADR-037 task-32.3 `[SPEC-DEFER:phase-future.chunk-source-type-filter]`）：`SearchFilters.source_type` + v1 proto `SearchFilters.source_type=1` + `RetrievalResult.source_type=3` 自 task-4.2 起就有契约，但 Phase 32 经核 chunks 表无该列、`SearchResult.source_type` 恒 `DEFAULT_SOURCE_TYPE=""`、遂据实定为 documented no-op。grounding：**source_type 可由 file_path 确定性派生**（task-42.1 `core/src/retriever/mod.rs classify_source_type`，镜像 `indexer::lang_hint_from_path`：扩展名 → 粗粒度桶 code/doc/config/other）→ 无须存储、无须 schema migration（§5.3 FROZEN），确定性派生 == 存储值。task-42.1 三构造点（`search()` BM25 / `get_chunk` / `assemble_vector_result`）populate 真实 source_type（填补 task-4.2 §2A v0.1 schema gap）+ `search()` BM25 source_type post-filter（镜像 language post-filter，空 → byte-equiv）；v1 `server.rs:440-453` 已映射 proto `filters.source_type` → retriever 真实过滤后 v1 gRPC/REST body 立即生效。
> - **console-api forward**（task-42.2）：`console_data_plane.proto` `SearchRequest` add-only `repeated string source_type = 9`（既有字段 1-8 冻结，ADR-015）+ `data_plane/search.rs` 按 `req.source_type` 对 populate 后的 hit post-filter（覆盖 BM25/semantic/hybrid 一致）+ Go `contractv1.SearchRequest.SourceType` + `handleSearch ?source_type=` query/body 并集（镜像 `?semantic`/`?hybrid`）+ grpcclient → `pb.source_type`。console 响应 `SearchResultItem.source_file_type=5` 响应侧早已就绪（task-42.1 populate 后立即显示真实值）。
> - **ADR-047-ratified**：ADR-047 据 D1-D4 per-D ratify `Proposed → Accepted`；ADR-037（source_type no-op 被真实过滤 supersede / agent_scope no-op 据实保持）add-only Phase-42 Amendment，**不**溯改 D-body（ADR-014 D5）。
> **诚实校正（ADR-013，本 phase 核心）**：`chunk-source-type-filter` 与 `chunk-agent-scope-filter` 经 grounding **不对称**——source_type 可由 file_path 确定性派生（0 migration）真实落地；`agent_scope` 是 memory 层概念（`memory_items` 0013 / `MemoryListFilter` / `ListMemory` scope / `internal/consoleapi/memstore.go:629-635`）、chunks 无 agent 关联、无可派生维度 → **本 phase 不伪造**，续 documented no-op + `[SPEC-DEFER:phase-future.chunk-agent-scope-filter]`（须 ingest-path schema 工程 + 价值不明，镜像 Phase 32/34/35 grounding 校正手法）。source_type value 由空串变真实派生值系填补 task-4.2 §2A v0.1 schema gap（契约本意），非破坏性默认变更（空 filter 过滤行为 byte-equiv，仅 value 字段由 "" 变派生值）。

## 1. 合入记录 (Phase 42, tasks 42.1–42.2)

| task | PR | delivery |
|---|---|---|
| 42.1 | #267 | chunk-source-type-derivation-and-filter：`core/src/retriever/mod.rs` add `pub(crate) fn classify_source_type(file_path) -> &'static str`（扩展名确定性桶 code/doc/config/other，镜像 `indexer::lang_hint_from_path`，纯 std `Path::extension`，无扩展名/未知/dotfile → other）+ 三构造点（`search()` BM25 / `get_chunk` / `assemble_vector_result`）`source_type` 由 `DEFAULT_SOURCE_TYPE` 改 `classify_source_type(&file_path)` 真实派生 + `search()` BM25 source_type post-filter（SQLite JOIN 后、provenance 前，镜像 `:386` language post-filter，空 → 不过滤 byte-equiv）+ 窄化 no-op 块仅 agent_scope。**grounding 校正**：`DEFAULT_SOURCE_TYPE` 三构造点改派生后成孤儿（`-D warnings` dead_code）→ 删除（规划稿「不删常量」plan 假设被编译期 grounding 覆盖）；populate 连带更新断言旧 schema-gap "" 的既有测试（`test_4_2_1`/`test_6_2_e1`/`server.rs` search wire/`phase4_smoke`/`phase6_smoke` 改断言「有效桶」）。TEST-42.1.1（classify 矩阵）+ TEST-42.1.2（真实过滤 + populate + agent_scope no-op）。**0 新 dep（纯 std）/ 0 schema migration（§5.3 FROZEN）** |
| 42.2 | #268 | console-api-source-type-forward：`console_data_plane.proto` `SearchRequest` add-only `repeated string source_type = 9`（既有字段 1-8 号冻结，ADR-015 add-only，`buf generate proto`）+ `core/src/data_plane/search.rs` `query()` 在 hits 装配后按 `req.source_type` post-filter（覆盖 BM25/semantic/hybrid 一致，利用 task-42.1 populate 的 `h.source_type`，空 → 不过滤）+ Go `internal/contractv1.SearchRequest` add-only `SourceType []string` + `handleSearch` `?source_type=` query param + body 并集合并（镜像 `?semantic`/`?hybrid`）+ grpcclient `SourceType: req.SourceType` → `pb.source_type`。**grounding 校正**：`buf generate proto`（module 根 `proto/buf.yaml`）非裸 `buf generate`；buf 重写的 4 不相关 pb.go 据实还原仅留 `console_data_plane.pb.go`（field+getter+rawDesc 字节重排属正常）；proto add-only 新字段令既有显式 `PbSearchRequest` 字面量补 `source_type: Vec::new()`（mod.rs + 2 integration test）。TEST-42.2.1（prost wire-tag field 9 → 0x4A）+ TEST-42.2.2（handleSearch 并集 + grpcclient 转发 + data_plane post-filter）。**0 新 dep + proto add-only + 空 filter byte-equiv** |
| 42.x | this | chunk-source-type-filter closeout。Phase 42 closeout：smoke v32 `[51/51]`（REAL `?source_type=code` 保留 JobRunner code hit（`source_file_type=code`）/ `?source_type=doc` 过滤掉它，distinguishing：no-op 会仍返回）+ `TestTask423`（no-regression [37/37]..[50/50]）+ v0.35.0 release docs + ADR-047 据 D1-D4 ratify + ADR-037 add-only Phase-42 Amendment（source_type no-op supersede / agent_scope no-op 保持）+ ADR-015/024/044/004/008 守线引用 + roadmap §3.24/§4 + phase-42 §6 + adapter + defer marker 更新 |

## 2. S2V 状态

- Phase 42 `chunk-source-type-filter`: Draft → **Done**, Tasks 0 → 3。
- phase-42 §6 AC 全 `[x]`（据 42.1–42.2 真实交付；AC1 source_type 派生 + 真实过滤 + populate 🟢 + AC2 console-api source_type 请求侧 forward 🟢 + AC3 agent_scope honest-defer + 0-dep/0-migration 守线 + v0.35.0 收口 🟢 + AC4 ADR-014 第三十三次激活）。
- 默认 `cargo test --workspace` 全过（core lib **225** = 222 baseline + net 3：retriever TEST-42.1.1/.2 替 TEST-32.3.2 net +1 + data_plane TEST-42.2.1/.2 +2）；`go test ./...` 全过；**0 新 dep / 0 network / 0 schema migration**；空 source_type filter byte-equiv；不溯改 Phase 1-41 spec / ADR-037 D-body（ADR-014 D5）。
- **本 release 兑现 chunk-source-type-filter**：source_type 真实过滤落地（关闭 Phase 32 据实记的 no-op 诚实缺口）；agent_scope 据 grounding honest-defer 续 no-op、其余检索 marker 据 ADR-013 据实保持延后、不强行扩面。

## 3. 验证证据

### 1. chunk-source-type-derivation-and-filter（task-42.1，🟢）

`cargo test -p contextforge-core --lib retriever::test_42_1` PASS（2/2）：
- TEST-42.1.1（`retriever::tests::test_42_1_1_classify_source_type_buckets`）——扩展名 → 桶矩阵穷举：code（.rs/.go/.py/.ts/.jsx/.java/.sh/.sql/.cpp）/ doc（.md/.txt/.rst/.adoc/.mdx）/ config（.toml/.yaml/.json/.ini/prod.env/.xml）/ other（.bin/.png/Makefile/LICENSE/noext/`.env` dotfile）+ 大小写不敏感（MAIN.RS→code / READ.MD→doc）。`.env` dotfile → other（`Path::extension` 对 leading-dot 文件返 None，确定性兜底）。
- TEST-42.1.2（`retriever::tests::test_42_1_2_source_type_filter_populate_and_agent_scope_noop`）——混合 fixture（a.rs + b.md + c.toml 共享 marker）：baseline（空 filter）3 hit 且每条 `source_type ∈ {code,doc,config}` 真实派生；`source_type=[doc]` 仅返 b.md（`source_type=="doc"`）/ `[code]` 仅返 a.rs / `[code,config]` 并集 2 hit 非 doc；**非空 agent_scope filter 返与 baseline byte-identical**（agent_scope 续 documented no-op）。

v1 读路径已就绪（`server.rs:440-453` 映射 proto `filters.source_type` → `RetrieverFilters`），retriever 真实过滤后 v1 gRPC / v1 REST body（`rest.go:137` 解码完整 proto SearchRequest 含 `filters`）立即生效（无须改 v1 server/proto）。**0 schema migration**：source_type 由 file_path query 时确定性派生（与 `language` 同源信号），chunks/files/provenance §5.3 保持 FROZEN。

### 2. console-api-source-type-forward（task-42.2，🟢）

`cargo test -p contextforge-core --lib data_plane::search::test_42_2` PASS + `go test ./internal/consoleapi/...` PASS：
- TEST-42.2.1（`data_plane::search::tests::test_42_2_1_source_type_proto_field_number`）——`SearchRequest{source_type:["x"]}.encode_to_vec() == [0x4A, 0x01, 0x78]`（field 9 repeated string，tag (9<<3)|2=0x4A）；空 `SearchRequest` → 0 字节（backward-compat，既有 client 不传 → 行为不变）；既有字段 1-8 号不动。
- TEST-42.2.2：(a) `data_plane::search::tests::test_42_2_2_dataplane_source_type_filter`——.rs+.md fixture：空 source_type → 2 hit byte-equiv / `[doc]` → 仅 b.md（`source_file_type="doc"`）/ `[code]` → 仅 a.rs（`source_file_type="code"`）；(b) `TestTask422_HandleSearchSourceTypeUnion`——`?source_type=code&source_type=doc`（repeated query param）+ body `source_type` 并集合并 → 下游 SearchClient（capturingSearch）；(c) `TestTask422_GrpcClient_Search_ForwardsSourceType`——`contractv1.SearchRequest.SourceType` → `pb.SearchRequest.source_type`（含空 → nil backward-compat）。

console 响应侧 `SearchResultItem.source_file_type=5`（`data_plane/search.rs:378`）早已就绪，task-42.1 populate `h.source_type` 后 console 响应立即显示真实派生值（无须改响应侧）。

### 3. agent_scope honest-defer（ADR-047 D4，ADR-013 据实）

`agent_scope` 经 grounding 为 memory 层概念（`memory_items` migration 0013 / `MemoryListFilter` / `ListMemory` scope filter / `internal/consoleapi/memstore.go:629-635` `item.AgentScope` 过滤）；chunks 无 agent 关联、无可派生维度。本 phase **不伪造** chunk-level agent_scope filter——retriever 窄化 no-op 块仅覆盖 agent_scope（非空 → byte-equiv，TEST-42.1.2 守护），`[SPEC-DEFER:phase-future.chunk-agent-scope-filter]` 据实保持（真实落地须 ingest-path schema 工程为 chunks 引入 agent 维度且价值不明）。

### 4. 默认构建守线 + 端到端 smoke

```bash
cargo test --workspace                                          # all PASS (core lib 225 = 222 baseline + net 3)
go test ./...                                                   # all pass (incl TestTask423 + TestTask422* + grpcclient forward)
cargo clippy --workspace --all-targets -- -D warnings          # 0 warning
go vet ./...                                                    # clean
bash -n scripts/console_smoke.sh                               # exit 0
go test ./internal/cli/ -run TestTask423                       # smoke v32[51/51] + no-regression [37/37]..[50/50]
bash scripts/spec_drift_lint.sh --touched origin/master        # 0 unannotated hits
```

smoke v32 `[51/51]`（REAL 模式，distinguishing）：`POST /v1/search?source_type=code`（query=runner）→ 保留 JobRunner code hit + `source_file_type=code`；`POST /v1/search?source_type=doc` → 过滤掉它（runner 仅作 JobRunner code 符号、source_type=doc 匹配 0 → 空；no-op 会仍返回该 chunk，故 distinguishing）。non-real / 不可达 → SKIP 诚实归因到单测（TEST-42.1.* / TEST-42.2.*，filter 无外部依赖 → 每次 CI 默认 gate 守护）。

## 4. 诚实边界（ADR-013，本 release 不夸大）

- **source_type 真实过滤兑现**：v1 BM25 path retriever-内过滤 + console data_plane post-filter（覆盖 semantic/hybrid）；source_type value 三构造点 populate。v1 semantic 路径 retriever-内过滤镜像 language 当前 scope（仅 BM25），续 `[SPEC-DEFER:phase-future.semantic-path-source-type-filter]`（console 经 data_plane post-filter 已覆盖 semantic/hybrid）。
- **agent_scope 据实 honest-defer**：memory 层概念、chunks 无 agent 维度 → 续 documented no-op `[SPEC-DEFER:phase-future.chunk-agent-scope-filter]`（不伪造）。
- **importer 显式 source_type 打标**（支持用户自定义 source_type，超 file_path 派生粗粒度桶）须 §5.3 解冻加 chunks 列 → `[SPEC-DEFER:phase-future.chunk-importer-source-type-tagging]`。
- **source_type value 变化据实记**：三构造点 source_type 由 `""` 变真实派生值是填补 task-4.2 §2A v0.1 schema gap（契约本意是真实 source_type），由 ADR-047 D1 据实记（可观测响应字段变化，空 filter 过滤行为 byte-equiv，非破坏性默认变更）。
- **其余检索 marker 据实保持延后不强行扩面**：`vector-dim-feature-enforce`（须 feature build）/ `tracestore-multi-workspace-strict`（余下读路径）据实续延后。

## 5. 真实发布凭据（post-tag-push backfill）

- master tag SHA：`<backfill: v0.35.0 commit SHA>`（v0.35.0 annotated tag 对象 `<backfill: v0.35.0 tag object SHA>`，用户**另行授权**下主 agent push，ADR-012）
- `release.yml` run：`<backfill: release.yml run id>`（conclusion success）
- ghcr digest：`ghcr.io/tajiaoyezi/contextforge-daemon:v0.35.0` + `:latest` = `<backfill: image digest sha256>`（linux/amd64）
- cosign Rekor tlog：`<backfill: cosign sign tlog index>`（sign）/ `<backfill: cosign attest SBOM tlog index>`（attest SBOM）

> tag SHA / run id / digest / tlog 未知前以 `<backfill: ...>` 标记，post-tag-push backfill PR 回填真实凭据（ADR-013 不预填）。
