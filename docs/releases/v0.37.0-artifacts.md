# ContextForge v0.37.0 Artifacts — memory-unpin-actor-propagation (闭环 pin/unpin actor 透传 + audit/event source 归因 + pin 顺带闭环 + ADR-049 ratified)

> Artifact inventory for the v0.37.0 minor release (Phase 44). Mirrors the v0.36.0 artifacts structure.
> 默认构建 **0 新依赖** / **0 network** / **0 schema migration** / **proto add-only**（UnpinMemoryRequest actor=2，ADR-015）/ **默认 byte-equiv**（空 actor → audit "console-api" / event "contextforge-core" 各自回落）。
> 闭环 Phase 40 task-40.1 留下的 pin/unpin actor 透传不对称。**诚实校正（ADR-013）**：grounding 发现 store pinned=false 丢弃 actor → 单纯透传到 store 是空透传；真实落点是 emit_audit_and_event source 归因。**actor 透传无外部依赖**（纯 Rust 内部 observability + Go header wire + proto add-only，0 网络），故 TEST-44.1.* 由**每次 CI run 全程守护**（无 honest-defer skip）。No breaking contract bump（v1 proto / console_data_plane proto add-only actor=2 字段号冻结；既有 MemoryClient.Unpin 签名变但 Go 控制面内部同步；默认 byte-equiv）。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：`internal/consoleapi/types.go` `MemoryClient.Unpin(memoryID string)` → `Unpin(memoryID, actor string)`（interface 变更）；`grpcclient/grpcclient.go` `memoryClient.Unpin(id string, actor string)` + `pb.UnpinMemoryRequest{MemoryId: id, Actor: actor}`；`handlers.go` `handleMemoryUnpin` 读 `r.Header.Get("X-Actor")`（镜像 :559 pin）+ 传 `Unpin(id, actor)`；`memstore.go` `MemMemoryStore.Unpin(id string, _actor string)`（签名对齐，fallback 无 audit/event 故 actor unused）；`cli/console_api_serve_degraded.go` degraded fallback 签名同步 + gofmt 对齐；`consoleapi/memstore_test.go` 既有 Unpin 调用补 `""`；`consoleapi/unpin_actor_test.go` TEST-44.1.4 源码 grep。运行时既有 22-endpoint + REST 契约不变；空 X-Actor → 空串 → Rust 回落 "console-api" byte-equiv。
- `contextforge-core`（Rust 数据面 daemon）：`core/src/data_plane/memory.rs` `emit_audit_and_event` 加 `actor: &str` 参数（audit source 非空用 actor / 空回落 "console-api"）+ `build_memory_event` 加 actor 参数（event source 非空用 actor / 空回落 "contextforge-core"）+ unpin handler 透传 actor（`let actor = if req.actor.is_empty() { "console-api" } else { req.actor.as_str() };` 镜像 pin）+ `set_pinned_with_actor(id, false, actor)`（store 丢弃，接口对称）+ `emit_audit_and_event(MemoryUnpin, id, &req.actor)` + pin handler 顺带传 `&req.actor`（消除残余不对称）+ deprecate/softdelete/harddelete 传 `""` byte-equiv + 既有 unpin 测试补 `..Default::default()` + TEST-44.1.1/.2/.3。memory_items schema（§5.3 FROZEN）+ migration 0 改动 + v1/console proto 0 改动（仅 UnpinMemoryRequest add-only actor=2）。`cargo test -p contextforge-core --lib` = **232** PASS（229 baseline + 3）。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.37.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network）+ cosign keyless 签 + SBOM + provenance（承 v0.21.0–v0.36.0）。linux/arm64 仍 DEFERRED。
- Feature 构建：本 release **0 新 feature flag**；feature-build 矩阵 10/10 不退化。

## 2. 源码档

- master tag SHA：`<backfill-after-tag-push>`（v0.37.0 annotated tag，用户**全权授权**下主 agent push（ADR-012））
- Phase 44 代码路径：
  - task-44.1（PR #280，MERGED；unpin-actor-propagation）：`proto/contextforge/console_data_plane/v1/console_data_plane.proto`（`UnpinMemoryRequest` add-only `actor=2`）+ `console_data_plane.pb.go` + `console_data_plane_grpc.pb.go`（buf generate）+ `core/src/data_plane/memory.rs`（emit_audit_and_event + build_memory_event 加 actor 参数 + unpin/pin handler 透传 + deprecate/softdelete/harddelete byte-equiv + TEST-44.1.1/.2/.3）+ `internal/consoleapi/types.go` + `grpcclient/grpcclient.go` + `handlers.go` + `memstore.go` + `memstore_test.go` + `unpin_actor_test.go` + `cli/console_api_serve_degraded.go`。**0 新依赖 / 0 schema migration / proto add-only / 默认 byte-equiv**。
  - task-44.3（this PR；closeout）：`scripts/console_smoke.sh`（smoke v34，step `[53/53]` unpin X-Actor 端到端）；`internal/cli/smoke_syntax_test.go`（`TestTask443` no-regression [37/37]..[52/52]）；`docs/releases/v0.37.0-{evidence,artifacts}.md`；`README.md` v0.37 段；`RELEASE_NOTES.md` v0.37.0 段；`docs/decisions/adr-049-...md`（ratify Proposed→Accepted，per-D）+ ADR-032/045 add-only Phase-44 Amendment；`docs/specs/phases/phase-44-...md`（§6 闭合）；`docs/s2v-adapter.md`；`docs/roadmap.md`（§3.26 推进记录 + §4 add-only）。

## 3. 测试档

- `cargo test --workspace`：全绿（lib 232 + integration + doc-test）。新增 `test_44_1_1_unpin_actor_in_event_source` / `test_44_1_2_pin_actor_in_event_source` / `test_44_1_3_empty_actor_event_source_byte_equiv`。
- `go test ./...`：全过（含 `TestTask441_UnpinActorPropagationWired` + `TestTask443_SmokeV34UnpinActorPropagationStep`）。
- `cargo clippy --workspace --all-targets -- -D warnings`：0 warning。
- `bash -n scripts/console_smoke.sh`：exit 0。
- CI spec-lint job：全绿（spec_drift_lint）。

## 4. 发布档

- GitHub Release：v0.37.0（tag push 后由 release.yml 自动建 Release + upload linux/amd64 tarball + source）。
- run id / ghcr digest / cosign tlog：`<backfill-after-tag-push>`（post-tag-push 回填，ADR-013 不预填）。
