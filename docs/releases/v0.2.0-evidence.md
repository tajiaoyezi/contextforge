# ContextForge v0.2.0 Evidence

**日期**：2026-05-24
**主干**：`master`
**当前 HEAD**：`0cc4fc2` (`chore(spec): Phase 9 closeout — phase-9 Status=Done + ADR-013 Accepted + adapter Phase 9=Done`)

## 合入记录（Phase 9 + spec/governance）

- PR #58 `chore/phase-9-spec-and-adr` → `cb43167`：Phase 9 顶层 spec + 6 task spec + ADR-013 + BDD feature + adapter/PRD/feature 索引同步（spec-only chore，主 agent §2A 自审 promote Draft→Ready 后合）。
- PR #59 `feat/task-9.1-proto-index-rpc` → `f310c3c`：`proto/contextforge/v1/index.proto`（IndexRequest 3 字段 + IndexProgress 7 字段）+ service.proto add-only `rpc Index(IndexRequest) returns (stream IndexProgress)` + buf generate 产物 + Rust trait method placeholder。
- PR #60 `feat/task-9.2-rust-grpc-index` → `c27bce8`：`CoreService::index` 流式实现 + `IndexSession::index_path_with_progress` per-file 回调 + `tokio-stream` direct dep lift + `phase9_index_smoke.rs` 集成测试。
- PR #61 `feat/task-9.3-go-cli-index` → `995b00e`：`Daemon.Index` callback-style 包装 + `internal/cli/index.go` 重写（IndexBackend injectable + human/JSONL + reliability manifest 集成）+ `TestCliIndex_E2E_RealCore` 真 cargo build + R4 redaction 守护。
- PR #62 `feat/task-9.4-go-cli-import` → `c0880c6`：`internal/cli/import.go` 三子命令（hermes/openclaw/agent-rules）+ YAML-frontmatter Markdown 序列化 + 5 fixture + 7 测试。
- PR #63 `feat/task-9.5-release-smoke-real` → `a3424a5`：删除 3 个 fake-evidence 测试 + 重写 AC1 真 binary tarball + `TestPhase9ReleaseSmoke_EndToEnd` 7-step CLI 真跑 + `scripts/release_smoke.sh` phase 9 段 + `PHASE_RELEASE_SMOKE_EXIT` 重命名。
- PR #64 `feat/task-9.6-readme-quickstart-verified` → `b6e50c1`：`examples/quickstart/` fixture + `scripts/quickstart_smoke.sh` + README rewrite + RELEASE_NOTES v0.2.0 + v0.2.0-{evidence,artifacts}.md + `.gitignore` 加 .exe。
- PR #<待合> `chore/phase-9-closeout` → `0cc4fc2`（本 closeout PR）：Phase 9 / Task 9.1-9.6 全 Done + ADR-013 Proposed→Accepted + adapter Phase 9 同步 + v0.2.0-evidence.md HEAD SHA 回填。

## S2V 状态

- `docs/s2v-adapter.md`：Phase 9 `cli-pipeline` = `Done`（closeout PR 内同步）。
- `task-9.1-proto-index-rpc.md` = `Done`（5/5 AC + 6 trade-off）。
- `task-9.2-rust-grpc-index.md` = `Done`（5/5 AC + 6 trade-off）。
- `task-9.3-go-cli-index.md` = `Done`（5/5 AC + 6 trade-off）。
- `task-9.4-go-cli-import.md` = `Done`（5/5 AC + 6 trade-off）。
- `task-9.5-release-smoke-real.md` = `Done`（5/5 AC + 6 trade-off + fake-evidence 双 gate 命中 0）。
- `task-9.6-readme-quickstart-verified.md` = `Done`（5/5 AC + 真 quickstart_smoke 跑过）。
- `phase-9-cli-pipeline.md` = `Done`，§6 phase smoke 已填实为 `bash scripts/quickstart_smoke.sh` + `bash scripts/release_smoke.sh`。
- `docs/decisions/adr-013-cli-data-plane-grpc-bridge.md` = `Accepted`。

## 验证证据

### 1. s2v baseline green

```bash
bash -lc 'source docs/s2v/scripts/lib/preflight.sh; source docs/s2v/scripts/lib/verify.sh; s2v_baseline_green "cmd/contextforge internal core/src core/tests"'
```

预期：

- `install`: ✅ `go mod download && cargo fetch`
- `typecheck`: ✅ `go vet ./... && cargo check --workspace`
- `unit-test`: ✅ `go test ./... && cargo test --workspace`
- 退出码：`FINAL_HEAD_BASELINE_EXIT=0`

### 2. release smoke gate

```bash
bash scripts/release_smoke.sh
```

预期：

- `release_smoke[1/4]`: go release harness（TestTask83 AC1 真 binary + AC3 benchmark）：✅
- `release_smoke[2/4]`: task 8 reliability/eval harness：✅
- `release_smoke[3/4]`: Rust gRPC search smoke：✅
- `release_smoke[4/4]`: phase 9 CLI end-to-end smoke：✅（真 binary + 7 step）
- 脚本输出：`PHASE_RELEASE_SMOKE_EXIT=0`

### 3. quickstart smoke gate（v0.2 新增）

```bash
bash scripts/quickstart_smoke.sh
```

实测：

- `[1/7]` build go + cargo binaries：✅
- `[2/7]` init：✅（`config.toml` 写出）
- `[3/7]` import hermes：✅（写出 `<data>/imports/hermes/*.md`）
- `[4/7]` index hermes records：✅（SQLite chunks 增长）
- `[5/7]` index sample project：✅（denylist 跳过 .env，secret redaction 重写 config.yaml）
- `[6/7]` search `configuration`：✅（命中 docs/config.md）
- `[7/7]` eval run：✅（30 golden questions 跑完）
- 脚本输出：`QUICKSTART_SMOKE_EXIT=0`

### 4. fake-evidence 双 gate（task-9.5 验证）

```bash
grep -rn 'StepPassed, Evidence: "ok"' internal/release/
grep -rn 'Status: StepPassed, Evidence:' internal/release/
```

预期：**两个 grep 都 0 命中**（fake-evidence pattern 全部消除）。

## Release 边界（继承 v0.1 + Phase 9 新增 CLI 数据通路）

v0.2 在 v0.1 基础上新增：

- CLI 数据通路真实可跑（v0.1 是 stub，v0.2 通过 ADR-013 真接通）。
- README Quick Start 命令字面可复制粘贴跑通（基于 `examples/quickstart/` fixture）。
- Release smoke 升级为真端到端（4 段，含 phase 9 7-step CLI binary 真跑），取代 v0.1 假证据 validator-self-test。

继承 v0.1 限制：

- 官方目标 Linux x86_64 / WSL2；macOS 应能跑，Windows 走 Git Bash / WSL。
- `LICENSE` 仍 all-rights-reserved。
- 真实 GitHub Release 上传、tag、checksum / signing、真实 amd64 runner 上的制品构建发布作业仍需外部 release pipeline 执行（task-9.6 §3 OOS 保持）。
