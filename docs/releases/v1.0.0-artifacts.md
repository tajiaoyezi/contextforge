# ContextForge v1.0.0 Artifacts — v1.0.0-release (v1.0 收口终点：ADR-050 完整 ratify + maturity label flip)

> Artifact inventory for the v1.0.0 major release (Phase 47). **0 代码逻辑改动 / 0 新依赖 / 0 network / 0 schema migration / 0 proto**。
> v1.0.0 是成熟度里程碑声明——maturity label flip + ADR-050 完整 ratify + known limitations catalog。无运行时行为变更。

## 1. 二进制档
- `contextforge`（Go 控制面 CLI）：本 release **0 Go 业务代码改动**（task-47.1 仅改文档 + smoke 脚本 + smoke_syntax_test.go）。
- `contextforge-core`（Rust 数据面）：本 release **0 Rust 代码改动**。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v1.0.0` + `:latest`（linux/amd64）+ cosign keyless 签 + SBOM + provenance（承 v0.21–v0.39）+ GitHub Release 对象自动创建（Phase 46 D4，v1.0.0 第二次实践）。
- Feature 构建：本 release **0 新 feature flag**；feature-build 矩阵 10/10 不退化。

## 2. 源码档
- master tag commit：`<backfill: tag-commit>`（v1.0.0 annotated tag 对象 `<backfill: tag-object>`，用户授权 ADR-012）
- Phase 47 代码路径：task-47.1 README.md（maturity label flip）+ ADR-050（完整 ratify）+ RELEASE_NOTES.md v1.0.0 段（known limitations）+ CHANGELOG.md [v1.0.0] + release docs + roadmap/adapter。

## 3. 测试档
- `go test ./...`：全过（含 TestTask471 + TestTask463 no-regression + 全量 cli 测试）。
- `cargo test --workspace`：全绿（0 Rust 改动）。
- `bash -n scripts/console_smoke.sh`：语法 PASS（v37[56/56]）。
- CI spec-lint：全绿。

## 4. 发布档
- GitHub Release：v1.0.0（tag push 后 release.yml **第二次自动创建 GitHub Release 对象**，D4 流程；Release URL `<backfill: release-url>`）。
- run id / ghcr digest / cosign tlog：`<backfill: run-id>` / `<backfill: ghcr-digest>` / `<backfill: tlog-sign> · <backfill: tlog-attest>`（post-tag-push 回填，ADR-013 不预填）。
- GitHub Release URL：`<backfill: release-url>`（D4 第二次实践，v1.0.0 major version）。

## 5. v1.0.0 交付清单
- **ADR-050 完整 ratify**：Proposed → Accepted（D1-D4 全真实交付验证）。
- **maturity label flip**：README Pre-1.0 → v1.0.0。
- **known limitations catalog**：active SPEC-DEFER 按 6 category（Multi-user/auth / Auto-update / Platform / Retrieval quality / Memory-observability / Interfaces-config-CI）归类列 v1.0 已知限制（ADR-013 honest-defer）。
