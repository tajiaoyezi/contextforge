# ContextForge v0.29.0 Artifacts — qdrant-live-vector-recall (live KNN recall harness + qdrant-recall CI service job + ADR-041 ratified)

> Artifact inventory for the v0.29.0 minor release (Phase 36). Mirrors the v0.28.0 artifacts structure.
> The default build is unchanged: 0-new-dependency, 0-network；默认行为 / proto 既有字段 / 既有契约不变。
> Close ADR-034 D2 honest-defer `[SPEC-DEFER:phase-future.qdrant-server-lifecycle]`——measure REAL live qdrant KNN recall + guard it permanently via a CI service container。qdrant 后端（connect/health/ensure-create/upsert/KNN/delete）自 Phase 25/29 起即已完整实现；v0.29.0 新增 env-gated harness（`core/tests/qdrant_live_recall.rs`，task-36.1）+ qdrant-recall CI job（带 qdrant service container，task-36.2）+ closeout（task-36.3）。**0 后端改动、0 新依赖**（`qdrant-client` 自 task-18.4 起即 optional）、默认构建 0-vector-dep / 0-network 不变（ADR-004/008）。**真实 CI 实测 recall@10 = 1.0000**（N=2000 语料 / dim=64 / M=50 queries，qdrant LIVE KNN vs BruteForce exact KNN ground truth），run `26961084355`（qdrant-recall job，"qdrant ready after 1 attempt(s)" / "test result: ok. 2 passed; 0 failed"）。诚实 caveat（ADR-013）：recall=1.0 因 N=2000 低于 qdrant HNSW indexing_threshold（默认 ~10000）→ qdrant 服务 EXACT KNN，故此为 live-KNN **正确性**证明（qdrant == brute-force exact ground truth），替换合成 `eval_integration.rs` 0.7/0.85 fixture；HNSW **逼近**regime（大语料 > indexing_threshold + optimizer 建索引）诚实延后 `[SPEC-DEFER:phase-future.vector-large-corpus-perf]`，**不声称已压测 HNSW 逼近**。既有 22-endpoint + 5 Memory RPC + 运行时二进制默认行为不变。No breaking contract bump。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：**0 改动**。本 release 为向量召回度量 + CI 守护，控制面二进制不涉及。运行时既有 22-endpoint + 5 Memory RPC 行为不变。
- `contextforge-core`（Rust 数据面 daemon）：**0 后端改动**——qdrant backend（`core/src/retriever/vector/qdrant.rs` connect/health/ensure-create/upsert/KNN/delete）自 Phase 25/29 起即完整，本 release 不改后端、不改控制流。新增 env-gated 集成测试 `core/tests/qdrant_live_recall.rs`（仅在 `CF_QDRANT_LIVE_URL` 置位时连接真实 qdrant，否则 skip；默认 `cargo test` 不触网）。`qdrant-client` 自 task-18.4 起 optional（默认构建 0-vector-dep / 0-network 不变）。`cargo test -p contextforge-core --lib` = lib all PASS（默认路径不变）。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.29.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network）+ cosign keyless 签 + SBOM + provenance（承 v0.21.0–v0.28.0）。linux/arm64 仍 DEFERRED。
- Feature 构建：本 release 无新 feature flag；live-recall harness 在 env-gated 集成测试路径（默认 skip / `qdrant-client` optional，不入默认构建）。

## 2. 源码档

- master tag SHA：`0a64c88dfdaf32d65d07a0639a98ef1000a6ade2`（v0.29.0 annotated tag 对象 `e7062794719d6e39f1865f977e9642129ed52f09`，无人值守授权下主 agent push（ADR-012））
- Phase 36 代码路径：
  - task-36.1（PR #236；live-recall harness）：`core/tests/qdrant_live_recall.rs`（env-gated `CF_QDRANT_LIVE_URL` harness：N=2000 dim=64 语料 upsert 进真实 qdrant，M=50 queries 跑 qdrant LIVE KNN vs in-process BruteForce exact KNN ground truth，断言 recall@10 == 1.0；未置位时 skip）；替换合成 `eval_integration.rs` 0.7/0.85 fixture 的诚实度量来源。0 后端改动 / 0 新依赖（`qdrant-client` 自 task-18.4 起 optional）。grounding：N=2000 < indexing_threshold → EXACT KNN，故为 live-KNN 正确性证明（非 HNSW 逼近压测），caveat 据实记录。
  - task-36.2（PR #237；qdrant-recall CI service job）：`.github/workflows/ci.yml`（新增 `qdrant-recall` job：起 `qdrant/qdrant` service container + readiness 轮询 + 置 `CF_QDRANT_LIVE_URL` + `cargo test -p contextforge-core --test qdrant_live_recall`，永久守护 live KNN recall）。CI 实测 run `26961084355`："qdrant ready after 1 attempt(s)" / "test result: ok. 2 passed; 0 failed"。
  - task-36.3（this PR）：closeout；`scripts/console_smoke.sh`（smoke v26，banner v25→v26，staging cf-v28-cfg）；`internal/cli/smoke_syntax_test.go`（`TestTask363` 无回退 [45/45]）；`docs/releases/v0.29.0-{evidence,artifacts}.md`；`README.md` v0.29 段；`RELEASE_NOTES.md` v0.29.0 段；`docs/decisions/adr-041-...md`（ratify Proposed→Accepted，per-D）+ ADR-034 add-only Phase 36 Amendment（标记 D2 qdrant-server-lifecycle 已兑现：live KNN recall 已度量 + CI 守护，不溯改 D-body，ADR-014 D5）；`docs/specs/phases/phase-36-qdrant-live-vector-recall.md`（§6 闭合）；`docs/s2v-adapter.md`；`test/features/phase-36-qdrant-live-vector-recall.feature`。

## 3. 验证脚本

```bash
# 默认构建（0 新依赖 / 0 network；qdrant-client optional 不入默认构建）
cargo test -p contextforge-core --lib                     # lib all PASS
go test ./...                                             # all pass (incl TestTask363)
cargo clippy --workspace --all-targets -- -D warnings     # 0 warning
go vet ./...                                              # clean
bash -n scripts/console_smoke.sh                          # exit 0

# task-36.1 live-recall harness（env-gated；需真实 qdrant 在 CF_QDRANT_LIVE_URL）
docker run -d -p 6334:6334 qdrant/qdrant                  # 本地复现 qdrant 容器
CF_QDRANT_LIVE_URL=http://127.0.0.1:6334 \
  cargo test -p contextforge-core --test qdrant_live_recall  # recall@10 == 1.0000（N=2000/dim=64/M=50）
# 未置 CF_QDRANT_LIVE_URL 时该 harness skip（默认 cargo test 不触网）

# task-36.2 qdrant-recall CI service job（qdrant/qdrant service container 守护）
# CI: .github/workflows/ci.yml qdrant-recall job → run 26961084355
#     "qdrant ready after 1 attempt(s)" / "test result: ok. 2 passed; 0 failed"

# 端到端 smoke v26（step 45 → [45/45]；既有 step 不退化）
go test ./internal/cli/ -run TestTask363

# D5 spec-drift lint
bash scripts/spec_drift_lint.sh --touched origin/master   # 0 unannotated hits
```

## 4. 镜像推送

- `release.yml`（`v*` tag push 触发）→ buildx → ghcr login → build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.29.0` + `:latest`（**linux/amd64**，net-zero 与 v0.28.0 一致）→ cosign keyless 签 + attest SPDX SBOM + provenance:mode=max（承 task-28.2 机制）。
- run ID + digest：`release.yml` run `26962915074`（**`success`**，build-push + cosign sign + attest SBOM + provenance:mode=max 步全绿）；`ghcr.io/tajiaoyezi/contextforge-daemon:v0.29.0` + `:latest` = `sha256:2f0d3b82b738dd4cc0007d267a46f9149c73806e39928910d822f46fc72b87e3`；git tag SHA `0a64c88dfdaf32d65d07a0639a98ef1000a6ade2`；cosign Rekor tlog idx `1721985223`（sign）/ `1721988377`（attest SBOM）。
- live-recall evidence artifact：qdrant-recall CI job run `26961084355`（`.github/workflows/ci.yml`，qdrant/qdrant service container）即本 release 的 live-KNN recall 证据——recall@10 = 1.0000（N=2000 / dim=64 / M=50，qdrant LIVE KNN vs BruteForce exact KNN ground truth），"qdrant ready after 1 attempt(s)" / "test result: ok. 2 passed; 0 failed"。亦本地对 `qdrant/qdrant` docker container 复现。

## 5. cosign verify（by-digest）

```bash
# 解析 tag → digest，按 digest（非 tag）验签（承 v0.21.0 follow-up 教训：cosign verify 务必按 digest）
DIGEST=$(cosign triangulate --type digest ghcr.io/tajiaoyezi/contextforge-daemon:v0.29.0)
cosign verify \
  --certificate-identity-regexp 'https://github.com/tajiaoyezi/contextforge/.github/workflows/release.yml@.*' \
  --certificate-oidc-issuer https://token.actions.githubusercontent.com \
  ghcr.io/tajiaoyezi/contextforge-daemon@${DIGEST}
# 期望 digest = sha256:2f0d3b82b738dd4cc0007d267a46f9149c73806e39928910d822f46fc72b87e3
```

## 6. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-041 qdrant-live-vector-recall | **Accepted**（per-D，D1 live-recall harness 🟢 + D2 qdrant-recall CI service job 🟢 + D3 honest EXACT-KNN caveat 🟢） | task-36.3 据 36.1/36.2 真实非合成验证 ratify：D1 Accepted（env-gated `qdrant_live_recall.rs` harness，N=2000/dim=64/M=50，qdrant LIVE KNN vs BruteForce exact KNN ground truth，recall@10 == 1.0000，替换合成 `eval_integration.rs` 0.7/0.85 fixture）/ D2 Accepted（`ci.yml` qdrant-recall job 起 `qdrant/qdrant` service container 永久守护，run 26961084355 "2 passed; 0 failed"）/ D3 honest caveat（recall=1.0 因 N=2000 < indexing_threshold → qdrant 服 EXACT KNN，为 live-KNN 正确性证明非 HNSW 逼近压测；HNSW 逼近 regime 诚实延后 `[SPEC-DEFER:phase-future.vector-large-corpus-perf]`）/ D4 default 0-dep/0-network + 0 后端改动（`qdrant-client` optional 不入默认构建，best-effort 既有契约不变）/ D5 baseline 不变（cargo/go/clippy/vet 绿；harness env-gated 旁路、默认 skip）|
| ADR-034 vector-store-trait（qdrant honest-defer） | Accepted（+ Phase 36 add-only Amendment：D2 qdrant-server-lifecycle 已兑现） | task-36.3 add-only 标记 ADR-034 D2 `[SPEC-DEFER:phase-future.qdrant-server-lifecycle]` 已兑现（live KNN recall 已度量 + CI 守护），**不溯改 D-body**（ADR-014 D5）；qdrant 后端实现自 Phase 25/29，本 phase 仅补度量 + 守护 |
| ADR-013 据实延后/校正 | Accepted（守线） | recall=1.0 = EXACT-KNN 正确性证明非 HNSW 逼近压测，caveat 据实记录；HNSW 逼近 regime 延后 `[SPEC-DEFER:phase-future.vector-large-corpus-perf]`；tag SHA / run id / digest / tlog 未知前以 `<backfill>` 占位不伪造凭据 |
| ADR-014 add-only / cross-validation | Accepted（守线，第二十七次激活） | env-gated harness + CI job add-only（0 后端改动 / 不改控制流）+ ADR-034 Amendment 不溯改正文（D5）|
| ADR-004 / ADR-008 / ADR-012 | Accepted（守线） | 默认 0 新依赖 / 0 network（`qdrant-client` optional 不入默认构建，Rust core 默认 0-vector-dep）；harness env-gated（未置 `CF_QDRANT_LIVE_URL` 即 skip 不触网）；tag-release 须授权（本轮经无人值守 /goal 授权 ADR-012）|

## 7. 版本声明（contract bump 评估）

- 本 release 新增 env-gated live-recall harness（`qdrant_live_recall.rs`，qdrant LIVE KNN vs BruteForce exact KNN，recall@10 = 1.0000）+ qdrant-recall CI service job（`qdrant/qdrant` 容器守护，run 26961084355）+ smoke v26——**无 proto 既有字段 / RPC / endpoint 删改、无默认运行时行为改动、0 后端改动**（harness env-gated 默认 skip / `qdrant-client` optional 不入默认构建）。
- 默认构建 0 新代码依赖 / 0 network（ADR-004 / ADR-008，Rust core 默认保持 0-vector-dep）。
- 故 **无 breaking contract bump**；既有 v0.6–v0.28 client + 既有索引/数据无需改动。**qdrant-live-vector-recall 收口**（ADR-034 D2 honest-defer 兑现）。

## 8. 后续模板（vector-recall follow-on）

- HNSW **逼近**regime 压测（大语料 > qdrant indexing_threshold + optimizer 建索引，度量 approximate-KNN recall < 1.0）`[SPEC-DEFER:phase-future.vector-large-corpus-perf]`。
- qdrant indexing_threshold / HNSW 参数（m / ef_construct）调优暴露 `[SPEC-DEFER:phase-future.qdrant-hnsw-tuning]`。
- linux/arm64 原生 runner multi-arch `[SPEC-DEFER:phase-future.multi-arch-native-runner]`。
- （承前）vector-dim feature-enforce `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]`；sqlite-vec in-process matrix `[SPEC-DEFER:phase-future.sqlite-vec-inprocess-matrix]`；structured metrics/counter facility `[SPEC-DEFER:phase-future.observability-metrics-facility]`。

## 9. 存档指引

- closeout PR 合入后，**无人值守授权下**主 agent push `v0.29.0` annotated tag 触发 `release.yml`；post-tag-push backfill PR 填实 tag SHA + run ID + 镜像 digest + cosign 凭据（承 v0.8–v0.28 pattern；§2/§4/§5 角括号 `<backfill: ...>` 标记由本 backfill PR 回填）。
- Dockerfile workspace member COPY 完整性预检（承 v0.11.0 PR #139 教训）：本 release **无新 migration、0 后端改动**——新增 `core/tests/qdrant_live_recall.rs`（既有 `core/` member 的 tests/，env-gated 不入默认构建）+ `.github/workflows/ci.yml` qdrant-recall job，无新依赖 / 无 proto 字段新增；镜像为默认构建（0 新依赖 / 0 network，linux/amd64）。
