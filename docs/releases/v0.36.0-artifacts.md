# ContextForge v0.36.0 Artifacts — governance-debt-cleanup-4 / indexing-replay-splice (Phase 33 indexing replay mapper 接进 live subscribe 路径 + since_ts 时序对齐 audit + 默认 byte-equiv + ADR-048 ratified)

> Artifact inventory for the v0.36.0 minor release (Phase 43). Mirrors the v0.35.0 artifacts structure.
> 默认构建 **0 新依赖** / **0 network** / **0 schema migration**（复用 Phase 33 migration 0019）/ **0 proto 改动** / **默认 byte-equiv**（since_ts<=0 / store=None 两退化路径）。
> 兑现 ADR-038 D3 `[SPEC-DEFER:phase-future.indexing-replay-e2e]` 的 **splice 维度**——把 Phase 33 task-33.3 交付但从未在 live 路径调用的 indexing replay mapper 拼接进 `EventsServer::subscribe`。**诚实校正（ADR-013）**：本 release 交付 unit 级 splice + 时序单测（🟢，TEST-43.1.1/.2a/.2b/.2c 全绿）；live daemon restart-then-replay 端到端 e2e（真起进程 + 跨 restart 双窗口）须 running daemon → 🟡 honest-defer `[SPEC-DEFER:phase-future.indexing-replay-daemon-e2e]`（不预填）；memory-actor-all-rpc（Deprecate/SoftDelete 7 层 + 新 migration / HardDelete 须 audit 重设计 = 非小债）据实延后留独立 phase。**splice 拼接无外部依赖**（纯 Rust 内部 read 路径 + 既有 mapper + 既有 store，0 网络），故 TEST-43.1.* 由**每次 CI run 全程守护**（无 honest-defer skip；splice 行为本体无外部 dep，仅 live daemon e2e 受阻）。No breaking contract bump（v1 proto / console_data_plane proto 零 delta；DataPlaneStores 字段 add-only 默认 None byte-equiv；既有 constructor / 单测退化路径 byte-identical）。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：本 release **0 Go 代码改动**（splice 是 Rust data_plane 内部 read 路径，不经 Go 控制面）。运行时既有 22-endpoint + REST 契约不变。
- `contextforge-core`（Rust 数据面 daemon）：`core/src/data_plane/indexing_events.rs` add `pub fn list_since(&self, limit: usize, since_ts: i64) -> Result<Vec<IndexingEventRow>, IndexingEventStoreError>`（since_ts>0 时 `WHERE (?1 = 0 OR ts_unix >= ?1) ORDER BY id ASC LIMIT ?2`，since_ts<=0 不过滤 byte-equiv `list()`；既有 `list(limit)` 不动）+ TEST-43.1.1。`core/src/data_plane/mod.rs` `DataPlaneStores` add `pub indexing_event_store: Option<Arc<SqliteIndexingEventStore>>` 字段 + `full()` 第 10 参数（既有 5 constructor `new`/`with_eval`/`with_memory`/`with_runner`/`with_runner_and_bus` 补 `None` byte-equiv）。`core/src/server.rs` serve_full `IndexSessionBackend::with_event_bus_and_indexing_store(event_bus, indexing_event_store.clone())`（写路径持 clone）+ `DataPlaneStores::full(..., Some(indexing_event_store))`（读路径 subscribe 可达，原件 move 进）。`core/src/data_plane/events.rs` `subscribe` replay 段（since_ts>0 分支）splice indexing replay（`self.stores.indexing_event_store.as_ref().and_then(|s| s.list_since(1000, req.since_ts).ok()).map(|rows| indexing_rows_to_pb_events(&rows)).unwrap_or_default()`，audit replay 后、live forward spawn 前）+ helper `server_with_replay_sources` + TEST-43.1.2a/.2b/.2c。`core/src/data_plane/memory.rs` test struct literal 补 `indexing_event_store: None`。chunks/files/provenance SQL_SCHEMA（§5.3 FROZEN）+ migration 0019（Phase 33 已落地）+ v1/console proto 0 改动。`cargo test -p contextforge-core --lib` = **229** PASS（225 baseline + 4）。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.36.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network）+ cosign keyless 签 + SBOM + provenance（承 v0.21.0–v0.35.0）。linux/arm64 仍 DEFERRED。
- Feature 构建：本 release **0 新 feature flag**；feature-build 矩阵 10/10 不退化。

## 2. 源码档

- master tag SHA：`2cd59777bb04c4f57fb1e16cbbd6facfa21133bf`（v0.36.0 annotated tag 对象 `bf14604f2f0d14f4ea236e571e661995bf135f51`，用户**另行授权**下主 agent push（ADR-012））
- Phase 43 代码路径：
  - task-43.1（PR #276，MERGED；indexing-replay-splice）：`core/src/data_plane/indexing_events.rs`（add `list_since` + TEST-43.1.1）+ `core/src/data_plane/mod.rs`（DataPlaneStores 字段 + full() 第 10 参数 + 5 constructor 补 None）+ `core/src/server.rs`（serve_full IndexSessionBackend clone + full() 传入）+ `core/src/data_plane/events.rs`（subscribe splice + helper + TEST-43.1.2a/.2b/.2c）+ `core/src/data_plane/memory.rs`（test struct literal 补字段）。**0 新依赖 / 0 schema migration（复用 0019）/ 0 proto 改动 / 默认 byte-equiv**。
  - task-43.3（this PR；closeout）：`scripts/console_smoke.sh`（smoke v33，step `[52/52]` indexing-replay-splice 可达断言 / 不可达诚实归因 unit）；`internal/cli/smoke_syntax_test.go`（`TestTask433` no-regression [37/37]..[51/51]）；`docs/releases/v0.36.0-{evidence,artifacts}.md`；`README.md` v0.36 段；`RELEASE_NOTES.md` v0.36.0 段；`docs/decisions/adr-048-...md`（ratify Proposed→Accepted，per-D）+ ADR-038 add-only Phase-43 Amendment（indexing-replay-e2e splice 维度兑现，live daemon e2e 续延后，**不溯改 D-body**，ADR-014 D5）；`docs/specs/phases/phase-43-...md`（§6 闭合）；`docs/s2v-adapter.md`；`docs/roadmap.md`（§3.25 推进记录 + §4 add-only）。

## 3. 测试档

- `cargo test --workspace`：全绿（lib 229 + integration + doc-test）。新增 `test_43_1_1_list_since_ts_filter_and_byte_equiv` / `test_43_1_2_subscribe_splices_indexing_then_audit_then_live` / `test_43_1_2b_since_ts_zero_no_replay_byte_equiv` / `test_43_1_2c_store_none_no_indexing_replay`。
- `go test ./...`：全过（含 `TestTask433_SmokeV33IndexingReplaySpliceStep`）。
- `cargo clippy --workspace --all-targets -- -D warnings`：0 warning。
- `bash -n scripts/console_smoke.sh`：exit 0。
- CI spec-lint job：全绿（spec_drift_lint）。

## 4. 发布档

- GitHub Release：v0.36.0（tag push 后由 release.yml 自动建 Release + upload linux/amd64 tarball + source）。
- run id / ghcr digest / cosign tlog：release run id `28526379425`（`success`）/ ghcr digest `sha256:b52d49a5c822ea8466cfcf1b44e8a0757a970bacbedca181fb1eb863fb258923`（ghcr.io/tajiaoyezi/contextforge-daemon:v0.36.0 + :latest，linux/amd64）/ cosign Rekor tlog `2037572029`（sign）·`2037574642`（attest）（post-tag-push 回填，ADR-013 不预填）。
