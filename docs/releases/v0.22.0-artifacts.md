# ContextForge v0.22.0 Artifacts — live-vector-recall (vector backend 工厂 + server.rs 热路径注入 + qdrant live KNN（honest-defer）+ lancedb 真实 IVF_PQ/HNSW 索引 + 多 backend 选择矩阵 + ADR-034 ratified)

> Artifact inventory for the v0.22.0 minor release (Phase 29). Mirrors the v0.21.0 artifacts structure.
> The default build is unchanged: 0-new-dependency, 0-network。semantic+hybrid 默认仍走 0-dep
> `BruteForceVectorBackend`；新增 `select_vector_backend` 工厂 + add-only `VectorStore` trait + qdrant /
> lancedb 真实索引均 **feature-gated**（默认不编译）。既有 22-endpoint + 5 Memory RPC + 运行时二进制不变。
> 0 新代码依赖（`core/Cargo.toml` / `Cargo.lock` / `go.mod` 未改）。No breaking contract bump。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：本 release **运行时无变化**——零 Go delta（仅 `internal/cli/smoke_syntax_test.go` 加 `TestTask294_*` + `scripts/console_smoke.sh` v19 step 38 文档化）。
- `contextforge-core`（Rust 数据面 daemon）：新增 `core/src/retriever/vector/factory.rs`（`select_vector_backend`）+ `traits.rs` add-only `VectorStore` 组合 trait + `server.rs:302/341` 经工厂注入（默认 byte-equivalent BruteForce）+ `lance_db.rs` `create_ann_index` / `compact`（feature `vector-lancedb`）。默认构建 0 新依赖 / 0 network；`cargo test --workspace` 191 lib + 全集成 0 failed。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.22.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network；vector backend feature-gated 不入默认镜像）+ cosign keyless 签 + SBOM + provenance（承 v0.21.0）。linux/arm64 仍 DEFERRED（`[SPEC-DEFER:phase-future.multi-arch-native-runner]`）。
- Feature 构建：本 release 无新 feature flag；既有 `vector-qdrant` / `vector-lancedb` / `embedding-fastembed` 之上新增真实 live KNN harness（29.2）+ 真实 ANN 索引/compaction（29.3）。CI 默认不构建该 feature（真实索引/召回经 dev box `--lib` scoped + example 实测）。

## 2. 源码档

- master tag SHA：`0289e4018bc81ac426a202b3cf712c4d31cb6696`（v0.22.0 annotated tag，无人值守授权下主 agent push（ADR-012））
- Phase 29 代码路径：
  - task-29.1（工厂 + 热路径注入）：`core/src/retriever/vector/factory.rs`（新增 `select_vector_backend`）；`core/src/retriever/vector/traits.rs`（add-only `VectorStore: VectorIndexer + VectorSearcher` + blanket impl）；`core/src/retriever/vector/mod.rs`（`pub mod factory` + re-export）；`core/src/server.rs`（`:302` hybrid + `:341` semantic 经工厂注入）。
  - task-29.2（qdrant live KNN harness）：`core/examples/phase29_recall_via_qdrant.rs`（clone phase20 + `QdrantBackend::connect` + `health()` honest-defer + 单节点部署基线注释）。
  - task-29.3（lancedb 真实 ANN + compaction + matrix）：`core/src/retriever/vector/lance_db.rs`（`create_ann_index`（`Index::IvfPq` / `Index::IvfHnswSq`）+ `compact`（`OptimizeAction::All`）+ TEST-29.3.1 真实矩阵 + TEST-29.3.3 compaction + `LANCEDB_DIR_LOCK` 测试隔离）；`docs/decisions/adr-030-production-vector-backend.md` + `adr-023-vector-backend-default.md`（`## Amendment (Phase 29)` 真实矩阵 add-only）。
  - task-29.4（this PR）：`scripts/console_smoke.sh`（v19 step 38）；`internal/cli/smoke_syntax_test.go`（`TestTask294_*`）；`docs/releases/v0.22.0-{evidence,artifacts}.md`；`README.md` v0.22 段；`RELEASE_NOTES.md` v0.22.0 段；`docs/decisions/adr-034-production-vector-live-recall.md`（ratify Proposed→Accepted，per-D，D2 honest-defer）；`docs/specs/phases/phase-29-live-vector-recall.md`（§6 闭合）；`docs/s2v-adapter.md`；`test/features/phase-29-live-vector-recall.feature`。

## 3. 验证脚本

```bash
# 默认构建（0 新依赖 / 0 network）
cargo test --workspace
go test ./...

# 默认 CI lint gate
cargo clippy --workspace --all-targets -- -D warnings

# task-29.1 工厂契约（deterministic，不连 server）
cargo test -p contextforge-core --lib retriever::vector::factory      # 4 passed

# task-29.2 qdrant live KNN harness（feature 开无 server → honest-defer exit 0）
cargo run -p contextforge-core --example phase29_recall_via_qdrant --features vector-qdrant,embedding-fastembed
#   → "no live qdrant ... health=Unreachable ... Exiting 0."（EXIT=0，零召回数；真实召回需 live server）

# task-29.3 lancedb 真实 ANN 索引 + compaction + matrix（--lib scoped，规避 broad-test ICE）
#   PROTOC 指向仓内 protoc-bin-vendored 的 protoc.exe（承 task-25.2）
cargo test -p contextforge-core --features vector-lancedb --lib retriever::vector::lance_db -- --nocapture   # 4 passed

# 端到端 smoke v19（含 live-vector-recall 文档化 step 38；既有 step 不退化）
bash -n scripts/console_smoke.sh     # 语法门 exit 0（CI go-test TestTask294_*）
go test ./internal/cli/ -run TestTask294

# D2 spec-drift lint
bash scripts/spec_drift_lint.sh --touched origin/master
```

## 4. 镜像推送

- `release.yml`（`v*` tag push 触发）→ buildx → ghcr login → build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.22.0` + `:latest`（**linux/amd64**，net-zero 与 v0.21.0 一致）→ cosign keyless 签 + cosign attest SPDX SBOM（syft）+ provenance:mode=max（承 v0.21.0 task-28.2 机制）。
- 镜像 = **默认构建**：0 新依赖 / 0 network；vector backend feature-gated 不入镜像；运行时行为与 v0.21.0 baseline 兼容。
- run ID + digest：`release.yml` run `26857582240`（**success**，build-push + cosign sign + attest SBOM + provenance:mode=max 步全绿）；`ghcr.io/tajiaoyezi/contextforge-daemon:v0.22.0` + `:latest` = `sha256:1f22651231ef304e8c718e1c3833373d98db3ae2d6001129d717a7591e9cb6b2`；git tag SHA `0289e4018bc81ac426a202b3cf712c4d31cb6696`；cosign Rekor tlog idx `1706805295`（sign）/ `1706805625`（attest SBOM）。

## 5. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-034 production-vector-live-recall | **Accepted**（per-D，D2 honest-defer 部分 ratify） | task-29.4 据 task-29.1/29.2/29.3 真实非合成验证 ratify（Proposed→Accepted）：D1 Accepted（工厂 + server.rs 注入，default-build CI 绿）/ D2 PARTIAL（qdrant live KNN wiring + honest-defer 真实达成；**真实召回无 live server → honest-defer 不强 ratify，不伪造**，`[SPEC-DEFER:phase-future.qdrant-server-lifecycle]`）/ D3 Accepted（lancedb 真实 IVF_PQ/IVF_HNSW_SQ 索引 + compaction + recall 实测）/ D4 Accepted（多 backend 选择矩阵真实测量 → ADR-030/023 add-only Amendment）/ D5 Accepted（default build 0 vector dep + BruteForce 基线不变）；受阻维度（qdrant live-server / lancedb 大语料 perf）据真实受限如实记录（ADR-013） |
| ADR-030 production-vector-backend | Accepted（+ Phase 29 add-only Amendment） | D2 真实建索引 + compaction 兑现 + D3 真实跨 backend 选择矩阵测量，以 `## Amendment (Phase 29 / v0.22.0)` 记录（不溯改 D1-D4 正文，ADR-014 D5） |
| ADR-023 vector-backend-default | Accepted（+ Phase 29 tier add-only Amendment） | D5 默认 0-dep BruteForce 档**实测背书** + D4 lancedb 真实召回/延迟 + D3 qdrant honest-defer，以 `## Amendment (Phase 29 / v0.22.0)` 记录（不溯改 D1-D6 正文，ADR-014 D5） |
| ADR-004 local-first-privacy-baseline | Accepted | 守线：vector backend 工厂注入默认 byte-equivalent BruteForce，qdrant/lancedb 真实索引全 feature-gated，默认构建 0 新依赖 + 0 network 不变 |
| ADR-012 main-agent-governance-autonomy | Accepted（守线） | tag / release outward-facing 不可逆 → 须用户显式授权（沿用 v0.13-v0.21 模式，不自行 tag）；实现 / 设计 / 选型主 agent 自治 |
| ADR-008 core-library-selection | Accepted（无 amendment） | 0 新 direct dep（`core/Cargo.toml` / `Cargo.lock` / `go.mod` 未改；qdrant-client/lancedb/arrow-array/futures/fastembed 自 task-18.4/18.5/22.2 即 optional）→ 无依赖变更 Amendment |
| ADR-013 no-fake-evidence | Accepted | 守线：ADR-034 ratify 据真实 test/run；qdrant live-server 召回无 server → honest-defer + 真实召回回填位 `<backfill>`、lancedb IVF_PQ run-to-run 抖动、大语料 perf 受阻 均诚实记录不伪造 |
| ADR-014 cross-phase-exit-criteria-validation | Accepted | 第二十次激活（v0.22）；history Phase 1-28 不 retro-edit |

## 6. 版本声明（contract bump 评估）

- 本 release 新增 `select_vector_backend` 工厂 + add-only `VectorStore` trait + qdrant/lancedb 真实索引（全 feature-gated）+ smoke v19 step——**无 proto / 无 RPC / 无 endpoint / 无默认运行时行为改动**（server.rs 热路径注入是默认 byte-equivalent BruteForce）。
- 默认构建 0 新代码依赖 / 0 network（ADR-004）；qdrant/lancedb 真实索引在各自 feature 下。
- 故 **无 breaking contract bump**；既有 v0.6–v0.21 client 无需改动。**live-vector-recall 收口**（默认运行时二进制行为不变）。

## 7. 后续模板（live-vector-recall follow-on）

- qdrant live KNN 真实召回回填（手动 / dev-box 单节点 server）`[SPEC-DEFER:phase-future.qdrant-server-lifecycle]`——本 release CI / 本 run 无 server → honest-defer，真实 recall 待 server 跑出回填。
- qdrant 集群 / 复制 / 部署拓扑 `[SPEC-DEFER:phase-future.qdrant-deployment-topology]`——本 release 仅单节点基线文档化。
- lancedb 大语料拐点性能基准 `[SPEC-DEFER:phase-future.vector-large-corpus-perf]`——本 release in-process modest n（~1024）实测；大语料拐点留后续。
- lancedb feature 全 target CI 构建门 `[SPEC-DEFER:phase-future.lancedb-build-prereq-ci]`——broad `cargo test --features vector-lancedb` 受 rustc 1.95.0 ICE 限，本 release `--lib` scoped 规避，CI 默认不构建该 feature。
- 向量 backend 名 / dim 从配置（env / proto）plumb 进 server `[SPEC-DEFER:phase-future.vector-backend-config-plumbing]`——本 release 工厂以默认名 byte-equivalent 注入；从外部配置选 backend 的 plumbing 留后续。
- sqlite-vec 接入工厂 + in-process 矩阵测量 `[SPEC-DEFER:phase-future.sqlite-vec-factory-arm]`——本 release 工厂覆盖 brute/qdrant/lancedb 三臂；sqlite-vec（task-23.2 已确证可构建）接入 + 矩阵测量留后续。

## 8. 存档指引

- closeout PR 合入后，**待用户授权**主 agent push `v0.22.0` annotated tag 触发 `release.yml`；post-tag-push backfill PR 填实 tag SHA + run ID + 镜像 digest + cosign 凭据（承 v0.8–v0.21 pattern）。
- Dockerfile workspace member COPY 完整性预检（承 v0.11.0 PR #139 教训）：本 release 新增源码均在 `core/src/`（工厂 / 真实索引）+ `core/examples/`（harness）+ docs/scripts member 内——**无新增 migration / proto**（Dockerfile 无需改动）；镜像为默认构建（0 新依赖 / 0 network，linux/amd64，vector backend feature-gated 不入镜像）。
