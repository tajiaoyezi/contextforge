# ContextForge v1.0.1 Artifacts — v1.0.1-patch (v1.0 收口审查残留修复)

> Artifact inventory for the v1.0.1 patch release (Phase 48). **0 新依赖 / 0 network / 0 schema migration / 0 proto**。仅 CLI version 输出从错误变正确 + 文档残留清理。

## 1. 二进制档
- `contextforge`（Go 控制面 CLI）：`internal/cli/cli.go` Version 默认值 `0.38.0-dev` → `1.0.1-dev`（task-48.1）；Dockerfile go-build 加 `ARG VERSION` + `-ldflags -X cli.Version`（build-time 注入 release tag）。
- `contextforge-core`（Rust 数据面）：本 release **0 Rust 代码改动**。
- 镜像构建：`release.yml` docker/build-push-action 加 `build-args: VERSION=${{ steps.ref.outputs.tag }}` → v1.0.1 镜像 `contextforge version` 报 `v1.0.1`（非旧 dev 串）。

## 2. 源码档
- master tag commit：`<backfill: tag-commit>`（v1.0.1 annotated tag 对象 `<backfill: tag-object>`，用户授权 ADR-012）

## 3. 测试档
- `go test ./...`：全过（含 TestTask481 + TestTask471 精确化 + no-regression）。
- `cargo test --workspace`：全绿（0 Rust 改动）。
- `gofmt -l`：clean。
- CI spec-lint：全绿。

## 4. 发布档
- GitHub Release：v1.0.1（tag push 后 release.yml 第三次自动创建 Release 对象；Release URL `<backfill: release-url>`）。
- run id / ghcr digest / cosign tlog：`<backfill: run-id>` / `<backfill: ghcr-digest>` / `<backfill: tlog-sign> · <backfill: tlog-attest>`（post-tag-push 回填，ADR-013 不预填）。

## 5. v1.0.1 修复清单
- **P0** CLI version 字符串：cli.go 默认值 + Dockerfile ldflags + release.yml build-args（D2 缺陷修复）。
- **P1** docs/decisions/README.md ADR-050 Accepted。
- **P2** README Latest 段描述 v1.0 收口终点。
- **P3** contextforge.example.toml header v1.0.1。
