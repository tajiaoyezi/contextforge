# ContextForge v0.26.0 Artifacts — governance-debt-cleanup-2 (L2 SQLite embedding-cache row-count cap + memstore access-order LRU + indexing.* event persistence/replay + TraceStore workspace isolation + export --timeout flag + ADR-038 ratified)

> Artifact inventory for the v0.26.0 minor release (Phase 33). Mirrors the v0.25.0 artifacts structure.
> The default build is unchanged: 0-new-dependency, 0-network；默认行为 / proto 既有字段 / 既有契约不变。
> L2 SQLite embedding-cache 加 row-count cap + rowid-FIFO eviction（add-only `with_sqlite_capacity` ctor，0 schema migration）+ console-api memstore chunk/trace 缓存 FIFO → access-order LRU（fallback-mode only）+ indexing.* 事件 add-only 持久化（migration 0019 + SqliteIndexingEventStore + replay 纯映射器，在既有 `eb.send` broadcast 之外 best-effort persist）+ TraceStore add-only `workspace_id` 隔离（GetSearchTrace/ListQueries，空 = aggregate-all 字节等价）+ export add-only `--timeout` flag（default 60s 字节等价旧硬编码）。既有 22-endpoint + 5 Memory RPC + 运行时二进制默认行为不变。No breaking contract bump。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：memstore chunk/trace 缓存由 FIFO 升级为 access-order LRU（read-hit `GetSourceChunk`/`GetSearchTrace` + existing-key overwrite 均经 `moveToMRU` move-to-front），仅 fallback-mode 生效；`export` 子命令加 add-only `--timeout` flag（`parseExportOpts` `fs.Duration("timeout", 60s)`，default 60s **字节等价**旧硬编码 `context.WithTimeout(60s)`，ADR-004，覆盖两次顺序 daemon spawn）；`handleMemoryPin` 按 ADR-022 D2 保持 lenient（honest non-change，无代码改动）。运行时既有 22-endpoint + 5 Memory RPC 行为不变。
- `contextforge-core`（Rust 数据面 daemon）：`embedding/cache.rs` `sqlite_put` 先写后 `DELETE ... WHERE provider=? AND dim=? AND rowid NOT IN (SELECT rowid ... ORDER BY rowid DESC LIMIT cap)`（rowid-FIFO eviction，`DEFAULT_L2_EMBEDDING_CACHE_CAP=50_000` + add-only `with_sqlite_capacity` ctor，0 新依赖 / 0 schema migration（隐式 rowid FIFO）；honest-caveat：`with_sqlite` 无生产调用点（test-only）→ opt-in-path 纵深防御，非确认 live leak；true-LRU honest-defer `[SPEC-DEFER:phase-future.l2-cache-true-lru]`）；`index_session_backend.rs` 4 emit 点（progress/error×2/cancelled）在既有 `eb.send` broadcast **之外** best-effort persist 经 `SqliteIndexingEventStore`（migration `0019_indexing_events` append/list id ASC）+ `events::indexing_rows_to_pb_events` 纯映射器（按 id ASC 重建 indexing.* `PbEvent`，real job_id/processed/total from rows，deterministic `evt-idx-{id}`），`server.rs` 生产接线该 store；`search_persist` get/list/search_fts + in-mem TraceStore get/list + handlers WHERE `workspace_id` 过滤（空 workspace_id = aggregate-all 字节等价，ADR-004），Rust 经 tonic `include_proto!` 编译期取 add-only proto 字段。默认构建 0 新依赖 / 0 network；`cargo test --workspace` = lib 207 + 全集成（含 search_persist_integration）all PASS。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.26.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network）+ cosign keyless 签 + SBOM + provenance（承 v0.21.0–v0.25.0）。linux/arm64 仍 DEFERRED。
- Feature 构建：本 release 无新 feature flag；indexing.* persistence + TraceStore workspace isolation + L2 cache cap 均在默认构建路径（0 新依赖）。

## 2. 源码档

- master tag SHA：`6a98fd70233ffbaa161918e1b821173e60489e82`（v0.26.0 annotated tag 对象 `360bc82bbcee257dcb6837e4e8616a49bd6ee71f`，无人值守授权下主 agent push（ADR-012））
- Phase 33 代码路径：
  - task-33.1（PR #218, squash 3ce290a；L2 SQLite embedding-cache row-count cap）：`core/src/embedding/cache.rs`（`sqlite_put` 先写后 `DELETE ... WHERE provider=? AND dim=? AND rowid NOT IN (SELECT rowid ... ORDER BY rowid DESC LIMIT cap)` rowid-FIFO eviction；`DEFAULT_L2_EMBEDDING_CACHE_CAP=50_000` + add-only `with_sqlite_capacity` ctor；0 新依赖 / 0 schema migration（隐式 rowid FIFO））+ TEST-33.1.1（`test_33_1_1_l2_cap_evicts_oldest_fifo`）+ TEST-33.1.2（`test_33_1_2_default_l2_cap_keeps_modest_workload`，cache 7/7）。honest-caveat：`with_sqlite` 无生产调用点（test-only）→ opt-in-path 纵深防御，非确认 live leak；true-LRU honest-defer `[SPEC-DEFER:phase-future.l2-cache-true-lru]`。0 新依赖。
  - task-33.2（PR #219, squash 7b92f22；console-api memstore access-order LRU + memory hard-delete no-dangling-ref invariant）：console-api memstore chunk/trace 缓存 FIFO → access-order LRU（read-hit `GetSourceChunk`/`GetSearchTrace` + existing-key overwrite 均经 `moveToMRU` helper move-to-front）+ `TestMemStore_CacheEviction_FIFO` 重写为 `_LRU` + 新 `_LRU_Trace`；memory hard-delete no-dangling-ref INVARIANT 测试（schema introspection 断言 memory_id 仅在 memory_items + `get(id)=None` after hard_delete——cascade 非问题，honest-defer `[SPEC-DEFER:phase-future.memory-harddelete-cascade]`）；`handleMemoryPin` 按 ADR-022 D2 保持 lenient（honest non-change，无代码改动）。TEST-33.2.1/.2（Go LRU）+ `test_33_2_3_hard_delete_no_dangling_refs`（Rust）。0 新依赖，fallback-mode only。
  - task-33.3（PR #220, squash 823beca；indexing.* event persistence/replay + TraceStore workspace isolation + drain-timeout verify-only）：(A) indexing.* 事件持久化——add-only migration `0019_indexing_events` + `SqliteIndexingEventStore`（append/list，id ASC）+ `index_session_backend.rs` 4 emit 点（progress/error×2/cancelled）在既有 `eb.send` broadcast **之外** best-effort persist（broadcast 路径不变）+ `events::indexing_rows_to_pb_events` 纯映射器（按 id ASC 重建 indexing.* `PbEvent`，real job_id/processed/total from rows，deterministic `evt-idx-{id}`），`server.rs` 生产接线 store；(B) TraceStore workspace isolation——add-only `GetSearchTraceRequest.workspace_id=2` + `ListQueriesRequest.workspace_id=2`（buf generate；无关 proto churn + grpc.pb.go EOL churn 已 revert 保持 surgical；Rust 经 tonic `include_proto!` 编译期取）+ `search_persist` get/list/search_fts + in-mem TraceStore get/list + handlers WHERE `workspace_id` 过滤（空 workspace_id = aggregate-all 字节等价，ADR-004）；(C) drain-timeout VERIFY-ONLY（Phase 26 已交付 `drainTimeoutFromEnv`，0 代码改动，引 `TestDrainTimeoutFromEnv` 5 子例）。`test_33_3_1`（mapper）+ `test_33_3_2`（store round-trip + fixture emit persist）+ `test_33_3_3`（persist ws filter）+ `test_33_3_4`（handler ws passthrough）。e2e honest-defer：`[SPEC-DEFER:phase-future.indexing-replay-e2e]` + `[SPEC-DEFER:phase-future.tracestore-multi-workspace-strict]`。0 新依赖。
  - task-33.4（this PR）：`internal/cli/export.go`（`parseExportOpts` add-only `fs.Duration("timeout", 60s)`，default 60s **字节等价**旧硬编码 `context.WithTimeout(60s)`，ADR-004，覆盖两次顺序 daemon spawn）+ `TestParseExportOpts_Timeout`；`scripts/console_smoke.sh`（smoke v23 step 42 → [42/42]）；`internal/cli/smoke_syntax_test.go`（`TestTask334`）；`docs/releases/v0.26.0-{evidence,artifacts}.md`；`README.md` v0.26 段；`RELEASE_NOTES.md` v0.26.0 段；`docs/decisions/adr-038-...md`（ratify Proposed→Accepted，per-D）+ ADR-031 / ADR-027 Phase 33 add-only Amendment；`docs/specs/phases/phase-33-governance-debt-cleanup-2.md`（§6 闭合）；`docs/s2v-adapter.md`；`test/features/phase-33-governance-debt-cleanup-2.feature`。dropped-nits（honest，NOT implemented）：`internal/cli/search.go:88` `%v`→`%w` 为 NON-BUG（terminal `Fprintf` 处 `%w` 是 vet-error，且 `err.Error()` 已携 grpc Status）/ tracestore-fts 跨版本 migration ALREADY-FIXED（`search_persist` open + `backfill_fts_if_empty`，TEST-26.1.4/.4b）/ datadir env→`daemon.Options.DataDir` REAL 但 🟡 `[SPEC-DEFER:phase-future.daemon-options-datadir]`。

## 3. 验证脚本

```bash
# 默认构建（0 新依赖 / 0 network）
cargo test --workspace                                    # lib 207 + 全集成 all PASS
go test ./...                                             # all pass (incl TestTask334)
cargo clippy --workspace --all-targets -- -D warnings     # 0 warning
go vet ./...                                              # clean
gofmt -l .                                               # clean

# task-33.1 L2 SQLite embedding-cache row-count cap（rowid-FIFO eviction）
cargo test -p contextforge-core test_33_1                 # TEST-33.1.1 / TEST-33.1.2

# task-33.2 console-api memstore access-order LRU + memory hard-delete invariant
go test ./internal/console/ -run TestMemStore_CacheEviction_LRU   # TEST-33.2.1 / .2 (FIFO→LRU + Trace)
cargo test -p contextforge-core test_33_2_3_hard_delete_no_dangling_refs

# task-33.3 indexing.* persistence/replay + TraceStore workspace isolation
cargo test -p contextforge-core test_33_3                 # mapper / store round-trip / ws filter / handler passthrough
# drain-timeout VERIFY-ONLY（Phase 26 已交付，0 代码改动）
go test ./internal/console/ -run TestDrainTimeoutFromEnv  # 5 子例

# task-33.4 export --timeout add-only flag（default 60s 字节等价旧硬编码）
go test ./internal/cli/ -run TestParseExportOpts_Timeout

# 端到端 smoke v23（step 42 → [42/42]；既有 step 不退化）
bash -n scripts/console_smoke.sh
go test ./internal/cli/ -run TestTask334

# D5 spec-drift lint
bash scripts/spec_drift_lint.sh --touched origin/master   # 0 unannotated hits
```

## 4. 镜像推送

- `release.yml`（`v*` tag push 触发）→ buildx → ghcr login → build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.26.0` + `:latest`（**linux/amd64**，net-zero 与 v0.25.0 一致）→ cosign keyless 签 + attest SPDX SBOM + provenance:mode=max（承 task-28.2 机制）。
- run ID + digest：`release.yml` run `26888395968`（**success**，build-push + cosign sign + attest SBOM + provenance:mode=max 步全绿）；`ghcr.io/tajiaoyezi/contextforge-daemon:v0.26.0` + `:latest` = `sha256:8e924e25da66031e6c1b8d8e7ef7d6c8d4dae07aedc76d5cb25f718a6eb1caaf`；git tag SHA `6a98fd70233ffbaa161918e1b821173e60489e82`；cosign Rekor tlog idx `1710104071`（sign）/ `1710105166`（attest SBOM）。

## 5. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-038 governance-debt-cleanup-2 | **Accepted**（per-D，D3 PARTIAL：code-local 🟢 + e2e 🟡 honest-defer；D4 PARTIAL：dropped-nits/datadir 🟡 honest-defer） | task-33.4 据 33.1/33.2/33.3 真实非合成验证 ratify：D1 Accepted（L2 cap rowid-FIFO，TEST-33.1.1/.2；honest-caveat test-only ctor + true-LRU honest-defer `[SPEC-DEFER:phase-future.l2-cache-true-lru]`）/ D2 Accepted（memstore access-order LRU TEST-33.2.1/.2 + memory hard-delete no-dangling-ref invariant `test_33_2_3`；cascade honest-defer `[SPEC-DEFER:phase-future.memory-harddelete-cascade]`；`handleMemoryPin` lenient honest non-change）/ D3 code-local 🟢（indexing.* persist+replay mapper `test_33_3_1/.2` + TraceStore ws isolation `test_33_3_3/.4`；indexing-replay-e2e / tracestore-isolation-e2e 🟡 honest-defer `[SPEC-DEFER:phase-future.indexing-replay-e2e]` + `[SPEC-DEFER:phase-future.tracestore-multi-workspace-strict]`；drain-timeout verify-only）/ D4 dropped-nits/datadir 🟡 honest-defer（search.go %v→%w NON-BUG / tracestore-fts ALREADY-FIXED / datadir `[SPEC-DEFER:phase-future.daemon-options-datadir]`；export --timeout add-only TEST 实交付）/ D5 baseline 不变（cargo/go/clippy/vet 绿；add-only migration 0019 + proto workspace_id 字段 + LRU + cache cap + export flag）|
| ADR-031 observability-hardening | Accepted（+ Phase 33 add-only Amendment） | task-33.3 加 indexing.* 事件持久化（migration 0019 + SqliteIndexingEventStore）+ replay 纯映射器（按 id ASC 重建 PbEvent）+ drain-timeout verify-only（Phase 26 已交付 `drainTimeoutFromEnv`）；broadcast `eb.send` 路径不变（在其之外 best-effort persist），不溯改 D1–D5 正文（ADR-014 D5） |
| ADR-027 embedding-provider-completion | Accepted（+ Phase 33 add-only Amendment） | task-33.1 加 L2 SQLite embedding-cache row-count cap（rowid-FIFO eviction，`DEFAULT_L2_EMBEDDING_CACHE_CAP=50_000` + add-only `with_sqlite_capacity` ctor）；honest-caveat test-only ctor + true-LRU honest-defer `[SPEC-DEFER:phase-future.l2-cache-true-lru]`；不溯改 D1–D5 正文（ADR-014 D5） |
| ADR-022 memory-ops | Accepted（守线） | `handleMemoryPin` 按 D2 保持 lenient（honest non-change，无代码改动）；memory hard-delete no-dangling-ref invariant 经 `test_33_2_3` 守线（cascade honest-defer `[SPEC-DEFER:phase-future.memory-harddelete-cascade]`） |
| ADR-013 据实延后 | Accepted（守线） | indexing-replay-e2e / tracestore-isolation-e2e / l2-cache-true-lru / memory-harddelete-cascade / daemon-options-datadir 受阻如实不伪造；tag SHA / run id / digest / tlog 未知前以 `<backfill>` 占位不伪造凭据 |
| ADR-014 add-only / 跨仓 signal | Accepted（守线） | proto `workspace_id=2`（GetSearchTrace/ListQueries）add-only + migration 0019 add-only + export `--timeout` add-only flag（default 字节等价）+ Amendment 不溯改正文（D5） |
| ADR-004 / ADR-008 / ADR-012 | Accepted（守线） | 默认 0 新依赖 / 0 network；空 workspace_id = aggregate-all 字节等价 + export --timeout default 60s 字节等价；tag-release 须授权（本轮已授权） |

## 6. 版本声明（contract bump 评估）

- 本 release 新增 L2 SQLite embedding-cache row-count cap（rowid-FIFO，add-only ctor，0 schema migration）+ console-api memstore access-order LRU（fallback-mode only）+ indexing.* 事件 add-only 持久化（migration 0019 + replay 映射器，broadcast 路径不变）+ TraceStore add-only `workspace_id` 隔离（空 = aggregate-all 字节等价）+ export add-only `--timeout` flag（default 60s 字节等价旧硬编码）+ smoke v23——**无 proto 既有字段 / RPC / endpoint 删改、无默认运行时行为改动**（cache/索引/trace/export 默认路径字节等价）。
- 默认构建 0 新代码依赖 / 0 network（ADR-004 / ADR-008）。
- 故 **无 breaking contract bump**；既有 v0.6–v0.25 client + 既有索引/数据无需改动。**governance-debt-cleanup-2 收口**。

## 7. 后续模板（governance-debt follow-on）

- L2 embedding-cache true-LRU（access-order eviction，非 rowid-FIFO）`[SPEC-DEFER:phase-future.l2-cache-true-lru]`；L2 cache 生产调用点接线（`with_sqlite` 当前 test-only）`[SPEC-DEFER:phase-future.l2-cache-production-wiring]`。
- indexing.* replay e2e（真实 session → persist → replay → 客户端重放断言）`[SPEC-DEFER:phase-future.indexing-replay-e2e]`；TraceStore 多 workspace 严格隔离 e2e `[SPEC-DEFER:phase-future.tracestore-multi-workspace-strict]`。
- memory hard-delete cascade（跨表级联，当前 memory_id 仅 memory_items）`[SPEC-DEFER:phase-future.memory-harddelete-cascade]`；daemon datadir env→`daemon.Options.DataDir` 接线 `[SPEC-DEFER:phase-future.daemon-options-datadir]`。
- （承前）sqlite-vec in-process recall/latency matrix `[SPEC-DEFER:phase-future.sqlite-vec-inprocess-matrix]`；chunk source_type filter `[SPEC-DEFER:phase-future.chunk-source-type-filter]`；chunk agent_scope filter `[SPEC-DEFER:phase-future.chunk-agent-scope-filter]`；qdrant live recall `[SPEC-DEFER:phase-future.qdrant-live-recall]`；lancedb build prereq CI `[SPEC-DEFER:phase-future.lancedb-build-prereq-ci]`；multi-arch 原生 arm64 runner `[SPEC-DEFER:phase-future.multi-arch-native-runner]`；GitHub 原生 attestation `[SPEC-DEFER:phase-future.github-native-attestation]`；compose 真实 TLS cert 自动签发 `[SPEC-DEFER:phase-future.compose-tls-auto-cert]`。

## 8. 存档指引

- closeout PR 合入后，**无人值守授权下**主 agent push `v0.26.0` annotated tag 触发 `release.yml`；post-tag-push backfill PR 填实 tag SHA + run ID + 镜像 digest + cosign 凭据（承 v0.8–v0.25 pattern；§7/§4 角括号 `<backfill>` 标记由本 backfill PR 回填）。
- Dockerfile workspace member COPY 完整性预检（承 v0.11.0 PR #139 教训）：本 release **新增 migration `0019_indexing_events`**——随 `core/` member（migrations 嵌入既有 member 路径）COPY，`workspace_id` add-only proto 字段 regenerated 的 `*.pb.go` 随 `proto/` member COPY，其余 Rust/Go 源码改动均在既有 member 内；镜像为默认构建（0 新依赖 / 0 network，linux/amd64）。
