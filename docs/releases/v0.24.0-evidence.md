# ContextForge v0.24.0 Evidence — governance-debt-cleanup (memstore-event parity + event-bus verify-only + cache LRU/cap + compose hardening + eval subtable + exporter full-content + 3 MCP nits + ADR-036 ratified)

> v0.24.0 ships Phase 31: 清理跨 Phase 累积的治理债——观测一致性（Go fallback memstore memory ops 发
> `memory.*` 入 fallback ring，与 workspace/job + Rust 数据面对齐）、缓存有界（embedding L1 LRU/cap +
> Go memstore cap 可配置）、部署硬化（生产 compose 资源限 + 可选 TLS 反代）、eval 可查询（per-case 子表
> + migration 0018）、exporter 全文保真（新 `ListAllChunks` RPC → 真实 content + 真实 ContentHash）、
> 3 个 MCP nits。一处诚实校正：**event-bus partition/capacity 经核 Phase 26 已交付**（非债）→ verify-only +
> roadmap §4 add-only 更正，不重复实现（ADR-013）。**默认行为 / proto 既有字段 / 既有契约不变**（ADR-004），
> 三门 + lint 不退化。**ADR-036** 据 D1–D5 per-D ratify `Proposed → Accepted`（D2 真实 TLS cert / D4
> native-runner·attestation 受阻维度 honest-defer）；**ADR-021/027/029/033** add-only Amendment。

## 1. 合入记录 (Phase 31, tasks 31.1–31.4)

| task | PR | delivery |
|---|---|---|
| 31.1 | #206 | `MemStore.EmitEvent` + `MemMemoryStore.SetEventSink`；Pin/Deprecate/SoftDelete/Unpin/HardDelete 发 `memory.*` 入 fallback ring（event_type 对齐 Rust `audit_op_to_event_type`，Pin/Unpin 共 memory.pin）+ `console_api_serve.go` 接线。event-bus partition/capacity verify-only（`cargo test data_plane::events` 6 passed，Phase 26 已交付）；TEST-31.1.* |
| 31.2 | #207 | `BoundedCache`（FIFO-on-insert，0 新 dep，cap 50_000）+ `with_capacity`；`resolveCacheCapacity`（env `CONTEXTFORGE_CONSOLEAPI_CACHE_CAP`）；compose `mem_limit`/`cpus` + `profiles:[tls]` caddy 反代 + Caddyfile；`docker compose config` + `--profile tls config` 实测 parse OK；真实 cert honest-defer；TEST-31.2.* |
| 31.3 | #208 | migration 0018 `eval_case_results` 子表 + 双写 + `query_case_results`/`case_pass_ratio` SQL 查询；`ListAllChunks` add-only RPC（proto+buf+Rust handler+daemon client+main.go+exporter ChunkLoader）→ exporter content 非空 + 真实 ContentHash；3 MCP nits（protocolVersion 日期解析 / audit err warn / allowlist mode warn）；C2/C3/C4 honest-defer 重申；TEST-31.3.* |
| 31.4 | this PR | Phase 31 closeout：v0.24.0 release docs + smoke v21 step 40 + ADR-036 据 D1-D5 per-D ratify + ADR-021/027/029/033 add-only Amendment + roadmap §4 event-bus 更正（规划 PR #196 已落地）+ phase-31 §6 闭合 + adapter + phase-31 BDD feature |

## 2. S2V 状态

- Phase 31 `governance-debt-cleanup`: Draft → **Done**, Tasks 0 → 4。
- phase-31 §6 AC1-5 全 `[x]`（据 31.1–31.4 真实交付；AC2 真实 TLS cert 维度 honest-defer 如实，compose-config parse 达成 → 部分 ratify；AC3 C2/C3/C4 honest-defer 重申）。
- 默认行为 / proto 既有字段 / 既有契约不变（ADR-004）；add-only RPC + add-only migration + 可选 compose 限值 + cache 默认值保留 + MCP nits 不破协议；不溯改 Phase 1-30 spec（ADR-014 D5）。
- **本 release 清 roadmap §4 长尾 backlog + Phase 28 follow-up + 旧 nits**：memstore-event-emit / cache-lru / cache-cap-configurable / compose-resource-limits / compose-tls-termination / case-results-subtable / exporter content="" / 3 MCP nits 兑现；event-bus-partition/capacity 经核已交付更正剔除；multi-arch-native-runner / github-native-attestation / rust-native-eval-runner 诚实重申延后。

## 3. 验证证据

### 1. observability + memstore parity（task-31.1，🟢）

`go test ./internal/consoleapi/ -run TestMemMemoryStore_EventParity` PASS：5 写操作各发对应 `memory.*` event_type（Pin/Unpin 共 memory.pin），error path 不发，无 sink 不 panic，返回契约不变。`cargo test -p contextforge-core data_plane::events` → **6 passed**（TEST-26.3.1a/b/c verify-only，Phase 26 已交付，不重复实现）；roadmap §4 add-only 更正规划 PR #196 line 294 已落地。

### 2. cache + deploy hardening（task-31.2，🟢/🟡）

`cargo test -p contextforge-core embedding::cache` → **5 passed**（TEST-31.2.1：cap=2 FIFO 驱逐 + 被逐 key 再 embed miss + 仍在缓存 key 命中 + L1 ≤ cap；既有 22.2.* 全绿）。`go test -run TestMemStoreCacheCap` PASS（env=2→缓存留 2 / 非法→256）。`docker compose -f deploy/docker-compose.production.yml config`（默认）+ `--profile tls config`（caddy 反代）**实测 parse OK**（mem_limit 渲染 536870912/2147483648）。**真实 TLS cert 签发须真实域名 → honest-defer `[SPEC-DEFER:phase-future.compose-tls-auto-cert]`，不伪造**（ADR-013）。

### 3. eval 子表 + exporter 全文 + MCP nits（task-31.3，🟢）

`cargo test -p contextforge-core eval::store` → **12 passed**（TEST-31.3.1：migration 0018 子表 `query_case_results` per-run + `case_pass_ratio` 跨 run 聚合 + 既有 `row_to_run` JSON 读不变 + rewrite 替换不累积）。`go test ./internal/exporter/` PASS（TEST-31.3.2：exporter 经 `ListAllChunks` RPC content 非空 + ContentHash 匹配真实正文 + nil loader 向后兼容）。`go test ./internal/mcpadapter/` PASS（TEST-31.3.3：protocolVersion YYYY-MM-DD 日期解析语义比较——forward-compat 保留、malformed 拒；audit.Write err stderr warn；allowlist os.Stat 过松 mode warn）。`ListAllChunks` add-only RPC（buf generate Go pb + Rust tonic handler 经 enumerate_chunks 读真实全文）不破 Search/SearchResponse。

### 3b. smoke v21（诚实记录）

`bash -n scripts/console_smoke.sh` exit 0（CI go-test 语法门 + `TestTask314_SmokeV21GovernanceDebtCleanupStep`）。v21 step 40 文档化治理债清理状态；多为 feature/内部修复无 console-api 运行时面，step 40 验默认构建仍 scaffold（默认行为/proto/契约不变，ADR-004）。既有 v13-v20 块 denominator 不溯改（新增 `[40/40]` add-only，ADR-014 D5）。

### 4. CI 状态（各 task PR 四门绿后自主合入）

| PR | cargo-test | go-test | spec-lint | lint |
|---|---|---|---|---|
| 31.1 (#206 memstore parity + event-bus verify-only) | PASS | PASS | PASS | PASS |
| 31.2 (#207 cache LRU + cap + compose 硬化) | PASS | PASS | PASS | PASS |
| 31.3 (#208 eval 子表 + exporter 全文 + MCP nits) | PASS | PASS | PASS | PASS（gofmt search.pb.go Go 1.19+ 注释 reflow 修后绿） |
| 31.4 (this PR closeout) | PASS | PASS | PASS | PASS |

注：task-31.3 buf generate 的 `search.pb.go` 触 Go 1.19+ gofmt doc-comment reflow（本机 protoc-gen-go 输出旧式注释）→ `gofmt -w` 修后 CI lint 绿；`core/tests/phase9_index_smoke.rs` Tantivy `LockBusy` 既有间歇 flake 命中时 `gh run rerun` 复跑至绿（承 v0.13–v0.23 pattern），never-merge-red 守线。

### 5. ADR-014 第二十二次激活 record（this closeout PR self-check）

`bash scripts/spec_drift_lint.sh --touched origin/master` → 0 未标注命中（CI spec-lint gate 复核）。各 task PR（31.1/31.2/31.3）触及行亦 0 未标注命中。

### 6. AC 达成（治理债清理 + v0.24.0 收口）

| Phase 31 §6 AC | 状态 | 依据 |
|---|---|---|
| AC1 observability + memstore parity + event-bus verify-only | ✅ met | task-31.1（#206）；memstore parity test + cargo events 6 passed（§3.1） |
| AC2 cache + deploy hardening | ✅ met（真实 TLS cert honest-defer 如实） | task-31.2（#207）；cache 5/5 + cap config + compose parse 🟢；真实 cert 🟡 延后（§3.2） |
| AC3 eval 子表 + exporter 全文 + MCP nits + honest defer 重申 | ✅ met | task-31.3（#208）；eval 12/12 + exporter 真实 content + MCP nits（§3.3）；C2/C3/C4 honest-defer 重申 |
| AC4 默认行为不变 + v0.24.0 closeout | ✅ met | task-31.4（this PR）；默认 `cargo test --workspace`/`go test ./...` 0 failed + release docs + smoke v21 + ADR-036 per-D ratify |
| AC5 ADR-014 cross-validation gate（第二十二次激活） | ✅ met | 本 closeout PR body（D1-D5，见 §6） |

## 4. 平台矩阵

| Platform | Build | Note |
|---|---|---|
| Windows MSVC (dev box) | 默认 build + cargo/go test PASS；clippy `-D warnings` exit 0；smoke `bash -n` exit 0；docker compose config parse OK | gofmt staged blob LF（autocrlf；search.pb.go Go 1.19+ 注释 reflow 已 gofmt -w 修）；rustc 1.95.0，2026-06-03 |
| Linux (GHA runner) | 默认 build + cargo/go test + spec-lint + lint PASS | CI 四门；真实 TLS cert 须域名（CI 不闭环）honest-defer |
| 镜像 linux/amd64 | release.yml 推 GHCR（默认构建 0 新 dep / 0 network） | 与 v0.23.0 baseline 一致 |

## 5. Upgrade / Rollback

- Upgrade：默认行为 / proto 既有字段 / 既有契约不变（ADR-004，既有 v0.6–v0.23 client + 既有索引/数据无需改动）。新增 `ListAllChunks` add-only RPC + cache 有界化（默认 cap 不破命中）+ memstore cap env-config（未设默认 256）+ compose 限值/TLS profile（默认 profile 不启）+ eval 子表 add-only migration 0018（旧 run JSON 读兼容）+ MCP nits 不破协议。
- Rollback：`git tag -d v0.24.0` + 删 GitHub Release / ghcr tag；与 v0.23.0 行为兼容（治理债修复均 add-only/默认值保留/可选化，运行时默认路径不变）。

## 6. ADR-014 第二十二次激活 record

- **D1（mapping）**：Phase 31 §6 AC1-5 ↔ task-31.1（§6 AC1-3）/ task-31.2（§6 AC1-4）/ task-31.3（§6 AC1-4）/ task-31.4（§6 AC1-3）。AC1↔31.1（memstore parity + event-bus verify-only）/ AC2↔31.2（cache + deploy）/ AC3↔31.3（eval + exporter + MCP nits）/ AC4↔31.4（release+ADR+§6）/ AC5↔本 PR body。
- **D2（lint）**：各 task PR（31.1/31.2/31.3）+ 本 closeout `spec_drift_lint.sh --touched origin/master` 0 未标注命中（CI spec-lint）。
- **D3（verified-by）**：每 AC 显式 owner TEST-id（TEST-31.1.x / 31.2.x / 31.3.x / 31.4.x）+ 真实证据（memstore parity + cargo events 6/6 / cache 5/5 + cap config + compose parse / eval 12/12 + exporter 真实 content + MCP nits）。
- **D4（自治）**：主 agent ADR-012 自治据真实数据推进；never-merge-red + 禁据合成 ratify（ADR-013）守线；ADR-036 据 D1（parity + verify-only）+ D2（cache/deploy code-local 达成；**真实 TLS cert honest-defer 不强 ratify、不伪造**）+ D3（eval/exporter/nits 达成）+ D4（honest-defer 重申）+ D5（baseline 不变）真实非合成验证 ratify Accepted；受阻维度（真实 cert / native arm64 runner / attestation）据真实受限**如实记录**（不伪造）。tag / release outward-facing 不可逆 → 须用户显式授权（沿用 v0.13-v0.23 红线，不自行 tag）。
- **D5（不溯改）**：Phase 1-30 spec 未溯改；治理债清理 + ratify 记录写在 ADR-036 Ratification + ADR-021/027/029/033 add-only Amendment（Phase 31）+ task-31.1-31.4 §10 + 本文件；migration 0018 add-only（不动 eval_runs）；ListAllChunks add-only RPC（Search/SearchResponse 不动）；smoke 既有 v13-v20 块 denominator 不溯改（新增 v21 step 40 add-only）；`internal/cli/smoke_syntax_test.go` 加 `TestTask314_*`（add-only）；ADR-014 history Phase 1-30 不 retro-edit（22nd activation add-only）。

## 7. tag / 镜像 SHA（post-tag-push backfill）

post-tag-push backfill（无人值守授权下主 agent push（ADR-012） tag 触发 `release.yml` 后填实，承 v0.8–v0.23 pattern）：

- v0.24.0 annotated tag SHA：`<backfill: v0.24.0 tag SHA @ task-31.4 closeout merge>`
- `release.yml` run ID：`<backfill: release run id + conclusion>`
- 镜像 digest：`ghcr.io/tajiaoyezi/contextforge-daemon:v0.24.0` + `:latest` = `<backfill: sha256 digest>`（linux/amd64）
- cosign keyless 签名凭据：`<backfill: Rekor tlog idx (sign + attest SBOM)>`
