# ContextForge v0.24.0 Artifacts — governance-debt-cleanup (memstore-event parity + cache LRU/cap + compose hardening + eval subtable + exporter full-content + 3 MCP nits + ADR-036 ratified)

> Artifact inventory for the v0.24.0 minor release (Phase 31). Mirrors the v0.23.0 artifacts structure.
> The default build is unchanged: 0-new-dependency, 0-network；默认行为 / proto 既有字段 / 既有契约不变。
> 新增 `ListAllChunks` add-only RPC + cache 有界化 + memstore cap env-config + compose 限值/TLS profile +
> eval 子表 add-only migration 0018 + 3 MCP nits 均不破既有契约。既有 22-endpoint + 5 Memory RPC + 运行时
> 二进制行为不变。No breaking contract bump。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：memstore-event parity（fallback ring 加 memory.* event）+ memstore cap env-config + exporter ChunkLoader（ListAllChunks 客户端）+ daemon `ListAllChunks` 方法 + main.go 接线 + 3 MCP nits。运行时既有 22-endpoint + 5 Memory RPC 行为不变。
- `contextforge-core`（Rust 数据面 daemon）：embedding-cache `BoundedCache` L1 有界 + eval `eval_case_results` 子表（migration 0018）+ `ListAllChunks` gRPC handler（经 enumerate_chunks 读真实全文）。默认构建 0 新依赖 / 0 network；`cargo test --workspace` 0 failed。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.24.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network）+ cosign keyless 签 + SBOM + provenance（承 v0.21.0–v0.23.0）。linux/arm64 仍 DEFERRED。
- Feature 构建：本 release 无新 feature flag；ListAllChunks RPC 在默认构建（add-only proto）。

## 2. 源码档

- master tag SHA：`<backfill: v0.24.0 annotated tag SHA，无人值守授权下主 agent push（ADR-012）>`
- Phase 31 代码路径：
  - task-31.1（memstore parity + event-bus verify-only）：`internal/consoleapi/memstore.go`（`MemStore.EmitEvent` + `MemMemoryStore.SetEventSink`/`emitMemoryEvent` + 5 写操作 emit + `memstore_test.go`）；`internal/cli/console_api_serve.go`（buildDeps 接线）；`core/src/data_plane/events.rs`（verify-only 不改）；`docs/roadmap.md` §4 更正（规划 PR #196 已落地）。
  - task-31.2（cache + deploy）：`core/src/embedding/cache.rs`（`BoundedCache` + `DEFAULT_EMBEDDING_CACHE_CAP` + `with_capacity` + TEST-31.2.1）；`internal/consoleapi/memstore.go`（`resolveCacheCapacity` + TEST-31.2.2）；`deploy/docker-compose.production.yml`（mem_limit/cpus + tls profile）+ `deploy/caddy/Caddyfile`；`docs/deploy/production.md`。
  - task-31.3（eval + exporter + MCP nits）：`core/migrations/0018_eval_case_results.sql` + `core/src/eval/store.rs`（双写 + query_case_results/case_pass_ratio + TEST-31.3.1）；`proto/contextforge/v1/{search,service}.proto`（ListAllChunks add-only）+ regenerated `*.pb.go`；`core/src/server.rs`（handler）；`internal/daemon/search.go`（client）；`cmd/contextforge/main.go`（接线）；`internal/exporter/source.go`（ChunkLoader + TEST-31.3.2）；`internal/mcpadapter/{server.go,allowlist.go}`（3 nits + TEST-31.3.3）。
  - task-31.4（this PR）：`scripts/console_smoke.sh`（v21 step 40）；`internal/cli/smoke_syntax_test.go`（`TestTask314_*`）；`docs/releases/v0.24.0-{evidence,artifacts}.md`；`README.md` v0.24 段；`RELEASE_NOTES.md` v0.24.0 段；`docs/decisions/adr-036-...md`（ratify Proposed→Accepted，per-D）+ ADR-021/027/029/033 add-only Amendment；`docs/specs/phases/phase-31-governance-debt-cleanup.md`（§6 闭合）；`docs/s2v-adapter.md`；`test/features/phase-31-governance-debt-cleanup.feature`。

## 3. 验证脚本

```bash
# 默认构建（0 新依赖 / 0 network）
cargo test --workspace
go test ./...
cargo clippy --workspace --all-targets -- -D warnings

# task-31.1 memstore parity + event-bus verify-only
go test ./internal/consoleapi/ -run TestMemMemoryStore_EventParity
cargo test -p contextforge-core data_plane::events      # 6 passed (TEST-26.3.1 verify-only)

# task-31.2 cache LRU + cap config + compose parse
cargo test -p contextforge-core embedding::cache         # 5 passed
go test ./internal/consoleapi/ -run TestMemStoreCacheCap
docker compose -f deploy/docker-compose.production.yml config
docker compose -f deploy/docker-compose.production.yml --profile tls config

# task-31.3 eval 子表 + exporter 全文 + MCP nits
cargo test -p contextforge-core eval::store              # 12 passed
go test ./internal/exporter/ ./internal/mcpadapter/

# 端到端 smoke v21（step 40；既有 step 不退化）
bash -n scripts/console_smoke.sh
go test ./internal/cli/ -run TestTask314

# D2 spec-drift lint
bash scripts/spec_drift_lint.sh --touched origin/master
```

## 4. 镜像推送

- `release.yml`（`v*` tag push 触发）→ buildx → ghcr login → build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.24.0` + `:latest`（**linux/amd64**，net-zero 与 v0.23.0 一致）→ cosign keyless 签 + attest SPDX SBOM + provenance:mode=max（承 task-28.2 机制）。
- run ID + digest：`<backfill: release.yml run id + conclusion + sha256 digest + cosign Rekor tlog>`。

## 5. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-036 governance-debt-cleanup | **Accepted**（per-D，D2 真实 cert / D4 native-runner·attestation honest-defer 部分） | task-31.4 据 31.1/31.2/31.3 真实非合成验证 ratify：D1 Accepted / D2 PARTIAL（cache/deploy code-local 达成；真实 TLS cert honest-defer `[SPEC-DEFER:phase-future.compose-tls-auto-cert]`）/ D3 Accepted / D4 honest-defer 重申 / D5 baseline 不变 |
| ADR-021 memory-event-bus-bridge | Accepted（+ Phase 31 add-only Amendment） | memstore-event Go parity 修 + event-bus partition/capacity 经核 Phase 26 已交付的更正（不溯改正文，ADR-014 D5） |
| ADR-027 embedding-provider | Accepted（+ Phase 31 add-only Amendment） | L1 cache LRU/cap 有界化（不溯改正文） |
| ADR-029 eval | Accepted（+ Phase 30 + Phase 31 add-only Amendment） | case-results 子表（migration 0018，可查询，不溯改正文 + Phase 30 Amendment） |
| ADR-033 release-ci-hardening | Accepted（+ Phase 31 add-only Amendment） | multi-arch-native-runner / github-native-attestation / rust-native-eval-runner 诚实重申延后（不溯改正文，不伪造完成） |
| ADR-031 observability-hardening | Accepted | event-bus partition/capacity verify-only 引用（Phase 26 已交付） |
| ADR-004 / ADR-012 / ADR-013 / ADR-014 | Accepted（守线） | 默认行为/契约不变 / tag-release 须授权 / 禁伪造（真实 cert·runner·attestation honest-defer）/ 第二十二次激活 |

## 6. 版本声明（contract bump 评估）

- 本 release 新增 `ListAllChunks` add-only RPC + cache 有界化 + memstore cap env-config + compose 限值/TLS profile + eval 子表 add-only migration 0018 + 3 MCP nits + smoke v21——**无 proto 既有字段 / RPC / endpoint 删改、无默认运行时行为改动**。
- 默认构建 0 新代码依赖 / 0 network（ADR-004）。
- 故 **无 breaking contract bump**；既有 v0.6–v0.23 client + 既有索引/数据无需改动。**governance-debt-cleanup 收口**。

## 7. 后续模板（governance-debt follow-on）

- compose 真实 TLS cert 自动签发（ACME / 域名后）`[SPEC-DEFER:phase-future.compose-tls-auto-cert]`。
- multi-arch 原生 arm64 runner `[SPEC-DEFER:phase-future.multi-arch-native-runner]`；GitHub 原生 attestation（仓库转 public / 升 GHEC）`[SPEC-DEFER:phase-future.github-native-attestation]`；rust-native-eval-runner（有 consumer 后）`[SPEC-DEFER:phase-future.rust-native-eval-runner]`。
- L2 SQLite 缓存上限 / 全局清理 `[SPEC-DEFER:phase-future.l2-cache-eviction]`；Go memstore 真 LRU（access-order）`[SPEC-DEFER:phase-future.memstore-true-lru]`。

## 8. 存档指引

- closeout PR 合入后，**无人值守授权下**主 agent push `v0.24.0` annotated tag 触发 `release.yml`；post-tag-push backfill PR 填实 tag SHA + run ID + 镜像 digest + cosign 凭据（承 v0.8–v0.23 pattern）。
- Dockerfile workspace member COPY 完整性预检（承 v0.11.0 PR #139 教训）：本 release 新增 `core/migrations/0018_eval_case_results.sql`（migration——已随 `core/migrations/` member COPY，eval store `include_str!` 编译期内嵌）+ 源码均在既有 member 内——**无新增运行时 proto 文件**（pb.go 随 `proto/` member）；镜像为默认构建（0 新依赖 / 0 network，linux/amd64）。
