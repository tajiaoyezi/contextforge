# ContextForge v1.0.1 Evidence — v1.0.1-patch (v1.0 收口审查残留修复：P0 CLI version ldflags + P1-P3 文档)

> v1.0.1 ships Phase 48（v1.0.1-patch）。v1.0.0 ship 后全面审查发现 4 个残留：(P0) CLI version 字符串过时（cli.go 默认值 + Dockerfile 无 ldflags → 镜像报错版本，D2 缺陷）+ (P1-P3) 文档过时（ADR-050 访客索引 / README Latest 描述 / example.toml header）。P0 修复：cli.go Version 默认值 `1.0.1-dev` + Dockerfile `ARG VERSION` + `-ldflags -X cli.Version` + release.yml `build-args VERSION`。**0 新 dep / 0 migration / 0 proto / 0 schema**。仅 CLI version 输出从错误变正确。

## 1. 合入记录 (Phase 48)

| task | PR | delivery |
|---|---|---|
| 48.1 | #295 | v1.0.1-patch：P0 cli.go Version 默认值 1.0.1-dev + Dockerfile ARG/ldflags + release.yml build-args；P1 docs/decisions/README.md ADR-050 Accepted；P2 README Latest 段 v1.0；P3 example.toml header v1.0.1；smoke v38[57/57] + TestTask481 |

## 2. S2V 状态
- Phase 48 `v1.0.1-patch`: Ready → **Done**, Tasks 0 → 1。
- §6 AC1-4 全 `[x]`。`go test ./...` 全过；gofmt clean；**0 dep / 0 migration / 0 proto / 0 schema**。

## 3. 验证证据
- TEST-48.1.1（TestTask481）：cli.go Version `"1.0.1-dev"`（无 `0.38.0-dev`）+ Dockerfile `ARG VERSION` + `ldflags` + `cli.Version` + release.yml `build-args` + `VERSION=` PASS。
- TEST-48.1.2（TestTask481）：docs/decisions/README.md ADR-050 行 Accepted（非 Proposed partial）+ README 无"第二步"过时描述 PASS。
- TEST-48.1.3（TestTask481）：smoke v38[57/57] + bash -n + no-regression [37/37]..[56/56] PASS。
- 全量 `go test ./...` PASS；gofmt clean。

## 4. ADR
- ADR-050（v1.0-definition）：**Accepted**（v1.0.0 完整 ratify，本 patch 不动 D-Body）；ADR-014（第三十九次激活 D1-D5）；ADR-004/008 守线。

## 5. 发版凭据（post-tag-push backfill，ADR-013）
- tag commit：`94ade88`（master HEAD）
- tag object：`69a6719`（annotated tag）
- release run：`28691648309`（success）
- ghcr digest：`sha256:dcfbfdac3b8b9f8cab4cfdbb82f1473dad1ca5b0541decc1423869821a071d84`
- cosign tlog：`2064785505` (sign) · `2064785586` (attest)
- GitHub Release object：`https://github.com/tajiaoyezi/contextforge/releases/tag/v1.0.1`（D4 第三次实践成功，isPrerelease=false）
- **P0 ldflags 实测成功**：build log 确认 `-ldflags "-X github.com/tajiaoyezi/contextforge/internal/cli.Version=v1.0.1"`（build-args `VERSION=v1.0.1` 传入）→ v1.0.1 镜像 `contextforge version` 报 `v1.0.1`（D2 缺陷修复验证）
