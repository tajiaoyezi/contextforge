# ContextForge v0.1 Evidence

**日期**：2026-05-23
**主干**：`master`
**当前 HEAD**：`daa4869` (`Merge pull request #55 from tajiaoyezi/feat/task-8.3-release-smoke`)

## 合入记录

- PR #52 `chore/governance-autonomy` → `afca395`：ADR-012 主 agent 自治放宽。
- PR #53 `feat/task-8.1-eval-harness` → `6bcf1a0`：recall eval harness。
- PR #54 `feat/task-8.2-reliability` → `ee875dc`：长任务恢复、资源预算、安全回归 guard。
- PR #55 `feat/task-8.3-release-smoke` → `daa4869`：release smoke gate、tarball contract、Phase 8 Done。

## S2V 状态

- `docs/s2v-adapter.md`：Phase 8 `eval-and-reliability` = `Done`。
- `task-8.1-eval-harness.md` = `Done`。
- `task-8.2-reliability.md` = `Done`。
- `task-8.3-release-smoke.md` = `Done`。
- `phase-8-eval-and-reliability.md` = `Done`，§6 phase smoke 已填实为 `bash scripts/release_smoke.sh`。

## 验证证据

最终 `master` 上已执行：

```bash
bash -lc 'source docs/s2v/scripts/lib/preflight.sh; source docs/s2v/scripts/lib/verify.sh; s2v_baseline_green "cmd/contextforge internal core/src core/tests"'
```

结果：

- `install`: ✅ `go mod download && cargo fetch`
- `typecheck`: ✅ `go vet ./... && cargo check --workspace`
- `unit-test`: ✅ `go test ./... && cargo test --workspace`
- Rust workspace：60 passed / 0 failed
- 退出码：`FINAL_MASTER_BASELINE_EXIT=0`

最终 `master` 上已执行：

```bash
bash scripts/release_smoke.sh
```

结果：

- `internal/release` release harness：✅
- `internal/eval` / `internal/reliability` task 8 harness：✅
- Rust gRPC search smoke `phase_6_search_grpc_end_to_end_smoke`：✅
- 脚本输出：`PHASE8_RELEASE_SMOKE_EXIT=0`

## Release 边界

v0.1 已具备 release contract gate：`contextforge-linux-amd64.tar.gz` 必须包含 `contextforge`、`contextforge-core`、`contextforge.example.toml`、`README.md`、`LICENSE`，并通过 smoke evidence 与 10 万 chunk P95 gate 校验。

未在本轮执行的发布动作：GitHub Release 上传、tag、checksum / signing、真实 amd64 runner 上的制品构建发布作业。这些属于 task-8.3 §3 Out Of Scope；当前仓库提供可执行 gate 与证据链，供后续 release job 复用。
