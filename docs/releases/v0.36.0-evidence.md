# ContextForge v0.36.0 Evidence — governance-debt-cleanup-4 / indexing-replay-splice (把 Phase 33 task-33.3 交付但未接进 live 路径的 indexing replay mapper 拼接进 EventsServer::subscribe；since_ts>0 订阅者现可收到 missed indexing.* 事件；默认 byte-equiv + ADR-048 ratified)

> v0.36.0 ships Phase 43（governance-debt-cleanup-4，第四轮治理债清扫，承 Phase 33 task-33.3 / ADR-038 D3 血脉的"最后一公里"）——**0 新 dep** / **0 network** / **0 schema migration**（复用 Phase 33 migration 0019）/ **0 proto 改动** / **默认 byte-equiv**（since_ts<=0 / store=None 两退化路径）——遵 ADR-013「诚实优先于凑数」：
> - **indexing-replay-splice**（兑现 ADR-038 D3 `[SPEC-DEFER:phase-future.indexing-replay-e2e]` 的 splice 维度）：Phase 33 task-33.3 交付了 indexing event **持久化**（add-only migration 0019 + `SqliteIndexingEventStore` + 4 emit 点 best-effort 持久写）+ **replay mapper**（`indexing_rows_to_pb_events`，`events.rs:438`，真实 job_id/processed/total 取持久行不合成，`test_33_3_2` 守护），但 mapper **从未在 live subscribe 路径被调用**（4 拼接缺口，grounding 已亲自核实）：(1) `list(limit)` 缺 since_ts；(2) `DataPlaneStores` 无 `indexing_event_store` 字段；(3) `serve_full` `full()` 未传入 store；(4) `subscribe` replay 只 splice audit 漏 indexing。task-43.1 补全 4 缺口：`list_since(limit, since_ts)`（since_ts>0 时 `WHERE ts_unix >= ?` 镜像 `replay_events_from_audit`，since_ts<=0 不过滤 byte-equiv；单 SQL `(?1=0 OR ts_unix>=?1)` 守护统一 params）+ `DataPlaneStores` add 字段 + `full()` 第 10 参数（既有 5 constructor 补 None byte-equiv）+ `server.rs` serve_full `IndexSessionBackend` 改 clone + `full()` 传入 + `events.rs` subscribe replay 段 splice indexing replay（since_ts>0 时 list_since + mapper，audit replay 后、live forward 前；store None/lock 失败 unwrap_or_default 空 best-effort 镜像 audit）。
> - **ADR-048-ratified**：ADR-048 据 D1-D4 per-D ratify `Proposed → Accepted`（D1/D2/D3 unit 🟢 / D4 live daemon e2e 🟡 honest-defer）；ADR-038（indexing-replay-e2e splice 维度兑现）add-only Phase-43 Amendment，**不**溯改 D-body（ADR-014 D5）。
> **诚实校正（ADR-013，本 phase 核心）**：本 phase 交付 splice **拼接** + unit 级时序单测（🟢）；live daemon restart-then-replay 端到端 e2e（真起进程 + 跨 restart 双窗口断言）须 running daemon（须 console 跨进程）→ 🟡 honest-defer `[SPEC-DEFER:phase-future.indexing-replay-daemon-e2e]`（不预填，不夸大 unit splice 为 live e2e 已验）；memory-actor-all-rpc（Deprecate/SoftDelete 7 层 + 新 migration / HardDelete 须 audit 重设计 = 非小债）据实延后留独立 phase（roadmap §3.17/§3.22 "据实排小不凑数"，不强行扩面）。

## 1. 合入记录 (Phase 43, task 43.1)

| task | PR | delivery |
|---|---|---|
| 43.1 | #276 | indexing-replay-splice：`core/src/data_plane/indexing_events.rs` add `list_since(limit, since_ts)`（since_ts>0 时 `WHERE (?1=0 OR ts_unix>=?1) ORDER BY id ASC LIMIT ?2` 镜像 audit since_ts，since_ts<=0 不过滤 byte-equiv `list()`；既有 `list(limit)` 不动）+ `core/src/data_plane/mod.rs` `DataPlaneStores` add `indexing_event_store: Option<Arc<SqliteIndexingEventStore>>` 字段 + `full()` 第 10 参数（5 既有 constructor 补 None byte-equiv）+ `core/src/server.rs` serve_full `IndexSessionBackend` 改 clone（写路径持 clone）+ `DataPlaneStores::full()` 传入 Some(原件)（读路径 subscribe 可达）+ `core/src/data_plane/events.rs` subscribe replay 段 splice indexing replay（since_ts>0 时 list_since + `indexing_rows_to_pb_events`，audit replay 后、live forward 前；store None/lock 失败 unwrap_or_default 空）+ `core/src/data_plane/memory.rs` test struct literal 补字段。TEST-43.1.1（list_since 时序过滤 + id ASC + since_ts<=0 byte-equiv）+ TEST-43.1.2a（subscribe splice 顺序 indexing→audit→live）+ TEST-43.1.2b（since_ts<=0 无 replay byte-equiv timeout 守护）+ TEST-43.1.2c（store=None 仅 audit replay 无 indexing）。**0 新 dep / 0 schema migration（复用 0019）/ 0 proto 改动 / 默认 byte-equiv** |
| 43.x | this | governance-debt-cleanup-4 closeout。Phase 43 closeout：smoke v32→v33 `[52/52]`（indexing-replay-splice 可达断言，不可达诚实归因 unit TEST-43.1.2a/b/c；REAL-mode subscribe-stream e2e 🟡 honest-defer）+ `TestTask433`（no-regression [37/37]..[51/51]）+ v0.36.0 release docs + ADR-048 据 D1-D4 ratify + ADR-038 add-only Phase-43 Amendment（indexing-replay-e2e splice 维度兑现，live daemon e2e 续延后）+ ADR-031/021/004/008 守线引用 + roadmap §3.25/§4 + phase-43 §6 + adapter + defer marker 更新 |

## 2. S2V 状态

- Phase 43 `governance-debt-cleanup-4`: Draft → **Done**, Tasks 0 → 2。
- phase-43 §6 AC 全 `[x]`（据 43.1 真实交付；AC1 indexing-replay-splice 接进 live subscribe 🟢 + AC2 honest-defer 边界 + v0.36.0 收口 + 默认零依赖守线 🟢 + AC3 ADR-014 第三十四次激活）。
- 默认 `cargo test --workspace` 全过（core lib **229** = 225 baseline + 4：indexing_events TEST-43.1.1 +1 + events TEST-43.1.2a/.2b/.2c +3）；`go test ./...` 全过（含 TestTask433）；**0 新 dep / 0 network / 0 schema migration / 0 proto 改动**；默认 byte-equiv（since_ts<=0 / store=None）；不溯改 Phase 1-42 spec / ADR-038 D-body（ADR-014 D5）。
- **本 release 兑现 indexing-replay-e2e splice 维度**：indexing replay mapper 接进 live subscribe 路径（关闭 Phase 33 mapper 写好却未调用的"最后一公里"缺口）；live daemon restart-then-replay e2e 据 ADR-013 honest-defer、memory-actor-all-rpc 据实延后留独立 phase、其余治理 marker 据实保持延后、不强行扩面。

## 3. 验证证据

### 1. indexing-replay-splice（task-43.1，🟢）

`cargo test -p contextforge-core --lib data_plane::indexing_events` + `data_plane::events` PASS：
- TEST-43.1.1（`data_plane::indexing_events::tests::test_43_1_1_list_since_ts_filter_and_byte_equiv`）——`list_since(limit, since_ts)`：append 3 行 ts 单调递增 → since_ts=ts2 返 r2+r3（含等号边界 `>=`）/ since_ts>max 返空 / since_ts<=0 返全量 byte-equiv `list(limit)` / limit clamp / re-open idempotent。
- TEST-43.1.2a（`data_plane::events::tests::test_43_1_2_subscribe_splices_indexing_then_audit_then_live`）——subscribe(since_ts=1)：先收到 indexing replay（evt-idx-* id ASC）2 条再 audit replay（evt-audit-* id ASC）2 条；splice 顺序 indexing→audit→live。
- TEST-43.1.2b（`test_43_1_2b_since_ts_zero_no_replay_byte_equiv`）——subscribe(since_ts=0)：无 replay（timeout 守护，150ms 内无事件 = replay batch 空，byte-equiv pre-task-43.1）。
- TEST-43.1.2c（`test_43_1_2c_store_none_no_indexing_replay`）——indexing_event_store=None：首条是 evt-audit-*（仅 audit replay，无 evt-idx-*，退化 pre-task-43.1）。

### 2. 不退化

`cargo test --workspace` 全绿（lib 229 + integration + doc-test）；`cargo clippy --workspace --all-targets -- -D warnings` 0 warning；`go test ./...` 全过（含 TestTask433）；`bash -n scripts/console_smoke.sh` exit 0；spec_drift_lint CI spec-lint job 全绿。既有 `test_33_3_2`（store round-trip）+ `test_33_3_1`（mapper）+ `test_replay_events_from_audit_*`（audit replay）+ `test_events_server_keepalive` 维持绿。

## 4. ADR

- **ADR-048**（indexing-replay-splice）：Proposed → **Accepted**（per-D ratify @ task-43.3；D1 list_since / D2 DataPlaneStores 接线 / D3 subscribe splice unit 🟢；D4 默认 byte-equiv 🟢 + live daemon e2e 🟡 honest-defer）。
- **ADR-038**（governance-debt-cleanup-2）：add-only `## Amendment (Phase 43 / v0.36.0)` 记 indexing-replay-e2e splice 维度兑现（mapper 接进 live subscribe + since_ts 时序），live daemon e2e 续延后；不溯改 D-body / §D3 / Ratification (v0.26.0)（ADR-014 D5）。
- ADR-031（replay 范式源 task-26.2）/ ADR-021（audit replay splice 镜像源）/ ADR-004（默认 byte-equiv + 既有契约不变）/ ADR-008（0 新依赖）/ ADR-013（禁伪造红线）/ ADR-014（D1-D5，第三十四次激活）/ ADR-012（tag/release outward-facing 须用户显式授权）—— 守线引用，不溯改正文。
