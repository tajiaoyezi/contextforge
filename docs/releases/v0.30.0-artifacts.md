# ContextForge v0.30.0 Artifacts — embedding-provider-remote-live (real remote embedding live recall harness + Go [remote] config env-bridge + ADR-042 ratified)

> Artifact inventory for the v0.30.0 minor release (Phase 37). Mirrors the v0.29.0 artifacts structure.
> The default build is unchanged: 0-new-dependency, 0-network；默认行为 / proto 既有字段 / 既有契约不变。
> Close ADR-027 honest-defer `[SPEC-DEFER:phase-future.embedding-provider-remote]`——首次对**真实远程 embedding 端点**端到端联调 + 实测语义召回 + 接通 Go `[remote]` config env-bridge。`RemoteEmbeddingProvider`（ureq、feature-gated）自 Phase 22（task-22.3）起即已实现契约测试；v0.30.0 新增 env-gated live-recall harness（`core/tests/remote_embedding_recall.rs`，task-37.1）+ Go `setRemoteEnv` 跨进程 config env-bridge（task-37.2）。**0 新依赖**（`ureq` 自 task-22.3 起 optional）、0 migration、默认构建 0-network / 0-remote-dep 不变（ADR-004/008），API key env-only 永不进 `config.toml`。**真实实测（主 agent 本机已认证 run，SiliconFlow `https://api.siliconflow.cn/v1/embeddings` + `Qwen/Qwen3-Embedding-8B`，dim=1024，3 次 run）**：remote recall@1 = 0.8667–0.9333（13–14/15，**跨 run 波动**，远程模型/服务非完全确定）/ recall@3 = 1.0000（15/15，**3 次 run 全稳定**）vs deterministic（model-free 基线）recall@1 = 0.0000 / recall@3 = 0.0667，delta@3 = +0.9333。诚实 caveat（ADR-013）：小型作者手工标注集（15 case / 16 doc 含近义干扰），recall@3 = 1.0 证明 remote 模型把明显语义对排在埋设的硬近义干扰（config_save↔config_load、hybrid↔bm25）之上、**非大基准质量断言**；大语料语义质量诚实延后 `[SPEC-DEFER:phase-future.embedding-large-corpus-recall]`。**CI honest-defer（关键诚实差异，ADR-013）**：remote 是**付费外部 API、无免费 service container**（与 qdrant 不同——qdrant 有免费 OSS service container 在每次 CI run 守护召回）；CI 无密钥时 harness 干净 honest-defer skip，真实召回**由本机已认证 run 实测**而非每次 CI run 守护，据实记录、**不夸大为「CI 每次 run 守护」**。既有 22-endpoint + 5 Memory RPC + 运行时二进制默认行为不变。No breaking contract bump。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：新增 `setRemoteEnv` 跨进程 config env-bridge（task-37.2，`[remote]` config → env 桥，镜像既有 `setVectorEnv`，env-wins）。运行时既有 22-endpoint + 5 Memory RPC 行为不变；API key env-only 永不进 config（仅 env 桥读取既有 env）。
- `contextforge-core`（Rust 数据面 daemon）：**0 后端改动**——`RemoteEmbeddingProvider`（`core/src/embedding/remote.rs`，ureq、feature-gated `embedding-remote`）自 Phase 22 task-22.3 起即完整，本 release 不改 provider、不改控制流。新增 env-gated 集成测试 `core/tests/remote_embedding_recall.rs`（仅在 `CONTEXTFORGE_REMOTE_API_KEY` 置位时连接真实远程端点，否则 skip；默认 `cargo test` 不触网）。`ureq` 自 task-22.3 起 optional（默认构建 0-remote-dep / 0-network 不变）。`cargo test -p contextforge-core --lib` = lib all PASS（默认路径不变）。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.30.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network）+ cosign keyless 签 + SBOM + provenance（承 v0.21.0–v0.29.0）。linux/arm64 仍 DEFERRED。
- Feature 构建：本 release 无新 feature flag；live-recall harness 在 env-gated 集成测试路径（默认 skip / `ureq` 自 task-22.3 起 optional / `embedding-remote` feature opt-in，不入默认构建）。

## 2. 源码档

- master tag SHA：`<backfill: v0.30.0 tag SHA (master HEAD at closeout merge)>`（v0.30.0 annotated tag 对象 `<backfill: v0.30.0 annotated tag object SHA>`，无人值守授权下主 agent push（ADR-012））
- Phase 37 代码路径：
  - task-37.1（PR #<backfill: task-37.1 PR number>；remote-embedding live-recall harness）：`core/tests/remote_embedding_recall.rs`（env-gated `CONTEXTFORGE_REMOTE_API_KEY` harness：作者手工标注集 15 case / 16 doc（含故意埋设的硬近义干扰 config_save↔config_load、hybrid↔bm25），real remote 模型 vs deterministic（model-free）基线在**同一** `BruteForceVectorBackend` 精确余弦路径上跑 recall@1 / @3；未置位时 honest-defer skip）；首次以真实远程端点度量语义召回。护栏：floor recall@3 >= 0.70 + remote@1 > det@1，本机每次 run 均过。0 provider 改动 / 0 新依赖（`ureq` 自 task-22.3 起 optional）。grounding：小型手工标注集 → recall@3 = 1.0 为语义正确性（模型把明显对排在近义干扰之上）证明，非大基准质量断言，caveat 据实记录。
  - task-37.2（PR #<backfill: task-37.2 PR number>；remote-embedding config env-bridge）：Go `RemoteProviderConfig` add-only `Model` 字段 + `setRemoteEnv` 跨进程 env-bridge（镜像既有 `setVectorEnv`，env-wins；**API key env-only，永不进 config**）。Rust 侧 0 toml dep（既有 env 读取路径不变）。0 migration / 0 默认行为变更（ADR-004/008）。
  - task-37.3（this PR）：closeout；`scripts/console_smoke.sh`（smoke v27，banner v26→v27，staging cf-v29-cfg）；`internal/cli/smoke_syntax_test.go`（`TestTask373` 无回退 [46/46]）；`docs/releases/v0.30.0-{evidence,artifacts}.md`；`README.md` v0.30 段；`RELEASE_NOTES.md` v0.30.0 段；`docs/decisions/adr-042-...md`（ratify Proposed→Accepted，per-D）+ ADR-027 add-only Phase 37 Amendment（标记 `[SPEC-DEFER:phase-future.embedding-provider-remote]` 已兑现：真实远程端点 live recall 已度量 + Go config env-bridge 接通，不溯改 D-body，ADR-014 D5）；`docs/specs/phases/phase-37-embedding-provider-remote-live.md`（§6 闭合）；`docs/s2v-adapter.md`；`test/features/phase-37-embedding-provider-remote-live.feature`。

## 3. 验证脚本

```bash
# 默认构建（0 新依赖 / 0 network；ureq optional / embedding-remote feature 不入默认构建）
cargo test -p contextforge-core --lib                     # lib all PASS
go test ./...                                             # all pass (incl TestTask373)
cargo clippy --workspace --all-targets -- -D warnings     # 0 warning
go vet ./...                                              # clean
bash -n scripts/console_smoke.sh                          # exit 0

# task-37.1 live-recall harness（env-gated；需真实远程端点凭据 + embedding-remote feature）
export CONTEXTFORGE_REMOTE_API_KEY=<your-remote-api-key>   # env-only，永不进 config
# 端点 / 模型经既有 env 桥提供（SiliconFlow https://api.siliconflow.cn/v1/embeddings + Qwen/Qwen3-Embedding-8B，dim=1024）
cargo test -p contextforge-core --features embedding-remote \
  --test remote_embedding_recall                          # recall@3 == 1.0000（15/15）/ recall@1 = 0.8667–0.9333（13–14/15，跨 run 波动）
# 未置 CONTEXTFORGE_REMOTE_API_KEY 时该 harness honest-defer skip（默认 cargo test 不触网）

# 真实召回由本机已认证 run 实测（remote 为付费外部 API，无免费 service container；
# CI 无密钥时 harness 干净 skip，不夸大为「CI 每次 run 守护」，ADR-013）

# 端到端 smoke v27（step 46 → [46/46]；既有 step 不退化）
go test ./internal/cli/ -run TestTask373

# D5 spec-drift lint
bash scripts/spec_drift_lint.sh --touched origin/master   # 0 unannotated hits
```

## 4. 镜像推送

- `release.yml`（`v*` tag push 触发）→ buildx → ghcr login → build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.30.0` + `:latest`（**linux/amd64**，net-zero 与 v0.29.0 一致）→ cosign keyless 签 + attest SPDX SBOM + provenance:mode=max（承 task-28.2 机制）。
- run ID + digest：`release.yml` run `<backfill: release.yml run id>`（**`success`** 待回填，build-push + cosign sign + attest SBOM + provenance:mode=max 步全绿）；`ghcr.io/tajiaoyezi/contextforge-daemon:v0.30.0` + `:latest` = `<backfill: ghcr image digest sha256:...>`；git tag SHA `<backfill: v0.30.0 tag SHA>`；cosign Rekor tlog idx `<backfill: tlog idx (sign)>`（sign）/ `<backfill: tlog idx (attest SBOM)>`（attest SBOM）。
- live-recall evidence artifact：本 release 的真实 live recall 证据为**主 agent 本机已认证 run**（SiliconFlow `https://api.siliconflow.cn/v1/embeddings` + `Qwen/Qwen3-Embedding-8B`，dim=1024，3 次 run）——remote recall@1 = 0.8667–0.9333（13–14/15，跨 run 波动）/ recall@3 = 1.0000（15/15，3 次全稳定）vs deterministic recall@1 = 0.0000 / recall@3 = 0.0667，delta@3 = +0.9333。**与 qdrant-recall 不同**：remote 为付费外部 API、无免费 service container，CI 无密钥时 harness honest-defer skip，**不声称 CI 每次 run 守护**（ADR-013）。

## 5. cosign verify（by-digest）

```bash
# 解析 tag → digest，按 digest（非 tag）验签（承 v0.21.0 follow-up 教训：cosign verify 务必按 digest）
DIGEST=$(cosign triangulate --type digest ghcr.io/tajiaoyezi/contextforge-daemon:v0.30.0)
cosign verify \
  --certificate-identity-regexp 'https://github.com/tajiaoyezi/contextforge/.github/workflows/release.yml@.*' \
  --certificate-oidc-issuer https://token.actions.githubusercontent.com \
  ghcr.io/tajiaoyezi/contextforge-daemon@${DIGEST}
# 期望 digest = <backfill: ghcr image digest sha256:...>
```

## 6. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-042 embedding-provider-remote-live | **Accepted**（per-D，D1 live-recall harness 🟢 + D2 Go config env-bridge 🟢 + D3 honest CI-defer caveat 🟢） | task-37.3 据 37.1/37.2 真实非合成验证 ratify：D1 Accepted（env-gated `remote_embedding_recall.rs` harness，手工标注集 15 case / 16 doc 含近义干扰，real remote 模型 vs deterministic 基线同一 `BruteForceVectorBackend` 精确余弦路径，remote recall@3 == 1.0000 / @1 = 0.8667–0.9333 vs det @1 = 0.0 / @3 = 0.0667，delta@3 = +0.9333，护栏 floor r3>=0.70 + remote@1>det@1 每次 run 过）/ D2 Accepted（Go `RemoteProviderConfig` add-only `Model` + `setRemoteEnv` 跨进程 env-bridge 镜像 `setVectorEnv`，env-wins，API key env-only 永不进 config，Rust 0 toml dep）/ D3 honest caveat（小型手工标注集，recall@3=1.0 为语义正确性证明非大基准断言，大语料续 `[SPEC-DEFER:phase-future.embedding-large-corpus-recall]`；remote 付费外部 API 无免费 service container → CI 无密钥 honest-defer skip，召回由本机已认证 run 实测、不夸大为 CI 每次 run 守护）/ D4 default 0-dep/0-network + 0 provider 改动（`ureq` optional / `embedding-remote` opt-in 不入默认构建，API key env-only 永不进 config，0 migration）/ D5 baseline 不变（cargo/go/clippy/vet 绿；harness env-gated 旁路、默认 skip）|
| ADR-027 embedding-provider-completion（remote honest-defer） | Accepted（+ Phase 37 add-only Amendment：remote-provider 已兑现） | task-37.3 add-only 标记 ADR-027 `[SPEC-DEFER:phase-future.embedding-provider-remote]` 已兑现（真实远程端点 live recall 已度量 + Go config env-bridge 接通），**不溯改 D-body**（ADR-014 D5）；`RemoteEmbeddingProvider` 契约自 Phase 22 task-22.3，本 phase 仅补真实联调 live recall + config 桥 |
| ADR-013 据实延后/校正 | Accepted（守线） | recall@3=1.0 = 小型手工标注集语义正确性证明非大基准质量断言，caveat 据实记录；大语料质量延后 `[SPEC-DEFER:phase-future.embedding-large-corpus-recall]`；**remote 无免费 service container → CI 无密钥 honest-defer skip，召回由本机已认证 run 实测、不夸大为 CI 每次 run 守护**（与 qdrant 关键差异）；tag SHA / run id / digest / tlog 未知前以 `<backfill>` 占位不伪造凭据 |
| ADR-014 add-only / cross-validation | Accepted（守线，第二十八次激活） | env-gated harness + Go config env-bridge add-only（0 provider 改动 / 不改控制流 / 0 migration）+ ADR-027 Amendment 不溯改正文（D5）|
| ADR-004 / ADR-008 / ADR-012 | Accepted（守线） | 默认 0 新依赖 / 0 network（`ureq` 自 task-22.3 起 optional / `embedding-remote` opt-in 不入默认构建，Rust core 默认 0-remote-dep）；harness env-gated（未置 `CONTEXTFORGE_REMOTE_API_KEY` 即 honest-defer skip 不触网）；API key env-only 永不进 config；tag-release 须授权（本轮经无人值守 /goal 授权 ADR-012）|

## 7. 版本声明（contract bump 评估）

- 本 release 新增 env-gated live-recall harness（`remote_embedding_recall.rs`，real remote 模型 vs deterministic 基线，remote recall@3 = 1.0000 / @1 = 0.8667–0.9333）+ Go `setRemoteEnv` config env-bridge（add-only `Model` 字段，env-wins，API key env-only）+ smoke v27——**无 proto 既有字段 / RPC / endpoint 删改、无默认运行时行为改动、0 provider 改动 / 0 migration**（harness env-gated 默认 skip / `ureq` optional + `embedding-remote` opt-in 不入默认构建）。
- 默认构建 0 新代码依赖 / 0 network（ADR-004 / ADR-008，Rust core 默认保持 0-remote-dep）；API key env-only 永不进 config。
- 故 **无 breaking contract bump**；既有 v0.6–v0.29 client + 既有索引/数据无需改动。**embedding-provider-remote 收口**（ADR-027 honest-defer 兑现）。

## 8. 后续模板（embedding-remote follow-on）

- 大语料真实远程语义召回质量基准（远超 15-case 手工标注集，多语种 / 多 provider 对比）`[SPEC-DEFER:phase-future.embedding-large-corpus-recall]`。
- 付费远程 API 在 CI 的低成本守护策略（密钥注入 / 配额 / 缓存 fixture vs live）`[SPEC-DEFER:phase-future.remote-embedding-ci-guard]`。
- 远程 embedding 多 provider 矩阵（OpenAI / Cohere / SiliconFlow 等端点契约 + dim 协商）`[SPEC-DEFER:phase-future.embedding-remote-provider-matrix]`。
- linux/arm64 原生 runner multi-arch `[SPEC-DEFER:phase-future.multi-arch-native-runner]`。
- （承前）HNSW 逼近 regime 压测 `[SPEC-DEFER:phase-future.vector-large-corpus-perf]`；vector-dim feature-enforce `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]`；structured metrics/counter facility `[SPEC-DEFER:phase-future.observability-metrics-facility]`。

## 9. 存档指引

- closeout PR 合入后，**无人值守授权下**主 agent push `v0.30.0` annotated tag 触发 `release.yml`；post-tag-push backfill PR 填实 tag SHA + run ID + 镜像 digest + cosign 凭据（承 v0.8–v0.29 pattern；§2/§4/§5 角括号 `<backfill: ...>` 标记由本 backfill PR 回填）。
- Dockerfile workspace member COPY 完整性预检（承 v0.11.0 PR #139 教训）：本 release **无新 migration、0 provider 改动**——新增 `core/tests/remote_embedding_recall.rs`（既有 `core/` member 的 tests/，env-gated 不入默认构建）+ Go `setRemoteEnv` env-bridge（add-only），无新依赖 / 无 proto 字段新增；镜像为默认构建（0 新依赖 / 0 network，linux/amd64）。
