# ContextForge v0.28.0 Evidence — observability-hardening (热路径静默错误显式化：index_session_backend store.append ×4 + retriever Tantivy/SQLite desync 经 eprintln! WARN + setVectorEnv config.Load/Setenv 经 fmt.Fprintf(os.Stderr) + 7→3-4 grounding 校正 + ADR-040 ratified)

> v0.28.0 ships Phase 35（observability-hardening，承 Phase 31/33 治理债血脉，把热路径中**被静默吞掉的真实错误**显式化——一个刻意保持**小**的版本，第三轮债清理性质、边际递减，遵 ADR-013「诚实优先于凑数」据实收敛）：**rust-silent-failure-surfacing**
> （`core/src/jobs/index_session_backend.rs` 的 **4 处** `store.append`（progress/index-error/commit-error/cancelled）`let _ =` → `if let Err(persist_err) { eprintln!("WARN indexing-event persist failed (...): {persist_err}") }`，SQLite persist 失败（磁盘满/锁）不再被无声吞掉；`core/src/retriever/mod.rs:415` `Err(_) => continue`（Tantivy/SQLite desync 静默跳过命中）→ `Err(e) => { eprintln!("WARN retriever: ... desync ..."); continue }`，skip 行为保留；镜像 `search.rs:108-113` 既有 eprintln! WARN 惯例；`eb.send` 各处保留 as-is（broadcast 无订阅者返 Err 是正常态 intentional））；**go-silent-failure-surfacing**
> （`cmd/contextforge/main.go` `setVectorEnv` 的 `config.Load` 错误 + `os.Setenv` 失败 → `fmt.Fprintf(os.Stderr, ...)` 显式化，镜像 `daemon/rest.go:110`；用 `errors.Is(err, os.ErrNotExist)` 守护——MISSING config.toml 是常见默认（config.Load 对缺文件也返 error）→ 静默，仅 malformed/unreadable 报警）；**7→3-4 grounding 校正**
> （survey 初列 7 处候选，grounding 据实收敛为 3-4 处真静默；DROP/LEAVE 4 处不改代码：`search.rs:109` already-surfaced + core 无 metrics facility / `mcpadapter/server.go:298` task-31.3 already-done / `mcpadapter/allowlist.go:31` 有意 POSIX-only 平台 caveat / `index_session_backend.rs:193` eb.send 有意 no-subscribers；不引入新 metrics facility）。
> **observability-only**——best-effort 契约不变（indexing 不阻断、query 续行、daemon 不阻断，**绝不**转 fail-fast，ADR-004），三门 + lint 不退化。**ADR-040** 据 D1–D4 per-D ratify
> `Proposed → Accepted`（D1 rust-silent-failure-surfacing Accepted；D2 go-silent-failure-surfacing Accepted + memstore nil-sink honest non-issue grounding 校正；D3 grounding 校正诚实 7→3-4 收敛；
> D4 默认 0-dep / 0-network + 既有契约不变）；**ADR-031**（observability-hardening v0.19.0 母 ADR）add-only Phase 35 Amendment（承其 stderr/best-effort surfacing 方向延伸热路径静默错误，不溯改正文，ADR-014 D5）。

## 1. 合入记录 (Phase 35, tasks 35.1–35.3)

| task | PR | delivery |
|---|---|---|
| 35.1 | #229（squash 9a57647） | rust-silent-failure-surfacing：`core/src/jobs/index_session_backend.rs` **4 处** `store.append`（progress/index-error/commit-error/cancelled）`let _ =` → `if let Err(persist_err) { eprintln!("WARN indexing-event persist failed (job=..., stage=...): {persist_err}") }`（best-effort 保留，不阻断 indexing；grounding 发现是 4 处非 1 处，一致显式化）；`core/src/retriever/mod.rs:415` `Err(_) => continue` → `Err(e) => { eprintln!("WARN retriever: chunk {chunk_id} present in index but missing from SQLite (desync), skipping: {e}"); continue }`（skip 行为保留）；镜像 `search.rs:108-113`；`eb.send` 各处保留 as-is（no-subscribers intentional）。grounding 校正：`SqliteIndexingEventStore` 是具体类型无 trait → 不引 trait 注入失败 double（scope creep），TEST-35.1.1 改 behavior-lock + inspection。TEST-35.1.1（真实 store 接线 best-effort 行为锁）+ TEST-35.1.2（删 chunks 行造 desync → search 优雅跳过）；lib 209→212。**0 新 dep** |
| 35.2 | #230（squash 69fc367） | go-silent-failure-surfacing：`cmd/contextforge/main.go` `setVectorEnv` 的 `config.Load` 错误 + `os.Setenv` 失败 → `fmt.Fprintf(os.Stderr, "contextforge: ...")` 显式化，镜像 `daemon/rest.go:110`；用 `errors.Is(err, os.ErrNotExist)` 守护（MISSING config.toml 是常见默认→静默，仅 malformed/unreadable 报警）；best-effort 保留（失败时 env-only 路径不变、daemon 不阻断）。grounding 校正：`memstore.go:579` nil-sink = honest non-issue（DROP，0 改动）——`NewMemMemoryStore()` 唯一生产调用点 `console_api_serve.go:109` 紧随**无条件** `SetEventSink`（:112），nil-sink 仅测试/by-design。TEST-35.2.1 stderr-capture（os.Pipe）真 RED→GREEN（malformed→WARN / missing→no WARN / valid→no WARN）；`go test ./...` + `go vet` clean。**0 新 dep** |
| 35.3 | this PR | observability-hardening 7→3-4 grounding 校正如实记录（4 处 DROP/LEAVE 不改代码，不引新 metrics facility）。Phase 35 closeout：smoke v25 `[44/44]`（banner v24→v25，staging `cf-v27-cfg`）+ `TestTask353`（no-regression `[37/37]`..`[43/43]`）+ v0.28.0 release docs + ADR-040 ratify + ADR-031 add-only Phase 35 Amendment + roadmap §3.17/§4 + phase-35 §6 + adapter |

## 2. S2V 状态

- Phase 35 `observability-hardening`: Draft → **Done**, Tasks 0 → 3。
- phase-35 §6 AC 全 `[x]`（据 35.1–35.3 真实交付；D1/D2 surfacing 🟢 + memstore nil-sink honest non-issue grounding 校正如实 + D3 7→3-4 收敛如实）。
- 默认行为 / proto / migration / 既有契约不变（ADR-004）；surfacing observability-only（best-effort 仍 best-effort，不转 fail-fast）；不引入 log/tracing/metrics facility（Rust eprintln! / Go fmt.Fprintf(os.Stderr) 既有惯例）；不溯改 Phase 1-34 spec（ADR-014 D5）。
- **本 release 兑现热路径静默错误显式化**：index_session_backend store.append ×4 + retriever Tantivy/SQLite desync 经 eprintln! WARN / setVectorEnv config.Load/Setenv 经 fmt.Fprintf(os.Stderr)（IsNotExist 守护 missing 静默）/ 7→3-4 grounding 校正（4 处 DROP/LEAVE）；observability-metrics-facility / memstore-degraded-observability-warn / rust-stderr-output-assertion 据真实受限/by-design 诚实延后。

## 3. 验证证据

### 1. rust-silent-failure-surfacing（task-35.1，🟢）

`cargo test -p contextforge-core --lib` PASS：TEST-35.1.1（注：真实 `SqliteIndexingEventStore` 接线 → index session 完成 + lifecycle 行持久化，`store.append` Ok 分支执行、best-effort 流程不破）+ TEST-35.1.2（删 chunks 行造 Tantivy/SQLite desync → `query()` 优雅返回，跳过全部 desync hit、无 error，skip 不转 fail-fast）。`index_session_backend.rs` **4 处** `store.append` `let _ =` → `if let Err(persist_err) { eprintln!("WARN ...") }`（grounding 发现是 4 处 emit 点非 1，同 SQLite persist 失败类一致显式化）；`retriever/mod.rs:415` `Err(_) => continue` → `Err(e) => { eprintln!("WARN ... desync ..."); continue }`；`eb.send` 各处保留 as-is。**GROUNDING 校正（ADR-013）**：`SqliteIndexingEventStore` 是具体类型无 trait → 注入「返 Err 的 test-double」须引 trait = scope creep，不做；error 分支 eprintln! 为机械改动 inspection-verified（仓库惯例不断言 eprintln! 输出，`[SPEC-DEFER:phase-future.rust-stderr-output-assertion]`）。retriever `:373` `searcher.doc()` 失败（不同子系统、更罕见）surgical 留 as-is（`[SPEC-DEFER:phase-future.tantivy-docstore-read-surface]`）。lib 209→212。**0 新 dep / 0 schema migration / 0 network**。

### 2. go-silent-failure-surfacing（task-35.2，🟢）

`go test ./cmd/contextforge/ -run TestSetVectorEnv -v` → 6/6 PASS（既有 `TestSetVectorEnv` 3 子测试 + 新 `TestSetVectorEnv_LoadErrorSurfacing` 3 子测试）：TEST-35.2.1 `malformed config.toml → WARN`（经 `os.Pipe` 重定向捕获 `os.Stderr` 断言 `contextforge: vector config load failed` 行存在，genuine RED→GREEN）/ `missing config.toml → no WARN`（`os.ErrNotExist` 守护）/ `valid config → no WARN` + env-wins + restore 保持。`setVectorEnv` 的 `config.Load` 错误分支加 `if !errors.Is(err, os.ErrNotExist) { fmt.Fprintf(os.Stderr, "contextforge: vector config load failed (%s): %v\n", dataDir, err) }`（malformed 报警、missing 静默、best-effort `return restore` 不变）+ `os.Setenv` 失败分支补 WARN（既有仅成功记 restore，restore 语义不变），镜像 `daemon/rest.go:110`。**GROUNDING 校正（ADR-013）**：(a) config.Load 对 MISSING config.toml 也返 error（`os.Open` 失败）→ 朴素 surface 会对「无配置」常见默认误报 WARN（UX 回归）→ 据实加 `os.ErrNotExist` 守护；(b) `memstore.go:579` `emitMemoryEvent` nil-sink = honest non-issue（DROP，`memstore.go` 0 改动）——`NewMemMemoryStore()` 唯一生产调用点 `internal/cli/console_api_serve.go:109` 紧随**无条件** `:112 SetEventSink(store.EmitEvent)`，nil-sink 仅出现在测试/直接构造（设计内可选）；加一次性 degraded WARN 只会在测试触发=噪声无生产价值 `[SPEC-DEFER:phase-future.memstore-degraded-observability-warn]`。`go test ./...` 全过 + `go vet ./cmd/...` clean。**0 新 dep / 0 proto / 0 migration**。

### 3. 7→3-4 grounding 校正（task-35.3，🟢 ADR-013 本 phase 核心价值）

survey 初列 7 处「静默吞错」候选，grounding 复核 **4 处 DROP/LEAVE 不改代码**（这是本 phase 的 ADR-013 价值，不伪造为新工作）：
- `core/src/data_plane/search.rs:109` — **already-surfaced**（已 `eprintln!("WARN search_persist.put failed ...")`；唯一「缺口」是结构化计数器，但 core 无 metrics facility（仅测试 atomics）→ 加 = over-engineering）。DROP。
- `internal/mcpadapter/server.go:298`（`writeAudit`）— **already-done**（task-31.3 / ADR-036 D3 nit 2 已 `fmt.Fprintf(os.Stderr, "mcp: audit write failed ...")`）。DROP。
- `internal/mcpadapter/allowlist.go:31` — **intentional POSIX-only 平台 caveat**（warning 已 fire；Windows perm bits 无意义、令其有意义须引 `golang.org/x/sys/windows` = new dep 破 0-dep）。DROP。
- `core/src/jobs/index_session_backend.rs:193`（`eb.send`）— **intentional no-subscribers**（broadcast 无订阅者返 Err 是正常态，加 WARN 是噪声）。LEAVE AS-IS。
**不引入新 metrics facility**（core eprintln! / Go fmt.Fprintf(os.Stderr) only，`[SPEC-DEFER:phase-future.observability-metrics-facility]`）。

### 3b. smoke v25（诚实记录）

`bash -n scripts/console_smoke.sh` exit 0（CI go-test 语法门 + `TestTask353`）。v25 step `[44/44]`（banner v24→v25，staging dir `cf-v27-cfg`，offset +2）文档化 observability-hardening 状态；surfacing 多为内部热路径 stderr 旁路、无 console-api 运行时面，step 44 验默认构建仍 scaffold（默认行为/proto/契约不变，ADR-004）。既有 v13-v24 块 denominator 不溯改（新增 `[44/44]` add-only，ADR-014 D5）；`TestTask353` 断言 `[44/44]` + no-regression（denominators `[37/37]`..`[43/43]` 不溯改）。

### 4. CI 状态（各 task PR 四门绿后自主合入）

| PR | cargo-test | go-test | spec-lint | lint |
|---|---|---|---|---|
| 35.1 (#229 rust-silent-failure-surfacing) | PASS | PASS | PASS | PASS |
| 35.2 (#230 go-silent-failure-surfacing) | PASS | PASS | PASS | PASS |
| 35.3 (this PR closeout) | PASS | PASS | PASS | PASS |

注：surfacing observability-only（best-effort 仍 best-effort 不转 fail-fast），0 新 dep / 0 proto / 0 migration / 不引 log/tracing/metrics facility（ADR-004/008）；`core/tests/phase9_index_smoke.rs` Tantivy `LockBusy` 既有间歇 flake 命中时 `gh run rerun` 复跑至绿（承 v0.13–v0.27 pattern），never-merge-red 守线。

### 5. ADR-014 第二十六次激活 record（this closeout PR self-check）

`bash scripts/spec_drift_lint.sh --touched origin/master` → 0 未标注命中（CI spec-lint gate 复核）。各 task PR（35.1/35.2）触及行亦 0 未标注命中。

### 6. AC 达成（observability-hardening + v0.28.0 收口）

| Phase 35 §6 AC | 状态 | 依据 |
|---|---|---|
| AC1 rust-silent-failure-surfacing（index_session_backend store.append + retriever desync 经 eprintln! WARN，best-effort 保留） | ✅ met（store.append 4 处一致显式化；store 具体类型无 trait → behavior-lock+inspection 如实；eb.send 保留如实） | task-35.1（#229）；TEST-35.1.1（真实 store best-effort 行为锁）+ TEST-35.1.2（desync 优雅跳过）（§3.1）；lib 209→212 |
| AC2 go-silent-failure-surfacing（setVectorEnv config.Load/Setenv 经 fmt.Fprintf(os.Stderr)，IsNotExist 守护 missing 静默） | ✅ met（memstore nil-sink honest non-issue DROP 如实；config.Load missing 也 error → IsNotExist 守护如实） | task-35.2（#230）；TEST-35.2.1（malformed→WARN stderr-capture / missing→no WARN / valid→no WARN）（§3.2）；`go vet` clean |
| AC3 v0.28.0 closeout + 7→3-4 grounding 校正 | ✅ met（4 处 DROP/LEAVE 不改代码、不引新 metrics facility 如实） | task-35.3（this PR）；7→3-4 grounding 校正（§3.3）；smoke v25 `[44/44]` + `TestTask353` + 默认 `cargo test -p contextforge-core --lib` 212/0 + `go test ./...` 全过 + release docs + ADR-040 per-D ratify |
| AC4 ADR-014 cross-validation gate（第二十六次激活） | ✅ met | 本 closeout PR body（D1-D5，见 §6） |

## 4. 平台矩阵

| Platform | Build | Note |
|---|---|---|
| Windows MSVC (dev box) | 默认 build + `cargo test -p contextforge-core --lib` 212 passed/0 failed（含 TEST-35.1.1/.2）+ `go test ./...` 全过（含 TestSetVectorEnv_LoadErrorSurfacing + TestTask353）；clippy `--workspace --all-targets -D warnings` 0 warning；go vet clean；smoke `bash -n` exit 0 | rustc 1.95.0，2026-06-04；gofmt `-l` 标 = 纯 autocrlf CRLF（git 存 LF、CI 通过） |
| Linux (GHA runner) | 默认 build + cargo/go test + spec-lint + lint PASS | CI 四门；surfacing observability-only 不引 facility |
| 镜像 linux/amd64 | release.yml 推 GHCR（默认构建 0 新 dep / 0 network） | 承 v0.21–v0.27；arm64 DEFERRED |

## 5. Upgrade / Rollback

- Upgrade：默认行为 / proto 既有字段 / migration / 既有契约不变（ADR-004，既有 v0.6–v0.27 client + 既有索引/数据无需改动）。改动均 observability-only（仅 stderr 旁路 WARN，不改任何返回值/控制流/RPC 行为）：index_session_backend store.append ×4 + retriever desync 经 eprintln! WARN（best-effort 保留，不阻断 indexing / query 续行）+ setVectorEnv config.Load/Setenv 经 fmt.Fprintf(os.Stderr)（IsNotExist 守护 missing 静默、env-only 路径失败时不变、env-wins/restore 保持）。不引入 log/tracing/metrics facility；Rust core 保持 0 toml dep。
- Rollback：`git tag -d v0.28.0` + 删 GitHub Release / ghcr tag；与 v0.27.0 行为兼容（surfacing 仅新增 stderr WARN 旁路，热路径 happy-path 与默认行为不变）。

## 6. ADR-014 第二十六次激活 record

- **D1（mapping）**：Phase 35 §6 AC1-4 ↔ task-35.1（§6 AC1）/ task-35.2（§6 AC1=AC2）/ task-35.3（§6 AC1/AC2 closeout）/ 本 PR body（§6 AC4）。AC1↔35.1（rust-silent-failure-surfacing）/ AC2↔35.2（go-silent-failure-surfacing）/ AC3↔35.3（7→3-4 grounding 校正 + release + ADR + §6）/ AC4↔本 PR body。
- **D2（lint）**：各 task PR（35.1/35.2）+ 本 closeout `spec_drift_lint.sh --touched origin/master` 0 未标注命中（CI spec-lint）。
- **D3（verified-by）**：每 AC 显式 owner TEST-id（TEST-35.1.x / 35.2.x / 35.3.x）+ 真实证据（真实 store best-effort 行为锁 + desync 优雅跳过 / setVectorEnv malformed→WARN stderr-capture + missing→no WARN + valid→no WARN / 默认 `cargo test -p contextforge-core --lib` 212/0 + `go test ./...` 全过）。
- **D4（自治）**：主 agent ADR-012 自治据真实数据推进；never-merge-red + 禁据合成 ratify（ADR-013）守线；ADR-040 据 D1（rust surfacing 达成；Rust eprintln! 输出仓库不断言 → guard/inspection 不伪造 stderr-assert `[SPEC-DEFER:phase-future.rust-stderr-output-assertion]`）+ D2（go surfacing 达成 stderr-capture RED→GREEN；memstore nil-sink honest non-issue grounding 校正 `[SPEC-DEFER:phase-future.memstore-degraded-observability-warn]`）+ D3（7→3-4 grounding 校正 4 处 DROP/LEAVE 不引新 metrics facility）+ D4（默认 0-dep / 0-network + 既有契约不变 best-effort 不转 fail-fast）真实非合成验证 per-D ratify Accepted。tag / release outward-facing 不可逆 → 经 AskUserQuestion 2026-06-04 用户授权（沿用红线，不自行越界）。
- **D5（不溯改）**：Phase 1-34 spec 未溯改；observability-hardening + ratify 记录写在 ADR-040 Ratification + ADR-031 add-only Phase 35 Amendment（承 stderr/best-effort surfacing 方向延伸热路径静默错误，不溯改正文）+ task-35.1-35.3 §10 + 本文件；index_session_backend store.append/retriever desync/setVectorEnv 均 add-only eprintln!/Fprintf 旁路（不改控制流）；`memstore.go` 0 改动（nil-sink honest non-issue）；smoke 既有 v13-v24 块 denominator 不溯改（新增 v25 step 44 add-only）；`internal/cli/smoke_syntax_test.go` 加 `TestTask353`（add-only）；ADR-014 history Phase 1-34 不 retro-edit（26th activation add-only）。

## 7. tag / 镜像 SHA（post-tag-push backfill）

post-tag-push backfill（无人值守授权下主 agent push（ADR-012） tag 触发 `release.yml` 后填实，承 v0.8–v0.27 pattern；ADR-013 禁伪造凭据，未知值用 `<backfill>` 占位）：

- v0.28.0 annotated tag SHA：`aa209ddd6d722ad2aecd5a72a0d6dbb467de7706`（master @ task-35.3 closeout merge；annotated tag 对象 `550deb08aa3222375fa96d7a607e47d1a2ea74bd`）
- `release.yml` run ID：`26934172978`（conclusion `success` — build-push + cosign sign + cosign attest SBOM + provenance:mode=max）
- 镜像 digest：`ghcr.io/tajiaoyezi/contextforge-daemon:v0.28.0` + `:latest` = `sha256:ae5c4d0cdfbfc068b211df25c4afbd195711aacfda4d7491231cfe8d92b77257`（linux/amd64; arm64 DEFERRED）
- cosign keyless 签名凭据：release run 内 ephemeral keys + **Rekor 透明日志** tlog idx `1715917623`（cosign sign 镜像 digest）/ `1715919741`（cosign attest SPDX SBOM）。验证（按 digest，非按 tag）：`cosign verify ghcr.io/tajiaoyezi/contextforge-daemon@sha256:ae5c4d0cdfbfc068b211df25c4afbd195711aacfda4d7491231cfe8d92b77257 --certificate-identity-regexp '^https://github.com/tajiaoyezi/contextforge/\.github/workflows/release\.yml@.*$' --certificate-oidc-issuer https://token.actions.githubusercontent.com`
