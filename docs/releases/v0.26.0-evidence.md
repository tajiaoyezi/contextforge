# ContextForge v0.26.0 Evidence — governance-debt-cleanup-2 (L2 SQLite embedding-cache row-count cap + rowid-FIFO eviction + console memstore access-order LRU + memory hard-delete no-dangling-ref invariant + indexing.* event persistence/replay mapper + TraceStore workspace isolation + export --timeout add-only + ADR-038 ratified)

> v0.26.0 ships Phase 33（governance-debt-cleanup-2，承 Phase 31 第一波治理债清理的第二波）：L2 SQLite embedding-cache
> 补 row-count cap + rowid-FIFO 淘汰（`sqlite_put` 写后 `DELETE ... WHERE provider=? AND dim=? AND rowid NOT IN (SELECT rowid ... ORDER BY rowid DESC LIMIT cap)`，
> `DEFAULT_L2_EMBEDDING_CACHE_CAP=50_000` + add-only `with_sqlite_capacity` ctor，隐式 rowid FIFO、0 schema migration）；console-api memstore
> chunk/trace 缓存 FIFO → access-order LRU（读命中 `GetSourceChunk`/`GetSearchTrace` + 同键覆盖均经 `moveToMRU` move-to-front）；memory
> hard-delete no-dangling-ref INVARIANT（schema 内省断言 `memory_id` 仅存于 `memory_items` + `get(id)=None`，cascade non-issue
> honest-defer `[SPEC-DEFER:phase-future.memory-harddelete-cascade]`）；`indexing.*` 事件持久化（add-only migration `0019_indexing_events`
> + `SqliteIndexingEventStore` append/list id ASC + `index_session_backend.rs` 4 emit point best-effort persist，**在既有 `eb.send` broadcast 之外**不动既有路径
> + `events::indexing_rows_to_pb_events` 纯映射重建 id ASC PbEvent）；TraceStore workspace 隔离（add-only `GetSearchTraceRequest.workspace_id=2`
> + `ListQueriesRequest.workspace_id=2`，empty workspace_id = aggregate-all 字节等价 ADR-004）；`export --timeout` add-only flag（default 60s
> 与既有硬编码 `context.WithTimeout(60s)` 字节等价，ADR-004）。
> **默认行为 / proto 既有字段 / 既有契约不变**（ADR-004），三门 + lint 不退化。**ADR-038** 据 D1–D5 per-D ratify
> `Proposed → Accepted`（D3 indexing-replay-e2e / tracestore-isolation-e2e CELL honest-defer
> `[SPEC-DEFER:phase-future.indexing-replay-e2e]` + `[SPEC-DEFER:phase-future.tracestore-multi-workspace-strict]`；D4 dropped-nits / datadir
> honest-defer `[SPEC-DEFER:phase-future.daemon-options-datadir]`）；**ADR-031** add-only Amendment（indexing.* persistence + replay mapper
> + drain-timeout verify-only）+ **ADR-027** add-only Amendment（L2 cache bound，不溯改 D body）。

## 1. 合入记录 (Phase 33, tasks 33.1–33.4)

| task | PR | delivery |
|---|---|---|
| 33.1 | #218（squash 3ce290a） | L2 SQLite embedding-cache row-count cap + rowid-FIFO 淘汰：`core/src/embedding/cache.rs` `sqlite_put` 写后 `DELETE ... WHERE provider=? AND dim=? AND rowid NOT IN (SELECT rowid ... ORDER BY rowid DESC LIMIT cap)`，`DEFAULT_L2_EMBEDDING_CACHE_CAP=50_000` + add-only `with_sqlite_capacity` ctor（隐式 rowid FIFO，**0 新 dep / 0 schema migration**）。HONEST CAVEAT：`with_sqlite` 当前无 production call site（test-only）→ opt-in-path defense-in-depth，**非已确认的 live leak**；true-LRU honest-defer `[SPEC-DEFER:phase-future.l2-cache-true-lru]`。TEST-33.1.1 `test_33_1_1_l2_cap_evicts_oldest_fifo` + TEST-33.1.2 `test_33_1_2_default_l2_cap_keeps_modest_workload`（cache 7/7） |
| 33.2 | #219（squash 7b92f22） | console-api memstore chunk/trace 缓存 FIFO → access-order LRU：读命中 `GetSourceChunk`/`GetSearchTrace` + 同键覆盖均经 `moveToMRU` helper move-to-front；`TestMemStore_CacheEviction_FIFO` 重写为 `_LRU` + 新 `_LRU_Trace`。memory hard-delete no-dangling-ref INVARIANT 测试（schema 内省断言 `memory_id` 仅存于 `memory_items` + `get(id)=None` after hard_delete）——cascade non-issue honest-defer `[SPEC-DEFER:phase-future.memory-harddelete-cascade]`。`handleMemoryPin` 按 ADR-022 D2 保持 lenient（honest non-change，无代码）。TEST-33.2.1/.2（Go LRU）+ TEST-33.2.3 `test_33_2_3_hard_delete_no_dangling_refs`（Rust）；0 新 dep，fallback-mode only |
| 33.3 | #220（squash 823beca） | (a) `indexing.*` 事件持久化——add-only migration `0019_indexing_events` + `SqliteIndexingEventStore`（append/list，id ASC）+ `index_session_backend.rs` 4 emit point（progress/error×2/cancelled）best-effort persist（**在既有 `eb.send` broadcast 之外**，既有路径不变）+ `events::indexing_rows_to_pb_events` 纯映射（重建 id ASC indexing.* PbEvent，real job_id/processed/total from rows，deterministic `evt-idx-{id}`）；`server.rs` production 接线 store。(b) TraceStore workspace 隔离——add-only `GetSearchTraceRequest.workspace_id=2` + `ListQueriesRequest.workspace_id=2`（buf generate；无关 proto churn + grpc.pb.go EOL churn 回退；Rust 经 `tonic include_proto!` compile-time）+ `search_persist` get/list/search_fts + in-mem TraceStore get/list + handlers WHERE workspace_id filter；empty workspace_id = aggregate-all 字节等价（ADR-004）。(c) drain-timeout VERIFY-ONLY（Phase 26 已交付 `drainTimeoutFromEnv`，0 代码改动，cite `TestDrainTimeoutFromEnv` 5 subcase）。TEST-33.3.1（mapper）+ TEST-33.3.2（store round-trip + fixture emit persist）+ TEST-33.3.3（persist ws filter）+ TEST-33.3.4（handler ws passthrough）；e2e honest-defer `[SPEC-DEFER:phase-future.indexing-replay-e2e]` + `[SPEC-DEFER:phase-future.tracestore-multi-workspace-strict]`；0 新 dep |
| 33.4 | this PR | `export --timeout` add-only flag（`internal/cli/export.go` `parseExportOpts` `fs.Duration("timeout", 60s)`，default 60s **字节等价**于旧硬编码 `context.WithTimeout(60s)`，ADR-004，覆盖两次 sequential daemon spawn）+ TEST `TestParseExportOpts_Timeout`。Phase 33 closeout：smoke v23 `[42/42]` + `TestTask334` + release docs + ADR-038 ratify + ADR-031/027 add-only Amendment + roadmap §3.15/§4 + phase-33 §6 + adapter |

## 2. S2V 状态

- Phase 33 `governance-debt-cleanup-2`: Draft → **Done**, Tasks 0 → 4。
- phase-33 §6 AC 全 `[x]`（据 33.1–33.4 真实交付；D3 indexing-replay-e2e / tracestore-isolation-e2e CELL honest-defer 如实，code-local 接线 + store round-trip + persist/handler ws filter 达成 → 部分 ratify；D4 dropped-nits / datadir honest-defer 重申）。
- 默认行为 / proto 既有字段 / 既有契约不变（ADR-004）；add-only L2 cap ctor + add-only proto workspace_id 字段 + add-only migration 0019 + add-only export flag + 既有 broadcast 路径不动，不破协议；不溯改 Phase 1-32 spec（ADR-014 D5）。
- **本 release 兑现第二波治理债清理**：L2 SQLite cache bound（cap + rowid-FIFO）/ console memstore access-order LRU / memory hard-delete invariant / indexing.* persistence + replay mapper / TraceStore workspace 隔离 / export --timeout 兑现；l2-cache-true-lru / memory-harddelete-cascade / indexing-replay-e2e / tracestore-multi-workspace-strict / daemon-options-datadir 据真实受限诚实重申延后。

## 3. 验证证据

### 1. L2 SQLite embedding-cache cap + rowid-FIFO（task-33.1，🟢）

`cargo test -p contextforge-core`（embedding cache）PASS：TEST-33.1.1 `test_33_1_1_l2_cap_evicts_oldest_fifo`——`sqlite_put` 写后 `DELETE ... WHERE provider=? AND dim=? AND rowid NOT IN (SELECT rowid ... ORDER BY rowid DESC LIMIT cap)`，超 cap 时最旧 rowid 行被淘汰；TEST-33.1.2 `test_33_1_2_default_l2_cap_keeps_modest_workload`——`DEFAULT_L2_EMBEDDING_CACHE_CAP=50_000` 下 modest workload 全保留（cache 7/7）。add-only `with_sqlite_capacity` ctor（隐式 rowid FIFO，**0 新 dep / 0 schema migration**）。**HONEST CAVEAT：`with_sqlite` 当前无 production call site（test-only）→ opt-in-path defense-in-depth，非已确认的 live leak**；true-LRU（access-order 而非 insert-order 淘汰）honest-defer `[SPEC-DEFER:phase-future.l2-cache-true-lru]`——隐式 rowid FIFO 是 insert-order，access-order true-LRU 须额外 last-access 列 + UPDATE 热路径，据真实 trade-off 不强做。

### 2. console memstore access-order LRU + memory hard-delete invariant（task-33.2，🟢）

`go test ./...`（memstore）PASS：TEST-33.2.1/.2 chunk/trace 缓存 FIFO → access-order LRU——读命中 `GetSourceChunk`/`GetSearchTrace` + 同键覆盖均经 `moveToMRU` helper move-to-front，`TestMemStore_CacheEviction_FIFO` 重写为 `_LRU`（chunk）+ 新 `_LRU_Trace`（trace），淘汰最久未访问而非最早插入。`cargo test -p contextforge-core` PASS：TEST-33.2.3 `test_33_2_3_hard_delete_no_dangling_refs`——schema 内省断言 `memory_id` 仅存于 `memory_items`（无外表悬挂引用）+ `get(id)=None` after hard_delete，cascade non-issue honest-defer `[SPEC-DEFER:phase-future.memory-harddelete-cascade]`（schema 内省证实无需 cascade，非伪造）。`handleMemoryPin` 按 ADR-022 D2 保持 lenient（honest non-change，无代码改动）。0 新 dep，fallback-mode only（不破 console-api 主路径）。

### 3. indexing.* event persistence/replay mapper + TraceStore workspace isolation + drain-timeout verify-only（task-33.3，🟢/🟡）

`cargo test -p contextforge-core` + `go test ./...` PASS：
- **(a) indexing.\* persistence/replay**：add-only migration `0019_indexing_events` + `SqliteIndexingEventStore`（append/list，id ASC）+ `index_session_backend.rs` 4 emit point（progress/error×2/cancelled）best-effort persist（**在既有 `eb.send` broadcast 之外**，既有 broadcast 路径不变）+ `events::indexing_rows_to_pb_events` 纯映射（重建 id ASC indexing.* PbEvent，real job_id/processed/total from rows，deterministic `evt-idx-{id}`）；`server.rs` production 接线 store。TEST-33.3.1 mapper round-trip（rows → id ASC PbEvent）+ TEST-33.3.2 store round-trip + fixture emit persist。
- **(b) TraceStore workspace 隔离**：add-only `GetSearchTraceRequest.workspace_id=2` + `ListQueriesRequest.workspace_id=2`（buf generate；无关 proto churn + grpc.pb.go EOL churn 回退保持 surgical；Rust 经 `tonic include_proto!` compile-time）+ `search_persist` get/list/search_fts + in-mem TraceStore get/list + handlers WHERE workspace_id filter。TEST-33.3.3 persist ws filter（WHERE workspace_id 隔离）+ TEST-33.3.4 handler ws passthrough；empty workspace_id = aggregate-all **字节等价**（ADR-004）。
- **(c) drain-timeout VERIFY-ONLY**：Phase 26 已交付 `drainTimeoutFromEnv`（0 代码改动），cite `TestDrainTimeoutFromEnv` 5 subcase。

e2e honest-defer `[SPEC-DEFER:phase-future.indexing-replay-e2e]`（真实索引 job 端到端持久化→replay 回放须运行时 daemon + 真实索引语料）+ `[SPEC-DEFER:phase-future.tracestore-multi-workspace-strict]`（多 workspace 严格隔离 e2e 须多租户运行时面）——据真实运行时受限不伪造（ADR-013）。0 新 dep。

### 3b. smoke v23（诚实记录）

`bash -n scripts/console_smoke.sh` exit 0（CI go-test 语法门 + `TestTask334`）。v23 step `[42/42]` 文档化第二波治理债清理状态；多为内部缓存/持久化/契约修复无 console-api 运行时面，step 42 验默认构建仍 scaffold（默认行为/proto/契约不变，ADR-004）。既有 v13-v22 块 denominator 不溯改（新增 `[42/42]` add-only，ADR-014 D5）。

### 4. CI 状态（各 task PR 四门绿后自主合入）

| PR | cargo-test | go-test | spec-lint | lint |
|---|---|---|---|---|
| 33.1 (#218 L2 SQLite cache cap + rowid-FIFO) | PASS | PASS | PASS | PASS |
| 33.2 (#219 memstore access-order LRU + memory hard-delete invariant) | PASS | PASS | PASS | PASS |
| 33.3 (#220 indexing.* persistence/replay + TraceStore ws 隔离 + drain-timeout verify-only) | PASS | PASS | PASS | PASS |
| 33.4 (this PR closeout + export --timeout) | PASS | PASS | PASS | PASS |

注：task-33.3 buf generate 的 Go binding 触无关 proto churn + grpc.pb.go EOL churn → 回退保持 surgical（add-only workspace_id=2 字段位移外不引入无关 diff）；`core/tests/phase9_index_smoke.rs` Tantivy `LockBusy` 既有间歇 flake 命中时 `gh run rerun` 复跑至绿（承 v0.13–v0.25 pattern），never-merge-red 守线。

### 5. ADR-014 第二十四次激活 record（this closeout PR self-check）

`bash scripts/spec_drift_lint.sh --touched origin/master` → 0 未标注命中（CI spec-lint gate 复核）。各 task PR（33.1/33.2/33.3）触及行亦 0 未标注命中。

### 6. AC 达成（第二波治理债清理 + v0.26.0 收口）

| Phase 33 §6 AC | 状态 | 依据 |
|---|---|---|
| AC1 L2 SQLite embedding-cache bound（cap + rowid-FIFO 淘汰） | ✅ met（true-LRU honest-defer 如实；with_sqlite test-only caveat 如实） | task-33.1（#218）；TEST-33.1.1/.2（§3.1）；cache 7/7 |
| AC2 console memstore access-order LRU + memory hard-delete no-dangling-ref invariant | ✅ met | task-33.2（#219）；TEST-33.2.1/.2（Go LRU）+ TEST-33.2.3（Rust invariant）（§3.2）；cascade non-issue honest-defer |
| AC3 indexing.* event persistence/replay mapper + TraceStore workspace 隔离 + drain-timeout verify-only | ✅ met（indexing-replay-e2e / tracestore-isolation-e2e CELL honest-defer 如实） | task-33.3（#220）；TEST-33.3.1-.4（§3.3）🟢；e2e CELL 🟡 延后 |
| AC4 默认行为不变 + export --timeout + v0.26.0 closeout | ✅ met | task-33.4（this PR）；`TestParseExportOpts_Timeout`（default 60s 字节等价）+ 默认 `cargo test --workspace` 207/0 + `go test ./...` 全过 + release docs + smoke v23 + ADR-038 per-D ratify |
| AC5 ADR-014 cross-validation gate（第二十四次激活） | ✅ met | 本 closeout PR body（D1-D5，见 §6） |

## 4. 平台矩阵

| Platform | Build | Note |
|---|---|---|
| Windows MSVC (dev box) | 默认 build + `cargo test --workspace` 207 passed/0 failed（lib 207 + all integration incl `search_persist_integration`）+ `go test ./...` 全过；clippy `--all-targets -D warnings` exit 0；gofmt clean；smoke `bash -n` exit 0 + `TestTask334` PASS + `TestParseExportOpts_Timeout` PASS | rustc 1.95.0，2026-06-03 |
| Linux (GHA runner) | 默认 build + cargo/go test + spec-lint + lint PASS | CI 四门；indexing-replay-e2e / tracestore-isolation-e2e CELL 须运行时 daemon + 多租户面（CI 默认不构建该 e2e）honest-defer |
| 镜像 linux/amd64 | release.yml 推 GHCR（默认构建 0 新 dep / 0 network） | 承 v0.21–v0.25；arm64 DEFERRED |

## 5. Upgrade / Rollback

- Upgrade：默认行为 / proto 既有字段 / 既有契约不变（ADR-004，既有 v0.6–v0.25 client + 既有索引/数据无需改动）。新增项均 add-only/默认字节等价：L2 cache cap（`with_sqlite_capacity` ctor add-only，`DEFAULT_L2_EMBEDDING_CACHE_CAP=50_000` 隐式 rowid FIFO，0 schema migration）+ memstore access-order LRU（fallback-mode only，淘汰策略内部变更不改对外行为）+ migration `0019_indexing_events`（add-only guarded migration，既有表不动）+ `GetSearchTraceRequest.workspace_id=2` / `ListQueriesRequest.workspace_id=2`（add-only proto 字段，empty=aggregate-all 字节等价，既有 client 忽略）+ `export --timeout`（default 60s 字节等价旧硬编码）+ indexing.* persist（**在既有 broadcast 之外**，既有路径不变）。
- Rollback：`git tag -d v0.26.0` + 删 GitHub Release / ghcr tag；与 v0.25.0 行为兼容（L2 cap 仅 opt-in ctor + 默认 cap 不影响 modest workload、memstore LRU 仅 fallback-mode 内部淘汰策略、migration 0019 add-only 表可保留、proto workspace_id add-only、export --timeout default 字节等价、indexing.* persist 在 broadcast 之外，默认路径不变）。

## 6. ADR-014 第二十四次激活 record

- **D1（mapping）**：Phase 33 §6 AC1-5 ↔ task-33.1（§6 AC1）/ task-33.2（§6 AC2）/ task-33.3（§6 AC3）/ task-33.4（§6 closeout）。AC1↔33.1（L2 SQLite cache cap + rowid-FIFO）/ AC2↔33.2（memstore access-order LRU + memory hard-delete invariant）/ AC3↔33.3（indexing.* persistence/replay + TraceStore ws 隔离 + drain-timeout verify-only）/ AC4↔33.4（export --timeout + release + ADR + §6）/ AC5↔本 PR body。
- **D2（lint）**：各 task PR（33.1/33.2/33.3）+ 本 closeout `spec_drift_lint.sh --touched origin/master` 0 未标注命中（CI spec-lint）。
- **D3（verified-by）**：每 AC 显式 owner TEST-id（TEST-33.1.x / 33.2.x / 33.3.x / 33.4.x）+ 真实证据（L2 cap rowid-FIFO 淘汰 + default cap modest workload 7/7 / Go memstore access-order LRU + Rust hard-delete no-dangling-ref schema 内省 / indexing.* mapper round-trip + store round-trip + persist/handler ws filter / `TestParseExportOpts_Timeout` default 60s 字节等价 + `cargo test --workspace` 207/0）。
- **D4（自治）**：主 agent ADR-012 自治据真实数据推进；never-merge-red + 禁据合成 ratify（ADR-013）守线；ADR-038 据 D1（L2 cache bound 达成）+ D2（memstore access-order LRU + memory hard-delete invariant 达成）+ D3（indexing.* persistence/replay + TraceStore ws 隔离 code-local 🟢 达成；**indexing-replay-e2e / tracestore-isolation-e2e CELL honest-defer 不强 ratify、不伪造**）+ D4（dropped-nits / datadir honest-defer 重申）+ D5（baseline 不变）真实非合成验证 per-D ratify Accepted；dropped-nits 如实记录（**非 implemented**）：`internal/cli/search.go:88` `%v→%w` 是 NON-BUG（terminal `Fprintf` 处 `%w` 为 vet-error，且 `err.Error()` 已携 grpc Status）/ tracestore-fts cross-version migration ALREADY-FIXED（`search_persist` open + `backfill_fts_if_empty`，TEST-26.1.4/.4b）/ datadir env→`daemon.Options.DataDir` REAL 但 🟡 `[SPEC-DEFER:phase-future.daemon-options-datadir]`；受阻维度（indexing-replay-e2e / tracestore-multi-workspace-strict / native arm64 runner）据真实受限**如实记录**（不伪造）。tag / release outward-facing 不可逆 → 须用户显式授权（沿用 v0.13-v0.25 红线，不自行 tag）。
- **D5（不溯改）**：Phase 1-32 spec 未溯改；治理债清理 + ratify 记录写在 ADR-038 Ratification + ADR-031 add-only Amendment（indexing.* persistence + replay mapper + drain-timeout verify-only）+ ADR-027 add-only Amendment（L2 cache bound）+ task-33.1-33.4 §10 + 本文件；L2 `with_sqlite_capacity` add-only ctor（既有 `with_sqlite` 不动）；memstore access-order LRU（fallback-mode 内部淘汰策略，对外行为不变）；migration `0019_indexing_events` add-only（既有表不动）；`GetSearchTraceRequest.workspace_id=2` / `ListQueriesRequest.workspace_id=2` add-only proto 字段（既有字段不动，empty=aggregate-all 字节等价）；`export --timeout` add-only flag（default 60s 字节等价）；indexing.* persist 在既有 `eb.send` broadcast 之外（既有 broadcast 路径不动）；smoke 既有 v13-v22 块 denominator 不溯改（新增 v23 step 42 add-only）；`internal/cli/smoke_syntax_test.go` 加 `TestTask334`（add-only）；ADR-014 history Phase 1-32 不 retro-edit（24th activation add-only）。

## 7. tag / 镜像 SHA（post-tag-push backfill）

post-tag-push backfill（无人值守授权下主 agent push（ADR-012） tag 触发 `release.yml` 后填实，承 v0.8–v0.25 pattern）：

- v0.26.0 annotated tag SHA：`<backfill>`（master @ task-33.4 closeout merge）
- `release.yml` run ID：`<backfill>`（conclusion **<backfill>** — build-push + cosign sign + cosign attest SBOM + provenance:mode=max 步全绿）
- 镜像 digest：`ghcr.io/tajiaoyezi/contextforge-daemon:v0.26.0` + `:latest` = `<backfill>`（linux/amd64; arm64 DEFERRED）
- cosign keyless 签名凭据：release run 内 ephemeral keys + **Rekor 透明日志** tlog idx `<backfill>`（cosign sign 镜像 digest）/ `<backfill>`（cosign attest SPDX SBOM）。验证：`cosign verify ghcr.io/tajiaoyezi/contextforge-daemon:v0.26.0 --certificate-identity-regexp '^https://github.com/tajiaoyezi/contextforge/\.github/workflows/release\.yml@.*$' --certificate-oidc-issuer https://token.actions.githubusercontent.com`
