# ContextForge v0.27.0 Artifacts — vector-config-completeness (vector-dim auto-negotiation + vector-backend config-file [vector] section + get_source_chunk workspace isolation verify-only + ADR-039 ratified)

> Artifact inventory for the v0.27.0 minor release (Phase 34). Mirrors the v0.26.0 artifacts structure.
> The default build is unchanged: 0-new-dependency, 0-network；默认行为 / proto 既有字段 / 既有契约不变。
> 完成 Phase 32 起的 vector-backend config story：vector-dim 自动协商（`select_vector_backend` 不再 `let _ = dim;` 静默丢弃 `CONTEXTFORGE_VECTOR_DIM`，改调 add-only 纯函数 `negotiate_vector_dim(requested, declared)` 复用既有 `VectorError::DimMismatch`；`VectorBackend` trait add-only default `expected_dim()->Option<usize>` 返回 None=dim-agnostic）+ Go config add-only `[vector]` 段（Backend/Dim）+ `setVectorEnv(dataDir)` helper 把 `[vector]` 桥到 `CONTEXTFORGE_VECTOR_BACKEND/_DIM`（ENV WINS，无 `[vector]` 段 → unset → BruteForce 字节等价）+ get_source_chunk workspace 隔离 VERIFY-ONLY（grounding-correction：task-12.2 已交付 `search.rs:421-423` 按 `req.workspace_id` scope candidates）。既有 22-endpoint + 5 Memory RPC + 运行时二进制默认行为不变。No breaking contract bump。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：`config.Config` 加 add-only `[vector]` 段（`Backend`/`Dim`，struct + encode/decode/assignVector，既有 `[collections]`/`[remote]`/`[embedding]` 段不受影响，legacy 零值兼容）；`cmd/contextforge/main.go` 加 `setVectorEnv(dataDir)` helper（best-effort `config.Load` + 把 `[vector]` 桥到 `CONTEXTFORGE_VECTOR_BACKEND` / `CONTEXTFORGE_VECTOR_DIM`），接线进 `doServe` + `doMCP`；**ENV WINS**（显式 env 覆盖 config）；无 `[vector]` 段 → 不导出 → unset → BruteForce **字节等价**（ADR-004）。运行时既有 22-endpoint + 5 Memory RPC 行为不变。
- `contextforge-core`（Rust 数据面 daemon）：`retriever/vector/factory.rs` `select_vector_backend` 不再 `let _ = dim;` 静默丢弃 `CONTEXTFORGE_VECTOR_DIM`——构造 backend 后调 add-only 纯函数 `negotiate_vector_dim(requested, declared)`（镜像 `embedding::factory::negotiate_dim），复用既有 `VectorError::DimMismatch`（`types.rs:83`）；`VectorBackend` trait 加 add-only default `expected_dim()->Option<usize>` 返回 None（dim-agnostic），`BruteForce` 保持 None。HONEST CAVEAT：default `BruteForce` 是 dim-agnostic → 默认构建接受任意 dim 且字节等价（ADR-004），真实强制仅在 dim-declaring feature backend 生效 `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]`。`get_source_chunk` workspace 隔离 VERIFY-ONLY（task-12.2 已交付 `search.rs:421-423` 按 `req.workspace_id` scope candidates，0 代码改动）。Rust core 保持 0 toml 依赖（`grep -c ^toml core/Cargo.toml = 0`）。默认构建 0 新依赖 / 0 network；`cargo test -p contextforge-core --lib` = lib 209 all PASS。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.27.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network）+ cosign keyless 签 + SBOM + provenance（承 v0.21.0–v0.26.0）。linux/arm64 仍 DEFERRED。
- Feature 构建：本 release 无新 feature flag；vector-dim negotiation 在默认构建路径（BruteForce dim-agnostic，字节等价），真实 dim enforce 仅对 dim-declaring feature backend 生效 `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]`。

## 2. 源码档

- master tag SHA：`a88e8f366e8c7789cee189c8b4315b2420dd7e60`（v0.27.0 annotated tag 对象 `118e89b15f85deb4dc259e335cba1d24a12c0c83`，无人值守授权下主 agent push（ADR-012））
- Phase 34 代码路径：
  - task-34.1（PR #224, squash fed7a90；vector-dim-auto-negotiation）：`core/src/retriever/vector/factory.rs`（`select_vector_backend` 不再 `let _ = dim;` 静默丢弃 `CONTEXTFORGE_VECTOR_DIM`，构造 backend 后调 add-only 纯函数 `negotiate_vector_dim(requested, declared)`，镜像 `embedding::factory::negotiate_dim`，复用既有 `VectorError::DimMismatch`（`types.rs:83`））+ `VectorBackend` trait add-only default `expected_dim()->Option<usize>` 返回 None（dim-agnostic），`BruteForce` 保持 None + TEST-34.1.1（`negotiate` 四路径）+ TEST-34.1.2（BruteForce any-dim 字节等价）；lib 207→209。HONEST CAVEAT：default BruteForce dim-agnostic → 默认构建接受任意 dim 且字节等价（ADR-004），真实强制仅在 dim-declaring feature backend `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]`。0 新依赖。
  - task-34.2（PR #225, squash a4ae446；vector-backend-config-file）：Rust core 无 toml 依赖（0-dep 规则），Go config.go 已解析 `[collections]`/`[remote]`/`[embedding]`，spawned core daemon 继承 Go env（`setDataDirEnv` / `CONTEXTFORGE_DATA_DIR`）；加 add-only `[vector]` 段（`Backend`/`Dim`）到 Go `config.Config`（struct + encode/decode/assignVector）+ `setVectorEnv(dataDir)` helper（`cmd/contextforge/main.go`，best-effort `config.Load` + 把 `[vector]` 桥到 `CONTEXTFORGE_VECTOR_BACKEND`/`_DIM`），接线进 `doServe` + `doMCP`。**ENV WINS**（显式 env 覆盖 config）；无 `[vector]` 段 → 不导出 → unset → BruteForce 字节等价（ADR-004）；Rust core 保持 0 toml 依赖（`grep -c ^toml core/Cargo.toml = 0`）。TEST-34.2.1（config `[vector]` round-trip + 既有段不受影响 + legacy 零值）+ TEST-34.2.2（`setVectorEnv` 导出 / env-wins / empty）；`go test ./...` + `go vet` clean。0 新依赖。
  - task-34.3（this PR）：`get_source_chunk` workspace 隔离 = VERIFY-ONLY guard（grounding-correction——task-12.2 已交付 `search.rs:421-423` 按 `req.workspace_id` scope candidates，survey 把它高估为 gap）；TEST-34.3.1 构建真实 2-state 索引并断言：`workspace_id` 设 → 仅该 workspace / cross-workspace → not_found / 空 → aggregate。`scripts/console_smoke.sh`（smoke v24 step 43 → [43/43]，banner v23→v24，staging cf-v26-cfg）；`internal/cli/smoke_syntax_test.go`（`TestTask343` 无回退 [37/37]..[42/42]）；`docs/releases/v0.27.0-{evidence,artifacts}.md`；`README.md` v0.27 段；`RELEASE_NOTES.md` v0.27.0 段；`docs/decisions/adr-039-...md`（ratify Proposed→Accepted，per-D）+ ADR-037 add-only Phase 34 Amendment；`docs/specs/phases/phase-34-vector-config-completeness.md`（§6 闭合）；`docs/s2v-adapter.md`；`test/features/phase-34-vector-config-completeness.feature`。

## 3. 验证脚本

```bash
# 默认构建（0 新依赖 / 0 network）
cargo test -p contextforge-core --lib                     # lib 209 all PASS
go test ./...                                             # all pass (incl TestTask343)
cargo clippy --workspace --all-targets -- -D warnings     # 0 warning
go vet ./...                                              # clean
bash -n scripts/console_smoke.sh                          # exit 0

# task-34.1 vector-dim auto-negotiation（negotiate 四路径 + BruteForce any-dim 字节等价）
cargo test -p contextforge-core test_34_1                 # TEST-34.1.1 / TEST-34.1.2

# task-34.2 vector-backend config-file（[vector] round-trip + setVectorEnv env-wins）
go test ./internal/config/ -run TestVectorConfig          # TEST-34.2.1
go test ./cmd/contextforge/ -run TestSetVectorEnv         # TEST-34.2.2

# task-34.3 get_source_chunk workspace isolation（VERIFY-ONLY，task-12.2 已交付）
cargo test -p contextforge-core test_34_3                 # TEST-34.3.1（2-state 索引 ws scope / cross-ws not_found / empty aggregate）

# 端到端 smoke v24（step 43 → [43/43]；既有 step 不退化）
go test ./internal/cli/ -run TestTask343

# D5 spec-drift lint
bash scripts/spec_drift_lint.sh --touched origin/master   # 0 unannotated hits
```

## 4. 镜像推送

- `release.yml`（`v*` tag push 触发）→ buildx → ghcr login → build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.27.0` + `:latest`（**linux/amd64**，net-zero 与 v0.26.0 一致）→ cosign keyless 签 + attest SPDX SBOM + provenance:mode=max（承 task-28.2 机制）。
- run ID + digest：`release.yml` run `26894328991`（**success**，build-push + cosign sign + attest SBOM + provenance:mode=max 步全绿）；`ghcr.io/tajiaoyezi/contextforge-daemon:v0.27.0` + `:latest` = `sha256:bf1119a37be3446cde891ab64598303f4e14a010da4e7dd55eb106b2e81020a2`；git tag SHA `a88e8f366e8c7789cee189c8b4315b2420dd7e60`；cosign Rekor tlog idx `1710499497`（sign）/ `1710500590`（attest SBOM）。

## 5. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-039 vector-config-completeness | **Accepted**（per-D，D1 PARTIAL：dim-negotiation 🟢 + feature-enforce 🟡 honest-defer；D3 grounding-correction verify-only） | task-34.3 据 34.1/34.2 真实非合成验证 ratify：D1 Accepted（dim-negotiation `negotiate_vector_dim` 四路径 TEST-34.1.1 + BruteForce any-dim 字节等价 TEST-34.1.2；feature-enforce 🟡 honest-defer `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]`）/ D2 Accepted（config-file `[vector]` 段 round-trip TEST-34.2.1 + `setVectorEnv` env-wins TEST-34.2.2，ENV WINS + 无段字节等价）/ D3 get_source_chunk isolation already-present verify-only grounding-correction（task-12.2 `search.rs:421-423`，TEST-34.3.1 2-state 索引断言 ws scope / cross-ws not_found / empty aggregate，0 代码改动）/ D4 default 0-dep/0-network + 既有契约不变（Rust core 0 toml 依赖 `grep -c ^toml core/Cargo.toml = 0`）/ D5 baseline 不变（cargo/go/clippy/vet 绿；add-only `expected_dim()` default + Go `[vector]` 段 + `setVectorEnv` helper）|
| ADR-037 vector-backend-config-plumbing-and-completeness | Accepted（+ Phase 34 add-only Amendment） | task-34.1/34.2 的 dim-negotiation + config-file 完成 Phase 32 的 env-plumbing；兑现其 `[SPEC-DEFER:phase-future.vector-backend-config-file]` Follow-up；不溯改 D1–D5 正文（ADR-014 D5） |
| ADR-034 vector-store-compose-trait | Accepted（守线） | `VectorBackend` trait 加 add-only default `expected_dim()->Option<usize>` 返回 None（dim-agnostic），BruteForce 保持 None；既有 compose trait 契约不变（add-only default method） |
| ADR-013 据实延后 | Accepted（守线） | vector-dim-feature-enforce 受阻如实不伪造（default BruteForce dim-agnostic，真实 enforce 须 dim-declaring feature backend）；tag SHA / run id / digest / tlog 未知前以 `<backfill>` 占位不伪造凭据 |
| ADR-014 add-only / 跨仓 signal | Accepted（守线） | `VectorBackend::expected_dim()` add-only default + Go `[vector]` 段 add-only（legacy 零值兼容）+ `setVectorEnv` helper add-only + Amendment 不溯改正文（D5） |
| ADR-004 / ADR-008 / ADR-012 | Accepted（守线） | 默认 0 新依赖 / 0 network（Rust core 0 toml）；default BruteForce dim-agnostic 字节等价 + 无 `[vector]` 段 → unset → BruteForce 字节等价；tag-release 须授权（本轮已授权） |

## 6. 版本声明（contract bump 评估）

- 本 release 新增 vector-dim 自动协商（`select_vector_backend` 调 add-only 纯函数 `negotiate_vector_dim`，复用既有 `VectorError::DimMismatch`，`VectorBackend` trait add-only default `expected_dim()` 返回 None=dim-agnostic）+ Go config add-only `[vector]` 段（Backend/Dim）+ `setVectorEnv` helper（ENV WINS，无段字节等价）+ get_source_chunk workspace 隔离 verify-only（task-12.2 已交付）+ smoke v24——**无 proto 既有字段 / RPC / endpoint 删改、无默认运行时行为改动**（default BruteForce dim-agnostic + 无 `[vector]` 段 → unset → BruteForce 字节等价）。
- 默认构建 0 新代码依赖 / 0 network（ADR-004 / ADR-008，Rust core 保持 0 toml 依赖）。
- 故 **无 breaking contract bump**；既有 v0.6–v0.26 client + 既有索引/数据无需改动。**vector-config-completeness 收口**。

## 7. 后续模板（vector-config follow-on）

- vector-dim feature-enforce（dim-declaring feature backend 真实强制 `expected_dim()`，default BruteForce 仍 dim-agnostic）`[SPEC-DEFER:phase-future.vector-dim-feature-enforce]`。
- （承前）sqlite-vec in-process recall/latency matrix `[SPEC-DEFER:phase-future.sqlite-vec-inprocess-matrix]`；chunk source_type filter `[SPEC-DEFER:phase-future.chunk-source-type-filter]`；chunk agent_scope filter `[SPEC-DEFER:phase-future.chunk-agent-scope-filter]`；qdrant live recall `[SPEC-DEFER:phase-future.qdrant-live-recall]`；lancedb build prereq CI `[SPEC-DEFER:phase-future.lancedb-build-prereq-ci]`；L2 embedding-cache true-LRU `[SPEC-DEFER:phase-future.l2-cache-true-lru]`；indexing.* replay e2e `[SPEC-DEFER:phase-future.indexing-replay-e2e]`；TraceStore 多 workspace 严格隔离 e2e `[SPEC-DEFER:phase-future.tracestore-multi-workspace-strict]`；memory hard-delete cascade `[SPEC-DEFER:phase-future.memory-harddelete-cascade]`；daemon datadir env→`daemon.Options.DataDir` 接线 `[SPEC-DEFER:phase-future.daemon-options-datadir]`；multi-arch 原生 arm64 runner `[SPEC-DEFER:phase-future.multi-arch-native-runner]`；GitHub 原生 attestation `[SPEC-DEFER:phase-future.github-native-attestation]`；compose 真实 TLS cert 自动签发 `[SPEC-DEFER:phase-future.compose-tls-auto-cert]`。

## 8. 存档指引

- closeout PR 合入后，**无人值守授权下**主 agent push `v0.27.0` annotated tag 触发 `release.yml`；post-tag-push backfill PR 填实 tag SHA + run ID + 镜像 digest + cosign 凭据（承 v0.8–v0.26 pattern；§7/§4 角括号 `<backfill>` 标记由本 backfill PR 回填）。
- Dockerfile workspace member COPY 完整性预检（承 v0.11.0 PR #139 教训）：本 release **无新 migration**——Rust `factory.rs` / `expected_dim()` 改动在既有 `core/` member，Go `[vector]` 段 + `setVectorEnv` helper 改动在既有 Go member，无新依赖 / 无 proto 字段新增；镜像为默认构建（0 新依赖 / 0 network，linux/amd64）。
