# ContextForge v0.27.0 Evidence — vector-config-completeness (vector-dim auto-negotiation seam + VectorBackend::expected_dim 默认 None dim-agnostic + Go [vector]→env 跨进程桥 env-wins + get_source_chunk workspace 隔离 verify-only grounding 校正 + ADR-039 ratified)

> v0.27.0 ships Phase 34（vector-config-completeness，承 Phase 32 起的 env-plumbing 对 vector-backend 配置故事的**收口补全**——Phase 31/33 两轮治理债清扫后绿色 backlog 已薄，遵 ADR-013「诚实优先于凑数」据实收敛为一个**刻意小**的版本）：**vector-dim 自动协商**
> （`core/src/retriever/vector/factory.rs` `select_vector_backend` 不再 `let _ = dim;` **静默丢弃** `CONTEXTFORGE_VECTOR_DIM`——构建 backend 后调新 `negotiate_vector_dim(requested, declared)` 纯函数协商 seam，镜像 `embedding::factory::negotiate_dim`，复用既存 `VectorError::DimMismatch`（`types.rs:83`）；`VectorBackend` trait 加 add-only **默认返回 None** 的 `expected_dim() -> Option<usize>`（dim-agnostic），`BruteForceVectorBackend` 沿用 None）；**vector-backend 配置文件**
> （Rust core 无 `toml` dep 故 0-dep 守线，Go `config.go` 已解析 `[collections]`/`[remote]`/`[embedding]` 段、且 spawn 的 core daemon 继承 Go 进程 env（`setDataDirEnv` / `CONTEXTFORGE_DATA_DIR`）——补 add-only `[vector]` 段（`Backend`/`Dim`）到 Go `config.Config`（struct + encode/decode/assignVector）+ `setVectorEnv(dataDir)` helper（`cmd/contextforge/main.go`）best-effort `config.Load` 把 `[vector]` 桥到 `CONTEXTFORGE_VECTOR_BACKEND`/`_DIM`，接到 `doServe` + `doMCP`；**ENV WINS**（显式 env 覆盖配置文件）；无 `[vector]` 段 → 不导出 → unset → BruteForce byte-equivalent（ADR-004）；Rust core 保持 0 toml dep）；**`get_source_chunk` workspace 隔离 = VERIFY-ONLY**
> （grounding 校正——自 task-12.2 已交付，`search.rs:421-423` 把候选集 scope 到 `req.workspace_id`，survey 把它夸大为 gap——交付 verify-only 守护测试记录既存隔离对称性、不写新代码）。
> **默认行为 / proto / migration / 既有契约不变**（ADR-004），三门 + lint 不退化。**ADR-039** 据 D1–D5 per-D ratify
> `Proposed → Accepted`（D1 dim-negotiation Accepted + feature-enforce 🟡 honest-defer `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]`；D2 config-file Accepted；D3 `get_source_chunk` 隔离 already-present verify-only grounding 校正；
> D4 默认 0-dep / 0-network + 既有契约不变）；**ADR-037**（vector-backend-config-plumbing-and-completeness）add-only Phase 34 Amendment（dim-negotiation + config-file
> 续接 Phase 32 env-plumbing、兑现其 `[SPEC-DEFER:phase-future.vector-backend-config-file]` Follow-up，不溯改正文，ADR-014 D5）。

## 1. 合入记录 (Phase 34, tasks 34.1–34.3)

| task | PR | delivery |
|---|---|---|
| 34.1 | #224（squash fed7a90） | vector-dim auto-negotiation：`core/src/retriever/vector/factory.rs` `select_vector_backend` 不再 `let _ = dim;`（**静默丢弃** `CONTEXTFORGE_VECTOR_DIM`）——构建 backend 后调新纯函数 `negotiate_vector_dim(requested, declared)`（镜像 `embedding::factory::negotiate_dim`：`requested==0` → Ok / `declared==None` → Ok / matching → Ok / 非零 `requested != Some(declared)` → `VectorError::DimMismatch{expected,got}`），复用既存 `VectorError::DimMismatch`（`types.rs:83`）。`VectorBackend` trait 加 add-only **默认 impl 返回 None** 的 `expected_dim(&self) -> Option<usize>`（dim-agnostic），`BruteForceVectorBackend` 沿用 None。HONEST CAVEAT：default BruteForce dim-agnostic → default build 协商接受任意 dim 且 byte-equivalent（ADR-004），真实 enforcement 仅对声明 dim 的 feature backend 生效 `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]`。TEST-34.1.1（negotiate 四路径）+ TEST-34.1.2（BruteForce any-dim byte-equiv）；lib 207→209。**0 新 dep** |
| 34.2 | #225（squash a4ae446） | vector-backend config-file：Rust core 无 `toml` dep（0-dep 守线），Go `config.go` 已解析 `[collections]`/`[remote]`/`[embedding]` 段、且 spawn 的 core daemon 继承 Go 进程 env（`setDataDirEnv` / `CONTEXTFORGE_DATA_DIR`）——补 add-only `[vector]` 段（`Backend`/`Dim`）到 Go `config.Config`（struct + encode/decode/assignVector）+ `setVectorEnv(dataDir)` helper（`cmd/contextforge/main.go`）best-effort `config.Load` 把 `[vector]` 桥到 `CONTEXTFORGE_VECTOR_BACKEND`/`CONTEXTFORGE_VECTOR_DIM`，接到 `doServe` + `doMCP`。**ENV WINS**（显式 env 覆盖配置文件）；无 `[vector]` 段 → 不导出 → unset → BruteForce byte-equivalent（ADR-004）；Rust core 保持 0 toml dep（`grep -c ^toml core/Cargo.toml = 0`）。TEST-34.2.1（config `[vector]` round-trip + 既有段不受影响 + legacy 零值）+ TEST-34.2.2（`setVectorEnv` 导出 / env-wins / empty）；`go test ./...` + `go vet` clean。**0 新 dep** |
| 34.3 | this PR | get_source_chunk workspace 隔离 = VERIFY-ONLY 守护（grounding 校正——自 task-12.2 已交付，`search.rs:421-423` 把候选集 scope 到 `req.workspace_id`，survey 把它夸大为 gap）。TEST-34.3.1 建真实 2-state 索引断言：workspace_id 设 → 仅该 workspace；跨 workspace → not_found；空 → aggregate。Phase 34 closeout：smoke v24 `[43/43]`（banner v23→v24，staging `cf-v26-cfg`）+ `TestTask343`（no-regression `[37/37]`..`[42/42]`）+ v0.27.0 release docs + ADR-039 ratify + ADR-037 add-only Phase 34 Amendment + roadmap §3.16/§4 + phase-34 §6 + adapter |

## 2. S2V 状态

- Phase 34 `vector-config-completeness`: Draft → **Done**, Tasks 0 → 3。
- phase-34 §6 AC 全 `[x]`（据 34.1–34.3 真实交付；D1 feature backend live enforcement 须 feature build → `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]` 如实，dim-negotiation seam + BruteForce dim-agnostic byte-equiv 达成 → 部分 ratify；D3 `get_source_chunk` 隔离 already-present verify-only grounding 校正如实）。
- 默认行为 / proto / migration / 既有契约不变（ADR-004）；add-only `VectorBackend::expected_dim` 默认 method + `negotiate_vector_dim` 纯函数 + Go add-only `[vector]` 段 + `setVectorEnv` 跨进程 env-bridge（env-wins）+ get_source_chunk 既有 scope 不动，不破协议；不溯改 Phase 1-33 spec（ADR-014 D5）。
- **本 release 兑现 vector-backend 配置故事收口补全**：vector-dim 自动协商（`CONTEXTFORGE_VECTOR_DIM` 不再静默丢弃）/ Go `[vector]`→env 跨进程桥（env-wins，no-section=byte-equiv，Rust 0 toml dep 保持）/ `get_source_chunk` workspace 隔离 verify-only grounding 校正兑现；vector-dim-feature-enforce / daemon-options-datadir / rust-side-toml-config-reader 据真实受限诚实重申延后。

## 3. 验证证据

### 1. vector-dim auto-negotiation（task-34.1，🟢）

`cargo test -p contextforge-core --lib` PASS：TEST-34.1.1 `negotiate_vector_dim` 纯函数四路径——`requested==0` → Ok / `declared==None` → Ok / matching → Ok / 非零 `requested != Some(declared)` → `VectorError::DimMismatch{expected,got}`（镜像 `embedding::factory::negotiate_dim`，复用既存 `types.rs:83` 的 variant，**0 新 type / 0 新 dep**）；TEST-34.1.2 BruteForce default 路径任意 dim 被接受 + byte-equivalent。`select_vector_backend` 构建 backend 后调 `negotiate_vector_dim(dim, backend.expected_dim())`，令 `CONTEXTFORGE_VECTOR_DIM` 不再被静默丢弃（替换原 `let _ = dim;`）；`VectorBackend` trait 加 add-only **默认 impl 返回 None** 的 `expected_dim(&self) -> Option<usize>`（dim-agnostic），`BruteForceVectorBackend` 沿用 None。**HONEST CAVEAT：default BruteForce `expected_dim()=None` → default build 协商对任意 dim 返回 Ok（无 enforcement、byte-equivalent 默认行为，ADR-004），非已确认的 live mismatch**；真实 enforcement 只对声明 dim 的 feature backend（qdrant/lancedb/sqlite-vec）生效、其 live exercise 须 feature build → honest-defer `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]`——据真实 build 受限不伪造（ADR-013）。lib 207→209。

### 2. vector-backend config-file（task-34.2，🟢）

`go test ./...` + `go vet` PASS：TEST-34.2.1 Go config `[vector]` 段 TOML round-trip——有/无段 + 既有 `[collections]`/`[remote]`/`[embedding]` 段不受影响 + legacy 零值兼容；TEST-34.2.2 `setVectorEnv`——段存在 → `CONTEXTFORGE_VECTOR_BACKEND`/`CONTEXTFORGE_VECTOR_DIM` 导出（`Dim>0` 才导出）/ env 已设 → 不覆盖（**env-wins**）/ 无段 → 不导出。add-only `[vector]` 段（`Backend string` toml `backend` tag / `Dim int` toml `dim` tag）镜像 `EmbeddingConfig`（struct + encode/decode/assignVector 三处对称，既有段不动）；`setVectorEnv(dataDir)` helper（`cmd/contextforge/main.go`）best-effort `config.Load` 把 `[vector]` 桥到 env，接到 `doServe` + `doMCP`，由 spawn 的 core daemon 经既有 `resolve_vector_backend` env 路径拾取（`daemon.go` `exec.Command` 继承父 env，已验证）。**ENV WINS**（显式 env 覆盖配置文件，后向兼容）；**无 `[vector]` 段 → 不导出 → unset → BruteForce byte-equivalent**（ADR-004 默认不变）。复用 `CONTEXTFORGE_DATA_DIR` 同形跨进程 env-bridge（**非** `daemon.Options.DataDir` field 重构——honest-defer `[SPEC-DEFER:phase-future.daemon-options-datadir]`）。**Rust core 保持 0 toml dep**（`grep -c ^toml core/Cargo.toml = 0`，ADR-008；Rust 端直读 honest-defer `[SPEC-DEFER:phase-future.rust-side-toml-config-reader]`）。0 新 dep。

### 3. get_source_chunk workspace 隔离 verify-only（task-34.3，🟢 grounding 校正）

`cargo test -p contextforge-core` PASS：TEST-34.3.1 建真实 2-state（两 workspace）索引并断言——workspace_id 设 → 仅该 workspace chunk（非空 `req.workspace_id` → `vec![req.workspace_id.clone()]`，仅该 workspace）；跨 workspace chunk_id → `not_found`；空 → aggregate（`workspace_store.list()` aggregate-all 探测）。**GROUNDING 校正（ADR-013，本 phase 核心价值）：`core/src/data_plane/search.rs:421-423` 自 task-12.2（ADR-017 D1 Wave 2）已把候选集 scope 到 `req.workspace_id`**——survey 把它夸大为 gap，实为已存在隔离对称性。本 phase 交付 verify-only 守护测试记录既存行为、**不写任何新代码**（CLAUDE.md 简单优先；ADR-013 不为已实现的对称性重写实现），在 ADR-039 D3 记 grounding 校正。0 新 dep。

### 3b. smoke v24（诚实记录）

`bash -n scripts/console_smoke.sh` exit 0（CI go-test 语法门 + `TestTask343`）。v24 step `[43/43]`（banner v23→v24，staging dir `cf-v26-cfg`，offset +2）文档化 vector-backend 配置故事收口补全状态；多为内部协商/配置桥/隔离 verify 无 console-api 运行时面，step 43 验默认构建仍 scaffold（默认行为/proto/契约不变，ADR-004；feature backend live enforcement 须 feature build 如实标注据实延后）。既有 v13-v23 块 denominator 不溯改（新增 `[43/43]` add-only，ADR-014 D5）；`TestTask343` 断言 `[43/43]` + no-regression（denominators `[37/37]`..`[42/42]` 不溯改）。

### 4. CI 状态（各 task PR 四门绿后自主合入）

| PR | cargo-test | go-test | spec-lint | lint |
|---|---|---|---|---|
| 34.1 (#224 vector-dim auto-negotiation) | PASS | PASS | PASS | PASS |
| 34.2 (#225 vector-backend config-file) | PASS | PASS | PASS | PASS |
| 34.3 (this PR closeout + get_source_chunk 隔离 verify-only) | PASS | PASS | PASS | PASS |

注：Rust core 保持 0 toml dep（`grep -c ^toml core/Cargo.toml = 0`），配置读取留 Go 端既有手写 TOML codec + 跨进程 `CONTEXTFORGE_DATA_DIR` 同形 env-bridge，不破 ADR-008 0-dep；`core/tests/phase9_index_smoke.rs` Tantivy `LockBusy` 既有间歇 flake 命中时 `gh run rerun` 复跑至绿（承 v0.13–v0.26 pattern），never-merge-red 守线。

### 5. ADR-014 第二十五次激活 record（this closeout PR self-check）

`bash scripts/spec_drift_lint.sh --touched origin/master` → 0 未标注命中（CI spec-lint gate 复核）。各 task PR（34.1/34.2）触及行亦 0 未标注命中。

### 6. AC 达成（vector-backend 配置故事收口补全 + v0.27.0 收口）

| Phase 34 §6 AC | 状态 | 依据 |
|---|---|---|
| AC1 vector-dim auto-negotiation（`negotiate_vector_dim` 纯函数 + `VectorBackend::expected_dim` 默认 None；`CONTEXTFORGE_VECTOR_DIM` 不再静默丢弃） | ✅ met（default BruteForce dim-agnostic byte-equiv 如实；feature-enforce 须 feature build honest-defer 如实） | task-34.1（#224）；TEST-34.1.1（negotiate 四路径）+ TEST-34.1.2（BruteForce any-dim byte-equiv）（§3.1）；lib 207→209 |
| AC2 vector-backend config-file（Go `[vector]` 段 + `setVectorEnv` 跨进程 env-bridge，env-wins，no-section=byte-equiv，Rust 0 toml dep 保持） | ✅ met（`daemon.Options.DataDir` field 化 honest-defer / Rust 端 toml 直读 honest-defer 如实） | task-34.2（#225）；TEST-34.2.1（`[vector]` round-trip + 既有段不受影响 + legacy 零值）+ TEST-34.2.2（`setVectorEnv` 导出 / env-wins / empty）（§3.2）；`go vet` clean |
| AC3 get_source_chunk 隔离 verify-only + v0.27.0 closeout | ✅ met（self-task-12.2 already-present grounding 校正如实，verify-only 不写新代码） | task-34.3（this PR）；TEST-34.3.1（workspace_id 设=仅该 workspace / 跨 workspace=not_found / 空=aggregate）（§3.3）；smoke v24 `[43/43]` + `TestTask343` + 默认 `cargo test -p contextforge-core --lib` 209/0 + `go test ./...` 全过 + release docs + ADR-039 per-D ratify |
| AC4 ADR-014 cross-validation gate（第二十五次激活） | ✅ met | 本 closeout PR body（D1-D5，见 §6） |

## 4. 平台矩阵

| Platform | Build | Note |
|---|---|---|
| Windows MSVC (dev box) | 默认 build + `cargo test -p contextforge-core --lib` 209 passed/0 failed（含 TEST-34.1.1/.2 + TEST-34.3.1）+ `go test ./...` 全过（含 TEST-34.2.1/.2 + TestTask343）；clippy `--workspace --all-targets -D warnings` 0 warning；go vet clean；smoke `bash -n` exit 0 | rustc 1.95.0，2026-06-03 |
| Linux (GHA runner) | 默认 build + cargo/go test + spec-lint + lint PASS | CI 四门；feature backend live dim enforcement 须 feature build（CI 默认不构建该 feature）honest-defer `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]` |
| 镜像 linux/amd64 | release.yml 推 GHCR（默认构建 0 新 dep / 0 network） | 承 v0.21–v0.26；arm64 DEFERRED |

## 5. Upgrade / Rollback

- Upgrade：默认行为 / proto 既有字段 / migration / 既有契约不变（ADR-004，既有 v0.6–v0.26 client + 既有索引/数据无需改动）。新增项均 add-only/默认字节等价：`VectorBackend::expected_dim` 默认 impl 返回 None（dim-agnostic，既有 backend 源码兼容、BruteForce 接受任意 dim、byte-equivalent）+ `negotiate_vector_dim` 纯函数协商 seam（default BruteForce `expected_dim()=None` → 任意 dim Ok）+ Go add-only `[vector]` 段（`Backend`/`Dim` toml tag，既有 `[collections]`/`[remote]`/`[embedding]` 段不动）+ `setVectorEnv` 跨进程 env-bridge（**env-wins**：显式 env 覆盖配置文件；无 `[vector]` 段 → 不导出 → unset → BruteForce byte-equivalent）+ `get_source_chunk` 既有 scope 不动（verify-only 守护测试）。Rust core 保持 0 toml dep（ADR-008）。
- Rollback：`git tag -d v0.27.0` + 删 GitHub Release / ghcr tag；与 v0.26.0 行为兼容（vector-dim 协商 default BruteForce dim-agnostic no-op、Go `[vector]`→env 仅 opt-in 段 + env-wins 不影响既有 env、get_source_chunk 既有 scope 不变、Rust 0 toml dep，默认路径不变）。

## 6. ADR-014 第二十五次激活 record

- **D1（mapping）**：Phase 34 §6 AC1-4 ↔ task-34.1（§6 AC1）/ task-34.2（§6 AC2）/ task-34.3（§6 AC3 closeout）/ 本 PR body（§6 AC4）。AC1↔34.1（vector-dim auto-negotiation：`negotiate_vector_dim` + `VectorBackend::expected_dim` 默认 None）/ AC2↔34.2（vector-backend config-file：Go `[vector]` 段 + `setVectorEnv` env-bridge）/ AC3↔34.3（get_source_chunk 隔离 verify-only + release + ADR + §6）/ AC4↔本 PR body。
- **D2（lint）**：各 task PR（34.1/34.2）+ 本 closeout `spec_drift_lint.sh --touched origin/master` 0 未标注命中（CI spec-lint）。
- **D3（verified-by）**：每 AC 显式 owner TEST-id（TEST-34.1.x / 34.2.x / 34.3.x）+ 真实证据（`negotiate_vector_dim` 四路径 + BruteForce any-dim byte-equiv / Go `[vector]` round-trip + `setVectorEnv` env-wins / get_source_chunk workspace_id 设=仅该 workspace + 跨 workspace=not_found + 空=aggregate / 默认 `cargo test -p contextforge-core --lib` 209/0 + `go test ./...` 全过）。
- **D4（自治）**：主 agent ADR-012 自治据真实数据推进；never-merge-red + 禁据合成 ratify（ADR-013）守线；ADR-039 据 D1（dim-negotiation seam + `expected_dim` 默认 None + BruteForce any-dim byte-equiv 达成；**feature backend live enforcement 须 feature build → `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]` honest-defer 不强 ratify、不伪造**）+ D2（Go `[vector]`→env config-file + env-wins + no-section byte-equiv + Rust 0 toml dep 达成）+ D3（`get_source_chunk` 隔离自 task-12.2 已存在 → verify-only grounding 校正，不重实现）+ D4（默认 0-dep / 0-network + 既有契约不变）真实非合成验证 per-D ratify Accepted；受阻 / 无驱动维度据真实受限**如实记录**（feature backend live dim enforcement / `daemon.Options.DataDir` field 化 / Rust 端 toml 直读 → honest-defer，不伪造）。tag / release outward-facing 不可逆 → 须用户显式授权（沿用 v0.13-v0.26 红线，不自行 tag）。
- **D5（不溯改）**：Phase 1-33 spec 未溯改；vector-backend 配置收口 + ratify 记录写在 ADR-039 Ratification + ADR-037 add-only Phase 34 Amendment（dim-negotiation + config-file 续接 Phase 32 env-plumbing、兑现其 `[SPEC-DEFER:phase-future.vector-backend-config-file]` Follow-up，不溯改正文）+ task-34.1-34.3 §10 + 本文件；`VectorBackend::expected_dim` add-only 默认 method（既有 `VectorStore` blanket impl 不动）；`negotiate_vector_dim` add-only 纯函数（`select_vector_backend` 仅替换 `let _ = dim`）；Go add-only `[vector]` 段（既有段 encode/decode 不动）；`setVectorEnv` add-only 跨进程 env-bridge（env-wins，无段 byte-equivalent）；`get_source_chunk` 既有 scope 不动（verify-only 守护测试）；Rust core 保持 0 toml dep；smoke 既有 v13-v23 块 denominator 不溯改（新增 v24 step 43 add-only）；`internal/cli/smoke_syntax_test.go` 加 `TestTask343`（add-only）；ADR-014 history Phase 1-33 不 retro-edit（25th activation add-only）。

## 7. tag / 镜像 SHA（post-tag-push backfill）

post-tag-push backfill（无人值守授权下主 agent push（ADR-012） tag 触发 `release.yml` 后填实，承 v0.8–v0.26 pattern；ADR-013 禁伪造凭据，未知值用 `<backfill>` 占位）：

- v0.27.0 annotated tag SHA：`a88e8f366e8c7789cee189c8b4315b2420dd7e60`（master @ task-34.3 closeout merge #226；annotated tag 对象 `118e89b15f85deb4dc259e335cba1d24a12c0c83`）
- `release.yml` run ID：`26894328991`（conclusion **success** — build-push + cosign sign + cosign attest SBOM + provenance:mode=max 步全绿）
- 镜像 digest：`ghcr.io/tajiaoyezi/contextforge-daemon:v0.27.0` + `:latest` = `sha256:bf1119a37be3446cde891ab64598303f4e14a010da4e7dd55eb106b2e81020a2`（linux/amd64; arm64 DEFERRED）
- cosign keyless 签名凭据：release run 内 ephemeral keys + **Rekor 透明日志** tlog idx `1710499497`（cosign sign 镜像 digest）/ `1710500590`（cosign attest SPDX SBOM）。验证（按 digest，非按 tag）：`cosign verify ghcr.io/tajiaoyezi/contextforge-daemon@sha256:bf1119a37be3446cde891ab64598303f4e14a010da4e7dd55eb106b2e81020a2 --certificate-identity-regexp '^https://github.com/tajiaoyezi/contextforge/\.github/workflows/release\.yml@.*$' --certificate-oidc-issuer https://token.actions.githubusercontent.com`
