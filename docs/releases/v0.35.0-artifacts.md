# ContextForge v0.35.0 Artifacts — chunk-source-type-filter (chunk 检索 `source_type` 过滤落地：`classify_source_type` 由 file_path 派生 + 真实 post-filter + console-api `?source_type=` forward + ADR-047 ratified)

> Artifact inventory for the v0.35.0 minor release (Phase 42). Mirrors the v0.34.0 artifacts structure.
> 默认构建 **0 新依赖**（`classify_source_type` 纯 std）/ **0 network** / **0 schema migration**（chunks/files/provenance §5.3 FROZEN，source_type 由 file_path 确定性派生不存储）/ **空 filter byte-equiv**（source_type 为空 → 不过滤）。
> 兑现 ADR-037 task-32.3 `[SPEC-DEFER:phase-future.chunk-source-type-filter]`——把 Phase 32 据实记的 chunk source_type documented no-op 落地为真实过滤。**诚实校正（ADR-013）**：`chunk-source-type-filter` 与 `chunk-agent-scope-filter` 经 grounding **不对称**——source_type 可由 file_path 确定性派生真实落地；`agent_scope` 是 memory 层概念（`memory_items` 0013 / `ListMemory` scope）、chunks 无 agent 维度 → 续 documented no-op + `[SPEC-DEFER:phase-future.chunk-agent-scope-filter]`（不伪造）。**派生 + 过滤 + console forward 无外部依赖**（Rust 纯 std 派生 + 本地检索 post-filter + proto add-only + Go query/body wire，0 网络），故 TEST-42.1.* / 42.2.* 由**每次 CI run 全程守护**（无 honest-defer skip）。No breaking contract bump（v1 proto / REST 零 delta；console_data_plane proto add-only 字段号冻结；source_type value 由空串变真实派生值填补 v0.1 schema gap，空 filter 过滤 byte-equiv）。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：`internal/contractv1/contractv1.go` `SearchRequest` add-only `SourceType []string`（json `source_type`，镜像 `Semantic`/`Hybrid`）；`internal/consoleapi/handlers.go` `handleSearch` 解析 `?source_type=`（repeated query param）+ body `source_type` 并集合并（镜像 `?semantic`/`?hybrid` OR-merge）；`internal/consoleapi/grpcclient/grpcclient.go` `SourceType: req.SourceType` 映射到 `pb.SearchRequest.source_type`。运行时既有 22-endpoint + REST 契约不变；空 `source_type` → 不过滤（backward-compat，既有 client 不传 → 行为不变）。
- `contextforge-core`（Rust 数据面 daemon）：`core/src/retriever/mod.rs` add `pub(crate) fn classify_source_type(file_path) -> &'static str`（扩展名确定性桶 code/doc/config/other，纯 std `Path::extension`，镜像 `indexer::lang_hint_from_path`）+ 三构造点（`search()` BM25 / `get_chunk` / `assemble_vector_result`）`source_type` 改 `classify_source_type(&file_path).to_string()` + `search()` BM25 source_type post-filter（SQLite JOIN 后，镜像 language post-filter，空 → 不过滤 byte-equiv）+ 窄化 no-op 块仅 agent_scope + 删除孤儿 `DEFAULT_SOURCE_TYPE` 常量（grounding 校正）。`core/src/data_plane/search.rs` `query()` 在 hits 装配后按 `req.source_type` post-filter（覆盖 BM25/semantic/hybrid 一致，利用 populate 的 `h.source_type`）。chunks/files/provenance SQL_SCHEMA（§5.3 FROZEN）+ Tantivy schema + v1 `server.rs:440-453` filter mapping backend 逻辑 0 改动。`cargo test -p contextforge-core --lib` = **225** PASS（222 baseline + net 3）。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.35.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network）+ cosign keyless 签 + SBOM + provenance（承 v0.21.0–v0.34.0）。linux/arm64 仍 DEFERRED。
- Feature 构建：本 release **0 新 feature flag**；feature-build 矩阵 10/10 不退化。

## 2. 源码档

- master tag SHA：`<backfill: v0.35.0 commit SHA>`（v0.35.0 annotated tag 对象 `<backfill: v0.35.0 tag object SHA>`，用户**另行授权**下主 agent push（ADR-012））
- Phase 42 代码路径：
  - task-42.1（PR #267，MERGED；chunk-source-type-derivation-and-filter）：`core/src/retriever/mod.rs`（add `classify_source_type` + 三构造点真实派生 + `search()` BM25 source_type post-filter + 窄化 no-op 块仅 agent_scope + 删除孤儿 `DEFAULT_SOURCE_TYPE` + TEST-42.1.1/.2）+ `core/src/server.rs` / `core/tests/phase4_smoke.rs` / `core/tests/phase6_smoke.rs`（既有 source_type=="" schema-gap 断言改「有效桶 ∈ {code,doc,config,other}」，populate 契约演进连带更新）。**0 新依赖（纯 std）/ 0 schema migration（§5.3 FROZEN）/ 空 filter byte-equiv**。
  - task-42.2（PR #268，MERGED；console-api-source-type-forward）：`proto/contextforge/console_data_plane/v1/console_data_plane.proto`（`SearchRequest` add-only `repeated string source_type = 9`）+ `console_data_plane.pb.go`（buf generate proto，field+getter+rawDesc 字节重排）+ `core/src/data_plane/search.rs`（post-filter + TEST-42.2.1/.2）+ `core/src/data_plane/mod.rs` / `core/tests/search_real_retriever.rs` / `core/tests/data_plane_integration.rs`（既有 `PbSearchRequest` 显式字面量补 `source_type: Vec::new()`）+ `internal/contractv1/contractv1.go`（add-only `SourceType []string`）+ `internal/consoleapi/handlers.go`（`?source_type=` 并集）+ `internal/consoleapi/grpcclient/grpcclient.go`（映射 pb）+ Go tests。**0 新 dep + proto add-only（字段号冻结）+ 空 filter byte-equiv**。
  - task-42.3（this PR；closeout）：`scripts/console_smoke.sh`（smoke v32，step `[51/51]` REAL `?source_type=code` 保留 JobRunner code hit / `?source_type=doc` 过滤掉它 distinguishing）；`internal/cli/smoke_syntax_test.go`（`TestTask423` no-regression [37/37]..[50/50]）；`docs/releases/v0.35.0-{evidence,artifacts}.md`；`README.md` v0.35 段；`RELEASE_NOTES.md` v0.35.0 段；`docs/decisions/adr-047-...md`（ratify Proposed→Accepted，per-D）+ ADR-037 add-only Phase-42 Amendment（source_type no-op supersede / agent_scope no-op 保持，**不溯改 D-body**，ADR-014 D5）；`docs/specs/phases/phase-42-...md`（§6 闭合）；`docs/s2v-adapter.md`；`test/features/phase-42-...feature`；`docs/roadmap.md`（§3.24 推进记录 + §4 add-only）。

## 3. 验证脚本

```bash
# 默认构建（0 新依赖 / 0 network / 0 schema migration / classify_source_type 纯 std）
cargo test --workspace                                     # all PASS (core lib 225 = 222 baseline + net 3)
go test ./...                                              # all pass (incl TestTask423 + TestTask422* + grpcclient forward)
cargo clippy --workspace --all-targets -- -D warnings     # 0 warning
go vet ./...                                               # clean
bash -n scripts/console_smoke.sh                          # exit 0

# task-42.1 — source_type 派生 + 真实过滤 + populate（无外部依赖，每次默认 test 守护）
cargo test -p contextforge-core --lib retriever::test_42_1  # TEST-42.1.1 (classify matrix) + TEST-42.1.2 (real filter + populate + agent_scope no-op)

# task-42.2 — console proto add-only + data_plane post-filter + Go forward（无外部依赖，每次默认 test 守护）
cargo test -p contextforge-core --lib data_plane::search::test_42_2  # TEST-42.2.1 (wire-tag field 9) + TEST-42.2.2 (data_plane post-filter)
go test ./internal/consoleapi/... -run TestTask422                   # handleSearch union + grpcclient forward

# 端到端 smoke v32（step 51 → [51/51]；REAL source_type=code 保留 / =doc 过滤；既有 step 不退化）
go test ./internal/cli/ -run TestTask423

# D5 spec-drift lint
bash scripts/spec_drift_lint.sh --touched origin/master   # 0 unannotated hits
```

## 4. 镜像推送

- `release.yml`（`v*` tag push 触发）→ buildx → ghcr login → build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.35.0` + `:latest`（**linux/amd64**，net-zero 与 v0.34.0 一致）→ cosign keyless 签 + attest SPDX SBOM + provenance:mode=max（承 task-28.2 机制）。
- run ID + digest：`release.yml` run `<backfill: release.yml run id>`（conclusion success，build-push + cosign sign + attest SBOM + provenance:mode=max 步全绿）；`ghcr.io/tajiaoyezi/contextforge-daemon:v0.35.0` + `:latest` = `<backfill: image digest sha256>`；git tag SHA `<backfill: v0.35.0 commit SHA>`；cosign Rekor tlog idx `<backfill: cosign sign tlog index>`（sign）/ `<backfill: cosign attest SBOM tlog index>`（attest SBOM）。
- chunk-source-type-filter evidence artifact：本 release 的 source_type 派生 + 过滤 + console forward 证据为**本机 + CI 真实测试**（TEST-42.1.1 classify 矩阵 / TEST-42.1.2 真实过滤 + populate + agent_scope no-op / TEST-42.2.1 prost wire-tag field 9 / TEST-42.2.2 data_plane post-filter + handleSearch 并集 + grpcclient 转发 / smoke v32[51/51] REAL source_type=code 保留 vs =doc 过滤 distinguishing）。**无外部付费 API 依赖**（Rust 纯 std 派生 + 本地检索 + proto add-only + Go wire，0 网络），故由**每次 CI run 全程守护**，无 honest-defer skip。

## 5. cosign verify（by-digest）

```bash
# 解析 tag → digest，按 digest（非 tag）验签（承 v0.21.0 follow-up 教训：cosign verify 务必按 digest）
DIGEST=$(cosign triangulate --type digest ghcr.io/tajiaoyezi/contextforge-daemon:v0.35.0)
cosign verify \
  --certificate-identity-regexp 'https://github.com/tajiaoyezi/contextforge/.github/workflows/release.yml@.*' \
  --certificate-oidc-issuer https://token.actions.githubusercontent.com \
  ghcr.io/tajiaoyezi/contextforge-daemon@${DIGEST}
# 期望 digest = <backfill: image digest sha256>
```

## 6. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-047 chunk-source-type-filter | **Accepted**（per-D，D1 source_type 由 file_path 派生 0 migration 🟢 + D2 v1 retriever 真实过滤 + 三路径 populate 空 filter byte-equiv 🟢 + D3 console proto add-only source_type=9 + data_plane post-filter + Go forward 🟢 + D4 agent_scope honest-defer 🟢） | task-42.3 据 42.1/42.2 真实非合成验证 ratify：D1 Accepted（`classify_source_type` 扩展名确定性桶 + 三构造点 populate + §5.3 FROZEN，TEST-42.1.1/.2）/ D2 Accepted（`search()` BM25 source_type post-filter 镜像 language + v1 path 立即生效，TEST-42.1.2）/ D3 Accepted（console_data_plane source_type=9 + data_plane post-filter + handleSearch 并集 + grpcclient 转发，TEST-42.2.1/.2）/ D4 Accepted（agent_scope memory 层概念续 documented no-op，不伪造 ADR-013）|
| ADR-037 vector-backend-config-plumbing-and-completeness（chunk filter no-op） | Accepted（+ Phase 42 add-only Amendment：source_type no-op superseded / agent_scope no-op reaffirmed） | task-42.3 add-only 标记 ADR-037 task-32.3 的 source_type documented no-op 被本 phase 真实过滤 supersede（`[SPEC-DEFER:phase-future.chunk-source-type-filter]` fulfilled）；agent_scope no-op 据实保持（`[SPEC-DEFER:phase-future.chunk-agent-scope-filter]` continue），**不溯改 D-body / Ratification (v0.25.0)**（ADR-014 D5）|
| ADR-015 console-contract-v1-compatibility（proto add-only） | Accepted（守线引用） | console_data_plane `SearchRequest` add-only `source_type=9`（既有字段 1-8 号 + 类型不变，空 → 不过滤 backward-compat）；v1 search.proto `SearchFilters.source_type=1` 既有不动；不溯改 ADR-015 正文 |
| ADR-024 / ADR-044（console 请求侧 forward 范式） | Accepted（守线引用） | console `?source_type=` 请求侧 forward 镜像 `?semantic`（ADR-024）/ `?hybrid`（ADR-044）的 query param + body 合并 → grpcclient 透传范式；不溯改正文 |
| ADR-004 local-first-privacy-baseline | Accepted（守线引用） | 空 source_type filter → 过滤行为 byte-equiv；source_type value 由空串变真实派生值填补 task-4.2 §2A v0.1 schema gap（契约本意）由 ADR-047 D1 据实记（可观测字段变化、非默认行为破坏）；不溯改 ADR-004 正文 |
| ADR-013 据实延后/校正 | Accepted（守线） | `chunk-source-type-filter` 与 `chunk-agent-scope-filter` 经 grounding 不对称据实——source_type 可派生真实落地、agent_scope memory 层概念续 honest-defer 不伪造；source_type 由 file_path 确定性派生非合成；真实过滤行为实测非预填；DEFAULT_SOURCE_TYPE 孤儿据实删除 + buf 不相关 pb.go 据实还原；tag SHA / run id / digest / tlog 未知前以 `<backfill>` 标记不伪造凭据 |
| ADR-008 / ADR-012 / ADR-014 | Accepted（守线，ADR-014 第三十三次激活） | 0 新依赖（`classify_source_type` 纯 std，ADR-008）；实现经用户授权、tag-release 须另行授权（ADR-012）；`classify_source_type` 新增 + console proto add-only + ADR-037 Amendment 不溯改正文（ADR-014 D5）|

## 7. 版本声明（contract bump 评估）

- 本 release 新增 `core/src/retriever/mod.rs` `classify_source_type()` + 三构造点真实 source_type + `search()` BM25 source_type post-filter + `data_plane/search.rs` post-filter + console_data_plane `SearchRequest` add-only `source_type=9` + Go `?source_type=` forward + smoke v32——**v1 proto / REST / RPC / endpoint 零 delta**（v1 `SearchFilters.source_type=1` 既有；console_data_plane proto add-only 字段号冻结）。**source_type value 由空串变真实派生值**：填补 task-4.2 §2A v0.1 schema gap（契约本意是真实 source_type，非永久空），由 ADR-047 据实记（可观测响应字段变化，空 source_type filter 下过滤行为 byte-equiv）；**0 schema migration**（chunks/files/provenance §5.3 FROZEN，source_type 由 file_path 派生不存储）。
- 默认构建 0 新代码依赖 / 0 network（`classify_source_type` 纯 std；ADR-004/008）。
- 故 **无 breaking contract bump**（v1 proto/REST 零 delta、console_data_plane proto add-only 字段号冻结向后兼容、空 source_type filter byte-equiv、0 schema migration）；既有 v0.6–v0.34 client 无需改动。**chunk-source-type-filter 收口**（ADR-037 source_type no-op 兑现为真实过滤）；用户可经 `?source_type=` / `{"filters":{"source_type":[...]}}` 按来源类型筛选。

## 8. 后续模板（chunk-source-type-filter follow-on）

- chunk-level `agent_scope` 真实过滤（memory 层概念、chunks 无 agent 维度，须 ingest-path schema 工程）`[SPEC-DEFER:phase-future.chunk-agent-scope-filter]`。
- importer 显式 source_type 打标（支持用户自定义 source_type，超 file_path 派生粗粒度桶，须 §5.3 解冻加 chunks 列）`[SPEC-DEFER:phase-future.chunk-importer-source-type-tagging]`。
- v1 semantic 路径 retriever-内 source_type 过滤（本 phase v1 `search()` BM25 内过滤镜像 language scope，console 经 data_plane post-filter 覆盖 semantic/hybrid）`[SPEC-DEFER:phase-future.semantic-path-source-type-filter]`。
- 仍真延后不强行扩面：vector backend dim 强校验 `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]`（须 feature build）/ tracestore 多 workspace 严格隔离余下读路径 `[SPEC-DEFER:phase-future.tracestore-multi-workspace-strict]`；linux/arm64 原生 runner multi-arch `[SPEC-DEFER:phase-future.multi-arch-native-runner]`。

## 9. 存档指引

- closeout PR 合入后，**用户另行授权下**主 agent push `v0.35.0` annotated tag 触发 `release.yml`；post-tag-push backfill PR 填实 tag SHA + run ID + 镜像 digest + cosign 凭据（承 v0.8–v0.34 pattern；§2/§4/§5/§6 角括号 `<backfill: ...>` 标记由本 backfill PR 回填）。
- Dockerfile workspace member COPY 完整性预检（承 v0.11.0 PR #139 教训）：本 release **无新 migration、v1 proto / REST 零 delta**——改动为 `core/src/retriever/mod.rs` + `core/src/data_plane/search.rs`（既有 `core/` member）+ console_data_plane proto add-only + Go config / handlers / grpcclient（既有 member）add-only + smoke/test/docs，无新依赖 / 无新 crate member；镜像为默认构建（0 新依赖 / 0 network，linux/amd64）。
