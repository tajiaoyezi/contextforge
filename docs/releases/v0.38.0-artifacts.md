# ContextForge v0.38.0 Artifacts — v1.0-api-cli-freeze (v1.0 收口第一步：ADR-050 锚点 + D2 API/CLI 冻结；daemon REST 移除 501 [BREAKING] + CLI --version/--help + example.toml)

> Artifact inventory for the v0.38.0 minor release (Phase 45). **0 新依赖** / **0 network** / **0 schema migration**。
> daemon REST 移除 2 个 501 未实装端点是 **v1.0 前 BREAKING change**（major 边界，release notes 显式记）。

## 1. 二进制档
- `contextforge`（Go 控制面 CLI）：`internal/cli/cli.go` add `Version` 变量 + subcommands 加 `version` + ExecuteWithIO 入口 `-h`/`--help`/`help` 处理（打印子命令清单 + 用法，不 exit 2）+ switch `case "version":`；`cmd/contextforge/main.go`（版本注入点）；`contextforge.example.toml` 补全 `[embedding]`/`[vector]`/`[reranker]`/`[retrieval]` 4 section + 头部 v0.1→v0.38.0；`internal/cli/cli_test.go` TEST-45.3.1/.2/.3 + TestTask14_AC4 want 更新（version，9→10）。
- `contextforge-core`（Rust 数据面）：本 release **0 Rust 代码改动**（D2 API/CLI 冻结是 Go 侧）。
- daemon REST（`internal/daemon/rest.go`）：移除 `POST /v1/import`（handleImport）+ `POST /v1/eval/run`（handleEval）501 未实装端点 + 路由注册；`handleCollections` chunk_count honest-defer（v1.0 known limitation，指向 console-api `/v1/stats/chunks`）；`rest_test.go` 移除 ImportStub501/EvalStub501 + 加 Task452_RemovedEndpointsAre404。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.38.0` + `:latest`（linux/amd64）+ cosign keyless 签 + SBOM + provenance（承 v0.21–v0.37）。
- Feature 构建：本 release **0 新 feature flag**；feature-build 矩阵 10/10 不退化。

## 2. 源码档
- master tag SHA：`<backfill-after-tag-push>`（用户全权授权 ADR-012）
- Phase 45 代码路径：task-45.1 ADR-050 + roadmap；task-45.2 rest.go + rest_test.go；task-45.3 cli.go + cli_test.go + main.go + example.toml；task-45.4 smoke + release docs + ADR ratify + Amendment + roadmap/adapter。

## 3. 测试档
- `go test ./...`：全过（含 TestTask452/453/454 + TestTask14_AC4 更新）。
- `cargo test --workspace`：全绿（0 Rust 改动）。
- `cargo clippy --workspace --all-targets -- -D warnings`：0 warning。
- CI spec-lint：全绿。

## 4. 发布档
- GitHub Release：v0.38.0（tag push 后 release.yml 自动建 GHCR 镜像；GitHub Release tarball 流程 Phase 46 加）。
- run id / ghcr digest / cosign tlog：`<backfill-after-tag-push>`（post-tag-push 回填，ADR-013 不预填）。
