# ContextForge v0.37.0 Evidence — memory-unpin-actor-propagation (闭环 pin/unpin actor 透传不对称 + audit/event source 归因；pin 顺带闭环；默认 byte-equiv + ADR-049 ratified)

> v0.37.0 ships Phase 44（memory-unpin-actor-propagation）——闭环 Phase 40 task-40.1 留下的 pin/unpin actor 透传不对称（pin 透传、unpin 硬编码 "console-api"）。**grounding 发现真实价值在 audit/event**（ADR-013）：store `set_pinned_with_actor(pinned=false)` 丢弃 actor → 单纯透传到 store 是"空透传"；真实落点是 `emit_audit_and_event`（不携 actor + source 硬编码）。task-44.1 闭环：unpin 透传 + `emit_audit_and_event` 加 actor 参数（audit/event source 归因真实调用方）+ pin 顺带闭环 + Go 透传链 + proto add-only actor=2。**0 新 dep** / **0 network** / **0 schema migration** / **proto add-only**（UnpinMemoryRequest actor=2，ADR-015）/ **默认 byte-equiv**（空 actor → audit "console-api" / event "contextforge-core" 各自回落）——遵 ADR-013：
> - **memory-unpin-actor-propagation**（兑现 roadmap 行 556 `memory-actor-all-rpc` 的 Unpin 子项 + `emit_audit_and_event` actor 参数共用基础）：task-44.1（PR #280）`UnpinMemoryRequest` add-only `actor=2` + Rust unpin handler 透传 actor（镜像 pin :231-235）+ `emit_audit_and_event` 加 `actor: &str` 参数（audit source :59 / `build_memory_event` event source 用 actor，空各自回落原值 byte-equiv）+ pin handler 顺带传 `&req.actor`（消除 pin audit/event 不归因残余不对称）+ deprecate/softdelete/harddelete 传 `""` byte-equiv + Go 4 处透传（types/grpcclient/handlers X-Actor/memstore）+ degraded fallback + gofmt 对齐。
> - **ADR-049-ratified**：ADR-049 据 D1-D4 per-D ratify `Proposed → Accepted`（D1-D3 unit 🟢 / D4 默认 byte-equiv 🟢 + 认证身份/其余 3 RPC 🔴 honest-defer）；ADR-032 + ADR-045 add-only Phase-44 Amendment，**不**溯改 D-body（ADR-014 D5）。
> **诚实校正（ADR-013）**：本轮交付**调用方透传**（audit/event source 归因）；认证身份（X-Actor → 已认证 auth subject）须 console-api 鉴权层 → 🔴 `[SPEC-DEFER:phase-future.memory-actor-authenticated-identity]`；deprecate/softdelete/harddelete actor 透传须 7 层+新 migration / audit 重设计 → 🔴 `[SPEC-DEFER:phase-future.memory-actor-all-rpc]`（本 phase 仅做 emit_audit_and_event actor 参数共用基础，这 3 RPC 未来顺带受益）。

## 1. 合入记录 (Phase 44, task 44.1)

| task | PR | delivery |
|---|---|---|
| 44.1 | #280 | unpin-actor-propagation：proto `UnpinMemoryRequest` add-only `actor=2`（既有 memory_id=1 冻结）+ buf generate + Rust unpin handler 透传 actor（镜像 pin）+ `emit_audit_and_event` 加 `actor: &str` 参数（audit source / `build_memory_event` event source 用 actor，空各自回落 byte-equiv）+ pin handler 顺带传 `&req.actor`（消除残余不对称）+ deprecate/softdelete/harddelete 传 `""` byte-equiv + Go 4 处透传（types/grpcclient/handlers X-Actor/memstore）+ degraded fallback + gofmt 对齐 + memstore_test。TEST-44.1.1（unpin actor 进 event source "bob"）+ TEST-44.1.2（pin 顺带闭环 "alice"）+ TEST-44.1.3（空 actor byte-equiv "contextforge-core"）+ TEST-44.1.4（Go 透传链源码 grep）。**0 新 dep / 0 migration / proto add-only / 默认 byte-equiv** |
| 44.x | this | memory-unpin-actor-propagation closeout：smoke v33→v34 `[53/53]`（unpin X-Actor 端到端 / 不可达诚实归因 unit）+ `TestTask443`（no-regression [37/37]..[52/52]）+ v0.37.0 release docs + ADR-049 per-D ratify + ADR-032/045 add-only Phase-44 Amendment + roadmap §3.26/§4 + adapter |

## 2. S2V 状态

- Phase 44 `memory-unpin-actor-propagation`: Draft → **Done**, Tasks 0 → 2。
- phase-44 §6 AC 全 `[x]`（据 44.1 真实交付；AC1 unpin actor 透传 + audit/event source 归因 🟢 + AC2 v0.37.0 closeout + 默认零依赖守线 🟢 + AC3 ADR-014 第三十五次激活）。
- 默认 `cargo test --workspace` 全过（core lib **232** = 229 baseline + 3：memory TEST-44.1.1/.2/.3）；`go test ./...` 全过（含 TestTask441 + TestTask443）；**0 新 dep / 0 network / 0 schema migration / proto add-only**；默认 byte-equiv（空 actor 各自回落）；不溯改 Phase 1-43 spec / ADR-032/045 D-body（ADR-014 D5）。

## 3. 验证证据

### 1. unpin-actor-propagation（task-44.1，🟢）

`cargo test -p contextforge-core --lib test_44_1` PASS（3/3）+ `go test ./internal/consoleapi/ -run TestTask441` PASS：
- TEST-44.1.1（`test_44_1_1_unpin_actor_in_event_source`）——unpin(actor="bob") → event source "bob"（audit/event source 归因真实调用方，非 store pinned_by——unpin 清 pin 快照）。
- TEST-44.1.2（`test_44_1_2_pin_actor_in_event_source`）——pin(actor="alice") → event source "alice"（pin 顺带闭环，消除 pin audit/event 不归因残余不对称）。
- TEST-44.1.3（`test_44_1_3_empty_actor_event_source_byte_equiv`）——空 actor → event source "contextforge-core"（byte-equivalent pre-task-44.1）。
- TEST-44.1.4（`TestTask441_UnpinActorPropagationWired`）——Go 透传链源码 grep（handleMemoryUnpin X-Actor + grpcclient pb.Actor + types Unpin(id, actor)）。

### 2. 不退化

`cargo test --workspace` 全绿（lib 232 + integration + doc-test）；`cargo clippy --workspace --all-targets -- -D warnings` 0 warning；`go test ./...` 全过（含 TestTask441 + TestTask443）；`bash -n scripts/console_smoke.sh` exit 0；CI spec-lint 全绿。既有 `test_memory_server_pin_propagates_actor`（task-40.1）+ `test_memory_server_unpin_explicit_and_idempotent`（task-27.2）+ `emit_audit_and_event` 相关测试维持绿。

## 4. ADR

- **ADR-049**（memory-unpin-actor-propagation）：Proposed → **Accepted**（per-D ratify @ task-44.3；D1 proto+Rust 透传 / D2 emit_audit_and_event actor 参数 pin/unpin 闭环 / D3 Go 透传链 unit 🟢；D4 默认 byte-equiv 🟢 + 认证身份/其余 3 RPC 🔴 honest-defer）。
- **ADR-032**（memory-ops-hardening）+ **ADR-045**（governance-debt-cleanup-3）：add-only `## Amendment (Phase 44 / v0.37.0)` 记 unpin actor 透传 + audit/event source 归因维度兑现（memory-actor-all-rpc Unpin 子项 + emit_audit_and_event 共用基础）；不溯改 D-body（ADR-014 D5）。
- ADR-021（emit_audit_and_event/build_memory_event 镜像源）/ ADR-022 D2（pin lenient body 保持）/ ADR-015（proto add-only）/ ADR-004（默认 byte-equiv）/ ADR-008（0 新依赖）/ ADR-013（禁伪造）/ ADR-014（D1-D5，第三十五次激活）/ ADR-012（用户全权授权 tag/release）—— 守线引用，不溯改正文。
