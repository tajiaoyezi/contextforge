# ContextForge v0.33.0 Artifacts — governance-debt-cleanup-3 (memory pin actor 入口透传 `PinMemoryRequest.actor=3` + L2 embedding 缓存访问序 LRU `sqlite_get` hit-bump + ADR-045 ratified)

> Artifact inventory for the v0.33.0 minor release (Phase 40). Mirrors the v0.32.0 artifacts structure.
> The default build is unchanged: 0-new-dependency, 0-schema-migration；默认行为 / proto 既有字段号 / 既有契约不变（proto add-only 既有字段号 `memory_id=1` / `pin=2` 冻结，ADR-015 D1）。
> 兑现 ADR-032 `[SPEC-DEFER:phase-future.memory-actor-propagation]` + ADR-038 `[SPEC-DEFER:phase-future.l2-cache-true-lru]`——清两组在 grounding 中确认为**真实且 code-local 可单测**的跨 Phase 治理 marker（镜像 Phase 31 / 33 的「核实-诚实化-补全」打法）。**0 新依赖、0 schema migration、默认 byte-equivalent**（ADR-004/008/015）：pin actor 默认（无 `X-Actor` header）→ 空 actor 回落 `"console-api"` 逐字节等价；L2 命中 bump 仅有限 cap 生效（cap==0 byte+perf-equiv）且返回结果不变。**诚实校正（ADR-013）**：pin actor 本轮交付**调用方透传**（认证身份 honest-defer `[SPEC-DEFER:phase-future.memory-actor-authenticated-identity]`）；L2 真-LRU 据实**更正** Phase 33「须加 created_at 列」假设（命中 bump 隐式 rowid，0 migration），写放大 + `with_sqlite` 无生产调用点现网零影响据实记。**两项均无外部依赖**（proto/Go/Rust wire + 本地 SQLite rowid bump，0 网络），故 TEST-40.1.* / 40.2.* 由**每次 CI run 全程守护**（无 honest-defer skip）。既有 22-endpoint + 5 Memory RPC + 运行时二进制默认行为不变。No breaking contract bump。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：`MemoryClient.Pin(id,pin)` → `Pin(id,pin,actor)`（`types.go` interface + `memoryClient.Pin` 填 `pb.PinMemoryRequest{..,Actor}` + `MemMemoryStore.Pin` fallback 对齐 + `degradedMemory.Pin` 对齐）+ `handleMemoryPin` 读 `r.Header.Get("X-Actor")`（缺省空串透传，ADR-022 D2 宽松 body 契约不改）。运行时既有 22-endpoint + 5 Memory RPC 行为不变；默认（无 `X-Actor` header）→ 空 actor → 服务端回落 `"console-api"` byte-equivalent（向后兼容）。
- `contextforge-core`（Rust 数据面 daemon）：console_data_plane proto add-only `PinMemoryRequest.actor=3`（`buf generate proto` 重生 `console_data_plane.pb.go`；既有字段号 `memory_id=1` / `pin=2` 冻结，ADR-015 D1）；`core/src/data_plane/memory.rs` `pin()` `set_pinned_with_actor` 第三参由硬编码 `"console-api"` 改为 `if req.actor.is_empty() { "console-api" } else { req.actor.as_str() }`（空回落 byte-equiv）；`core/src/embedding/cache.rs` `sqlite_get` 命中分支（仅 `l2_cap > 0`）`INSERT OR REPLACE` 原样回写命中行 bump 隐式 rowid → 既有 `sqlite_put` rowid 序驱逐由插入序 FIFO 升访问序 LRU（cap==0 不 bump）。`set_pinned_with_actor` store 字段（task-27.1）+ `sqlite_put` rowid 序驱逐（task-33.1）backend 逻辑 0 改动；默认 byte-equivalent；`CREATE TABLE` 不改（复用隐式 rowid，0 schema migration）。`cargo test -p contextforge-core --lib` = lib all PASS（默认路径不变，220 tests）。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.33.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network）+ cosign keyless 签 + SBOM + provenance（承 v0.21.0–v0.32.0）。linux/arm64 仍 DEFERRED。
- Feature 构建：本 release **0 新 feature flag**；feature-build 矩阵 9/9 不退化。

## 2. 源码档

- master tag SHA：`<backfill: git rev-parse v0.33.0^{commit}>`（v0.33.0 annotated tag 对象 `<backfill: git rev-parse v0.33.0>`，用户授权下主 agent push（ADR-012））
- Phase 40 代码路径：
  - task-40.1（PR #257，MERGED；memory-actor-propagation）：`proto/contextforge/console_data_plane/v1/console_data_plane.proto`（add-only `PinMemoryRequest.actor=3`，既有 `memory_id=1` / `pin=2` 冻结）+ `console_data_plane.pb.go`（`buf generate proto` 重生，`Actor` 字段 + getter + rawDesc 重编码无 message/service 重排，4 个不相关 `.pb.go` 还原）+ `internal/consoleapi/types.go`（`MemoryClient.Pin` 加 `actor string`）+ `internal/consoleapi/grpcclient/grpcclient.go`（`memoryClient.Pin` 填 `pb.PinMemoryRequest.Actor`）+ `internal/consoleapi/memstore.go`（`MemMemoryStore.Pin` 对齐）+ `internal/cli/console_api_serve_degraded.go`（`degradedMemory.Pin` 对齐）+ `internal/consoleapi/handlers.go`（`handleMemoryPin` 读 `X-Actor`）+ `core/src/data_plane/memory.rs`（`pin()` 空回落 + in-crate TEST-40.1.1/40.1.2）+ `internal/consoleapi/memory_actor_test.go`（TEST-40.1.3）+ `internal/consoleapi/grpcclient/grpcclient_test.go`（TEST-40.1.4）+ `core/tests/memory_integration.rs` / `internal/consoleapi/memstore_test.go`（struct literal / 调用点对齐）。认证身份 honest-defer `[SPEC-DEFER:phase-future.memory-actor-authenticated-identity]`。**0 新依赖 / proto add-only / 默认 byte-equiv**。
  - task-40.2（PR #258，MERGED；l2-embedding-cache-true-lru）：`core/src/embedding/cache.rs`（`sqlite_get` 命中分支仅 `l2_cap > 0` `INSERT OR REPLACE` 原样回写命中行 bump 隐式 rowid + in-crate TEST-40.2.1（LRU 驱逐最久未用 vs FIFO）+ TEST-40.2.2（cap 门控 bump + 结果不变））。`sqlite_put` rowid 序驱逐 0 改动；据实更正 Phase 33 真-LRU 假设（命中 bump = 访问序 LRU，0 schema migration，与 Go memstore move-to-front task-33.2 同技法）。**0 新 dep / 0 schema migration / 默认结果不变**（消费既有隐式 rowid）。
  - task-40.3（this PR；closeout）：`scripts/console_smoke.sh`（smoke v30，banner 注 v29→v30，step `[49/49]` REAL 模式 `POST /v1/memory/{id}/pin` 带 `X-Actor` → `pinned_by` 端到端断言）；`internal/cli/smoke_syntax_test.go`（`TestTask403` no-regression [37/37]..[48/48]）；`docs/releases/v0.33.0-{evidence,artifacts}.md`；`README.md` v0.33 段；`RELEASE_NOTES.md` v0.33.0 段；`docs/decisions/adr-045-...md`（ratify Proposed→Accepted，per-D）+ ADR-032 / ADR-038 / ADR-027 / ADR-015 add-only Phase-40 Amendment（标记 memory-actor-propagation / l2-cache-true-lru fulfilled + 真-LRU 假设据实更正，**不溯改 D-body**，ADR-014 D5）；`docs/specs/phases/phase-40-...md`（§6 闭合）；`docs/s2v-adapter.md`；`test/features/phase-40-...feature`。

## 3. 验证脚本

```bash
# 默认构建（0 新依赖 / 0 network / 0 schema migration）
cargo test --workspace                                     # all PASS (core lib 220 + memory_integration 5 + embedding::cache 9)
go test ./...                                              # all pass (incl TestTask403 + TestTask401_*)
cargo clippy -p contextforge-core --all-targets           # 0 warning
go vet ./...                                               # clean
bash -n scripts/console_smoke.sh                           # exit 0

# task-40.1 — pin actor 入口透传（无外部依赖，每次默认 test 守护）
cargo test -p contextforge-core --lib data_plane::memory  # TEST-40.1.1 (prost wire-tag actor=3 = 0x1A) + TEST-40.1.2 (Rust pin 透传/空回落)
go test ./internal/consoleapi/... -run TestTask401        # TEST-40.1.3 (handleMemoryPin X-Actor) + TEST-40.1.4 (grpcclient Actor)

# task-40.2 — L2 访问序 LRU（无外部依赖，每次默认 test 守护）
cargo test -p contextforge-core --lib embedding::cache    # TEST-40.2.1 (LRU evicts LRU not FIFO) + TEST-40.2.2 (cap gates bump + results unchanged)

# proto add-only 重生（既有字段号冻结校验：diff 仅 actor 字段 + rawDesc）
buf generate proto && git diff --stat proto/

# 端到端 smoke v30（step 49 → [49/49]；REAL 模式 X-Actor → pinned_by；既有 step 不退化）
go test ./internal/cli/ -run TestTask403

# D5 spec-drift lint
bash scripts/spec_drift_lint.sh --touched origin/master   # 0 unannotated hits
```

## 4. 镜像推送

- `release.yml`（`v*` tag push 触发）→ buildx → ghcr login → build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.33.0` + `:latest`（**linux/amd64**，net-zero 与 v0.32.0 一致）→ cosign keyless 签 + attest SPDX SBOM + provenance:mode=max（承 task-28.2 机制）。
- run ID + digest：`release.yml` run `<backfill: gh run list --workflow release.yml>`（conclusion **`success`**，build-push + cosign sign + attest SBOM + provenance:mode=max 步全绿）；`ghcr.io/tajiaoyezi/contextforge-daemon:v0.33.0` + `:latest` = `<backfill: docker buildx imagetools inspect>`；git tag SHA `<backfill: git rev-parse v0.33.0^{commit}>`；cosign Rekor tlog idx `<backfill: sign tlog>`（sign）/ `<backfill: attest SBOM tlog>`（attest SBOM）。
- governance-debt evidence artifact：本 release 的 pin actor 透传 + L2 访问序 LRU 证据为**本机 + CI 真实测试**（TEST-40.1.1 prost wire-tag actor=3 / TEST-40.1.2 Rust pin 透传+空回落 / TEST-40.1.3 Go X-Actor / TEST-40.1.4 grpcclient Actor / TEST-40.2.1 LRU 驱逐最久未用 vs FIFO / TEST-40.2.2 cap 门控 bump + 结果不变 / smoke v30[49/49] REAL 模式 `X-Actor`→`pinned_by`）。**两项均无外部付费 API 依赖**（proto/Go/Rust wire + 本地 SQLite rowid bump，0 网络），故由**每次 CI run 全程守护**，无 honest-defer skip。

## 5. cosign verify（by-digest）

```bash
# 解析 tag → digest，按 digest（非 tag）验签（承 v0.21.0 follow-up 教训：cosign verify 务必按 digest）
DIGEST=$(cosign triangulate --type digest ghcr.io/tajiaoyezi/contextforge-daemon:v0.33.0)
cosign verify \
  --certificate-identity-regexp 'https://github.com/tajiaoyezi/contextforge/.github/workflows/release.yml@.*' \
  --certificate-oidc-issuer https://token.actions.githubusercontent.com \
  ghcr.io/tajiaoyezi/contextforge-daemon@${DIGEST}
# 期望 digest = <backfill: docker buildx imagetools inspect>
```

## 6. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-045 governance-debt-cleanup-3 | **Accepted**（per-D，D1 memory pin actor add-only 透传 🟢 + D2 L2 embedding 缓存访问序 LRU 🟢 + D3 默认行为 + proto add-only + 0-dep/0-network + honest-defer 边界 🟢） | task-40.3 据 40.1/40.2 真实非合成验证 ratify：D1 Accepted（`PinMemoryRequest.actor=3` 既有字段号冻结 + Go `Pin(id,pin,actor)` 参数链 + `handleMemoryPin` 读 `X-Actor` + Rust `pin()` 空回落 byte-equiv + 认证身份 honest-defer，TEST-40.1.1/40.1.2/40.1.3/40.1.4）/ D2 Accepted（`sqlite_get` 命中 bump 隐式 rowid 仅有限 cap → 驱逐由 FIFO 升 LRU + cap==0 不 bump + 0 schema migration + 据实更正 Phase 33 假设，TEST-40.2.1/40.2.2）/ D3 Accepted（默认 `hybrid` 无关——pin actor 空回落 + L2 cap==0 / 命中 bump 结果不变 byte-equiv + 0 新 dep + proto add-only 既有字段号冻结 + honest-defer 边界据实）|
| ADR-032 memory-ops-hardening（memory-actor-propagation defer） | Accepted（+ Phase 40 add-only Amendment：memory-actor-propagation 入口透传维度 fulfilled + 认证身份续延后） | task-40.3 add-only 标记 ADR-032 `[SPEC-DEFER:phase-future.memory-actor-propagation]` 入口透传维度已兑现（proto add-only `actor=3` + Go 参数链 + REST `X-Actor` header + Rust 空回落），认证身份续 `[SPEC-DEFER:phase-future.memory-actor-authenticated-identity]`，**不溯改 D-body / Ratification (v0.20.0)**（ADR-014 D5）|
| ADR-038 governance-debt-cleanup-2（l2-cache-true-lru defer） | Accepted（+ Phase 40 add-only Amendment：l2-cache-true-lru fulfilled + 真-LRU 假设据实更正） | task-40.3 add-only 标记 `[SPEC-DEFER:phase-future.l2-cache-true-lru]` 已兑现（命中 bump 隐式 rowid，0 schema migration）+ 据实更正 ADR-038 A2/D4「真 LRU 须加 created_at 列」假设（命中 bump 即得访问序 LRU），**不溯改 ADR-038 D-body / Ratification (v0.26.0)**（ADR-014 D5）|
| ADR-027 embedding-provider-abstraction（L2 cache 有界化前序） | Accepted（+ Phase 40 add-only Amendment：L2 访问序 LRU 增量） | task-40.3 add-only 标记 L2 cache 在 Phase 33 有界化（rowid-FIFO）之上 Phase 40 补访问序 LRU（命中 bump），**不溯改 ADR-027 D-body**（ADR-014 D5）|
| ADR-015 console-contract-v1-compatibility（proto add-only） | Accepted（+ Phase 40 add-only Amendment：`PinMemoryRequest.actor=3`） | `PinMemoryRequest` add-only `actor=3`（既有 `memory_id=1` / `pin=2` 字段号冻结，D1 + add-only D2），既有 client 兼容（proto3 未知字段忽略）|
| ADR-022 D2 memory pin lenient body contract | Accepted（守线引用，不改） | `handleMemoryPin` 宽松 body 契约保持不变——本 phase 仅在入口加 `X-Actor` header 读取，不改 body 解析回落 `pin=true` 行为 |
| ADR-013 据实延后/校正 | Accepted（守线） | pin actor 调用方透传 vs 认证身份 honest-defer（不夸大为已认证身份）；L2 命中 bump 写放大 = 访问序 LRU 固有代价、`with_sqlite` 无生产调用点现网零影响（opt-in 路径语义补全非已确认线上问题）；真-LRU 假设据实更正非否定 Phase 33；其余 marker（vector-dim-feature-enforce / tracestore-multi-workspace-strict / chunk-source-type-filter）据实保持延后不强行扩面；tag SHA / run id / digest / tlog 未知前以 `<backfill>` 占位不伪造凭据 |
| ADR-014 add-only / cross-validation | Accepted（守线，第三十一次激活） | proto add-only（既有字段号 1-2 冻结）+ Go interface add-only 参数 + L2 `sqlite_get` 命中 bump 均 add-only（`set_pinned_with_actor` / `sqlite_put` 0 改动、默认 byte-equivalent / 0 migration）+ ADR-032/038/027/015 Amendment 不溯改正文（D5）|
| ADR-004 / ADR-008 / ADR-012 | Accepted（守线） | 默认（无 `X-Actor`）pin actor 回落 `"console-api"` 字节等价 + L2 cap==0 / 命中 bump 结果不变（ADR-004）；0 新依赖（ADR-008，复用 `set_pinned_with_actor` / `sqlite_put` / `buf` 工具链 / `r.Header.Get`）；tag-release 须授权（本轮经用户显式授权 ADR-012）|

## 7. 版本声明（contract bump 评估）

- 本 release 新增 console_data_plane proto add-only `PinMemoryRequest.actor=3`（既有 `memory_id=1` / `pin=2` 字段号冻结）+ Go `MemoryClient.Pin` add-only `actor` 参数 + `handleMemoryPin` 读 `X-Actor` header + Rust `pin()` 空回落 + `cache.rs` `sqlite_get` 命中 bump + smoke v30——**proto 既有字段号全冻结（add-only 新字段 3）、无 RPC / endpoint 删改、无默认运行时行为改动（默认无 `X-Actor` → `pinned_by="console-api"` byte-equivalent；L2 命中 bump 仅改 rowid 不改返回 vector）、0 schema migration**（复用既有隐式 rowid）。
- 默认构建 0 新代码依赖 / 0 network（ADR-004 / ADR-008，pin actor 透传 + L2 LRU 均无外部依赖）；`handleMemoryPin` 宽松 body 契约保持（ADR-022 D2）。
- 故 **无 breaking contract bump**；既有 v0.6–v0.32 client + 既有索引/数据无需改动（proto3 未知字段忽略、Go interface 加参数是源码内部改动、`embedding_cache` schema 未改 v0.32↔v0.33 数据文件互通）。**memory-actor-propagation 入口透传收口**（ADR-032 defer 兑现）+ **l2-cache-true-lru 收口**（ADR-038 defer 兑现 + 真-LRU 假设据实更正）。

## 8. 后续模板（governance-debt-cleanup-3 follow-on）

- pin actor 认证身份（把 `X-Actor` header 值校验映射为已认证 auth subject，须 console-api 鉴权层）`[SPEC-DEFER:phase-future.memory-actor-authenticated-identity]`；其它 memory RPC 的 actor 透传 `[SPEC-DEFER:phase-future.memory-actor-all-rpc]`。
- L2 命中 bump 写放大优化（批量 bump / 计数阈值 bump）`[SPEC-DEFER:phase-future.l2-lru-bump-batching]`；L2 TTL 时间过期 `[SPEC-DEFER:phase-future.l2-cache-ttl]`；`with_sqlite` 接入生产 daemon `[SPEC-DEFER:phase-future.l2-cache-production-wire]`。
- 仍真延后不强行扩面：vector backend dim 强校验 `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]`（须 feature build）/ TraceStore 多 workspace 余下读路径 `[SPEC-DEFER:phase-future.tracestore-multi-workspace-strict]` / chunk source_type/agent_scope 过滤 `[SPEC-DEFER:phase-future.chunk-source-type-filter]` + `[SPEC-DEFER:phase-future.chunk-agent-scope-filter]`（须 import-path schema migration）。
- linux/arm64 原生 runner multi-arch `[SPEC-DEFER:phase-future.multi-arch-native-runner]`。

## 9. 存档指引

- closeout PR 合入后，**用户授权下**主 agent push `v0.33.0` annotated tag 触发 `release.yml`；post-tag-push backfill PR 填实 tag SHA + run ID + 镜像 digest + cosign 凭据（承 v0.8–v0.32 pattern；§2/§4/§5 角括号 `<backfill: ...>` 标记由本 backfill PR 回填）。
- Dockerfile workspace member COPY 完整性预检（承 v0.11.0 PR #139 教训）：本 release **无新 migration、proto add-only（既有字段号冻结）**——改动为 console_data_plane proto add-only 1 字段（`buf generate` 重生既有 `console_data_plane.pb.go`）+ `core/src/data_plane/memory.rs` + `core/src/embedding/cache.rs`（既有 `core/` member）+ Go console-api add-only（既有 member）+ smoke/test/docs，无新依赖 / 无新 crate member；镜像为默认构建（0 新依赖 / 0 network，linux/amd64）。
