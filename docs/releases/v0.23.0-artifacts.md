# ContextForge v0.23.0 Artifacts — cjk-true-segmenter (jieba 真分词 analyzer feature-gated + 双站点注册 + reindex 迁移工具 + 真实 CJK recall delta + ADR-035 ratified)

> Artifact inventory for the v0.23.0 minor release (Phase 30). Mirrors the v0.22.0 artifacts structure.
> The default build is unchanged: 0-new-dependency, 0-network；默认 `content` tokenization + 6-field schema
> 不变。新增 `cjk-segmenter` feature（jieba-rs，optional，默认 off）+ `IndexSession::reindex_with_tokenizer`
> 迁移工具均不影响默认路径；bigram `code_cjk` opt-in 保留作 0-dep fallback。既有 22-endpoint + 5 Memory RPC
> + 运行时二进制不变。No breaking contract bump。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：本 release **运行时无变化**——仅 `internal/cli/smoke_syntax_test.go` 加 `TestTask303_*` + `scripts/console_smoke.sh` v20 step 39 文档化。
- `contextforge-core`（Rust 数据面 daemon）：新增 `cjk_segmenter` analyzer（feature `cjk-segmenter`，jieba-rs）+ `IndexSession::reindex_with_tokenizer` 迁移工具 + `RetrieverConfig.tokenizer` doc（schema-driven 对称）。默认构建 0 新依赖 / 0 network（jieba 默认不编译）；`cargo test --workspace` 0 failed。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.23.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network；cjk-segmenter feature-gated 不入默认镜像）+ cosign keyless 签 + SBOM + provenance（承 v0.21.0/v0.22.0）。linux/arm64 仍 DEFERRED。
- Feature 构建：本 release 新增 `cjk-segmenter` feature flag（jieba-rs optional dep，主 agent R7 chore + ADR-008 add-only）。CI 默认不构建该 feature（真分词单测/召回经 dev box feature build 实测）。

## 2. 源码档

- master tag SHA：`dd8a0e5ac5bf434a02ccdeed0c6ed7cd55d6cd1f`（v0.23.0 annotated tag，无人值守授权下主 agent push（ADR-012））
- Phase 30 代码路径：
  - task-30.1（真分词 analyzer + 双站点）：`core/Cargo.toml`（optional `jieba-rs = "0.7"` + `cjk-segmenter` feature）；`core/src/indexer/mod.rs`（`CJK_SEGMENTER_TOKENIZER` + `tokenize_cjk_segmenter` + `CjkSegmenterTokenizer` + `build_cjk_segmenter_analyzer` + `register_cjk_segmenter` + index 站点注册 + `build_tantivy_schema` opt-in 分支扩 + TEST-30.1.1/30.1.2）；`core/src/retriever/mod.rs`（query 站点对称注册）。
  - task-30.2（reindex 工具 + recall delta）：`core/src/indexer/mod.rs`（`reindex_with_tokenizer` + TEST-30.2.2/30.2.2b）；`core/src/retriever/mod.rs`（`RetrieverConfig.tokenizer` schema-driven doc）；`core/examples/phase24_tokenizer_recall.rs`（真分词 path）；`test/fixtures/eval/golden-semantic.jsonl`（+5 CJK case）。
  - task-30.3（this PR）：`scripts/console_smoke.sh`（v20 step 39）；`internal/cli/smoke_syntax_test.go`（`TestTask303_*`）；`docs/releases/v0.23.0-{evidence,artifacts}.md`；`README.md` v0.23 段；`RELEASE_NOTES.md` v0.23.0 段；`docs/decisions/adr-035-...md`（ratify Proposed→Accepted，per-D，D3 honest-defer）+ `adr-029-...md`（add-only Amendment Phase 30）；`docs/specs/phases/phase-30-cjk-true-segmenter.md`（§6 闭合）；`docs/s2v-adapter.md`；`test/features/phase-30-cjk-true-segmenter.feature`。

## 3. 验证脚本

```bash
# 默认构建（0 新依赖 / 0 network）
cargo test --workspace
go test ./...
cargo clippy --workspace --all-targets -- -D warnings

# task-30.1 真分词 analyzer + 双站点（feature）
cargo test -p contextforge-core --features cjk-segmenter --lib test_30_1      # 2 passed

# task-30.2 reindex 迁移工具（默认构建可验）+ feature reindex
cargo test -p contextforge-core --lib test_30_2_2                            # default-build PASS
cargo test -p contextforge-core --features cjk-segmenter --lib test_30_2     # +30.2.2b PASS

# task-30.2 真实 recall delta（default vs bigram vs 真分词）
cargo run -p contextforge-core --features cjk-segmenter --example phase24_tokenizer_recall
#    → segmenter vs bigram delta +0.0000（小语料 file-level 持平，诚实零 delta，ADR-013）

# 扩 CJK golden Go validator
go test ./internal/eval/...

# 端到端 smoke v20（含 cjk-true-segmenter 文档化 step 39；既有 step 不退化）
bash -n scripts/console_smoke.sh
go test ./internal/cli/ -run TestTask303

# D2 spec-drift lint
bash scripts/spec_drift_lint.sh --touched origin/master
```

## 4. 镜像推送

- `release.yml`（`v*` tag push 触发）→ buildx → ghcr login → build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.23.0` + `:latest`（**linux/amd64**，net-zero 与 v0.22.0 一致）→ cosign keyless 签 + cosign attest SPDX SBOM（syft）+ provenance:mode=max（承 v0.21.0 task-28.2 机制）。
- 镜像 = **默认构建**：0 新依赖 / 0 network；cjk-segmenter feature-gated 不入镜像；运行时行为与 v0.22.0 baseline 兼容。
- run ID + digest：`release.yml` run `26860021043`（**success**，build-push + cosign sign + attest SBOM + provenance:mode=max 步全绿）；`ghcr.io/tajiaoyezi/contextforge-daemon:v0.23.0` + `:latest` = `sha256:bd1eb8c531b9184ea3e34c695929efa40bd54530c5694f7e6ad4f5b5d6c1c83f`；git tag SHA `dd8a0e5ac5bf434a02ccdeed0c6ed7cd55d6cd1f`；cosign Rekor tlog idx `1707015238`（sign）/ `1707015982`（attest SBOM）。

## 5. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-035 cjk-true-segmenter-and-tokenizer-default | **Accepted**（per-D，D3 default flip honest-defer 部分） | task-30.3 据 task-30.1/30.2 真实非合成验证 ratify：D1 Accepted（jieba 真分词 feature-gated）/ D2 Accepted（parallel name 双站点对称）/ D3 PARTIAL（reindex 迁移工具 + schema-driven 对称达成；**default flip honest-defer `[SPEC-DEFER:phase-future.tokenizer-default-on]`**）/ D4 Accepted（真实 recall delta，含诚实零 delta seg−bigram=+0.0000）/ D5 Accepted（默认 tokenization + 0-dep baseline 不变）；受阻维度（lindera 重词典 / 大语料 / default flip）据真实受限如实记录（ADR-013） |
| ADR-029 code-and-cjk-tokenizer-and-eval-hardening | Accepted（+ Phase 30 add-only Amendment） | `[SPEC-DEFER:phase-future.cjk-true-segmenter]` 兑现（feature-gated，bigram fallback 保留）+ `[SPEC-DEFER:phase-future.tokenizer-default-on]` 部分兑现（迁移工具 + 评估，default flip 续延后），以 `## Amendment (Phase 30 / v0.23.0)` 记录（不溯改 D1-D5 正文，ADR-014 D5） |
| ADR-004 local-first-privacy-baseline | Accepted | 守线：cjk-segmenter feature-gated，jieba 默认不编译，默认构建 0 新依赖 + 0 network + 默认 tokenization 不变 |
| ADR-008 core-library-selection | Accepted（+ jieba-rs add-only） | task-30.1 经主 agent R7 chore 加 optional `jieba-rs 0.7`（pure-Rust，默认 off），ADR-008 add-only；默认构建 0 新 dep（`Cargo.lock` 含 jieba 条目是 optional dep 正常记录，同 ureq 先例，默认不编译） |
| ADR-012 main-agent-governance-autonomy | Accepted（守线） | tag / release outward-facing 不可逆 → 须用户显式授权（沿用 v0.13-v0.22 模式，不自行 tag）；实现 / 设计 / 选型主 agent 自治 |
| ADR-013 no-fake-evidence | Accepted | 守线：ADR-035 ratify 据真实 test/harness；真分词 vs bigram recall delta +0.0000 诚实零（不伪造提升）、default flip honest-defer、小语料不外推 均如实记录 |
| ADR-014 cross-phase-exit-criteria-validation | Accepted | 第二十一次激活（v0.23）；history Phase 1-29 不 retro-edit |

## 6. 版本声明（contract bump 评估）

- 本 release 新增 `cjk-segmenter` feature + jieba-rs（optional）+ `reindex_with_tokenizer` 工具 + smoke v20——**无 proto / 无 RPC / 无 endpoint / 无默认运行时行为改动**（默认 tokenization + 6-field schema 不变）。
- 默认构建 0 新代码依赖 / 0 network（ADR-004）；jieba-rs 在 `cjk-segmenter` feature 下。
- 故 **无 breaking contract bump**；既有 v0.6–v0.22 client + 既有默认索引无需改动。**cjk-true-segmenter 收口**（默认运行时二进制行为不变）。

## 7. 后续模板（cjk-true-segmenter follow-on）

- tokenizer default flip（全量翻默认）`[SPEC-DEFER:phase-future.tokenizer-default-on]`——本 release 迁移工具已备 + schema-driven 对称，翻默认是产品决策诚实延后。
- 真分词器用户自定义词典 / 领域词扩充 `[SPEC-DEFER:phase-future.cjk-segmenter-user-dict]`。
- 重词典 / 多语种分词器（lindera IPADIC/ko-dic，日/韩）`[SPEC-DEFER:phase-future.multilang-segmenter]`——本 release 选轻量 jieba（中文）。
- 大规模 CJK 语料 recall 基准（破小语料局限）`[SPEC-DEFER:phase-future.large-cjk-corpus-eval]`——本 release 16 q 小语料实测 +0.0000 delta，大语料拐点留后续。
- 向量侧（embedding）CJK 分词协同 `[SPEC-DEFER:phase-future.cjk-embedding-tokenization]`——本 release 限倒排 BM25 analyzer。

## 8. 存档指引

- closeout PR 合入后，**无人值守授权下**主 agent push `v0.23.0` annotated tag 触发 `release.yml`；post-tag-push backfill PR 填实 tag SHA + run ID + 镜像 digest + cosign 凭据（承 v0.8–v0.22 pattern）。
- Dockerfile workspace member COPY 完整性预检（承 v0.11.0 PR #139 教训）：本 release 新增源码均在 `core/src/`（analyzer / reindex 工具）+ `core/examples/`（harness）+ `test/fixtures/`（golden）+ docs/scripts member 内——**无新增 migration / proto**（Dockerfile 无需改动）；镜像为默认构建（0 新依赖 / 0 network，linux/amd64，cjk-segmenter feature-gated 不入镜像）。
