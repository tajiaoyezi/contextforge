# ContextForge v0.11.0 Evidence — vector-backend-selection (infra + spike + ADR-023 Proposed)

**日期**：2026-05-30
**主干**：`master`
**Phase 18 closeout PR merge**：`8f61219` (`Merge PR #138: chore(release) v0.11.0 — Phase 18 closeout (honest scope) ...`)
**Dockerfile fix (release.yml unblock)**：`6ebea46` (`Merge PR #139: fix(docker) copy bench/ workspace member ...`)
**当前 HEAD / v0.11.0 tag SHA**：`6ebea469d3d5bcc64ffdb006039a29992233f97d` (`git rev-parse v0.11.0^{}`；tag 指向 Dockerfile fix HEAD,含 closeout #138 + fix #139)
**parent release**：v0.10.0 tag → master @ `cbaafe6`
**release.yml run**：[26680454339](https://github.com/tajiaoyezi/contextforge/actions/runs/26680454339) GREEN（start 2026-05-30T09:31:24Z end 09:40:09Z，~8m45s）→ `ghcr.io/tajiaoyezi/contextforge-daemon:v0.11.0` + `:latest` pushed。

> **tag re-point 记录（诚实）**：v0.11.0 首次打 tag 指向 closeout `8f61219`,但 release.yml run [26680273750](https://github.com/tajiaoyezi/contextforge/actions/runs/26680273750) **FAILED** — Docker rust-build stage `failed to load manifest for workspace member /src/bench`（task-18.2 #130 加 `bench` 到 workspace `members`,但 Dockerfile 仅 COPY core/+proto/；ci.yml 构建全 repo 故未捕获,release.yml 仅 tag push 触发故此时暴露）。PR #139 修（`COPY bench/`,隔离 context build 实测 47s exit 0），删 tag 重指向 `6ebea46` 重跑 release.yml GREEN。首次 run 未 push 任何 image,re-point 安全。

> **诚实缩范围声明**：v0.11.0 是**向量 backend 基础设施 + 选型里程碑**,非「语义搜索 live」。Phase 18 §6 退出标准 **AC1/AC2/AC5/AC6 达成**；**AC3 partial**（ADR-023 落地为 `Proposed`,4 backend trade-off matrix 齐全,但合成 recall 不可区分故未 ratify 到 `Accepted`）；**AC4 deferred**（默认 backend 生产 retriever 集成 + smoke v9 语义搜索后置——本 Phase spike 显式用确定性种子向量规避 ONNX,无 embedding provider）。AC3 ratify + AC4 集成 → 后继 `[SPEC-OWNER:phase-future.vector-retrieval-integration]`。按 ADR-013 不 fake `[x]`。用户 2026-05-30 授权「切 v0.11.0 诚实缩范围」。

## 1. 合入记录 (Phase 18, tasks 18.1–18.9)

- PR [#128](https://github.com/tajiaoyezi/contextforge/pull/128) task-18.1 — vector trait abstraction + `NoopVectorBackend` + `Retriever::with_vector_searcher` seam（+#129 review remediation：`VectorError #[non_exhaustive]` / AC3 test rigor / spec integrity）。
- PR [#130](https://github.com/tajiaoyezi/contextforge/pull/130) task-18.2 — `bench/` spike harness：确定性 corpus（splitmix64,无 rand dep）+ 5 维 measure + runner over trait + Noop smoke。
- PR [#133](https://github.com/tajiaoyezi/contextforge/pull/133) task-18.3 — **sqlite-vec** backend（`vec0`,stable `=0.1.9`；`0.1.10-alpha.4` 打包缺 `sqlite-vec-diskann.c` 不可编译）。Linux gcc 实测。
- PR [#134](https://github.com/tajiaoyezi/contextforge/pull/134) task-18.4 — **qdrant** backend（qdrant-client 1.18 gRPC → 本机 Qdrant v1.18.1 musl binary；async 经自带 tokio runtime block_on）。
- PR [#135](https://github.com/tajiaoyezi/contextforge/pull/135) task-18.5 — **lancedb** backend（embedded Lance 0.30 + Arrow `RecordBatch`；arrow-array pin 58 匹配 lance；build 需 protoc）。
- PR [#131](https://github.com/tajiaoyezi/contextforge/pull/131) task-18.6 — **hnsw** backend（instant-distance 纯 Rust HNSW；首个真实数据 backend）。
- PR [#136](https://github.com/tajiaoyezi/contextforge/pull/136) task-18.7 — **ADR-023 (Proposed)** 默认 backend 选型 + 4 backend comparison + hnsw evidence 补 Linux RSS/100k。
- PR [#137](https://github.com/tajiaoyezi/contextforge/pull/137) task-18.8 — `internal/eval` **SemanticRecall@K** 度量 + `SummarizeHybrid` 双路 + `MeetsRecallGate` + ADR-006 Amendment A1。
- PR [#138](https://github.com/tajiaoyezi/contextforge/pull/138) (closeout, `8f61219`) — Phase 18 §6/§8 诚实状态（AC1/2/5/6 met / AC3 partial / AC4 deferred）+ v0.11.0 release docs（README + RELEASE_NOTES + evidence + artifacts）。
- PR [#139](https://github.com/tajiaoyezi/contextforge/pull/139) (`6ebea46`) — `fix(docker)`：Dockerfile rust-build COPY bench/ workspace member，解 release.yml v0.11.0 image build `failed to load manifest for /src/bench`。
- (this backfill PR) — 填实 tag SHA `6ebea46` + release.yml run [26680454339](https://github.com/tajiaoyezi/contextforge/actions/runs/26680454339) + tag re-point 记录。

## 2. S2V 状态

- `docs/s2v-adapter.md`：Phase 18 `Done`；task-18.1–18.9 全 `Done`；ADR-023 row `Proposed`。
- `docs/specs/phases/phase-18-vector-backend-selection.md` = §6 AC1/2/5/6 `[x]`；AC3 partial（ADR Proposed）；AC4 deferred；§8 DoD 缩范围声明。
- `docs/specs/tasks/task-18.{1..9}-*.md` = `Done`。
- `docs/decisions/adr-023-vector-backend-default.md` = `Proposed`（ratify 后置）。
- `docs/decisions/adr-006-recall-eval-acceptance-gate.md` = `Accepted` + Amendment A1（SemanticRecall@K gate，Proposed）。
- `docs/decisions/adr-014-cross-phase-exit-criteria-validation.md` = `Accepted`（**v0.11 第九次激活** — vector-backend-selection）。

## 3. 验证证据

### 1. 四 backend 5 维实测（task-18.2 harness, release, Linux WSL2 Ubuntu 26.04, rustc 1.96.0）

n=5000 / dim=64：

| backend | recall@5/10 | P95 (ms) | idle/index RSS (MB) | cold-start (ms) | reindex (ms) |
|---|---|---|---|---|---|
| hnsw | 1.0/1.0 | 0.382 | 4.4/11.0 | 836 | 798 |
| sqlite-vec | 1.0/1.0 | 0.167 | 6.0/8.5 | 36.8 | 54.4 |
| qdrant | 1.0/1.0 | 0.650 | server ~105 | 30.9 | 29.8 |
| lancedb | 1.0/1.0 | 1.551 | 30.5/50.9 | 7.4 | 5.8 |

n=100000 / dim=64：

| backend | recall@5/10 | P95 (ms) | idle/index RSS (MB) | cold-start (ms) | reindex (ms) |
|---|---|---|---|---|---|
| hnsw | 1.0/1.0 | 0.871 | 55.3/180.0 | 28432 | 28984 |
| sqlite-vec | 1.0/1.0 | 3.198 | 56.7/90.7 | 760 | 1114 |
| qdrant | 1.0/1.0 | 0.947 | client 57.5/91.6 · server ~166 | 385 | 355 |
| lancedb | 1.0/1.0 | 10.893 | 68.0/90.8 | 50.4 | 56.6 |

数据源：各 `docs/spikes/phase-18-{hnsw,sqlite-vec,qdrant,lancedb}.md` + `docs/spikes/phase-18-comparison.md`。合成 recall 全 1.0 不可区分（已记录）。

### 2. cargo test --workspace（默认 feature；各 PR CI 实测）

默认构建不含任一 vector backend（feature-gated）。各 PR CI cargo-test job GREEN；默认 `cargo test --workspace` 0 failed（Linux 实测 ~2-4s，无回归）。

### 3. go test ./...（各 PR CI 实测）

task-18.8 内 `internal/eval` 加 4 新单测（`TestTask188_*`：SemanticRecall@K 数学 / SummarizeHybrid 双路 / 空 semantic 退 BM25 / MeetsRecallGate 阈值），全 PASS；全 `go test ./...` 21+ package GREEN。

### 4. CI 状态（各 task PR 三门绿后自主合入）

| PR | cargo-test | go-test | spec-lint |
|---|---|---|---|
| #133 | PASS | PASS | PASS |
| #134 | PASS (3m8s) | PASS (3m4s) | PASS (2m1s) |
| #135 | PASS (3m6s) | PASS (3m28s) | PASS (1m24s) |
| #136 | PASS (2m4s) | PASS (3m27s) | PASS (1m56s) |
| #137 | PASS (1m34s) | PASS (3m19s) | PASS (2m5s) |

### 5. ADR-014 D2 lint（this closeout PR self-check）

```
✅ spec_drift_lint --touched master: 0 unannotated hits in changed lines
```

### 6. 缩范围 — 未达 / 后置项（诚实记录）

| Phase §6 AC | 状态 | 原因 / owner |
|---|---|---|
| AC1 trait | ✅ met | task-18.1 |
| AC2 4 backend × 5 维 | ✅ met | task-18.2–18.6,4 路真实数据 |
| AC3 ADR-023 | ⚠️ partial | ADR 落地为 **Proposed**；合成 recall 不可区分,无真实数据不 ratify。`→ Accepted` 后置 `[SPEC-OWNER:phase-future.vector-retrieval-integration]` |
| AC4 生产集成 + smoke v9 | ⛔ deferred | 需 embedding provider + 生产向量召回 wiring（ADR-023 D6）。后置 `[SPEC-OWNER:phase-future.vector-retrieval-integration]` |
| AC5 SemanticRecall@K | ✅ met | task-18.8 度量 + 门禁 + 单测（live 值待 provider） |
| AC6 ADR-014 D1-D5 | ✅ met | 第九次激活,各 PR + closeout |

## 4. 平台矩阵

| Platform | Build | Note |
|---|---|---|
| Linux x86_64 (WSL2 Ubuntu 26.04) | 默认 + 全 vector feature build OK | 4 backend spike 实测平台（rustc 1.96.0；sqlite-vec gcc / lancedb protoc / qdrant 本机 server） |
| Windows MSVC (dev box) | 默认 + vector-hnsw build OK | sqlite-vec MSVC 受阻 `[SPEC-DEFER:phase-future.sqlite-vec-cross-platform]`；hnsw 纯 Rust 跨平台 |
| Linux (GHA runner) | 默认 build + cargo/go test PASS | CI 三门；vector feature 默认关闭,CI 不构建 backend |

## 5. Upgrade / Rollback

详 `RELEASE_NOTES.md` v0.11.0 §Upgrade path / §Rollback path。要点：无 migration、无 breaking、`vector-*` 默认关闭（默认构建 BM25-only 不变）；`docker pull ghcr.io/tajiaoyezi/contextforge-daemon:v0.11.0`（tag push 后）；回退 = `git tag -d v0.11.0` + 删 release（对默认构建 no-op）。

## 6. ADR-014 第九次激活 record

```
2026-05-30: ADR-014 第九次激活 — Phase 18 vector-backend-selection.
  Cumulative: v0.3 / v0.4 / v0.5 / v0.6 / v0.7 / v0.8 / v0.9 / v0.10 / **v0.11 (本次)**.
  9-phase 累计稳定性。本次跨 5 个 task PR (#133-137) + closeout 全程 D1-D5：
    D1 mapping (Phase §6 ↔ task §6) / D2 lint 0 hits 各 PR / D3 verified-by 各 AC /
    D4 主 agent 自治合入 / D5 历史 Phase 1-17 不溯改（仅 ADR-006 add-only amendment,经 phase spec 授权）。
```
