# ContextForge v0.25.0 Evidence — vector-backend-config-plumbing-and-completeness (server hot-path backend select via env + sqlite-vec factory arm + console vector_score provenance add-only + retrieval-filter no-op contract honesty + ADR-037 ratified)

> v0.25.0 ships Phase 32: 把向量后端从"工厂能选"接到"运行时按配置生效"——server.rs hybrid/semantic 两条热路径经
> `resolve_vector_backend`/`parse_vector_backend` 读 `CONTEXTFORGE_VECTOR_BACKEND`（+ 可选
> `CONTEXTFORGE_VECTOR_DIM`，镜像 `resolve_data_dir` env pattern）选后端，未设/"" → BruteForce 字节等价（默认行为不变）、
> unknown/feature-off → factory 诚实 Err 经 `Status::internal` 暴露（无静默回退，ADR-013）；factory `select_vector_backend`
> 补 `"sqlite-vec"` arm（feature `vector-sqlite` double-half cfg 门控，对齐 qdrant/lancedb）；console
> `SearchResultItem` add-only `vector_score=16`（对齐 v1 `RetrievalResult.vector_score=13`）端到端贯通
> Rust producer → Go grpcclient → contractv1；retriever `mod.rs:325` 误导 WARN → 准确 no-op 契约说明。
> **默认行为 / proto 既有字段 / 既有契约不变**（ADR-004），三门 + lint 不退化。**ADR-037** 据 D1–D5 per-D ratify
> `Proposed → Accepted`（D2 sqlite-vec in-process recall/latency CELL honest-defer
> `[SPEC-DEFER:phase-future.sqlite-vec-inprocess-matrix]`；D4 真实 chunk source_type/agent_scope filter 受阻维度
> honest-defer）；**ADR-034** add-only Amendment（factory backend 覆盖补全，不溯改 D1-D5 body）。

## 1. 合入记录 (Phase 32, tasks 32.1–32.4)

| task | PR | delivery |
|---|---|---|
| 32.1 | #212（squash c7358ed） | `server.rs` hybrid(:340) + semantic(:382) 两热路径经 `resolve_vector_backend`/`parse_vector_backend` 选后端（读 `CONTEXTFORGE_VECTOR_BACKEND` + 可选 `CONTEXTFORGE_VECTOR_DIM`，镜像 `resolve_data_dir` env pattern）；未设/"" → BruteForce 字节等价（默认行为不变）；unknown/feature-off → factory 诚实 Err 经 `Status::internal`（无静默回退，ADR-013）；TEST-32.1.1（env name+dim parse/trim/blank→0 + unknown 诚实 Err）+ TEST-32.1.2（unset →("",0)→brute-force 字节等价）；0 新 dep |
| 32.2 | #213（squash 76a3137） | `factory.rs` `select_vector_backend` 补 `"sqlite-vec"` arm（feature `vector-sqlite` double-half cfg 门控，对齐 qdrant/lancedb）：feat on → `SqliteVecBackend::new()`（`name()`="sqlite-vec"）/ feat off → 诚实 Err（命名 sqlite-vec + vector-sqlite）；默认构建 TEST-32.2.1（feat-off 诚实 Err）+ TEST-32.2.2（selection-matrix wiring）→ factory 6/6；feat-on **真实 x86_64-pc-windows-msvc `cargo test --features vector-sqlite` 构建通过** → arm wiring 真实验证；in-process recall/latency CELL honest-defer `[SPEC-DEFER:phase-future.sqlite-vec-inprocess-matrix]`；0 新 dep |
| 32.3 | #214（squash eaa37bd） | (A) `console_data_plane.proto` `SearchResultItem` add-only `vector_score=16`（对齐 v1 `RetrievalResult.vector_score=13`）→ buf generate proto 重生 Go binding（真实 rawDesc descriptor 位移 0xdc→0xff，非 EOL churn；grpc/v1 文件回退保持 surgical）→ Rust producer `core/src/data_plane/search.rs` 写 vector_score（"vector" 命中=cosine / BM25=0，镜像 v1 server.rs:407）→ Go grpcclient `protoToSearchResult` 映射 VectorScore → contractv1 add-only VectorScore（ADR-015 add-only，承 task-20.1 Semantic 先例）。(B) `retriever/mod.rs:325` 误导 WARN → 准确 no-op 契约（chunks 表 SQL_SCHEMA §5.3 FROZEN 无 source_type/agent_scope 列；agent_scope 属 memory 层概念）→ 新 backlog `[SPEC-DEFER:phase-future.chunk-source-type-filter]` + `[SPEC-DEFER:phase-future.chunk-agent-scope-filter]`；TEST-32.3.1（grpcclient 携 VectorScore：semantic 真值/BM25=0）+ TEST-32.3.2（filter no-op：非空 source_type/agent_scope filter 结果与空 filter 逐字节相同） |
| 32.4 | this PR | Phase 32 closeout：v0.25.0 release docs + smoke v22 step 41 + ADR-037 据 D1-D5 per-D ratify + ADR-034 add-only Amendment（Phase 32）+ phase-32 §6 闭合 + adapter + phase-32 BDD feature |

## 2. S2V 状态

- Phase 32 `vector-backend-config-plumbing-and-completeness`: Draft → **Done**, Tasks 0 → 4。
- phase-32 §6 AC 全 `[x]`（据 32.1–32.4 真实交付；D2 sqlite-vec in-process recall/latency CELL honest-defer 如实，arm wiring + 真实 MSVC feat-on 构建达成 → 部分 ratify；D4 真实 chunk source_type/agent_scope filter honest-defer 重申）。
- 默认行为 / proto 既有字段 / 既有契约不变（ADR-004）；add-only proto 字段 + factory arm + no-op 契约说明不破协议；不溯改 Phase 1-31 spec（ADR-014 D5）。
- **本 release 兑现 backend config plumbing + factory 覆盖补全 + console provenance 贯通 + filter 契约诚实**：vector-backend-config-plumbing（server 热路径）/ sqlite-vec factory arm（覆盖补全）/ console vector_score add-only / filter no-op 契约更正兑现；sqlite-vec-inprocess-matrix / chunk-source-type-filter / chunk-agent-scope-filter / vector-backend-config-file / vector-dim-auto-negotiation 据真实受限诚实重申延后。

## 3. 验证证据

### 1. backend config plumbing（task-32.1，🟢）

`cargo test -p contextforge-core`（server backend resolve/parse）PASS：TEST-32.1.1 `parse_vector_backend`/`resolve_vector_backend` 读 `CONTEXTFORGE_VECTOR_BACKEND` env name + 可选 `CONTEXTFORGE_VECTOR_DIM`（parse/trim/blank→0），unknown name → factory 诚实 Err；TEST-32.1.2 env 未设 → `("", 0)` → BruteForce 路径与 v0.24.0 字节等价（默认行为不变）。server.rs hybrid(:340) + semantic(:382) 两热路径接线（镜像 `resolve_data_dir` env pattern）；unknown/feature-off 经 `Status::internal` 诚实暴露（无静默回退，ADR-013）。0 新 dep。

### 2. sqlite-vec factory arm（task-32.2，🟢/🟡）

默认构建 `cargo test -p contextforge-core`（factory selection）→ factory **6/6**：TEST-32.2.1 feat-off → 诚实 Err（命名 sqlite-vec + vector-sqlite）；TEST-32.2.2 selection-matrix wiring（brute/qdrant/lancedb/sqlite-vec arm 接线）。feat-on **真实 x86_64-pc-windows-msvc `cargo test --features vector-sqlite` 构建通过** → `sqlite_vec_with_feature_returns_sqlite_vec_backend`（`SqliteVecBackend::new()`，`name()`="sqlite-vec"）+ matrix feat-on 分支真实验证（非仅结构性）。**in-process recall/latency CELL（真实 KNN recall@k + latency on a corpus）honest-defer `[SPEC-DEFER:phase-future.sqlite-vec-inprocess-matrix]` —— 须本机 MSVC feature build + 真实语料，不伪造数值**（ADR-013）。0 新 dep（sqlite-vec 已为 Cargo.toml optional）。

### 3. console provenance add-only + retrieval-filter contract honesty（task-32.3，🟢）

`go test ./...`（grpcclient）PASS：TEST-32.3.1 grpcclient `protoToSearchResult` 携 VectorScore（semantic "vector" 命中=真实 cosine / BM25=0），端到端贯通 Rust producer `core/src/data_plane/search.rs`（镜像 v1 server.rs:407）→ Go grpcclient → contractv1 add-only VectorScore（ADR-015 add-only）。`console_data_plane.proto` `SearchResultItem` add-only `vector_score=16`（对齐 v1 `RetrievalResult.vector_score=13`）→ buf generate proto 重生 Go binding（真实 rawDesc descriptor 位移 0xdc→0xff，非 EOL churn；grpc/v1 文件回退保持 surgical）。TEST-32.3.2 filter no-op：非空 source_type/agent_scope filter 结果与空 filter **逐字节相同**——chunks 表（indexer SQL_SCHEMA §5.3 FROZEN）无 source_type/agent_scope 列、`SearchResult.source_type` 为 `DEFAULT_SOURCE_TYPE` 常量、agent_scope 恒空（属 memory 层概念，memory_items/migration 0013）；`retriever/mod.rs:325` 误导 WARN → 准确 no-op 契约说明，真实 chunk filter 属 import-path feature（importer 侧 source_type 标注 + schema migration）→ 新 backlog `[SPEC-DEFER:phase-future.chunk-source-type-filter]` + `[SPEC-DEFER:phase-future.chunk-agent-scope-filter]`（ADR-013，不伪造）。

### 3b. smoke v22（诚实记录）

`bash -n scripts/console_smoke.sh` exit 0（CI go-test 语法门 + `TestTask324`）。v22 step `[41/41]` 文档化向量后端配置接线状态；多为内部接线/契约修复无 console-api 运行时面，step 41 验默认构建仍 scaffold（默认行为/proto/契约不变，ADR-004）。既有 v13-v21 块 denominator 不溯改（新增 `[41/41]` add-only，ADR-014 D5）。

### 4. CI 状态（各 task PR 四门绿后自主合入）

| PR | cargo-test | go-test | spec-lint | lint |
|---|---|---|---|---|
| 32.1 (#212 server 热路径 backend select via env) | PASS | PASS | PASS | PASS |
| 32.2 (#213 sqlite-vec factory arm + double-half 门控) | PASS | PASS | PASS | PASS |
| 32.3 (#214 console vector_score add-only + filter no-op 契约) | PASS | PASS | PASS | PASS |
| 32.4 (this PR closeout) | PASS | PASS | PASS | PASS |

注：task-32.3 buf generate 的 Go binding 触真实 rawDesc descriptor 位移（0xdc→0xff，非 EOL churn）→ grpc/v1 文件回退保持 surgical；`core/tests/phase9_index_smoke.rs` Tantivy `LockBusy` 既有间歇 flake 命中时 `gh run rerun` 复跑至绿（承 v0.13–v0.24 pattern），never-merge-red 守线。

### 5. ADR-014 第二十三次激活 record（this closeout PR self-check）

`bash scripts/spec_drift_lint.sh --touched origin/master` → 0 未标注命中（CI spec-lint gate 复核）。各 task PR（32.1/32.2/32.3）触及行亦 0 未标注命中。

### 6. AC 达成（向量后端配置接线 + 覆盖补全 + v0.25.0 收口）

| Phase 32 §6 AC | 状态 | 依据 |
|---|---|---|
| AC1 backend config plumbing（server 热路径按 env 选后端 + 默认字节等价） | ✅ met | task-32.1（#212）；TEST-32.1.1/.2（§3.1） |
| AC2 sqlite-vec factory arm（覆盖补全 + 真实 MSVC feat-on 构建） | ✅ met（in-process matrix CELL honest-defer 如实） | task-32.2（#213）；factory 6/6 + 真实 feat-on 构建 🟢；in-process recall/latency CELL 🟡 延后（§3.2） |
| AC3 console vector_score add-only 贯通 + retrieval-filter no-op 契约诚实 | ✅ met | task-32.3（#214）；grpcclient 携 VectorScore 端到端 + filter 逐字节相同（§3.3）；chunk filter honest-defer 新 backlog |
| AC4 默认行为不变 + v0.25.0 closeout | ✅ met | task-32.4（this PR）；默认 `cargo test --workspace` 199/0 + `go test ./...` 全过 + release docs + smoke v22 + ADR-037 per-D ratify |
| AC5 ADR-014 cross-validation gate（第二十三次激活） | ✅ met | 本 closeout PR body（D1-D5，见 §6） |

## 4. 平台矩阵

| Platform | Build | Note |
|---|---|---|
| Windows MSVC (dev box) | 默认 build + `cargo test --workspace` 199 passed/0 failed + `go test ./...` 全过；clippy `-D warnings` exit 0；`go vet` clean；gofmt clean；smoke `bash -n` exit 0 | feat-on `cargo test --features vector-sqlite` 真实通过（x86_64-pc-windows-msvc）；rustc 1.95.0，2026-06-03 |
| Linux (GHA runner) | 默认 build + cargo/go test + spec-lint + lint PASS | CI 四门；sqlite-vec in-process matrix CELL 须本机 MSVC feature build + 语料（CI 默认不构建该 feature）honest-defer |
| 镜像 linux/amd64 | release.yml 推 GHCR（默认构建 0 新 dep / 0 network） | 承 v0.21–v0.24；arm64 DEFERRED |

## 5. Upgrade / Rollback

- Upgrade：默认行为 / proto 既有字段 / 既有契约不变（ADR-004，既有 v0.6–v0.24 client + 既有索引/数据无需改动）。新增 `CONTEXTFORGE_VECTOR_BACKEND`（+ 可选 `CONTEXTFORGE_VECTOR_DIM`）env 未设默认 → BruteForce 字节等价 + `SearchResultItem.vector_score=16` add-only proto 字段（既有 client 忽略）+ `select_vector_backend` 补 sqlite-vec arm（feature 门控，默认构建不启）+ retriever filter no-op 契约更正（行为本就 no-op，不破契约）。
- Rollback：`git tag -d v0.25.0` + 删 GitHub Release / ghcr tag；与 v0.24.0 行为兼容（backend 接线均 env-gated/默认 BruteForce 保留、proto/factory arm 均 add-only、filter 契约更正不改运行时行为，默认路径不变）。

## 6. ADR-014 第二十三次激活 record

- **D1（mapping）**：Phase 32 §6 AC1-5 ↔ task-32.1（§6 AC1-2）/ task-32.2（§6 AC1-2）/ task-32.3（§6 AC1-2）/ task-32.4（§6 closeout）。AC1↔32.1（backend config plumbing）/ AC2↔32.2（sqlite-vec factory arm）/ AC3↔32.3（console vector_score + filter no-op 契约）/ AC4↔32.4（release+ADR+§6）/ AC5↔本 PR body。
- **D2（lint）**：各 task PR（32.1/32.2/32.3）+ 本 closeout `spec_drift_lint.sh --touched origin/master` 0 未标注命中（CI spec-lint）。
- **D3（verified-by）**：每 AC 显式 owner TEST-id（TEST-32.1.x / 32.2.x / 32.3.x / 32.4.x）+ 真实证据（server backend resolve/parse + 默认字节等价 / factory 6/6 + 真实 MSVC feat-on 构建 / grpcclient 携 VectorScore 端到端 + filter 逐字节相同 / `cargo test --workspace` 199/0）。
- **D4（自治）**：主 agent ADR-012 自治据真实数据推进；never-merge-red + 禁据合成 ratify（ADR-013）守线；ADR-037 据 D1（backend config plumbing 达成）+ D2（sqlite-vec arm + double-half 门控 + selection-matrix WIRING + **真实 MSVC feat-on 构建** 达成；**in-process recall/latency CELL honest-defer 不强 ratify、不伪造**）+ D3（console provenance + filter 契约诚实达成）+ D4（honest-defer boundary 重申）+ D5（baseline 不变）真实非合成验证 ratify Accepted；受阻维度（sqlite-vec in-process matrix CELL / 真实 chunk source_type/agent_scope filter / native arm64 runner）据真实受限**如实记录**（不伪造）。tag / release outward-facing 不可逆 → 须用户显式授权（沿用 v0.13-v0.24 红线，不自行 tag）。
- **D5（不溯改）**：Phase 1-31 spec 未溯改；backend 接线 + ratify 记录写在 ADR-037 Ratification + ADR-034 add-only Amendment（Phase 32）+ task-32.1-32.4 §10 + 本文件；`SearchResultItem.vector_score=16` add-only proto 字段（既有字段不动）；`select_vector_backend` sqlite-vec arm add-only（brute/qdrant/lancedb 不动）；retriever filter no-op 契约更正（运行时行为本就 no-op，不改既有契约）；smoke 既有 v13-v21 块 denominator 不溯改（新增 v22 step 41 add-only）；`internal/cli/smoke_syntax_test.go` 加 `TestTask324`（add-only）；ADR-014 history Phase 1-31 不 retro-edit（23rd activation add-only）。

## 7. tag / 镜像 SHA（post-tag-push backfill）

post-tag-push backfill（无人值守授权下主 agent push（ADR-012） tag 触发 `release.yml` 后填实，承 v0.8–v0.24 pattern）：

- v0.25.0 annotated tag SHA：`cb3bb08ec4f9eafc63805d89cd88c373a0cc2b26`（master @ task-32.4 closeout merge #215）
- `release.yml` run ID：`26874124924`（conclusion **success** — build-push + cosign sign + cosign attest SBOM + provenance:mode=max 步全绿）
- 镜像 digest：`ghcr.io/tajiaoyezi/contextforge-daemon:v0.25.0` + `:latest` = `sha256:d758f6760370553290362f8a2a36f325f993244512b93abd5bd74084973b97f4`（linux/amd64; arm64 DEFERRED）
- cosign keyless 签名凭据：release run 内 ephemeral keys + **Rekor 透明日志** tlog idx `1708460120`（cosign sign 镜像 digest）/ `1708461746`（cosign attest SPDX SBOM）。验证：`cosign verify ghcr.io/tajiaoyezi/contextforge-daemon:v0.25.0 --certificate-identity-regexp '^https://github.com/tajiaoyezi/contextforge/\.github/workflows/release\.yml@.*$' --certificate-oidc-issuer https://token.actions.githubusercontent.com`
