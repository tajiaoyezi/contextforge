# ContextForge v0.3.0 Artifacts

**日期**：2026-05-24
**Release Tag**：`v0.3.0` (annotated git tag pushed to origin)
**Commit (HEAD)**：`<待回填 — closeout PR merge SHA>`

## 制品清单（per ADR-007 minimal-tarball-distribution）

> 与 v0.2.0 一致 — v0.3 不改 release artifact 格式；新增的 Console Contract v1
> REST surface 通过 `contextforge` 二进制内嵌的 `console-api-serve` 子命令暴露
> （无新独立二进制）。

| 文件名 | 平台 | 大小 (近似) | 备注 |
|---|---|---|---|
| `contextforge-linux-amd64-v0.3.0.tar.gz` | Linux x86_64 | ~50MB | 官方 release tarball |

### tarball 内容

```
contextforge-linux-amd64-v0.3.0/
├── README.md                     # v0.3 quick start + Console 集成段
├── RELEASE_NOTES.md              # v0.3.0 + 历史
├── LICENSE                       # all-rights-reserved (待 v1.0 切换)
├── bin/
│   ├── contextforge              # Go 控制面 CLI（含 console-api-serve 子命令）
│   └── contextforge-core         # Rust 数据面 daemon
├── examples/
│   └── quickstart/               # v0.2 fixture 不变
├── deploy/
│   └── console-stack.yml         # docker compose stack（v0.3 新增）
├── docs/
│   └── consoleapi/
│       └── openapi.yaml          # 9 endpoint OpenAPI 3.0（v0.3 新增）
└── scripts/
    ├── quickstart_smoke.sh       # v0.2 7-step CLI smoke
    └── console_smoke.sh          # v0.3 Console Contract v1 9-endpoint smoke
```

### 校验和

```
SHA256(contextforge-linux-amd64-v0.3.0.tar.gz) = <待回填 release pipeline 实际产物>
```

## 平台矩阵

| 平台 | v0.1 | v0.2 | v0.3 | 备注 |
|---|---|---|---|---|
| Linux x86_64 | ✅ release tarball | ✅ release tarball | ✅ release tarball | 官方目标 |
| Linux ARM64 | ❌ | ❌ | ❌ | v0.4 评估 |
| macOS x86_64 | 🟡 源码 build | 🟡 源码 build | 🟡 源码 build | nice-to-have；v0.3 smoke 跑过 |
| macOS ARM64 | 🟡 源码 build | 🟡 源码 build | 🟡 源码 build | nice-to-have |
| Windows native | 🟡 nice-to-have | 🟡 nice-to-have | 🟡 nice-to-have | 单元 / e2e 测试可跑（PR #15+ 起）；P0 release gate 仍 Linux/WSL2 |
| WSL2 | ✅ | ✅ | ✅ | Linux 派生 |
| Docker | 🟡 通过源码 | ✅ docker compose ready | ✅ multi-stage Dockerfile + 5-service compose | v0.3 新增 deploy/console-stack.yml |

## Docker 制品（v0.3 新增）

| 制品 | 形态 | 备注 |
|---|---|---|
| `contextforge-daemon:v0.3.0` | 本仓 Dockerfile build | `docker compose -f deploy/console-stack.yml up -d contextforge` 自动 build |
| `contextforge-console-api:v1.0.0` | 外部 (Console 仓库 ship) | 公网未发布；env `CONSOLE_API_IMAGE` override |
| `contextforge-console-web:v1.0.0` | 外部 (Console 仓库 ship) | 公网未发布；env `CONSOLE_WEB_IMAGE` override |

## Release 上传策略（继承 v0.2 + v0.3 调整）

1. **手动**：未在 v0.3.0 期间触发；本 release 文档化产物清单 + 校验和占位
2. **GitHub Release**：留 release pipeline 在 release runner 上构建实际 tarball + Docker image + push GHCR
3. **校验签名**：未在 v0.3.0 实施；留 v1.0

## 下载 / 升级路径

- v0.2.0 → v0.3.0：替换 binary 即可（Migration notes 见 RELEASE_NOTES v0.3.0
  段）。Console UI 集成依赖 Console v1.0 docker image 公网发布。
- v0.1.0 → v0.3.0：先 v0.1 → v0.2 (Phase 9 CLI 数据通路修复)，再 v0.2 → v0.3
  (Console Contract v1)。
