# ContextForge v0.28.0 Artifacts — observability-hardening (rust-silent-failure-surfacing + go-silent-failure-surfacing + 7→3-4 grounding correction + ADR-040 ratified)

> Artifact inventory for the v0.28.0 minor release (Phase 35). Mirrors the v0.27.0 artifacts structure.
> The default build is unchanged: 0-new-dependency, 0-network；默认行为 / proto 既有字段 / 既有契约不变。
> 承 Phase 31/33 治理债血脉，把热路径中被静默吞掉的真实错误显式化（surface genuinely-swallowed errors），镜像仓库既有 stderr 惯例（Rust `eprintln!` / Go `fmt.Fprintf(os.Stderr)`）：index_session_backend **4 处** `store.append`（`let _ =` → `if let Err(persist_err) { eprintln! }`）+ retriever `:415` Tantivy/SQLite desync（`Err(_) => continue` → `Err(e) => { eprintln!; continue }`）+ setVectorEnv `config.Load`/`os.Setenv` 失败（→ `fmt.Fprintf(os.Stderr)`，`errors.Is(os.ErrNotExist)` 守护 missing 静默）。**observability-only**——best-effort 契约不变（indexing 不阻断、query 续行、daemon 不阻断，绝不转 fail-fast）。7→3-4 grounding 校正：DROP/LEAVE 4 处已显式化/有意 site（不引新 metrics facility）。既有 22-endpoint + 5 Memory RPC + 运行时二进制默认行为不变。No breaking contract bump。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：`cmd/contextforge/main.go` `setVectorEnv` 的 `config.Load` 错误分支 add-only `if !errors.Is(err, os.ErrNotExist) { fmt.Fprintf(os.Stderr, "contextforge: vector config load failed (%s): %v\n", dataDir, err) }`（malformed/unreadable 报警、MISSING config.toml 静默、best-effort `return restore` 不变）+ `os.Setenv` 失败分支补 `fmt.Fprintf(os.Stderr, ...)`（既有仅成功记 restore，restore 语义不变）；import `errors`。运行时既有 22-endpoint + 5 Memory RPC 行为不变。`internal/consoleapi/memstore.go` **0 改动**（nil-sink honest non-issue：唯一生产调用点 `console_api_serve.go:109` 紧随无条件 `SetEventSink`:112）。
- `contextforge-core`（Rust 数据面 daemon）：`jobs/index_session_backend.rs` **4 处** `store.append`（progress/index-error/commit-error/cancelled）`let _ =` → `if let Err(persist_err) { eprintln!("WARN indexing-event persist failed (job=..., stage=...): {persist_err}") }`（best-effort 保留，不阻断 indexing）；`eb.send` 各处保留 as-is（broadcast 无订阅者返 Err 是正常态 intentional）。`retriever/mod.rs:415` `Err(_) => continue` → `Err(e) => { eprintln!("WARN retriever: chunk {chunk_id} present in index but missing from SQLite (desync), skipping: {e}"); continue }`（skip 行为保留）。镜像 `search.rs:108-113` 既有 eprintln! WARN 惯例；不引入 log/tracing/metrics facility（core 无，severity 是消息前缀）。默认构建 0 新依赖 / 0 network；`cargo test -p contextforge-core --lib` = lib 212 all PASS。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.28.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network）+ cosign keyless 签 + SBOM + provenance（承 v0.21.0–v0.27.0）。linux/arm64 仍 DEFERRED。
- Feature 构建：本 release 无新 feature flag；surfacing 在默认构建路径（observability-only，best-effort 不转 fail-fast）。

## 2. 源码档

- master tag SHA：`<backfill: tag commit SHA>`（v0.28.0 annotated tag 对象 `<backfill: tag object SHA>`，无人值守授权下主 agent push（ADR-012））
- Phase 35 代码路径：
  - task-35.1（PR #229, squash 9a57647；rust-silent-failure-surfacing）：`core/src/jobs/index_session_backend.rs`（**4 处** `store.append` `let _ =` → `if let Err(persist_err) { eprintln! }`，best-effort 保留；`eb.send` 保留 as-is）+ `core/src/retriever/mod.rs`（`:415` `Err(_) => continue` → `Err(e) => { eprintln!; continue }`，skip 保留）+ TEST-35.1.1（真实 store best-effort 行为锁）+ TEST-35.1.2（删 chunks 行造 desync → search 优雅跳过）；lib 209→212。grounding：store 具体类型无 trait → 不引 trait 注入失败 double（scope creep），error 分支 inspection-verified；retriever `:373` surgical 留 as-is。0 新依赖。
  - task-35.2（PR #230, squash 69fc367；go-silent-failure-surfacing）：`cmd/contextforge/main.go` `setVectorEnv` `config.Load` 错误（`errors.Is(os.ErrNotExist)` 守护：missing 静默/malformed 报警）+ `os.Setenv` 失败 → `fmt.Fprintf(os.Stderr)`，镜像 `daemon/rest.go:110`，best-effort 保留 + `main_test.go` `captureStderr`（os.Pipe）+ `TestSetVectorEnv_LoadErrorSurfacing`（malformed→WARN / missing→no WARN / valid→no WARN）；`internal/consoleapi/memstore.go` 0 改动（nil-sink honest non-issue grounding 校正）。`go test ./...` + `go vet` clean。0 新依赖。
  - task-35.3（this PR）：observability-hardening 7→3-4 grounding 校正（4 处 DROP/LEAVE 不改代码，不引新 metrics facility）；`scripts/console_smoke.sh`（smoke v25 step 44 → [44/44]，banner v24→v25，staging cf-v27-cfg）；`internal/cli/smoke_syntax_test.go`（`TestTask353` 无回退 [37/37]..[43/43]）；`docs/releases/v0.28.0-{evidence,artifacts}.md`；`README.md` v0.28 段；`RELEASE_NOTES.md` v0.28.0 段；`docs/decisions/adr-040-...md`（ratify Proposed→Accepted，per-D）+ ADR-031 add-only Phase 35 Amendment；`docs/specs/phases/phase-35-observability-hardening.md`（§6 闭合）；`docs/s2v-adapter.md`；`test/features/phase-35-observability-hardening.feature`。

## 3. 验证脚本

```bash
# 默认构建（0 新依赖 / 0 network）
cargo test -p contextforge-core --lib                     # lib 212 all PASS
go test ./...                                             # all pass (incl TestSetVectorEnv_LoadErrorSurfacing + TestTask353)
cargo clippy --workspace --all-targets -- -D warnings     # 0 warning
go vet ./...                                              # clean
bash -n scripts/console_smoke.sh                          # exit 0

# task-35.1 rust-silent-failure-surfacing（真实 store best-effort 行为锁 + retriever desync 优雅跳过）
cargo test -p contextforge-core test_35_1                 # TEST-35.1.1 / TEST-35.1.2

# task-35.2 go-silent-failure-surfacing（setVectorEnv malformed→WARN / missing→no WARN / valid→no WARN）
go test ./cmd/contextforge/ -run TestSetVectorEnv         # TEST-35.2.1（含既有 TestSetVectorEnv）

# 端到端 smoke v25（step 44 → [44/44]；既有 step 不退化）
go test ./internal/cli/ -run TestTask353

# D5 spec-drift lint
bash scripts/spec_drift_lint.sh --touched origin/master   # 0 unannotated hits
```

## 4. 镜像推送

- `release.yml`（`v*` tag push 触发）→ buildx → ghcr login → build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.28.0` + `:latest`（**linux/amd64**，net-zero 与 v0.27.0 一致）→ cosign keyless 签 + attest SPDX SBOM + provenance:mode=max（承 task-28.2 机制）。
- run ID + digest：`release.yml` run `<backfill: run id>`（**`<backfill: success>`**，build-push + cosign sign + attest SBOM + provenance:mode=max 步全绿）；`ghcr.io/tajiaoyezi/contextforge-daemon:v0.28.0` + `:latest` = `<backfill: sha256 digest>`；git tag SHA `<backfill: tag commit SHA>`；cosign Rekor tlog idx `<backfill: sign tlog>`（sign）/ `<backfill: attest tlog>`（attest SBOM）。

## 5. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-040 observability-hardening | **Accepted**（per-D，D1 rust surfacing 🟢 + D2 go surfacing 🟢 + memstore nil-sink honest non-issue grounding 校正；D3 7→3-4 grounding 校正） | task-35.3 据 35.1/35.2 真实非合成验证 ratify：D1 Accepted（index_session_backend store.append ×4 + retriever desync 经 eprintln! WARN，best-effort 保留，TEST-35.1.1/.2；Rust eprintln! 输出 guard/inspection 不伪造 stderr-assert `[SPEC-DEFER:phase-future.rust-stderr-output-assertion]`）/ D2 Accepted（setVectorEnv config.Load/Setenv 经 fmt.Fprintf(os.Stderr)，IsNotExist 守护 missing 静默，stderr-capture RED→GREEN TEST-35.2.1；memstore nil-sink honest non-issue DROP `[SPEC-DEFER:phase-future.memstore-degraded-observability-warn]`）/ D3 7→3-4 grounding 校正（4 处 DROP/LEAVE，不引新 metrics facility）/ D4 default 0-dep/0-network + 既有契约不变（best-effort 不转 fail-fast）/ D5 baseline 不变（cargo/go/clippy/vet 绿；add-only eprintln!/Fprintf 旁路）|
| ADR-031 observability-hardening | Accepted（+ Phase 35 add-only Amendment） | task-35.1/35.2 承其 stderr/best-effort surfacing 方向延伸热路径静默错误；不溯改 D1–Dn 正文 + 既有 Amendment 正文（ADR-014 D5） |
| ADR-036 governance-debt-cleanup | Accepted（守线·镜像引用） | task-31.3 确立的 Go `fmt.Fprintf(os.Stderr)` audit-surfacing pattern 即本 phase task-35.2 镜像之源；其已修的 server.go:298 即本 phase D3 grounding 校正 DROP（already-done）的站点 |
| ADR-013 据实延后/校正 | Accepted（守线） | Rust eprintln! 输出仓库不断言 → guard/inspection 不伪造 stderr-assert；memstore nil-sink honest non-issue（生产 sink 总接线）；7→3-4 grounding 校正据实记录；tag SHA / run id / digest / tlog 未知前以 `<backfill>` 占位不伪造凭据 |
| ADR-014 add-only / cross-validation | Accepted（守线，第二十六次激活） | eprintln!/Fprintf add-only 旁路（不改控制流）+ memstore.go 0 改动 + Amendment 不溯改正文（D5）|
| ADR-004 / ADR-008 / ADR-012 | Accepted（守线） | 默认 0 新依赖 / 0 network（不引 log/tracing/metrics facility，Rust core 0 toml）；surfacing observability-only（best-effort 不转 fail-fast）；tag-release 须授权（本轮经 AskUserQuestion 2026-06-04 已授权）|

## 6. 版本声明（contract bump 评估）

- 本 release 新增热路径静默错误显式化（index_session_backend store.append ×4 + retriever desync 经 eprintln! WARN / setVectorEnv config.Load/Setenv 经 fmt.Fprintf(os.Stderr)）+ 7→3-4 grounding 校正（4 处 DROP/LEAVE）+ smoke v25——**无 proto 既有字段 / RPC / endpoint 删改、无默认运行时行为改动**（surfacing observability-only，仅 stderr 旁路 WARN，best-effort 不转 fail-fast）。
- 默认构建 0 新代码依赖 / 0 network（ADR-004 / ADR-008，不引 log/tracing/metrics facility，Rust core 保持 0 toml 依赖）。
- 故 **无 breaking contract bump**；既有 v0.6–v0.27 client + 既有索引/数据无需改动。**observability-hardening 收口**。

## 7. 后续模板（observability follow-on）

- structured metrics/counter facility for observability（core 现无 facility，引入须 simplicity-first + exposure path 评估）`[SPEC-DEFER:phase-future.observability-metrics-facility]`。
- memstore nil-sink degraded warn（经 grounding 校正为 honest non-issue：生产 sink 总接线；仅当未来出现 sink-optional 的生产路径才需要）`[SPEC-DEFER:phase-future.memstore-degraded-observability-warn]`。
- Rust eprintln! stderr-output 自动断言（仓库惯例不断言 eprintln! 输出，须 stderr-capture 设施）`[SPEC-DEFER:phase-future.rust-stderr-output-assertion]`。
- retriever `:373` `searcher.doc()` 失败 surface（Tantivy doc-store 读，不同子系统）`[SPEC-DEFER:phase-future.tantivy-docstore-read-surface]`。
- （承前）vector-dim feature-enforce `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]`；sqlite-vec in-process matrix `[SPEC-DEFER:phase-future.sqlite-vec-inprocess-matrix]`；qdrant live recall `[SPEC-DEFER:phase-future.qdrant-live-recall]`；L2 embedding-cache true-LRU `[SPEC-DEFER:phase-future.l2-cache-true-lru]`；indexing.* replay e2e `[SPEC-DEFER:phase-future.indexing-replay-e2e]`；daemon datadir env→`daemon.Options.DataDir` `[SPEC-DEFER:phase-future.daemon-options-datadir]`；multi-arch 原生 arm64 runner `[SPEC-DEFER:phase-future.multi-arch-native-runner]`。

## 8. 存档指引

- closeout PR 合入后，**无人值守授权下**主 agent push `v0.28.0` annotated tag 触发 `release.yml`；post-tag-push backfill PR 填实 tag SHA + run ID + 镜像 digest + cosign 凭据（承 v0.8–v0.27 pattern；§7/§4 角括号 `<backfill>` 标记由本 backfill PR 回填）。
- Dockerfile workspace member COPY 完整性预检（承 v0.11.0 PR #139 教训）：本 release **无新 migration**——Rust `index_session_backend.rs` / `retriever/mod.rs` 改动在既有 `core/` member，Go `setVectorEnv` 改动在既有 Go member，无新依赖 / 无 proto 字段新增；镜像为默认构建（0 新依赖 / 0 network，linux/amd64）。
