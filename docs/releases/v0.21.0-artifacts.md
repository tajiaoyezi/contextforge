# ContextForge v0.21.0 Artifacts — release-ci-hardening (multi-arch（arm64 deferred）+ 匿名可拉取守护 + cosign 签名 + SBOM + provenance + CI 强 lint + ADR-033 ratified)

> Artifact inventory for the v0.21.0 minor release (Phase 28). Mirrors the v0.20.0 artifacts structure.
> The default build is unchanged: 0-new-dependency, 0-network. 全部改动在 `.github/workflows/*`（release.yml
> cosign 签名 + SBOM + provenance / verify-image.yml 匿名 pull + cosign verify / ci.yml lint job）+ surgical
> clippy/gofmt 修复——镜像运行时行为 + 默认构建 baseline 不变；既有 22-endpoint + 5 Memory RPC + 运行时二进制
> 不变。0 新代码依赖（cosign / syft / QEMU 为 CI action 工具）。No breaking contract bump。arm64 镜像 DEFERRED。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：本 release **运行时无变化**——既有 22-endpoint + 5 memory route + console-api 行为不变；仅 surgical gofmt 修复（15 real files，CI/LF 权威；non-functional 格式化，0 行为改动）+ go vet 0 清。
- `contextforge-core`（Rust 数据面 daemon）：本 release **运行时无变化**——仅 surgical clippy 修复（clippy `--fix` + manual + 2 targeted allow：generated `pb` / `pb_console` `#![allow(clippy::all)]` + `EventBus::send` `#[allow(clippy::result_large_err)]`；既有签名不变）；clippy `-D warnings` exit 0 + `cargo test --workspace` all pass（core lib 187 passed）。默认构建 0 新依赖 / 0 network。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.21.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network；与 v0.20.0 baseline 一致 net-zero）+ cosign keyless 签 manifest digest + cosign attest SPDX SBOM（syft）+ provenance:mode=max。**linux/arm64 DEFERRED**（QEMU emulation 实测不可行 → `[SPEC-DEFER:phase-future.multi-arch-native-runner]`）。
- Feature 构建：本 release 无新 feature；既有 `vector-*`/`embedding-*`/`reranker-*` 不变。clippy/gofmt 修复 + workflow 改动全用既有 toolchain（无 Cargo.toml / go.mod 改动）。

## 2. 源码档

- master tag SHA：`08ba7202921c7e8cfcee9dfb76c2a634dd3f0c94`（v0.21.0 annotated tag，用户授权 2026-06-02 主 agent push（ADR-012））
- Phase 28 代码路径：
  - task-28.1（multi-arch + 匿名可拉取）：`.github/workflows/verify-image.yml`（add-only 未鉴权 logged-out `docker pull` 守护步 + index/digest parity 复核；既有鉴权 pull + `/v1/health` `contract_version=v1` 步保留）；`.github/workflows/release.yml`（`platforms: linux/amd64` 保单架构——arm64 经 `setup-qemu-action` workflow_dispatch 实测不可行 DEFERRED，net-zero）。
  - task-28.2（cosign 签名 + SBOM + provenance）：`.github/workflows/release.yml`（`permissions: id-token: write` + `sigstore/cosign-installer` + `cosign sign` manifest digest + `cosign attest` SPDX SBOM via `anchore/sbom-action`（syft）+ `docker/build-push-action` `provenance: mode=max`；GitHub-native `actions/attest-*` 试后因私有 repo 不可用 → 切 cosign）；`.github/workflows/verify-image.yml`（`cosign verify` + `cosign verify-attestation` 步）。
  - task-28.3（CI 强 lint + 存量修复）：`.github/workflows/ci.yml`（add-only `lint` job：clippy `-D warnings` + gofmt + go vet 全阻断）；clippy 存量修复（`core/src/**` clippy `--fix` + manual + `core/src/pb*`/`pb_console*` `#![allow(clippy::all)]` + `EventBus::send` `#[allow(clippy::result_large_err)]`）；gofmt 修复（`internal/**` 15 real files `gofmt -w` + strip-pipe）。
  - task-28.4（this PR）：`scripts/console_smoke.sh`（v18 发布硬化文档化 step）；`internal/cli/smoke_syntax_test.go`（`TestTask284_*`）；`docs/releases/v0.21.0-{evidence,artifacts}.md`；`README.md` v0.21 段；`RELEASE_NOTES.md` v0.21.0 段；`docs/decisions/adr-033-release-ci-hardening.md`（ratify Proposed→Accepted，D1 PARTIAL）+ `adr-007-minimal-tarball-distribution.md`（add-only Amendment）；`docs/specs/phases/phase-28-release-ci-hardening.md`（§6 闭合）；`docs/s2v-adapter.md`；`test/features/phase-28-release-ci-hardening.feature`。

## 3. 验证脚本

```bash
# 默认构建（0 新依赖 / 0 network）
cargo test --workspace
go test ./...

# CI 强 lint（task-28.3，阻断门）
cargo clippy --workspace -- -D warnings      # TEST-28.3.1 exit 0（存量全修 + 2 targeted allow）
gofmt -l ./internal ./cmd                    # TEST-28.3.2 CI/LF 权威 0（local Windows-CRLF false positive 不计）
go vet ./...                                  # TEST-28.3.2 0

# 端到端 smoke v18（含发布硬化文档化 step；既有 step 不退化）
bash scripts/console_smoke.sh        # 期望末行 CONSOLE_REAL_SMOKE_EXIT=0
bash -n scripts/console_smoke.sh     # 语法门 exit 0（CI go-test TestTask284_*）

# D2 spec-drift lint
bash scripts/spec_drift_lint.sh --touched origin/master

# 发布硬化（CI / release run 层，非本机；真实 run IDs 见 evidence §3）
#   verify-image.yml 匿名 pull 守护    → run 26788773926 (success)
#   cosign sign + attest + verify      → local registry run 26799480280 (success)
#   ci.yml lint job                    → run 26820737785 (PASS)
#   真实 GHCR 签名 / multi-arch         → v0.21.0 release run（post-tag-push backfill）
```

## 4. 镜像推送

- `release.yml`（`v*` tag push 触发）→ `docker/setup-buildx-action` → ghcr login → `docker/build-push-action@v5` build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.21.0` + `:latest`（**linux/amd64**，net-zero 与 v0.20.0 一致）→ 取 build-push-action 输出 digest → `cosign sign`（keyless OIDC `id-token: write`）+ `cosign attest` SPDX **SBOM（syft）** + `provenance: mode=max`。
- 镜像 = **默认构建**：0 新依赖 / 0 network；运行时行为与 v0.20.0 baseline 兼容（仅 surgical clippy/gofmt 格式化，0 行为改动）。**linux/arm64 DEFERRED**（QEMU emulation 实测不可行 → `[SPEC-DEFER:phase-future.multi-arch-native-runner]`）。
- **cosign 签名机制经 local registry 端到端 VERIFIED**（run `26799480280` success：sign + attest + verify + verify-attestation 全通过）；**真实 GHCR 镜像签名 / SBOM / provenance attestation 已于 v0.21.0 release run（用户授权 2026-06-02）兑现**（run `26822648951` success：cosign sign + cosign attest SBOM 步绿 + Rekor tlog idx 1703992493/1703993393）。
- run ID + digest：`release.yml` run `26822648951`（**success**，cosign sign + attest + provenance 步全绿，真实 GHCR digest keyless 签名）；`ghcr.io/tajiaoyezi/contextforge-daemon:v0.21.0` + `:latest` = `sha256:f2cb3aadd5608a42e247aad562cb64f1c260dee1a5f4909c862e6484044d1e46`；git tag SHA `08ba7202921c7e8cfcee9dfb76c2a634dd3f0c94`；独立 `cosign verify` + `cosign verify-attestation` 经 `verify-image.yml` run `26823246086`（tag=v0.21.0）。

## 5. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-033 release-ci-hardening | **Accepted** | task-28.4 据 task-28.1/28.2/28.3 真实非合成验证 ratify（Proposed→Accepted）：D1 PARTIAL（anon-pull 守护真实 run `26788773926` success；**arm64 multi-arch QEMU emulation 实测不可行 run `26757640892` 45min cancelled → DEFERRED `[SPEC-DEFER:phase-future.multi-arch-native-runner]`**）/ D2 Accepted（cosign sign + attest SBOM + provenance 机制经 local registry run `26799480280` 端到端验过；GitHub-native attestation 私有 repo 不可用 run `26789731232` failure → 切 cosign；**真实 GHCR 签名已于 release run `26822648951` 兑现**：cosign sign + attest 步绿 + Rekor tlog，digest `sha256:f2cb…4d1e46`）/ D3 Accepted（`lint` job 入门 + 真实存量 CI/LF 权威 gofmt 15 / go vet 0 / clippy ~33 全修 + clippy exit 0，run `26820737785` PASS）/ D4 Accepted（镜像运行时 / 默认 0-dep / 0-network baseline 不变）；受阻维度（arm64 / 真实签名 / native-attestation）据真实受限如实记录（ADR-013，不伪造） |
| ADR-007 minimal-tarball-distribution | Accepted（+ Phase 28 add-only Amendment） | 部署发布面由「最小 tarball + 单架构镜像」扩展到 **multi-arch 签名 OCI 镜像 + SBOM + provenance**（arm64 经实测 DEFERRED→native-runner），以 add-only Amendment 记录扩展结果（不溯改正文 D1-D5，ADR-014 D5）；详见 ADR-007 §Amendment (Phase 28 / v0.21.0) |
| ADR-004 local-first-privacy-baseline | Accepted | 守线：multi-arch / 签名 / lint 均 build / publish / CI 层，镜像运行时 0-network + 默认构建 0 新依赖 baseline 不变 |
| ADR-012 main-agent-governance-autonomy | Accepted（守线） | tag / release outward-facing 不可逆（签名 / 多架构推送）→ 须用户显式授权（沿用 v0.13-v0.20 模式，不自行 tag）；CI / lint 改动主 agent 自治推进 |
| ADR-008 core-library-selection | Accepted（无 amendment） | 0 新 direct dep（`core/Cargo.toml` / `Cargo.lock` / `go.mod` 未改；cosign / syft / QEMU 为 CI action 工具非 build dep）→ 无依赖变更 Amendment |
| ADR-013 no-fake-evidence | Accepted | 守线：ADR-033 ratify 据真实 CI run（verify-image / local-registry / lint）；arm64 DEFERRED + 真实 GHCR 签名尚未发生 + GitHub-native attestation 私有 repo 不可用 + local-CRLF vs CI-LF 存量差异 均诚实记录不伪造 |
| ADR-014 cross-phase-exit-criteria-validation | Accepted | 第十九次激活（v0.21）；history Phase 1-27 不 retro-edit |

## 6. 版本声明（contract bump 评估）

- 本 release 改动全在 `.github/workflows/*`（release.yml cosign + SBOM + provenance / verify-image.yml 匿名 pull + cosign verify / ci.yml lint job）+ surgical clippy/gofmt 修复——**无 proto / 无 RPC / 无 endpoint / 无运行时行为改动**；clippy/gofmt 修复为 non-functional 格式化 + 2 targeted allow（既有签名不变）。
- 默认构建 0 新代码依赖 / 0 network（ADR-004）；cosign / syft / QEMU 为 CI action 工具（非 Cargo / go.mod direct dep）。
- 故 **无 breaking contract bump**；既有 v0.6–v0.20 client 无需改动。multi-arch（arm64 deferred）/ 签名 / SBOM / 匿名 pull 守护 / CI lint 均 build / publish / CI 层。**CI / release-only 收口**（运行时二进制不变）。

## 7. 后续模板（release-ci-hardening follow-on）

- 多架构构建加速 / 原生 arm64 runner（替代 QEMU emulation）`[SPEC-DEFER:phase-future.multi-arch-native-runner]`——本 release arm64 经 QEMU emulation 实测不可行 DEFERRED；原生 runner / 交叉编译留后续。
- 签名密钥轮换 / 私有 KMS / Rekor 透明日志自托管 `[SPEC-DEFER:phase-future.signing-key-management]`——本 release 用 cosign keyless（Fulcio / Rekor 公共实例）。
- clippy / gofmt 存量告警全量清零（含 generated `pb` / `EventBus::send` 2 targeted allow 的根因消除）`[SPEC-DEFER:phase-future.lint-backlog-cleanup]`——本 release 阻断门入 + 触及存量全修，2 处 surgical allow 留后续根因清零。
- GitHub-native attestation（`actions/attest-build-provenance` / `actions/attest-sbom`）`[SPEC-DEFER:phase-future.github-native-attestation]`——本 release 因 user-owned 私有 repo 不可用切 cosign；repo 转 public / org 后可评估 native attestation。
- rustfmt CI 卡红门 `[SPEC-DEFER:phase-future.rustfmt-gate]`——本 release lint job 含 clippy + gofmt + go vet；rustfmt `--check` 卡红留后续。
- golangci-lint（超 gofmt + go vet 的全量 linter 集）`[SPEC-DEFER:phase-future.golangci-lint]`——本 release Go 侧门为 gofmt + go vet。
- 镜像瘦身 / distroless 运行时基座 `[SPEC-DEFER:phase-future.image-slim-distroless]`——本 release 镜像运行时基座不变。
- release.yml 之外的 SLSA L3+ / 可复现构建（reproducible build）`[SPEC-DEFER:phase-future.reproducible-build-slsa]`——本 release 落 provenance:mode=max + SBOM；SLSA L3+ / reproducible build 留后续。

## 8. 存档指引

- closeout PR（#191）合入后，**用户授权 2026-06-02** 主 agent push `v0.21.0` annotated tag（`08ba720`）触发 `release.yml`（run `26822648951` success）；**v0.21.0 真实 GHCR 镜像 cosign 签名 / SBOM / provenance attestation 已于此 release run 兑现**（cosign sign + attest 步绿 + Rekor tlog）；本 post-tag-push backfill PR 填实 tag SHA + run ID + 镜像 digest + cosign 凭据（承 v0.8–v0.20 pattern）。
- Dockerfile workspace member COPY 完整性预检（承 v0.11.0 PR #139 教训）：本 release 改动均在 `.github/workflows/*` + `core/src/`（surgical clippy）+ `internal/`（surgical gofmt）+ docs/scripts member 内——**无新增运行时源码 / migration / proto**（Dockerfile 无需改动）；镜像为默认构建（0 新依赖 / 0 network，linux/amd64，arm64 DEFERRED）。
