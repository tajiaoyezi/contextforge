# ContextForge v0.39.0 Artifacts — v1.0-docs-and-release-flow (v1.0 收口第二步：D3 文档对齐 + D4 GitHub Release 流程)

> Artifact inventory for the v0.39.0 minor release (Phase 46). **0 代码逻辑改动 / 0 新依赖 / 0 network / 0 schema migration / 0 proto**。
> 纯文档 + 1 CI step（release.yml GitHub Release 对象自动创建）。D4 首次实践：v0.39.0 tag push 将首次创建 GitHub Release 对象。

## 1. 二进制档
- `contextforge`（Go 控制面 CLI）：本 release **0 Go 业务代码改动**（task-46.3 仅改 release.yml CI + smoke 脚本 + smoke_syntax_test.go）。
- `contextforge-core`（Rust 数据面）：本 release **0 Rust 代码改动**。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.39.0` + `:latest`（linux/amd64）+ cosign keyless 签 + SBOM + provenance（承 v0.21–v0.38）+ **新增 GitHub Release 对象自动创建**（softprops/action-gh-release@v2，tag push 触发，body 从 RELEASE_NOTES.md v0.39.0 段提取 + signed-image/SBOM provenance footer）。
- Feature 构建：本 release **0 新 feature flag**；feature-build 矩阵 10/10 不退化。

## 2. 源码档
- master tag SHA：`<backfill: tag-sha>`（v0.39.0 annotated tag 对象 `<backfill: tag-object>`，用户授权 ADR-012）
- Phase 46 代码路径：task-46.1 README.md（重构）+ task-46.2 CHANGELOG.md + docs/decisions/README.md（新增）+ task-46.3 release.yml + console_smoke.sh + smoke_syntax_test.go + release docs + ADR ratify + Amendment + roadmap/adapter。

## 3. 测试档
- `go test ./...`：全过（含 TestTask463 + TestTask454 no-regression + 全量 cli 测试）。
- `cargo test --workspace`：全绿（0 Rust 改动）。
- `bash -n scripts/console_smoke.sh`：语法 PASS（v36[55/55]）。
- CI spec-lint：全绿。

## 4. 发布档
- GitHub Release：v0.39.0（tag push 后 release.yml **首次自动创建 GitHub Release 对象**，D4 落地；body 从 RELEASE_NOTES.md v0.39.0 段 + signed-image/SBOM provenance footer）。
- run id / ghcr digest / cosign tlog：`<backfill: run-id>` / `<backfill: ghcr-digest>` / `<backfill: tlog-sign> · <backfill: tlog-attest>`（post-tag-push 回填，ADR-013 不预填）。
- GitHub Release URL：`<backfill: release-url>`（D4 首次实践）。

## 5. D3/D4 交付清单
- **D3 文档对齐**：README.md（776→153 行重构）+ CHANGELOG.md（Keep a Changelog 1.1.0，v0.1.0→v0.38.0 + Unreleased）+ docs/decisions/README.md（49 ADR 5-category 分类导航）。
- **D4 GitHub Release 流程**：release.yml softprops/action-gh-release@v2 step + contents:write permission + RELEASE_NOTES.md body 提取 + cosign/SBOM provenance footer + prerelease auto-detect（tag 含 `-` → prerelease）。
