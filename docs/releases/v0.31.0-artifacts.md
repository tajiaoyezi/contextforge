# ContextForge v0.31.0 Artifacts — embedding-remote-reranker-live (real remote reranker live MRR harness + Go [reranker] config env-bridge + data-plane with_reranker wiring + ADR-043 ratified)

> Artifact inventory for the v0.31.0 minor release (Phase 38). Mirrors the v0.30.0 artifacts structure.
> The default build is unchanged: 0-new-dependency, 0-network；默认行为 / proto 既有字段 / 既有契约不变。
> Close ADR-026 cross-encoder honest-defer 余项 + ADR-042 后续——首次对**真实远程 reranker 端点**端到端联调 + 实测 rerank MRR / recall@1 + 接通 Go `[reranker]` config env-bridge + **首次数据面 opt-in `with_reranker` 接线**。`RemoteRerankerProvider`（ureq、feature-gated `reranker-remote`）于 v0.31.0 新增（镜像既有 `CrossEncoderReranker` by-index map-back + `RemoteEmbeddingProvider` 纯 build/parse + ureq + Debug-never-logs-api_key）；新增 env-gated live-MRR harness（`core/tests/remote_rerank_recall.rs`，task-38.1）+ Go `setRerankerEnv` 跨进程 config env-bridge（task-38.2）+ 数据面 `with_reranker` 接线（`server.rs` hybrid+semantic / `data_plane/search.rs` semantic，task-38.2）。**0 新依赖**（`ureq` 自 task-22.3 起 optional）、0 proto、0 migration、默认构建 0-network / 0-remote-dep 不变（ADR-004/008），默认 unset → None → byte-equivalent 不 rerank（向后兼容），API key env-only 永不进 `config.toml`。**真实实测（主 agent 本机已认证 run，SiliconFlow `https://api.siliconflow.cn/v1/rerank` + `Qwen/Qwen3-VL-Reranker-8B`，与 embedding 同 URL+key、不同模型，3 次 run）**：remote rerank MRR = 1.0000 / recall@1 = 1.0000（14/14 relevant 均排 #1，**3 次 run 全稳定**）vs identity（model-free 基线，uniform score → chunk_id tie-break）MRR = 0.4762 / recall@1 = 0.0000，delta_MRR = +0.5238 / delta_recall@1 = +1.0000。诚实 caveat（ADR-013）：小型作者手工标注集（14 case，含故意埋设的硬近义干扰 config_save↔config_load、bm25↔hybrid、cjk_index↔cjk_vector、cosine↔vector_backend、cache↔chunk），MRR = 1.0 证明真实 cross-encoder 把明显语义对排在埋设的硬近义干扰之上、**非大基准质量断言**；大语料 / 标准基准 rerank 质量诚实延后 `[SPEC-DEFER:phase-future.reranker-large-corpus-quality]`。**与 Phase 37 embedding 不同**：embedding recall@1 跨 run 波动 0.8667–0.9333，本 rerank **3 次 run 全稳定**（cross-encoder 联合打分在此小集上更具决断性）。**CI honest-defer（关键诚实差异，ADR-013）**：remote reranker 是**付费外部 API、无免费 service container**（与 qdrant 不同——qdrant 有免费 OSS service container 在每次 CI run 守护召回）；CI 无密钥时 harness 干净 honest-defer skip，真实 MRR / recall@1 **由本机已认证 run 实测**而非每次 CI run 守护，据实记录、**不夸大为「CI 每次 run 守护」**（复用 `[SPEC-DEFER:phase-future.embedding-remote-ci-credential]`，不另造 reranker-ci-credential marker）。既有 22-endpoint + 5 Memory RPC + 运行时二进制默认行为不变。No breaking contract bump。

## 1. 二进制档

- `contextforge`（Go 控制面 CLI / console-api）：新增 `setRerankerEnv` 跨进程 config env-bridge（task-38.2，`[reranker]` config → env 桥，镜像既有 `setRemoteEnv`，env-wins；**API key 永不桥接**，env-only）。`RerankerConfig` 无 api-key 字段。运行时既有 22-endpoint + 5 Memory RPC 行为不变；默认 unset → reranker None → byte-equivalent 不 rerank（向后兼容）。
- `contextforge-core`（Rust 数据面 daemon）：新增 `RemoteRerankerProvider`（`core/src/reranker/remote.rs`，ureq、feature-gated `reranker-remote`）+ `select_reranker` 工厂 + `reranker_from_env()` + **首次数据面 opt-in `with_reranker` 接线**（`server.rs` hybrid+semantic / `data_plane/search.rs` semantic）。镜像既有 `CrossEncoderReranker` by-index map-back + `RemoteEmbeddingProvider` 纯 build/parse + ureq + Debug-never-logs-api_key。默认 reranker None → byte-equivalent 不 rerank（默认控制流不变）；feature-off / unknown → 显式 `Status::internal`（**无静默 fallback**）。新增 env-gated 集成测试 `core/tests/remote_rerank_recall.rs`（`#![cfg(feature = "reranker-remote")]`，仅在 `CONTEXTFORGE_RERANKER_API_KEY` 置位时连接真实远程端点，否则 honest-defer skip；默认 `cargo test` 不触网）。`ureq` 自 task-22.3 起 optional（默认构建 0-remote-dep / 0-network 不变）。`cargo test -p contextforge-core --lib` = lib all PASS（默认路径不变）。
- 镜像构建：`release.yml` 推 `ghcr.io/tajiaoyezi/contextforge-daemon:v0.31.0` + `:latest`（**linux/amd64**，默认构建 0 新依赖 / 0 network）+ cosign keyless 签 + SBOM + provenance（承 v0.21.0–v0.30.0）。linux/arm64 仍 DEFERRED。
- Feature 构建：本 release 新增 `reranker-remote` feature flag（**0 新依赖**——`ureq` 自 task-22.3 起 optional）；live-MRR harness 在 env-gated 集成测试路径（默认 skip / `reranker-remote` opt-in，不入默认构建）。

## 2. 源码档

- master tag SHA：`41a6823487c239b45735413002af9537763d15f3`（v0.31.0 annotated tag 对象 `3adaaf2a510cd86e8a704547d65e46b3715e48c0`，无人值守授权下主 agent push（ADR-012））
- Phase 38 代码路径：
  - task-38.1（PR #247，MERGED；remote-reranker provider + live-MRR harness）：`core/src/reranker/remote.rs`（`RemoteRerankerProvider` + `select_reranker` 工厂，feature-gated `reranker-remote`，镜像 `CrossEncoderReranker` by-index map-back + `RemoteEmbeddingProvider` 纯 build/parse + ureq + Debug-never-logs-api_key）+ `core/tests/remote_rerank_recall.rs`（`#![cfg(feature = "reranker-remote")]`，env-gated `CONTEXTFORGE_RERANKER_API_KEY` harness：作者手工标注集 14 query×candidate case（含故意埋设的硬近义干扰 config_save↔config_load、bm25↔hybrid、cjk_index↔cjk_vector、cosine↔vector_backend、cache↔chunk），real remote reranker vs identity（model-free，uniform score → chunk_id tie-break）基线度量 rerank MRR / recall@1；未置位时 honest-defer skip）；首次以真实远程 reranker 端点度量 rerank 质量。护栏：floor MRR_remote >= 0.70 + MRR_remote > MRR_identity，本机每次 run 均过。**0 新依赖**（`ureq` 自 task-22.3 起 optional）。grounding：小型手工标注集 → MRR = 1.0 为 cross-encoder 排序正确性（把明显对排在近义干扰之上）证明，非大基准质量断言，caveat 据实记录。
  - task-38.2（PR #248，MERGED；reranker config env-bridge + 数据面接线）：Go `RerankerConfig`（**无 api-key 字段**）+ `setRerankerEnv` 跨进程 env-bridge（镜像既有 `setRemoteEnv`，env-wins；**API key 永不桥接**，env-only）+ Rust `reranker_from_env()` + **首次数据面 opt-in `with_reranker` 接线**（`server.rs` hybrid+semantic / `data_plane/search.rs` semantic）。默认 unset → None → byte-equivalent 不 rerank（向后兼容）；feature-off / unknown → 显式 `Status::internal`（**无静默 fallback**）。Rust core 0 toml dep（既有 env 读取路径不变）。0 proto / 0 migration / 0 默认行为变更（ADR-004/008）。
  - task-38.3（this PR；closeout）：`scripts/console_smoke.sh`（smoke v28，banner v27→v28，staging cf-v29-cfg）；`internal/cli/smoke_syntax_test.go`（`TestTask383` 无回退 [47/47]）；`docs/releases/v0.31.0-{evidence,artifacts}.md`；`README.md` v0.31 段；`RELEASE_NOTES.md` v0.31.0 段；`docs/decisions/adr-043-...md`（ratify Proposed→Accepted，per-D）+ ADR-026 / ADR-042 add-only Phase 38 Amendment（标记已兑现：真实远程 reranker live MRR 已度量 + Go config env-bridge 接通 + 数据面 with_reranker 接线，**不溯改 D-body**，ADR-014 D5）；`docs/specs/phases/phase-38-embedding-remote-reranker-live.md`（§6 闭合）；`docs/s2v-adapter.md`；`test/features/phase-38-embedding-remote-reranker-live.feature`。

## 3. 验证脚本

```bash
# 默认构建（0 新依赖 / 0 network；ureq optional / reranker-remote feature 不入默认构建）
cargo test -p contextforge-core --lib                     # lib all PASS
go test ./...                                             # all pass (incl TestTask383)
cargo clippy --workspace --all-targets -- -D warnings     # 0 warning
go vet ./...                                              # clean
bash -n scripts/console_smoke.sh                          # exit 0

# task-38.1 live-MRR harness（env-gated；需真实远程 reranker 凭据 + reranker-remote feature）
export CONTEXTFORGE_RERANKER_API_KEY=<your-reranker-api-key>   # env-only，永不进 config
# 端点 / 模型经既有 env 桥提供（SiliconFlow https://api.siliconflow.cn/v1/rerank + Qwen/Qwen3-VL-Reranker-8B，与 embedding 同 URL+key、不同模型）
cargo test -p contextforge-core --features reranker-remote \
  --test remote_rerank_recall                            # MRR == 1.0000 / recall@1 == 1.0000（14/14，3 次 run 全稳定）
# 未置 CONTEXTFORGE_RERANKER_API_KEY 时该 harness honest-defer skip（默认 cargo test 不触网）

# 真实 MRR / recall@1 由本机已认证 run 实测（remote 为付费外部 API，无免费 service container；
# CI 无密钥时 harness 干净 skip，不夸大为「CI 每次 run 守护」，ADR-013；
# 复用 [SPEC-DEFER:phase-future.embedding-remote-ci-credential]，不另造 reranker-ci-credential marker）

# 端到端 smoke v28（step 47 → [47/47]；既有 step 不退化）
go test ./internal/cli/ -run TestTask383

# D5 spec-drift lint
bash scripts/spec_drift_lint.sh --touched origin/master   # 0 unannotated hits
```

## 4. 镜像推送

- `release.yml`（`v*` tag push 触发）→ buildx → ghcr login → build + push `ghcr.io/tajiaoyezi/contextforge-daemon:v0.31.0` + `:latest`（**linux/amd64**，net-zero 与 v0.30.0 一致）→ cosign keyless 签 + attest SPDX SBOM + provenance:mode=max（承 task-28.2 机制）。
- run ID + digest：`release.yml` run `27061306508`（**`success`** 待回填，build-push + cosign sign + attest SBOM + provenance:mode=max 步全绿）；`ghcr.io/tajiaoyezi/contextforge-daemon:v0.31.0` + `:latest` = `sha256:dfa1f35bec0b7819eb19da25b084ea5d1e4ddbc204fa66985592f9f17115c47c`；git tag SHA `41a6823487c239b45735413002af9537763d15f3`；cosign Rekor tlog idx `1739487553`（sign）/ `1739488426`（attest SBOM）。
- live-MRR evidence artifact：本 release 的真实 live rerank 证据为**主 agent 本机已认证 run**（SiliconFlow `https://api.siliconflow.cn/v1/rerank` + `Qwen/Qwen3-VL-Reranker-8B`，与 embedding 同 URL+key、不同模型，3 次 run）——remote MRR = 1.0000 / recall@1 = 1.0000（14/14 relevant 均排 #1，3 次全稳定）vs identity（model-free，uniform score → chunk_id tie-break）MRR = 0.4762 / recall@1 = 0.0000，delta_MRR = +0.5238 / delta_recall@1 = +1.0000。de-risk 可行性探针（feasibility 证据，**非** harness MRR/recall）：query "how to save config to file" → config_save relevance_score=0.7356 排 #1 vs 近义干扰 config_load=0.0158（~46x，前次 session）；本 session curl 探针 config_load 0.2196 排 #1，HTTP 200。**与 qdrant-recall 不同**：remote 为付费外部 API、无免费 service container，CI 无密钥时 harness honest-defer skip，**不声称 CI 每次 run 守护**（ADR-013）。

## 5. cosign verify（by-digest）

```bash
# 解析 tag → digest，按 digest（非 tag）验签（承 v0.21.0 follow-up 教训：cosign verify 务必按 digest）
DIGEST=$(cosign triangulate --type digest ghcr.io/tajiaoyezi/contextforge-daemon:v0.31.0)
cosign verify \
  --certificate-identity-regexp 'https://github.com/tajiaoyezi/contextforge/.github/workflows/release.yml@.*' \
  --certificate-oidc-issuer https://token.actions.githubusercontent.com \
  ghcr.io/tajiaoyezi/contextforge-daemon@${DIGEST}
# 期望 digest = sha256:dfa1f35bec0b7819eb19da25b084ea5d1e4ddbc204fa66985592f9f17115c47c
```

## 6. ADR 状态记录（this release）

| ADR | Status | 说明 |
|---|---|---|
| ADR-043 embedding-remote-reranker-live | **Accepted**（per-D，D1 live-MRR harness 🟢 + D2 Go config env-bridge + 数据面 with_reranker 接线 🟢 + D3 honest CI-defer caveat 🟢） | task-38.3 据 38.1/38.2 真实非合成验证 ratify：D1 Accepted（`#![cfg(feature = "reranker-remote")]` env-gated `remote_rerank_recall.rs` harness，手工标注集 14 case 含 5 组硬近义干扰，real remote reranker vs identity 基线，remote MRR == 1.0000 / recall@1 == 1.0000 vs identity MRR = 0.4762 / recall@1 = 0.0000，delta_MRR = +0.5238 / delta_recall@1 = +1.0000，护栏 floor MRR_remote>=0.70 + MRR_remote>MRR_identity 每次 run 过，**3 次 run 全稳定**）/ D2 Accepted（`RemoteRerankerProvider` + `select_reranker` 工厂 + `reranker_from_env()`；Go `RerankerConfig`（无 api-key 字段）+ `setRerankerEnv` 跨进程 env-bridge 镜像 `setRemoteEnv`，env-wins，API key 永不桥接 env-only；**首次数据面 opt-in `with_reranker` 接线** server.rs hybrid+semantic / data_plane/search.rs semantic；默认 unset → None → byte-equivalent 不 rerank，feature-off/unknown → 显式 `Status::internal` 无静默 fallback，Rust 0 toml dep）/ D3 honest caveat（小型手工标注集，MRR=1.0 为 cross-encoder 排序正确性证明非大基准断言，大语料 / 标准基准续 `[SPEC-DEFER:phase-future.reranker-large-corpus-quality]`；remote 付费外部 API 无免费 service container → CI 无密钥 honest-defer skip，MRR/recall 由本机已认证 run 实测、不夸大为 CI 每次 run 守护，复用 `[SPEC-DEFER:phase-future.embedding-remote-ci-credential]`）/ D4 default 0-dep/0-network/0-proto/0-migration + 默认 reranker None byte-equivalent 不 rerank（`ureq` optional / `reranker-remote` opt-in 不入默认构建，API key env-only 永不进 config）/ D5 baseline 不变（cargo/go/clippy/vet 绿；harness env-gated 旁路、默认 skip）|
| ADR-026 cross-encoder reranker（remote honest-defer） | Accepted（+ Phase 38 add-only Amendment：remote-reranker 已兑现） | task-38.3 add-only 标记 ADR-026 remote-reranker honest-defer 已兑现（真实远程 reranker 端点 live MRR/recall@1 已度量 + Go config env-bridge 接通 + 数据面 with_reranker 接线），**不溯改 D-body**（ADR-014 D5）；`CrossEncoderReranker` 本地契约自 Phase 21 task-21.3，本 phase 补真实远程联调 live MRR + config 桥 + 首次数据面接线 |
| ADR-042 embedding-provider-remote-live | Accepted（+ Phase 38 add-only Amendment：reranker 镜像已兑现） | task-38.3 add-only 标记：Phase 38 reranker live harness + Go config env-bridge **镜像** Phase 37 embedding pattern（`RemoteRerankerProvider` 镜像 `RemoteEmbeddingProvider` 纯 build/parse + ureq + Debug-never-logs-api_key；`setRerankerEnv` 镜像 `setRemoteEnv`），**不溯改 ADR-042 D-body**（ADR-014 D5）|
| ADR-013 据实延后/校正 | Accepted（守线） | MRR=1.0 / recall@1=1.0 = 小型手工标注集 cross-encoder 排序正确性证明非大基准质量断言，caveat 据实记录；大语料 / 标准基准质量延后 `[SPEC-DEFER:phase-future.reranker-large-corpus-quality]`；**remote 无免费 service container → CI 无密钥 honest-defer skip，MRR/recall 由本机已认证 run 实测、不夸大为 CI 每次 run 守护**（与 qdrant 关键差异，复用 `[SPEC-DEFER:phase-future.embedding-remote-ci-credential]` 不另造 marker）；**rerank 3 次 run 全稳定**（与 Phase 37 embedding recall@1 跨 run 波动 0.8667–0.9333 不同，据实记录）；identity 基线 MRR=0.4762 / recall@1=0.0000 为 no-semantic control；tag SHA / run id / digest / tlog 未知前以 `<backfill>` 占位不伪造凭据 |
| ADR-014 add-only / cross-validation | Accepted（守线，第二十九次激活） | env-gated harness + Go config env-bridge + 数据面 with_reranker 接线 add-only（默认 reranker None byte-equivalent 不 rerank / 0 proto / 0 migration）+ ADR-026 / ADR-042 Amendment 不溯改正文（D5）|
| ADR-004 / ADR-008 / ADR-012 | Accepted（守线） | 默认 0 新依赖 / 0 network（`ureq` 自 task-22.3 起 optional / `reranker-remote` opt-in 不入默认构建，Rust core 默认 0-remote-dep）；harness env-gated（未置 `CONTEXTFORGE_RERANKER_API_KEY` 即 honest-defer skip 不触网）；API key env-only 永不进 config；tag-release 须授权（本轮经无人值守 /goal 授权 ADR-012）|

## 7. 版本声明（contract bump 评估）

- 本 release 新增 env-gated live-MRR harness（`remote_rerank_recall.rs`，real remote reranker vs identity 基线，remote MRR = 1.0000 / recall@1 = 1.0000）+ `RemoteRerankerProvider` + `select_reranker` 工厂 + Go `setRerankerEnv` config env-bridge（`RerankerConfig` 无 api-key 字段，env-wins，API key 永不桥接）+ **首次数据面 opt-in `with_reranker` 接线** + smoke v28——**无 proto 既有字段 / RPC / endpoint 删改、无默认运行时行为改动（默认 reranker None → byte-equivalent 不 rerank）、0 migration**（harness env-gated 默认 skip / `ureq` optional + `reranker-remote` opt-in 不入默认构建）。
- 默认构建 0 新代码依赖 / 0 network（ADR-004 / ADR-008，Rust core 默认保持 0-remote-dep）；API key env-only 永不进 config；feature-off / unknown → 显式 `Status::internal`（无静默 fallback）。
- 故 **无 breaking contract bump**；既有 v0.6–v0.30 client + 既有索引/数据无需改动。**embedding-remote-reranker-live 收口**（ADR-026 remote-reranker honest-defer 兑现）。

## 8. 后续模板（reranker-remote follow-on）

- 大语料 / 标准基准真实远程 rerank 质量基准（远超 14-case 手工标注集，多语种 / 多 provider 对比，nDCG / MAP）`[SPEC-DEFER:phase-future.reranker-large-corpus-quality]`。
- 付费远程 API 在 CI 的低成本守护策略（密钥注入 / 配额 / 缓存 fixture vs live，embedding+reranker 共用）`[SPEC-DEFER:phase-future.embedding-remote-ci-credential]`。
- 远程 reranker 多 provider 矩阵（Cohere / Jina / SiliconFlow 等 rerank 端点契约 + top_n 协商）`[SPEC-DEFER:phase-future.reranker-remote-provider-matrix]`。
- linux/arm64 原生 runner multi-arch `[SPEC-DEFER:phase-future.multi-arch-native-runner]`。
- （承前）大语料远程语义召回质量基准 `[SPEC-DEFER:phase-future.embedding-large-corpus-recall]`；HNSW 逼近 regime 压测 `[SPEC-DEFER:phase-future.vector-large-corpus-perf]`；vector-dim feature-enforce `[SPEC-DEFER:phase-future.vector-dim-feature-enforce]`；structured metrics/counter facility `[SPEC-DEFER:phase-future.observability-metrics-facility]`。

## 9. 存档指引

- closeout PR 合入后，**无人值守授权下**主 agent push `v0.31.0` annotated tag 触发 `release.yml`；post-tag-push backfill PR 填实 tag SHA + run ID + 镜像 digest + cosign 凭据（承 v0.8–v0.30 pattern；§2/§4/§5 角括号 `<backfill: ...>` 标记由本 backfill PR 回填）。
- Dockerfile workspace member COPY 完整性预检（承 v0.11.0 PR #139 教训）：本 release **无新 migration、0 proto 字段新增**——新增 `core/src/reranker/remote.rs`（既有 `core/` member，feature-gated 不入默认构建）+ `core/tests/remote_rerank_recall.rs`（既有 `core/` member 的 tests/，env-gated 不入默认构建）+ Go `setRerankerEnv` env-bridge + 数据面 `with_reranker` 接线（add-only），无新依赖；镜像为默认构建（0 新依赖 / 0 network，linux/amd64）。
