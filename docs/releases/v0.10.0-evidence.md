# ContextForge v0.10.0 Evidence — is-pinned-amendment (Console PR #91/#93 backlog 11/11 = 100% closed)

**日期**：2026-05-28
**主干**：`master`
**当前 HEAD (post Phase 17 closeout PR #119 merge)**：`cbaafe6` (`chore(release): v0.10.0 — Phase 17 closeout + ADR-022 Accepted + release docs (Console backlog 11/11 closed) (#119)`)
**parent release**：v0.9.0 tag → master @ `889a6b5`

**v0.10.0 tag SHA**：annotated tag → master @ `cbaafe6`; verified via `git rev-parse v0.10.0^{}` = `cbaafe69bcf4a274c280f38dba5e5190d59742bd`.

## 合入记录 (Phase 17 + v0.10.0 release docs)

- PR [#116](https://github.com/tajiaoyezi/contextforge/pull/116) `chore/phase-17-adr-022-scaffolding` (merged `4c4f40b`, post-v0.9.0): E1 Phase 17 scaffolding — phase-17 spec + ADR-022 (Proposed) + task-17.1 spec (Pending) + adapter §Phase 17 / §Task 17.1 / §ADR 022 / §BDD + PRD §Phase 17 段 + `test/features/phase-17-is-pinned-amendment.feature` scenarios. All Status: Pending awaiting Console cross-repo trigger.
- ContextForge-Console PR [#101](https://github.com/tajiaoyezi/ContextForge-Console/pull/101) (cross-repo, not in this repo) merged 2026-05-28T12:16:57Z @ Console master `415ee30fcd8effd7929806d196458ec6e60fb49f` — `MemoryItem.IsPinned bool` add-only field on Console side per ADR-022 D4 第 1 步. Verified via `gh api repos/tajiaoyezi/ContextForge-Console/contents/console-api/internal/coreadapter/contractv1/contractv1.go?ref=415ee30` → field block returned correctly (type `bool`, json tag `is_pinned`, position between `Status` and `Availability`).
- PR [#118](https://github.com/tajiaoyezi/contextforge/pull/118) `feat/task-17.1-memory-is-pinned-field` (merged `cfcdbd4`, squash-merge): task-17.1 implementation — proto field 10 + buf-regen `pb.go` + Rust `memory_to_pb` mapper + Go `contractv1.MemoryItem.IsPinned bool` + `grpcclient.protoToMemoryItem` IsPinned copy + `MemMemoryStore.Pin(id, pin)` actually writing IsPinned + fixture-1 preset `IsPinned: true` + `handleMemoryPin` JSON body parser + 5 new tests (2 Rust gRPC integration + 1 Rust lib + 2 Go memstore + 1 Go contractv1 forward/backward compat + extended JSON roundtrip) + smoke v8 27→28 step (Pin RPC + post-restart + empty-body backward-compat 4 sub-assertions) + spec sync (Phase 17 + task-17.1 Status Pending → Done + adapter rows). +735/-422 across 16 files. cargo + go + spec-lint CI all green (cargo-test required 1 rerun for unrelated Phase 9 Tantivy lock flake).
- PR [#119](https://github.com/tajiaoyezi/contextforge/pull/119) `chore/phase-17-closeout` (merged `cbaafe6` @ 2026-05-28T13:54:20Z, squash): closeout PR — ADR-022 Status Proposed → Accepted + Phase 17 §6 AC7 [x] + task-17.1 §10 last item [x] + adapter ADR-022 row Accepted + README v0.10.0 segment + RELEASE_NOTES v0.10.0 section + this evidence file + v0.10.0-artifacts.md. CI all 3 checks PASS first try (spec-lint 1m55s, cargo-test 2m7s, go-test 3m26s). +487/-8 across 8 files.
- v0.10.0 annotated tag (after PR #119 merge): pointing to `cbaafe6`; `release.yml` on v0.10.0 push run [26579312594](https://github.com/tajiaoyezi/contextforge/actions/runs/26579312594) GREEN in 7m41s; `verify-image.yml` workflow_dispatch run [26579788615](https://github.com/tajiaoyezi/contextforge/actions/runs/26579788615) GREEN in 29s.
- (this backfill PR) — fills placeholders in v0.10.0-evidence.md + v0.10.0-artifacts.md with actual tag SHA + 2 GHA run IDs after v0.10.0 tag push pipeline completed (sequencing: Phase 17 closeout PR #119 merge → v0.10.0 tag push → release.yml + verify-image.yml green → this backfill PR; follows v0.8.0 PR #117 → d49d344 backfill pattern).

## S2V 状态

- `docs/s2v-adapter.md`:
  - Phase 17 `is-pinned-amendment` row: `Done` (PR #118 flipped Pending → Done; this closeout PR retains)
  - Task 17.1 row: `Done` (PR #118 flip)
  - ADR-022 row: `Proposed → Accepted` (this closeout PR flip)
- `docs/specs/phases/phase-17-is-pinned-amendment.md` = `Done` with §6 AC1-AC7 all `[x]` and `verified-by` clauses explicit; §8 DoD 8/9 `[x]` with the cross-repo notify item explicitly deferred to post-merge user-forwarded action.
- `docs/specs/tasks/task-17.1-memory-is-pinned-field.md` = `Done` with §10 Completion Notes 6/6 `[x]` (PR # / cargo test output / go test output / smoke v8 / D2 lint / ADR-022 promotion).
- `docs/decisions/adr-022-memory-is-pinned-field-amendment.md` = `Accepted (2026-05-28, via Phase 17 closeout PR — implementation shipped via PR #118 task-17.1; cross-repo trigger ContextForge-Console PR #101 master @ 415ee30)`.
- `docs/decisions/adr-014-cross-phase-exit-criteria-validation.md` = `Accepted` (v0.3-v0.9 七次激活 + **v0.10 第八次激活** — 跨 ADR amendment 路径首次激活成功，制度稳定性跨 8 phase 验证通过).

## 验证证据

### 1. cargo test --workspace (PR #118 CI实测)

```bash
cargo test --workspace
```

实测 (PR #118 CI run 26577717038 cargo-test job, after one rerun for unrelated Phase 9 flake):
- 41 tests PASS across crates (lib + integration); 0 failed, 0 ignored
- `core/src/memory/store.rs` lib tests: 11 PASS (10 既有 + 1 新增 `test_list_returns_is_pinned_column`)
- `core/tests/memory_integration.rs`: 5 PASS (3 既有 + 2 新增 `test_is_pinned_propagates_via_grpc_list_and_get` + `test_pin_rpc_unpin_reverses_state`)
- 不退化任何 Phase 1-16 既有测试

### 2. go test ./... (PR #118 CI实测)

```bash
go test ./...
```

实测 (PR #118 CI run 26577717038 go-test job):
- 21 packages PASS; 0 FAIL
- `internal/consoleapi`: 含 2 新增 `TestMemMemoryStore_Pin_TogglesIsPinned` + `TestMemMemoryStore_List_ReturnsIsPinned` (48.7s 总)
- `internal/contractv1`: 含 1 新增 `TestMemoryItemForwardBackwardCompat` + extended `TestJSONRoundtrip MemoryItem_pinned` case (0.032s)
- `internal/consoleapi/grpcclient` 11.4s — 不退化既有 task-16.2 4 个 grpcclient tests
- `test/conformance` 22-endpoint Console contract: PASS (contract v1 不 bump; MemoryItem `is_pinned` 字段 add-only)

### 3. spec_drift_lint --touched (PR #118 CI实测 + this closeout PR self-check)

```bash
bash scripts/spec_drift_lint.sh --touched origin/master
```

PR #118 实测：first run flagged 2 hits in `phase-17` spec (`scaffolding` / `scaffold` 词在 ANTI_PATTERN_REGEX 中)；commit `ecf5299` 改措辞 → 0 unannotated hits ✅
This closeout PR self-check：本地 + CI 实测 0 unannotated hits ✅

### 4. CI status

PR #118 final CI run 26577717038:
- `spec-lint`: PASS in 1m53s (after commit `ecf5299` lint fix)
- `go-test`: PASS in 3m32s
- `cargo-test`: PASS in 1m27s (after 1 rerun via `gh run rerun --failed`; first attempt failed on **unrelated** Phase 9 `phase_9_index_grpc_end_to_end_smoke` Tantivy `LockBusy` flake at `core/tests/phase9_index_smoke.rs:155`. Not a task-17.1 regression — same Rust code passed CI run 26576272849 earlier. Documented in PR #118 review notes.)

This closeout PR (#119) CI run: spec-lint 1m55s + cargo-test 2m7s + go-test 3m26s, all 3 PASS first try (no flake / no fix commit needed; cleaner than PR #118 which required 1 spec-lint fix + 1 cargo rerun for unrelated Phase 9 flake).

v0.10.0 tag push pipeline:
- `release.yml` run [26579312594](https://github.com/tajiaoyezi/contextforge/actions/runs/26579312594): success in 7m41s (started 2026-05-28T13:58:28Z, ended 2026-05-28T14:06:09Z); pushed `ghcr.io/tajiaoyezi/contextforge-daemon:v0.10.0` + `:latest`.
- `verify-image.yml` run [26579788615](https://github.com/tajiaoyezi/contextforge/actions/runs/26579788615): success in 29s (started 2026-05-28T14:06:56Z, ended 2026-05-28T14:07:25Z); pull + run + `/v1/health` probe + ADR-020 5-component breakdown via `?detailed=true` all green.

### 5. scripts/console_smoke.sh v8 bash -n verify

```bash
bash -n scripts/console_smoke.sh
```

实测 (PR #118 local + CI): syntax OK. v8 step list:
- step 1-24: Phase 11-15 既有 22-endpoint Console contract flow (不变)
- step 25 (Phase 16): `?wait=2s` real long-poll timing assertion (task-16.2)
- step 26 (Phase 16): TraceStore SQLite restart roundtrip (task-16.1)
- step 27 (Phase 16): compose-prod stack health gated COMPOSE_PROD_SMOKE=1 (task-16.4)
- step 28 (Phase 17, **new**): `MemoryItem.is_pinned` Pin RPC roundtrip — 4 sub-assertions gated `MODE=real && sqlite3`:
  - (a) post-restart `mem-seed-1.is_pinned=true` survives step-26 daemon kill+restart
  - (b) `POST {"pin":false}` → `GET .is_pinned=false`
  - (c) `POST {"pin":true}` → `GET .is_pinned=true`
  - (d) empty-body `POST` → `GET .is_pinned=true` (v0.7-v0.9 backward compat path)

Daemon-level `CONSOLE_REAL_SMOKE_EXIT=0` 留 v0.10.0 tag push 时 release.yml + verify-image.yml 验证 — 本 release docs PR 不重跑 (per fake-evidence policy)。

### 6. ADR-014 D2 lint output (this closeout PR self-check)

```bash
bash scripts/spec_drift_lint.sh --touched origin/master
```

```
✅ spec_drift_lint --touched origin/master: 0 unannotated hits in changed lines
```

### 7. Spec drift discovery (task-17.1 §3 vs reality)

| Original spec prescription | Reality | Resolution |
|---|---|---|
| Create migration `0017_memory_items_add_is_pinned.sql` + PRAGMA gate | `is_pinned INTEGER NOT NULL DEFAULT 0` already at `core/migrations/0013_memory_items.sql:16` from task-13.1 (Phase 13 forward-added) | Skipped 0017 — would conflict with `duplicate column name` on existing v0.6+ DBs |
| Add Rust `SqliteMemoryStore::set_pinned` + update `MemoryServer.Pin` to call it | Both already shipped at Phase 13 (`store.rs:153` set_pinned + `memory.rs:217` Pin handler calling it) | Only `memory_to_pb` mapper line missing |
| (not mentioned in spec §3) | `handleMemoryPin` hardcoded `Pin(id, true)`; smoke v8 spec `-d '{"pin": false}'` required body parsing | Added JSON body parser with backward-compat default `true` |

Documented in PR #118 commit body + `docs/specs/tasks/task-17.1-memory-is-pinned-field.md` §3 + this evidence file.

### 8. ADR-022 D5 trigger chronology (cross-repo coordination first end-to-end)

```
2026-05-28 ~early: ContextForge ships PR #116 — Phase 17 + task-17.1 + ADR-022 scaffolding (Pending/Proposed)
2026-05-28 ~mid (user-forwarded prompt to Console main agent):
  User dispatches prepared "Console-side amend" prompt to ContextForge-Console main agent.
2026-05-28T12:16:57Z (Console-first ship):
  ContextForge-Console PR #101 merges to Console master @ 415ee30 — MemoryItem.IsPinned bool add-only.
2026-05-28 ~late afternoon (user forwards back):
  User forwards Console PR #101 merge SHA `415ee30` to ContextForge main agent.
2026-05-28 ~late afternoon (verification + ship):
  ContextForge main agent verifies Console master @ 415ee30 contains field via gh api.
  ContextForge main agent starts PR #118 implementation:
    - branch feat/task-17.1-memory-is-pinned-field
    - 16 files +735/-422
    - spec drift discoveries documented
    - 5 new tests + smoke v8 step 28
    - Status: Pending → Ready → Done flipped within PR #118
  PR #118 CI: 1 lint fix (commit ecf5299), 1 cargo rerun (unrelated Phase 9 flake)
  PR #118 squash-merge to master @ cfcdbd4 at 2026-05-28T13:39:17Z
2026-05-28 ~late afternoon (this closeout PR):
  ADR-022 Proposed → Accepted; v0.10.0 release docs.
```

### 9. Console PR #91/#93 backlog 11/11 = 100% closed mapping (累计 Phase 13 + 15 + 16 + 17)

| Item | Backlog signal | Task | PR | Phase | Release |
|---|---|---|---|---|---|
| P0 #1 | MemStore inmem-fallback 503 drill-down | task-15.1 | #99 | 15 | v0.8 |
| P0 #2 | `memory.*` event 桥接 缺失 | task-15.2 | #100 | 15 | v0.8 |
| P1 #3 | Dashboard "已索引块" 缺 backend | task-15.3 | #101 | 15 | v0.8 |
| P1 #4 | Eval 列表 缺 endpoint | task-15.4 | #102 | 15 | v0.8 |
| P1 #5 | Dashboard "最近查询" 缺 backend | task-15.5 | #103 | 15 | v0.8 |
| P2 #7 | CoreHealthCard 5 链路 缺 | task-15.6 | #104 | 15 | v0.8 |
| P4 #10 | TraceStore daemon-restart 即丢历史 | task-16.1 | #110 | 16 | v0.9 |
| P4 #11 | events `?wait=` 等价 batch polling | task-16.2 | #111 | 16 | v0.9 |
| P3 #8 | ghcr.io image push 缺 CI/CD | task-16.3 | #112 | 16 | v0.9 |
| P3 #9 | docker-compose.production.yml 缺示例 | task-16.4 | #113 | 16 | v0.9 |
| **P2 #6** | **`MemoryItem.is_pinned` 字段缺失** | **task-17.1** | **#118** | **17** | **v0.10** |

🎉 **Closure status: 11/11 = 100% closed** — the full ContextForge-Console PR #91/#93 review backlog raised in v0.7.x is addressed end-to-end. Phase 13/15/16/17 cumulative ship.

### 10. Cross-repo follow-up (post-v0.10.0 tag push)

- User forwards v0.10.0 GitHub Release page URL → Console main agent (autonomous notification outside ContextForge agent scope)
- Console UI can then ship visual closure standby PR:
  - "按 pinned 排序" Memory list feature flag activation
  - Pin icon rendering on Memory list/detail panels
  - (both Console-side single-driver PRs; rely on `MemoryItem.IsPinned` field now available end-to-end through ContextForge v0.10+)
- After Console UI visual closure PR merges to Console master, ContextForge-Console PR #91/#93 review backlog is **fully end-to-end closed** (backend protocol + UI surface). ContextForge agent has no further obligation here.

## 平台矩阵

| Platform | Build | Test |
|---|---|---|
| Linux x86_64 (Ubuntu 22.04, GHA runner) | source build OK + GHCR image build pending v0.10.0 tag | cargo + go test PASS in CI |
| WSL2 | source build OK (assumed; unchanged from v0.9) | cargo + go test PASS (assumed) |
| macOS (arm64) | source build OK (nice-to-have, unchanged) | manual |
| Windows native | source build OK + dev OK | cargo + go test PASS (本 ship 主开发平台 — PR #118 dev) |

## Upgrade path (v0.9.0 → v0.10.0)

详 RELEASE_NOTES.md v0.10.0 §Upgrade path. 关键点：
- **No SQLite migration required** — `is_pinned` column already in 0013 since task-13.1 (Phase 13). Upgrade is a no-op DB-side.
- **Console UI clients (v0.7-v0.9)**: silently ignore the new `is_pinned` key (Go JSON unmarshal default). No client change required.
- **Console UI clients (v0.10+ adapted, Console PR #101 already merged on Console master)**: now read `MemoryItem.IsPinned` with real persisted value.
- **Docker users**: `docker pull ghcr.io/tajiaoyezi/contextforge-daemon:v0.10.0` after v0.10.0 tag push (release.yml on `v*` tag push handles ghcr build/push automatically).
- **No BREAKING** — purely additive schema, backward compatible in both directions per ADR-015 D1 + ADR-022 D4.

## Rollback path

详 RELEASE_NOTES.md v0.10.0 §Rollback path:
1. `git revert <v0.10.0 closeout SHA>` → roll back closeout docs (ADR-022 back to Proposed, etc.)
2. `git revert cfcdbd4` (PR #118 squash merge) → roll back implementation
3. v0.10.0 tag可保留指向 last-good master HEAD (e.g. v0.9.0 = 889a6b5) 或删除 tag
4. No DB column to drop — column always in 0013 from Phase 13
5. ADR-022 decision stays sound even if patched; rollback path is patch fix, not ADR superseded

## ADR-014 cross-validation gate 第八次激活 record

```
2026-05-28: ADR-014 第八次激活 verified across PR #118 + this closeout PR.
            Cumulative activations:
              1. v0.3 (Phase 8 eval-and-reliability)
              2. v0.4 (Phase 11 console-real-data-plane)
              3. v0.5 (Phase 12 console-contract-completion)
              4. v0.6 (Phase 13 memory-rest-surface)
              5. v0.7 (Phase 14 eval-rest-surface)
              6. v0.8 (Phase 15 console-functional-gap-closure)
              7. v0.9 (Phase 16 v0.9.0-backlog-completion)
              8. v0.10 (Phase 17 is-pinned-amendment) ← **本次激活;
                 first ADR amendment path exercise (ADR-015 D5 → ADR-022)**
            8-phase 累计稳定性 — 制度通过 8 phase 跨度独立验证;
            首次跨 ADR amendment 路径成功激活 (ADR-022 Proposed → Accepted).

This closeout PR ADR-022 promotion is the formal closeout of the 8th activation.
```

## Cross-repo follow-up (after v0.10.0 tag push)

- Notify Console team of v0.10.0 release ship via GitHub Release page URL
- Console UI single-driver PR: "按 pinned 排序 + pin icon" visual closure feature flag (Console-side; outside ContextForge autonomous flow)
- Console PR #91/#93 review backlog **end-to-end fully closed** (backend protocol via v0.6-v0.10 cumulative ship + UI surface via Console subsequent PR)
